{"id":"1.1-graph-core","phase":"1.1","title":"Graph core","items":["Graph(name, opts?) constructor","graph.add(name, node) / graph.remove(name)","graph.get(name) / graph.set(name, value) / graph.node(name)","graph.connect(fromName, toName) / graph.disconnect(fromName, toName)","Edge validation (no transforms, pure wires)"]}
{"id":"1.2-composition","phase":"1.2","title":"Composition","items":["graph.mount(name, childGraph) — subgraph embedding","Double-colon qualified paths: \"parent::child::node\"","graph.resolve(path) — node lookup by qualified path","Lifecycle signal propagation through mount hierarchy"]}
{"id":"1.3-introspection","phase":"1.3","title":"Introspection","items":["graph.describe() → JSON (nodes, edges, subgraphs, meta)","graph.observe(name?) → live message stream","Type inference in describe output (state/derived/producer/operator/effect)","Meta node registration: graph.add() must register node.meta.* sub-nodes so describe(), observe(), and signal() reach them (::__meta__:: paths + signal→meta; TEARDOWN cascades from primary only)"]}
{"id":"1.4-lifecycle-persistence","phase":"1.4","title":"Lifecycle & persistence","items":["graph.signal(messages) — broadcast to all nodes","graph.destroy() — TEARDOWN all","graph.snapshot() / graph.restore(data) / Graph.fromSnapshot(data)","graph.toJSON() — deterministic serialization"]}
{"id":"1.4b-seamless-persistence","phase":"1.4b","title":"Seamless persistence","items":["graph.autoCheckpoint(adapter, opts?) — debounced reactive persistence wired to observe(); trigger gate uses messageTier (>=2)","Incremental snapshots — diff-based persistence via Graph.diff(), periodic full snapshot compaction","Selective checkpoint filter — { filter: (name, described) => boolean }","Graph.registerFactory(pattern, factory) — register node factory by name glob pattern","Graph.unregisterFactory(pattern)","Graph.fromSnapshot(data) registry integration — auto-reconstruct dynamic graphs without build callback; topological dep resolution","restore(data, { only }) — selective restore (partial hydration by node name pattern)","Guard reconstruction from data — policyFromRules() pattern for rebuilding guard fns from persisted policy rules"]}
{"id":"1.5-actor-guard","phase":"1.5","title":"Actor & Guard (access control)","items":["Actor type: { type: \"human\" | \"llm\" | \"wallet\" | \"system\" | string, id: string, ...claims }","Actor context parameter on down(), set(), signal() — optional, defaults to { type: \"system\" }","guard node option: (actor, action) => boolean — checked on down()/set()/signal(); throws GuardDenied","policy() declarative builder — CASL-style ergonomics","Scoped describe(actor?) / observe(name?, actor?) — filters output to nodes the actor may observe","Attribution: each mutation records { actor, timestamp } on the node (accessible via node.lastMutation)","meta.access derived from guard when present","GuardDenied error type with { actor, node, action }"]}
{"id":"1.6-tests","phase":"1.6","title":"Graph tests","items":["Graph add/remove/connect/disconnect","Mount and namespace resolution","describe() output validation against JSON schema","observe() message stream tests","Snapshot round-trip tests","Cross-subgraph signal propagation","Guard enforcement: allowed/denied writes, signals, observe filtering","Policy builder: allow/deny precedence, wildcard, composed policies","Actor attribution: mutation records, actor propagation through subgraphs","Scoped describe: filtered output matches guard permissions","GuardDenied error: correct actor/node/action in diagnostics"]}
