{
	"name": "@graphrefly/parity-tests",
	"version": "0.0.0",
	"private": true,
	"description": "Cross-impl parity test runner. Phase E rustImpl activation slice (D074): parameterized vitest describe.each over (pureTsImpl, rustImpl). Phase 13.9.B target: trace-record/trace-replay (rigor-infra Project 2).",
	"type": "module",
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"dependencies": {
		"@graphrefly/native": "^0.0.5",
		"@graphrefly/pure-ts": "workspace:*"
	},
	"devDependencies": {
		"@types/node": "^25.5.0",
		"typescript": "^5.7.0",
		"vitest": "^3.0.0"
	}
}
