---
title: "memoryAgenticMemoryPassiveStoreFrameAdapter()"
description: "Creates a host-owned in-memory adapter for encoded D585 AgenticMemory store frames. The adapter stores opaque bytes only. It does not decode frames, mutate graph records, hydrate a live graph, or report a durable commit acknowledgement; status/cursor material describes this adapter's own read/write operations."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Creates a host-owned in-memory adapter for encoded D585 AgenticMemory store frames.

The adapter stores opaque bytes only. It does not decode frames, mutate graph
records, hydrate a live graph, or report a durable commit acknowledgement;
status/cursor material describes this adapter's own read/write operations.

## Import

```ts
import { memoryAgenticMemoryPassiveStoreFrameAdapter } from "@graphrefly/ts/adapters";
```

## Signature

```ts
function memoryAgenticMemoryPassiveStoreFrameAdapter(): AgenticMemoryPassiveStoreFrameAdapter
```

## Returns

An in-memory passive store-frame adapter.

## Example

```ts
import { memoryAgenticMemoryPassiveStoreFrameAdapter } from "@graphrefly/ts/adapters";
```

## Source

`packages/ts/src/adapters/agentic-memory-storage.ts`
