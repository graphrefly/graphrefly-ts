---
title: "EnvironmentDrivers"
description: "Graph-local bundle of environment drivers. Instances are immutable snapshots of the process, HTTP, SSE, WebSocket, and webhook driver set consumed by source/adapter bodies."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Graph-local bundle of environment drivers.

Instances are immutable snapshots of the process, HTTP, SSE, WebSocket, and webhook
driver set consumed by source/adapter bodies.

## Import

```ts
import { EnvironmentDrivers } from "@graphrefly/ts/graph";
```

## Signature

```ts
class EnvironmentDrivers
```

## Example

```ts
import { EnvironmentDrivers, fetchHttpDriver } from "@graphrefly/ts/graph";

const drivers = new EnvironmentDrivers().withHttp(fetchHttpDriver());
```

## Source

`packages/ts/src/graph/environment.ts`
