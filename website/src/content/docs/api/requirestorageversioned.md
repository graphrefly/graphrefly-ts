---
title: "requireStorageVersioned()"
description: "Require D108 versioned support and produce a clear adapter error when absent."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Require D108 versioned support and produce a clear adapter error when absent.

## Import

```ts
import { requireStorageVersioned } from "@graphrefly/ts/storage";
```

## Signature

```ts
function requireStorageVersioned(
	backend: StorageBackend,
	label = "storage backend",
): VersionedStorageBackend
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `backend` | `StorageBackend` | backend value used by the helper. |
| `label` | `unknown` | label value used by the helper. |

## Returns

A `VersionedStorageBackend` value.

## Example

```ts
import { requireStorageVersioned } from "@graphrefly/ts/storage";
```

## Source

`packages/ts/src/storage/backend.ts`
