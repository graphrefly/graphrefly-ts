---
title: "agenticMemoryCommittedFactLogAppendAttempt()"
description: "Append a canonical committed fact batch and project D590 durability DATA. This helper is an explicit runtime/source-boundary composition over D589 and D590. It appends only caller-supplied canonical committed fact batches through the supplied fact log and returns durability DATA for explicit downstream policy. A result is fact-log persistence progress only: not application acknowledgement, live graph truth, record mutation authority, hot hydration, restore, or a graph wave/batch commit barrier."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Append a canonical committed fact batch and project D590 durability DATA.

This helper is an explicit runtime/source-boundary composition over D589 and
D590. It appends only caller-supplied canonical committed fact batches through
the supplied fact log and returns durability DATA for explicit downstream
policy. A result is fact-log persistence progress only: not application
acknowledgement, live graph truth, record mutation authority, hot hydration,
restore, or a graph wave/batch commit barrier.

## Import

```ts
import { agenticMemoryCommittedFactLogAppendAttempt } from "@graphrefly/ts/solutions";
```

## Signature

```ts
function agenticMemoryCommittedFactLogAppendAttempt(
	log: AgenticMemoryCommittedFactLog<TJson>,
	batch: AgenticMemoryCommittedFactBatch<TJson>,
	opts: AgenticMemoryCommittedFactLogAppendAttemptOptions = {},
): | AgenticMemoryCommittedFactLogAppendAttemptResult<TJson>
	| PromiseLike<AgenticMemoryCommittedFactLogAppendAttemptResult<TJson>>
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `log` | `AgenticMemoryCommittedFactLog<TJson>` | D589/D592-compatible committed fact log. |
| `batch` | `AgenticMemoryCommittedFactBatch<TJson>` | Canonical committed fact batch to append. |
| `opts` | `AgenticMemoryCommittedFactLogAppendAttemptOptions` | Optional evaluation counter and explicit downstream policy. |

## Returns

Append attempt composition DATA, or a PromiseLike when the log is async.

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@example`.

## Source

`packages/ts/src/solutions/agentic-memory/fact-log-runtime-persistence.ts`
