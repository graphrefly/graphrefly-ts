---
title: "agenticMemoryCommittedFactLogStartupRead()"
description: "Read committed facts at runtime start and project D591/D593 DATA. This helper is an explicit runtime/source-boundary composition over D589, D591, and D593. It calls the supplied fact log's `read` method, materializes the returned committed facts with library-owned rules, and exposes caller-wirable records, priorEvidence, evidence, status, issues, audit, and cursor DTOs. It does not read a concrete backend directly, hydrate/restore a graph, mutate live records, refresh subscribers, or create a graph commit barrier."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Read committed facts at runtime start and project D591/D593 DATA.

This helper is an explicit runtime/source-boundary composition over D589,
D591, and D593. It calls the supplied fact log's `read` method, materializes
the returned committed facts with library-owned rules, and exposes
caller-wirable records, priorEvidence, evidence, status, issues, audit, and
cursor DTOs. It does not read a concrete backend directly, hydrate/restore a
graph, mutate live records, refresh subscribers, or create a graph commit
barrier.

## Import

```ts
import { agenticMemoryCommittedFactLogStartupRead } from "@graphrefly/ts/solutions";
```

## Signature

```ts
function agenticMemoryCommittedFactLogStartupRead(
	log: AgenticMemoryCommittedFactLog<TJson>,
	opts: AgenticMemoryCommittedFactLogStartupReadOptions = {},
): | AgenticMemoryCommittedFactLogStartupReadResult<TJson>
	| PromiseLike<AgenticMemoryCommittedFactLogStartupReadResult<TJson>>
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `log` | `AgenticMemoryCommittedFactLog<TJson>` | D589/D592-compatible committed fact log. |
| `opts` | `AgenticMemoryCommittedFactLogStartupReadOptions` | Optional read cursor/limit and evaluation counter. |

## Returns

Startup read composition DATA, or a PromiseLike when the log is async.

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@example`.

## Source

`packages/ts/src/solutions/agentic-memory/fact-log-runtime-persistence.ts`
