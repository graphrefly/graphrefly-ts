---
title: "agenticMemoryCommittedFactLogBackendStatus()"
description: "Build D592 backend status DATA. Backend status is diagnostic DATA only. It does not define protocol semantics, acknowledge application success, mutate records, hydrate/restore graph state, refresh subscribers, or create a graph commit barrier."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Build D592 backend status DATA.

Backend status is diagnostic DATA only. It does not define protocol
semantics, acknowledge application success, mutate records, hydrate/restore
graph state, refresh subscribers, or create a graph commit barrier.

## Import

```ts
import { agenticMemoryCommittedFactLogBackendStatus } from "@graphrefly/ts/solutions";
```

## Signature

```ts
function agenticMemoryCommittedFactLogBackendStatus(
	state: AgenticMemoryCommittedFactLogBackendStatusState,
	opts: {
		readonly backend?: string;
		readonly capabilities?: readonly AgenticMemoryCommittedFactLogBackendCapability[];
		readonly issues?: readonly DataIssue[];
		readonly audit?: readonly AgenticMemoryCommittedFactLogBackendAuditEntry[];
	} = {},
): AgenticMemoryCommittedFactLogBackendStatus
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `state` | `AgenticMemoryCommittedFactLogBackendStatusState` | Backend availability/status classification. |
| `opts` | `{ 		readonly backend?: string; 		readonly capabilities?: readonly AgenticMemoryCommittedFactLogBackendCapability[]; 		readonly issues?: readonly DataIssue[]; 		readonly audit?: readonly AgenticMemoryCommittedFactLogBackendAuditEntry[]; 	}` | Optional backend name, capabilities, issues, and audit entries. |

## Returns

A normalized D592 backend status DATA object.

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@example`.

## Source

`packages/ts/src/solutions/agentic-memory/committed-fact-log-backend.ts`
