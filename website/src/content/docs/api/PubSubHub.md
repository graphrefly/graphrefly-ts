---
title: "PubSubHub()"
description: "In-memory lazy topic registry: each topic is an independent  node (compare `graphrefly-py` `PubSubHub` with a process-wide lock)."
---

In-memory lazy topic registry: each topic is an independent  node (compare `graphrefly-py` `PubSubHub` with a process-wide lock).

## Signature

```ts
class PubSubHub
```
