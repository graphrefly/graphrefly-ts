---
title: "reactiveLog()"
description: "Create an append-only reactive log (D54/D60). DELTA + lazy pull SNAPSHOT + pullId via {@link collectionCore}; this layer adds the typed append surface + incremental view/scan."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Create an append-only reactive log (D54/D60). DELTA + lazy pull SNAPSHOT + pullId via
{@link collectionCore}; this layer adds the typed append surface + incremental view/scan.

## Import

```ts
import { reactiveLog } from "@graphrefly/ts/data-structures";
```

## Signature

```ts
function reactiveLog(
	initial?: readonly T[],
	options: ReactiveLogOptions = {},
): ReactiveLog<T>
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `initial` | `readonly T[]` | Missing @param initial JSDoc. |
| `options` | `ReactiveLogOptions` | Missing @param options JSDoc. |

## Returns

`ReactiveLog<T>`

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@returns`.
- Missing `@param initial`.
- Missing `@param options`.
- Missing `@category`.
- Missing `@example`.

## Source

`packages/ts/src/graph/data-structures/reactive-log.ts`
