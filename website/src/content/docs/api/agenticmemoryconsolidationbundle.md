---
title: "agenticMemoryConsolidationBundle()"
description: "Creates an agentic memory consolidation bundle."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Creates an agentic memory consolidation bundle.

## Import

```ts
import { agenticMemoryConsolidationBundle } from "@graphrefly/ts/solutions";
```

## Signature

```ts
function agenticMemoryConsolidationBundle(
	graph: Graph,
	opts: AgenticMemoryConsolidationBundleOptions<T>,
): AgenticMemoryConsolidationBundle<T>
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `graph` | `Graph` | Graph that owns the created nodes or projector. |
| `opts` | `AgenticMemoryConsolidationBundleOptions<T>` | Options that configure the helper. |

## Returns

A bundle of graph-visible nodes for the recipe.

## Example

```ts
import { agenticMemoryConsolidationBundle } from "@graphrefly/ts/solutions/agentic-memory";
```

## Remarks

- D151: each input DATA is one complete identity/revision/digest/source-refs occurrence. Policy and prior records travel in its value, never independent latest-value dependencies. Outputs preserve that identity; replay conflicts and retention overflow fail closed. The fixed recipe topology is retained for its graph lifetime; input snapshots are bounded by maxOccurrences, not a claim that one protocol wave is one business occurrence.

## Source

`packages/ts/src/solutions/agentic-memory/consolidation.ts`
