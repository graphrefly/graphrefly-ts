---
title: "Dispatcher"
description: "First-class dispatcher (D21). Owns pools; graph binds to one (default = process-global, D26 — the only global singleton). Pool trait is pluggable for WorkerPool/RemotePool (D20)."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

First-class dispatcher (D21). Owns pools; graph binds to one (default = process-global,
D26 — the only global singleton). Pool trait is pluggable for WorkerPool/RemotePool (D20).

## Import

```ts
import { Dispatcher } from "@graphrefly/ts/core";
```

## Signature

```ts
class Dispatcher
```

## Example

```ts
import { Dispatcher } from "@graphrefly/ts";

const dispatcher = new Dispatcher();
```

## Source

`packages/ts/src/dispatcher/index.ts`
