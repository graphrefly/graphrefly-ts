---
title: "agenticMemoryCommittedFactReadMaterializationBundle()"
description: "Create a graph-visible D591 read materialization bundle over explicit read-result DATA. The bundle performs no fact-log read and owns no backend adapter. Hosts supply read results as ordinary DATA; the bundle projects records, priorEvidence, evidence, status, issues, audit, and cursor DATA for explicit caller wiring."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Create a graph-visible D591 read materialization bundle over explicit read-result DATA.

The bundle performs no fact-log read and owns no backend adapter. Hosts supply
read results as ordinary DATA; the bundle projects records, priorEvidence,
evidence, status, issues, audit, and cursor DATA for explicit caller wiring.

## Import

```ts
import { agenticMemoryCommittedFactReadMaterializationBundle } from "@graphrefly/ts/solutions";
```

## Signature

```ts
function agenticMemoryCommittedFactReadMaterializationBundle(
	graph: Graph,
	opts: AgenticMemoryCommittedFactReadMaterializationBundleOptions<TJson>,
): AgenticMemoryCommittedFactReadMaterializationBundle<TJson>
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `graph` | `Graph` | Graph that owns the created nodes. |
| `opts` | `AgenticMemoryCommittedFactReadMaterializationBundleOptions<TJson>` | Read-result input node and optional bundle name. |

## Returns

Projection and read-model nodes for D591 caller-wired re-entry DATA.

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@example`.

## Source

`packages/ts/src/solutions/agentic-memory/fact-log-read-materialization.ts`
