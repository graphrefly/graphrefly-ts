---
title: "createKeyedRateLimitOutcome()"
description: "Create one externally authoritative outcome without handwritten canonical encoding. The host assigns the outcome id after a valid authority decision. This helper owns no durable receipt, transaction, clock, evaluator, or admission."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Create one externally authoritative outcome without handwritten canonical encoding.

The host assigns the outcome id after a valid authority decision. This helper owns no durable
receipt, transaction, clock, evaluator, or admission.

## Import

```ts
import { createKeyedRateLimitOutcome } from "@graphrefly/ts/rate-limit";
```

## Signature

```ts
function createKeyedRateLimitOutcome(
	requestValue: unknown,
	opts: CreateKeyedRateLimitOutcomeOptions,
): KeyedRateLimitOutcome
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `requestValue` | `unknown` | The complete strict request decided by the host authority. |
| `opts` | `CreateKeyedRateLimitOutcomeOptions` | The authoritative result, outcome id, quota material, and provenance. |

## Returns

The immutable strict outcome for adapter delivery.

## Example

```ts
import { createKeyedRateLimitOutcome } from "@graphrefly/ts/rate-limit";
```

## Source

`packages/ts/src/rate-limit/keyed-rate-limit.ts`
