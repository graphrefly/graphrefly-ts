---
title: "blueprintToMermaid()"
description: "Render a portable D629/D630 GraphBlueprint directly as deterministic Mermaid source. Unlike {@link describeToMermaid}, this helper consumes only the versioned Blueprint evidence envelope. It never reads runtime values, status, or a live Graph."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Render a portable D629/D630 GraphBlueprint directly as deterministic Mermaid source.

Unlike {@link describeToMermaid}, this helper consumes only the versioned Blueprint evidence
envelope. It never reads runtime values, status, or a live Graph.

## Import

```ts
import { blueprintToMermaid } from "@graphrefly/ts/graph";
```

## Signature

```ts
function blueprintToMermaid(
	blueprint: GraphBlueprintEvidence,
	opts: GraphBlueprintToMermaidOptions = {},
): string
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `blueprint` | `GraphBlueprintEvidence` | Portable GraphBlueprint evidence. |
| `opts` | `GraphBlueprintToMermaidOptions` | Diagram direction. |

## Returns

Deterministic Mermaid flowchart source preserving subgraph groups.

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@example`.

## Source

`packages/ts/src/graph/render.ts`
