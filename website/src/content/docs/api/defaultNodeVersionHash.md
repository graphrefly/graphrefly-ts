---
title: "defaultNodeVersionHash()"
description: "Default synchronous D112 V1 node-version hash over strict canonical JSON UTF-8 bytes. When hashing a host value directly, encode first: `defaultNodeVersionHash(strictCanonicalJsonBytes(value))`."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Default synchronous D112 V1 node-version hash over strict canonical JSON UTF-8 bytes.

When hashing a host value directly, encode first:
`defaultNodeVersionHash(strictCanonicalJsonBytes(value))`.

## Import

```ts
import { defaultNodeVersionHash } from "@graphrefly/ts/core";
```

## Signature

```ts
function defaultNodeVersionHash(bytes: Uint8Array): string
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `bytes` | `Uint8Array` | Missing @param bytes JSDoc. |

## Returns

`string`

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@returns`.
- Missing `@param bytes`.
- Missing `@category`.
- Missing `@example`.

## Source

`packages/ts/src/node/versioning.ts`

