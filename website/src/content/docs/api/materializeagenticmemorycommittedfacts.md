---
title: "materializeAgenticMemoryCommittedFacts()"
description: "Deterministically materialize AgenticMemory re-entry DATA from committed facts. The input order is the committed fact stream order supplied by an explicit read/materialization boundary. This helper does not read storage, apply or admit records, mutate live graph truth, refresh subscribers, or create a graph commit barrier."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Deterministically materialize AgenticMemory re-entry DATA from committed facts.

The input order is the committed fact stream order supplied by an explicit
read/materialization boundary. This helper does not read storage, apply or
admit records, mutate live graph truth, refresh subscribers, or create a
graph commit barrier.

## Import

```ts
import { materializeAgenticMemoryCommittedFacts } from "@graphrefly/ts/solutions";
```

## Signature

```ts
function materializeAgenticMemoryCommittedFacts(
	facts: readonly AgenticMemoryCommittedFact<TJson>[],
	opts: {
		readonly records?: readonly AgenticMemoryRecord<TJson>[];
		readonly priorEvidence?: AgenticMemoryRecordApplicationPriorEvidence;
	} = {},
): AgenticMemoryCommittedFactMaterialization<TJson>
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `facts` | `readonly AgenticMemoryCommittedFact<TJson>[]` | Committed facts in stream order. |
| `opts` | `{ 		readonly records?: readonly AgenticMemoryRecord<TJson>[]; 		readonly priorEvidence?: AgenticMemoryRecordApplicationPriorEvidence; 	}` | Optional prefix materialization seed used by snapshot+tail equivalence. |

## Returns

Records/priorEvidence derived by library-owned materialization rules.

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@example`.

## Source

`packages/ts/src/solutions/agentic-memory/committed-fact-log.ts`
