---
title: "nodeProcessDriver()"
description: "Node.js process EnvironmentDriver for D130/D131 adapters. Import this from `@graphrefly/ts/sources/node` and install it with `EnvironmentDrivers.empty().withProcess(nodeProcessDriver())`. The child process is an adapter-boundary side effect; graph nodes only observe ordinary ProcessResult DATA or ERROR."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Node.js process EnvironmentDriver for D130/D131 adapters.

Import this from `@graphrefly/ts/sources/node` and install it with
`EnvironmentDrivers.empty().withProcess(nodeProcessDriver())`. The child process is an
adapter-boundary side effect; graph nodes only observe ordinary ProcessResult DATA or ERROR.

## Import

```ts
import { nodeProcessDriver } from "@graphrefly/ts/sources/node";
```

## Signature

```ts
function nodeProcessDriver(opts: NodeProcessDriverOptions = {}): LocalProcessDriver
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `opts` | `NodeProcessDriverOptions` | Options that configure the helper. |

## Returns

A `LocalProcessDriver` value.

## Example

```ts
import { nodeProcessDriver } from "@graphrefly/ts/sources/node";
```

## Source

`packages/ts/src/sources/node.ts`
