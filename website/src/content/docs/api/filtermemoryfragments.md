---
title: "filterMemoryFragments()"
description: "Filter and rank fragments by confidence desc, then transaction time desc."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Filter and rank fragments by confidence desc, then transaction time desc.

## Import

```ts
import { filterMemoryFragments } from "@graphrefly/ts/patterns";
```

## Signature

```ts
function filterMemoryFragments(
	fragments: Iterable<MemoryFragment<T>>,
	query: MemoryQuery = {},
): readonly MemoryFragment<T>[]
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `fragments` | `Iterable<MemoryFragment<T>>` | Missing @param fragments JSDoc. |
| `query` | `MemoryQuery` | Missing @param query JSDoc. |

## Returns

`readonly MemoryFragment<T>[]`

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@returns`.
- Missing `@param fragments`.
- Missing `@param query`.
- Missing `@category`.
- Missing `@example`.

## Source

`packages/ts/src/patterns/semantic-memory.ts`
