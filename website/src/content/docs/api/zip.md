---
title: "zip()"
description: "zip: combine one value from EACH dep, in lockstep, into a tuple — buffering per-dep queues until every dep has at least one queued value, then shifting one from each. `partial:true` + `ctx.state` queues. Completes as soon as ANY dep is terminal AND its queue is empty (no further tuple can form) — `completeWhenDepsComplete:false + terminalAsRealInput:true`. Any dep ERROR propagates (default errorWhenDepsError)."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

zip: combine one value from EACH dep, in lockstep, into a tuple — buffering per-dep queues until
every dep has at least one queued value, then shifting one from each. `partial:true` +
`ctx.state` queues. Completes as soon as ANY dep is terminal AND its queue is empty (no further
tuple can form) — `completeWhenDepsComplete:false + terminalAsRealInput:true`. Any dep ERROR
propagates (default errorWhenDepsError).

## Import

```ts
import { zip } from "@graphrefly/ts";
```

## Signature

```ts
function zip(): Operator<unknown, T>
```

## Returns

A `Operator<unknown, T>` value.

## Example

```ts
import { zip } from "@graphrefly/ts";
```

## Source

`packages/ts/src/graph/combinators.ts`
