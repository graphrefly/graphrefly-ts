---
title: "evaluateSlidingWindowRateLimitTransition()"
description: "Evaluate one exact D650 sliding-window-v1 successful-consumption ledger transition. See {@link evaluateFixedWindowRateLimitTransition} for the mandatory receipt-first durable host ordering. This calculator is synchronous, pure, host-side, and never approximates or evicts an active ledger entry to admit work."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Evaluate one exact D650 sliding-window-v1 successful-consumption ledger transition.

See {@link evaluateFixedWindowRateLimitTransition} for the mandatory receipt-first durable host
ordering. This calculator is synchronous, pure, host-side, and never approximates or evicts an
active ledger entry to admit work.

## Import

```ts
import { evaluateSlidingWindowRateLimitTransition } from "@graphrefly/ts/rate-limit";
```

## Signature

```ts
function evaluateSlidingWindowRateLimitTransition(value: unknown): SlidingWindowRateLimitTransition
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `value` | `unknown` | Strict sliding-window request, policy, state, and supplied observation input. |

## Returns

One immutable ready transition with the exact bounded ledger `nextState`.

## Example

```ts
import { evaluateSlidingWindowRateLimitTransition } from "@graphrefly/ts/rate-limit";
```

## Source

`packages/ts/src/rate-limit/keyed-rate-limit-algorithms.ts`
