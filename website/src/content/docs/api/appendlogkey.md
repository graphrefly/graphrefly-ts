---
title: "appendLogKey()"
description: "Build the deterministic storage key for an append-log sequence number."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Build the deterministic storage key for an append-log sequence number.

## Import

```ts
import { appendLogKey } from "@graphrefly/ts/storage";
```

## Signature

```ts
function appendLogKey(
	prefix: string,
	seq: number,
): string
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `prefix` | `string` | Missing @param prefix JSDoc. |
| `seq` | `number` | Missing @param seq JSDoc. |

## Returns

`string`

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@returns`.
- Missing `@param prefix`.
- Missing `@param seq`.
- Missing `@category`.
- Missing `@example`.

## Source

`packages/ts/src/storage/append-log.ts`
