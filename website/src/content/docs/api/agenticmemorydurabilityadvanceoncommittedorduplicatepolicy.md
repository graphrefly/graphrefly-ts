---
title: "agenticMemoryDurabilityAdvanceOnCommittedOrDuplicatePolicy()"
description: "D590 explicit policy for workflows that may advance after fact-log durability. The returned policy only allows `committed` and `duplicate` fact-log commit statuses. It does not turn a durability result into an application acknowledgement, live graph truth, record mutation authority, hot hydration, or a graph wave/batch commit barrier."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

D590 explicit policy for workflows that may advance after fact-log durability.

The returned policy only allows `committed` and `duplicate` fact-log commit
statuses. It does not turn a durability result into an application
acknowledgement, live graph truth, record mutation authority, hot hydration,
or a graph wave/batch commit barrier.

## Import

```ts
import { agenticMemoryDurabilityAdvanceOnCommittedOrDuplicatePolicy } from "@graphrefly/ts/solutions";
```

## Signature

```ts
function agenticMemoryDurabilityAdvanceOnCommittedOrDuplicatePolicy(policyId = AGENTIC_MEMORY_DURABILITY_ADVANCE_ON_COMMITTED_OR_DUPLICATE_POLICY): AgenticMemoryDurabilityDownstreamAdvancePolicy
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `policyId` | `unknown` | Optional caller-visible policy identifier. |

## Returns

A D590 downstream-advance policy.

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@example`.

## Source

`packages/ts/src/solutions/agentic-memory/durable-result.ts`
