---
title: "useNodeInput()"
description: "Bind a writable GraphReFly node as `[value, setValue]`. The setter identity is stable for a stable node identity, and writes through the node's reactive DATA boundary rather than a presentation-owned trigger."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Bind a writable GraphReFly node as `[value, setValue]`.

The setter identity is stable for a stable node identity, and writes through
the node's reactive DATA boundary rather than a presentation-owned trigger.

## Import

```ts
import { useNodeInput } from "@graphrefly/ts/adapters/react";
```

## Signature

```ts
function useNodeInput(node: WritableNode<T>): readonly [T | undefined, (value: T) => void]
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `node` | `WritableNode<T>` | Node to observe, adapt, or connect. |

## Returns

A `readonly [T | undefined, (value: T) => void]` value.

## Example

```ts
import { useNodeInput } from "@graphrefly/ts/adapters/react";
```

## Source

`packages/ts/src/adapters/react.ts`
