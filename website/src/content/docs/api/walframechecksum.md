---
title: "walFrameChecksum()"
description: "Hash the canonical WAL frame body, excluding the checksum field itself."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Hash the canonical WAL frame body, excluding the checksum field itself.

## Import

```ts
import { walFrameChecksum } from "@graphrefly/ts/storage";
```

## Signature

```ts
function walFrameChecksum(body: WalFrameBody<T>): Promise<string>
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `body` | `WalFrameBody<T>` | Missing @param body JSDoc. |

## Returns

`Promise<string>`

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@returns`.
- Missing `@param body`.
- Missing `@category`.
- Missing `@example`.

## Source

`packages/ts/src/storage/wal.ts`
