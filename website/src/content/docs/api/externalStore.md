---
title: "externalStore()"
description: "Build the tiny shape React's `useSyncExternalStore` expects, without importing React."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Build the tiny shape React's `useSyncExternalStore` expects, without importing React.

## Import

```ts
import { externalStore } from "@graphrefly/ts/adapters";
```

## Signature

```ts
function externalStore(
	node: Node<T>,
	opts: Pick<SubscribeValuesOptions<T>, "getSnapshot" | "onError" | "onComplete"> = {},
): ExternalStore<T>
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `node` | `Node<T>` | Missing @param node JSDoc. |
| `opts` | `Pick<SubscribeValuesOptions<T>, "getSnapshot" \| "onError" \| "onComplete">` | Missing @param opts JSDoc. |

## Returns

`ExternalStore<T>`

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@returns`.
- Missing `@param node`.
- Missing `@param opts`.
- Missing `@category`.
- Missing `@example`.

## Source

`packages/ts/src/adapters/store.ts`

