---
title: "authenticatedWssManagedCloudTransport()"
description: "Concrete authenticated worker-initiated WSS JSON adapter with per-epoch duplicate suppression."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Concrete authenticated worker-initiated WSS JSON adapter with per-epoch duplicate suppression.

## Import

```ts
import { authenticatedWssManagedCloudTransport } from "@graphrefly/ts/executors/managed-cloud-postgresql";
```

## Signature

```ts
function authenticatedWssManagedCloudTransport(
	factory: ManagedCloudPostgresqlWssFactory,
	verifier: ManagedCloudPostgresqlWssVerifier,
): ManagedCloudPostgresqlTransportDriver
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `factory` | `ManagedCloudPostgresqlWssFactory` | Missing @param factory JSDoc. |
| `verifier` | `ManagedCloudPostgresqlWssVerifier` | Missing @param verifier JSDoc. |

## Returns

`ManagedCloudPostgresqlTransportDriver`

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@returns`.
- Missing `@param factory`.
- Missing `@param verifier`.
- Missing `@category`.
- Missing `@example`.

## Source

`packages/ts/src/executors/managed-cloud-postgresql.ts`
