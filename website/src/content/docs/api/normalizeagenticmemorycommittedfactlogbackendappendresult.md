---
title: "normalizeAgenticMemoryCommittedFactLogBackendAppendResult()"
description: "Normalize a D592 backend append output into D589 commit result DATA. The normalized result describes committed fact-log persistence only. Backend diagnostics are preserved as issues/audit DATA and never become application acknowledgement, live graph truth, record mutation authority, hot hydration, restore, or a graph commit barrier."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Normalize a D592 backend append output into D589 commit result DATA.

The normalized result describes committed fact-log persistence only. Backend
diagnostics are preserved as issues/audit DATA and never become application
acknowledgement, live graph truth, record mutation authority, hot hydration,
restore, or a graph commit barrier.

## Import

```ts
import { normalizeAgenticMemoryCommittedFactLogBackendAppendResult } from "@graphrefly/ts/solutions";
```

## Signature

```ts
function normalizeAgenticMemoryCommittedFactLogBackendAppendResult(
	output: unknown,
	opts: {
		readonly fallbackCursor?: AgenticMemoryCommittedFactCursor;
		readonly expectedFacts?: number;
	} = {},
): AgenticMemoryFactCommitResult
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `output` | `unknown` | Backend-supplied append output. |
| `opts` | `{ 		readonly fallbackCursor?: AgenticMemoryCommittedFactCursor; 		readonly expectedFacts?: number; 	}` | Optional fallback fact-stream cursor for malformed outputs. |

## Returns

A D589 `AgenticMemoryFactCommitResult`.

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@example`.

## Source

`packages/ts/src/solutions/agentic-memory/committed-fact-log-backend.ts`
