---
title: "agenticMemoryDurabilityResultMayAdvance()"
description: "Evaluate the D590 explicit downstream-advance policy. Only `committed` and `duplicate` can return true, and only when the supplied policy explicitly allows that status."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Evaluate the D590 explicit downstream-advance policy.

Only `committed` and `duplicate` can return true, and only when the supplied
policy explicitly allows that status.

## Import

```ts
import { agenticMemoryDurabilityResultMayAdvance } from "@graphrefly/ts/solutions";
```

## Signature

```ts
function agenticMemoryDurabilityResultMayAdvance(
	status: AgenticMemoryFactCommitStatus,
	policy: AgenticMemoryDurabilityDownstreamAdvancePolicy,
): boolean
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `status` | `AgenticMemoryFactCommitStatus` | D589 fact-log commit status. |
| `policy` | `AgenticMemoryDurabilityDownstreamAdvancePolicy` | Explicit downstream-advance policy. |

## Returns

Whether a downstream workflow may advance by the supplied policy.

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@example`.

## Source

`packages/ts/src/solutions/agentic-memory/durable-result.ts`
