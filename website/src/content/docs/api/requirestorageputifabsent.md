---
title: "requireStoragePutIfAbsent()"
description: "Require D85 conditional-create support and produce a clear adapter error when absent."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Require D85 conditional-create support and produce a clear adapter error when absent.

## Import

```ts
import { requireStoragePutIfAbsent } from "@graphrefly/ts/storage";
```

## Signature

```ts
function requireStoragePutIfAbsent(
	backend: StorageBackend,
	label = "storage backend",
): PutIfAbsentStorageBackend
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `backend` | `StorageBackend` | Missing @param backend JSDoc. |
| `label` | `unknown` | Missing @param label JSDoc. |

## Returns

`PutIfAbsentStorageBackend`

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@returns`.
- Missing `@param backend`.
- Missing `@param label`.
- Missing `@category`.
- Missing `@example`.

## Source

`packages/ts/src/storage/backend.ts`
