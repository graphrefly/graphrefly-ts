---
title: "layoutNextLine()"
description: "Lay out one line from a cursor, returning the next cursor for continuation."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Lay out one line from a cursor, returning the next cursor for continuation.

## Import

```ts
import { layoutNextLine } from "@graphrefly/ts/solutions/reactive-layout";
```

## Signature

```ts
function layoutNextLine(
	segments: readonly PreparedSegment[],
	cursor: LayoutCursor,
	slotWidth: number,
	ctx?: LayoutNextLineContext,
): LayoutNextLineResult | null
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `segments` | `readonly PreparedSegment[]` | Missing @param segments JSDoc. |
| `cursor` | `LayoutCursor` | Missing @param cursor JSDoc. |
| `slotWidth` | `number` | Missing @param slotWidth JSDoc. |
| `ctx` | `LayoutNextLineContext` | Missing @param ctx JSDoc. |

## Returns

`LayoutNextLineResult | null`

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@returns`.
- Missing `@param segments`.
- Missing `@param cursor`.
- Missing `@param slotWidth`.
- Missing `@param ctx`.
- Missing `@category`.
- Missing `@example`.

## Source

`packages/ts/src/solutions/reactive-layout/line.ts`
