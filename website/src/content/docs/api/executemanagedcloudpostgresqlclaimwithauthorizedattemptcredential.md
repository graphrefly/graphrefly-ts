---
title: "executeManagedCloudPostgresqlClaimWithAuthorizedAttemptCredential()"
description: "D618 managed-cloud worker entry: current authorization/revocation is checked immediately before the host-private SPIRE/OpenBao attempt credential lifecycle."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

D618 managed-cloud worker entry: current authorization/revocation is checked
immediately before the host-private SPIRE/OpenBao attempt credential lifecycle.

## Import

```ts
import { executeManagedCloudPostgresqlClaimWithAuthorizedAttemptCredential } from "@graphrefly/ts/executors/managed-cloud-postgresql";
```

## Signature

```ts
function executeManagedCloudPostgresqlClaimWithAuthorizedAttemptCredential(
	claim: Extract<ManagedCloudPostgresqlControlMessage, { kind: "claim-granted" }>,
	driver: ManagedCloudPostgresqlWorkerDriver,
	settlementId: string,
	messageId: string,
	signal: AbortSignal,
	now: () => number,
	credentialDriver: ManagedCloudPostgresqlAttemptCredentialDriver,
	authorizationDriver: Pick<
		ManagedCloudPostgresqlAuthorizationRecheckDriver,
		"compatibility" | "authorizeCredentialIssuance"
	>,
): Promise<Extract<ManagedCloudPostgresqlWorkerMessage, { kind: "settle" }>>
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `claim` | `Extract<ManagedCloudPostgresqlControlMessage, { kind: "claim-granted" }>` | Missing @param claim JSDoc. |
| `driver` | `ManagedCloudPostgresqlWorkerDriver` | Missing @param driver JSDoc. |
| `settlementId` | `string` | Missing @param settlementId JSDoc. |
| `messageId` | `string` | Missing @param messageId JSDoc. |
| `signal` | `AbortSignal` | Missing @param signal JSDoc. |
| `now` | `() => number` | Missing @param now JSDoc. |
| `credentialDriver` | `ManagedCloudPostgresqlAttemptCredentialDriver` | Missing @param credentialDriver JSDoc. |
| `authorizationDriver` | `Pick< 		ManagedCloudPostgresqlAuthorizationRecheckDriver, 		"compatibility" \| "authorizeCredentialIssuance" 	>` | Missing @param authorizationDriver JSDoc. |

## Returns

`Promise<Extract<ManagedCloudPostgresqlWorkerMessage, { kind: "settle" }>>`

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@returns`.
- Missing `@param claim`.
- Missing `@param driver`.
- Missing `@param settlementId`.
- Missing `@param messageId`.
- Missing `@param signal`.
- Missing `@param now`.
- Missing `@param credentialDriver`.
- Missing `@param authorizationDriver`.
- Missing `@category`.
- Missing `@example`.

## Source

`packages/ts/src/executors/managed-cloud-postgresql.ts`
