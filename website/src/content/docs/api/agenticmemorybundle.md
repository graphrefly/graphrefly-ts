---
title: "agenticMemoryBundle()"
description: "Creates a lower-level agentic memory projection/retrieval bundle. This bundle does not implement D643/D789 record-use authorization. A governed consumer must compose `agenticMemoryRecordUseGateBundle` first, select the exact authorized occurrence matching its query, and pass only that occurrence's `allowedRecords.value` as this bundle's `records` input. Do not erase identity across multiple uses, fill from an unrelated latest value, or add a raw-record edge."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Creates a lower-level agentic memory projection/retrieval bundle.

This bundle does not implement D643/D789 record-use authorization. A governed
consumer must compose `agenticMemoryRecordUseGateBundle` first, select the
exact authorized occurrence matching its query, and pass only that occurrence's
`allowedRecords.value` as this bundle's `records` input. Do not erase identity
across multiple uses, fill from an unrelated latest value, or add a raw-record edge.

## Import

```ts
import { agenticMemoryBundle } from "@graphrefly/ts/solutions";
```

## Signature

```ts
function agenticMemoryBundle(
	graph: Graph,
	opts: AgenticMemoryBundleOptions<T>,
): AgenticMemoryBundle<T>
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `graph` | `Graph` | Graph that owns the created nodes or projector. |
| `opts` | `AgenticMemoryBundleOptions<T>` | Options that configure the helper. |

## Returns

A bundle of graph-visible nodes for the recipe.

## Example

```ts
import { agenticMemoryBundle } from "@graphrefly/ts/solutions/agentic-memory";
```

## Source

`packages/ts/src/solutions/agentic-memory/bundle.ts`
