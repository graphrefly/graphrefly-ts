---
title: "agenticMemoryRecordApplicationEvidenceFactsBundle()"
description: "Creates a graph-visible D584 decision-to-evidence projection bundle. The output `evidenceFacts` are append material for a later evaluation or an explicit host/storage/event boundary. This bundle intentionally does not connect that output to an application bundle's current `priorEvidence`."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Creates a graph-visible D584 decision-to-evidence projection bundle.

The output `evidenceFacts` are append material for a later evaluation or an
explicit host/storage/event boundary. This bundle intentionally does not
connect that output to an application bundle's current `priorEvidence`.

## Import

```ts
import { agenticMemoryRecordApplicationEvidenceFactsBundle } from "@graphrefly/ts/solutions";
```

## Signature

```ts
function agenticMemoryRecordApplicationEvidenceFactsBundle(
	graph: Graph,
	opts: AgenticMemoryRecordApplicationEvidenceFactsBundleOptions<T>,
): AgenticMemoryRecordApplicationEvidenceFactsBundle<T>
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `graph` | `Graph` | Graph that owns the created nodes. |
| `opts` | `AgenticMemoryRecordApplicationEvidenceFactsBundleOptions<T>` | Application decisions input node and optional bundle name. |

## Returns

Projection node plus evidenceFacts/priorEvidence/status/issues/audit/cursor nodes.

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@example`.

## Source

`packages/ts/src/solutions/agentic-memory/application-history.ts`
