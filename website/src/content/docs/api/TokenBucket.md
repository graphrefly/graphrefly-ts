---
title: "TokenBucket()"
description: "Token-bucket meter (capacity + refill rate per second). Use with  or custom gates."
---

Token-bucket meter (capacity + refill rate per second). Use with  or custom gates.

## Signature

```ts
class TokenBucket
```
