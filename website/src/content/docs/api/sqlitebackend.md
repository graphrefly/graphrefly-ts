---
title: "sqliteBackend()"
description: "Optional Node SQLite byte backend (D106 passive storage). Uses `node:sqlite` when the current Node runtime provides it; otherwise throws a clear runtime error at construction time. The caller owns `close()`."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Optional Node SQLite byte backend (D106 passive storage).

Uses `node:sqlite` when the current Node runtime provides it; otherwise throws a clear
runtime error at construction time. The caller owns `close()`.

## Import

```ts
import { sqliteBackend } from "@graphrefly/ts/storage/node";
```

## Signature

```ts
function sqliteBackend(
	path: string,
	opts: SqliteBackendOptions = {},
): ClosableStorageBackend
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `path` | `string` | Missing @param path JSDoc. |
| `opts` | `SqliteBackendOptions` | Missing @param opts JSDoc. |

## Returns

`ClosableStorageBackend`

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@returns`.
- Missing `@param path`.
- Missing `@param opts`.
- Missing `@category`.
- Missing `@example`.

## Source

`packages/ts/src/storage/node.ts`
