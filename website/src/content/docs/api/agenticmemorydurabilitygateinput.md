---
title: "agenticMemoryDurabilityGateInput()"
description: "Pair a canonical D589 fact batch with its explicit fact-log commit result. The graph bundle consumes this paired DATA shape so it never correlates a stale result from one batch with another batch. The pair is still only fact-log persistence progress, not an application acknowledgement or graph commit barrier."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Pair a canonical D589 fact batch with its explicit fact-log commit result.

The graph bundle consumes this paired DATA shape so it never correlates a
stale result from one batch with another batch. The pair is still only
fact-log persistence progress, not an application acknowledgement or graph
commit barrier.

## Import

```ts
import { agenticMemoryDurabilityGateInput } from "@graphrefly/ts/solutions";
```

## Signature

```ts
function agenticMemoryDurabilityGateInput(
	batch: AgenticMemoryCommittedFactBatch<TJson>,
	commitResult: AgenticMemoryFactCommitResult,
): AgenticMemoryDurabilityGateInput<TJson>
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `batch` | `AgenticMemoryCommittedFactBatch<TJson>` | Canonical D589 committed fact batch. |
| `commitResult` | `AgenticMemoryFactCommitResult` | D589 fact-log commit result for this batch attempt. |

## Returns

A D590 durability-gate input DATA object.

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@example`.

## Source

`packages/ts/src/solutions/agentic-memory/durable-result.ts`
