---
title: "createAgenticMemoryRecordUseDecision()"
description: "Create one strict external decision pinned to an exact current request and record."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Create one strict external decision pinned to an exact current request and record.

## Import

```ts
import { createAgenticMemoryRecordUseDecision } from "@graphrefly/ts/solutions/agentic-memory";
```

## Signature

```ts
function createAgenticMemoryRecordUseDecision(
	request: AgenticMemoryRecordUseRequest,
	record: AgenticMemoryRecord<TJson>,
	options: AgenticMemoryRecordUseDecisionOptions,
): AgenticMemoryRecordUseDecision
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `request` | `AgenticMemoryRecordUseRequest` | Exact use request authorized or denied by the external authority. |
| `record` | `AgenticMemoryRecord<TJson>` | Exact current record covered by the decision. |
| `options` | `AgenticMemoryRecordUseDecisionOptions` | External decision id and allowed/denied state. |

## Returns

An immutable strict v1 decision.

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@example`.

## Source

`packages/ts/src/solutions/agentic-memory/record-use.ts`
