---
title: "assertKeyedRateLimitRequest()"
description: "Assert and deeply normalize one strict D648 keyed rate-limit request. Unknown fields, accessors, functions, BigInt, non-plain objects, unsafe numbers, and other non-strict-JSON material fail instead of being ignored."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Assert and deeply normalize one strict D648 keyed rate-limit request.

Unknown fields, accessors, functions, BigInt, non-plain objects, unsafe numbers, and other
non-strict-JSON material fail instead of being ignored.

## Import

```ts
import { assertKeyedRateLimitRequest } from "@graphrefly/ts/rate-limit";
```

## Signature

```ts
function assertKeyedRateLimitRequest(value: unknown): KeyedRateLimitRequest
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `value` | `unknown` | Candidate complete request material. |

## Returns

The immutable normalized strict request.

## Example

```ts
import { assertKeyedRateLimitRequest } from "@graphrefly/ts/rate-limit";
```

## Source

`packages/ts/src/rate-limit/keyed-rate-limit.ts`
