---
title: "keyedRateLimitAdmissionBundle()"
description: "Build the D648/D649 bounded fact-stream admission gate. `opts.requests` contains authority-admitted requests, not arbitrary raw requests. When using {@link attachKeyedRateLimitAuthority}, use its `authorityRequests` projection or composed `admission` bundle. The graph validates correlation and currentness only. The host authority owns durable quota, clocks, algorithms, and atomic consume. Replay suppression is exact while a completed correlation remains retained; cross-restart business-effect idempotency remains executor/application-owned."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Build the D648/D649 bounded fact-stream admission gate.

`opts.requests` contains authority-admitted requests, not arbitrary raw requests. When using
{@link attachKeyedRateLimitAuthority}, use its `authorityRequests` projection or composed
`admission` bundle. The graph validates correlation and currentness only. The host authority
owns durable quota, clocks, algorithms, and atomic consume. Replay suppression is exact while a
completed correlation remains retained; cross-restart business-effect idempotency remains
executor/application-owned.

## Import

```ts
import { keyedRateLimitAdmissionBundle } from "@graphrefly/ts/rate-limit";
```

## Signature

```ts
function keyedRateLimitAdmissionBundle(
	graph: Graph,
	opts: KeyedRateLimitAdmissionBundleOptions,
): KeyedRateLimitAdmissionBundle
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `graph` | `Graph` | Graph that owns the declared correlation and projection nodes. |
| `opts` | `KeyedRateLimitAdmissionBundleOptions` | One bounded authority-admitted correlation lane and retention bounds. |

## Returns

Graph-visible admissions, denials, status, issues, audit, and cursor projections.

## Example

```ts
import { keyedRateLimitAdmissionBundle } from "@graphrefly/ts/rate-limit";
```

## Source

`packages/ts/src/rate-limit/keyed-rate-limit.ts`
