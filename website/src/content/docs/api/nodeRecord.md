---
title: "nodeRecord()"
description: "Read a keyed record of nodes as a Svelte readable store. `factory` must have stable identity. Recreating it during component churn rebuilds the record subscriptions."
---

<!-- generated by website/scripts/gen-api-docs.mjs; do not edit -->

Read a keyed record of nodes as a Svelte readable store.

`factory` must have stable identity. Recreating it during component churn
rebuilds the record subscriptions.

## Import

```ts
import { nodeRecord } from "@graphrefly/ts/adapters/svelte";
```

## Signature

```ts
function nodeRecord(
	keysNode: Node<readonly K[]>,
	factory: (key: K) => { [P in keyof R]: Node<R[P]> },
): Readable<Record<K, R>>
```

## Parameters

| Parameter | Type | Description |
|---|---|---|
| `keysNode` | `Node<readonly K[]>` | Missing @param keysNode JSDoc. |
| `factory` | `(key: K) => { [P in keyof R]: Node<R[P]> }` | Missing @param factory JSDoc. |

## Returns

`Readable<Record<K, R>>`

## Documentation Status

This page was generated from source JSDoc, but the structured API tags are incomplete:

- Missing `@returns`.
- Missing `@param keysNode`.
- Missing `@param factory`.
- Missing `@category`.
- Missing `@example`.

## Source

`packages/ts/src/adapters/svelte.ts`

