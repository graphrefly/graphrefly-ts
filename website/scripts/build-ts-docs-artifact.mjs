#!/usr/bin/env node

import { cpSync, existsSync, mkdirSync, readFileSync, rmSync, writeFileSync } from "node:fs";
import path from "node:path";
import { fileURLToPath, pathToFileURL } from "node:url";

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const websiteRoot = path.resolve(__dirname, "..");
const repoRoot = path.resolve(websiteRoot, "..");
const apiDir = path.join(websiteRoot, "src", "content", "docs", "api");
const sidebarPath = path.join(websiteRoot, "src", "generated", "api-sidebar.mjs");
const distRoot = path.join(websiteRoot, "dist");
const basePath = normalizeBasePath(process.env.TS_DOCS_BASE_PATH ?? "/");
const routeRoot = basePath === "/" ? distRoot : path.join(distRoot, basePath.replace(/^\/|\/$/g, ""));
const customDomain = process.env.TS_DOCS_CUSTOM_DOMAIN ?? "ts.graphrefly.dev";

function normalizeBasePath(value) {
	const withLeading = value.startsWith("/") ? value : `/${value}`;
	return withLeading.endsWith("/") ? withLeading : `${withLeading}/`;
}

function escapeHtml(value) {
	return String(value)
		.replaceAll("&", "&amp;")
		.replaceAll("<", "&lt;")
		.replaceAll(">", "&gt;")
		.replaceAll('"', "&quot;");
}

function escapeAttr(value) {
	return escapeHtml(value).replaceAll("'", "&#39;");
}

function inlineMarkdown(value) {
	return escapeHtml(value)
		.replace(/\*\*([^*]+)\*\*/g, "<strong>$1</strong>")
		.replace(/`([^`]+)`/g, "<code>$1</code>");
}

function slugify(value) {
	return String(value)
		.toLowerCase()
		.replace(/[^a-z0-9]+/g, "-")
		.replace(/^-|-$/g, "");
}

function parseFrontmatter(markdown) {
	const match = markdown.match(/^---\n([\s\S]*?)\n---\n([\s\S]*)$/);
	if (!match) return { frontmatter: {}, body: markdown };
	const frontmatter = {};
	for (const line of match[1].split(/\r?\n/)) {
		const separator = line.indexOf(":");
		if (separator === -1) continue;
		const key = line.slice(0, separator).trim();
		const raw = line.slice(separator + 1).trim();
		try {
			frontmatter[key] = JSON.parse(raw);
		} catch {
			frontmatter[key] = raw.replace(/^"|"$/g, "");
		}
	}
	return { frontmatter, body: match[2] };
}

function flushParagraph(parts, lines) {
	if (parts.length === 0) return;
	lines.push(`<p>${inlineMarkdown(parts.join(" "))}</p>`);
	parts.length = 0;
}

function flushList(items, lines) {
	if (items.length === 0) return;
	lines.push(`<ul>${items.map((item) => `<li>${inlineMarkdown(item)}</li>`).join("")}</ul>`);
	items.length = 0;
}

function splitTableRow(row) {
	const cells = [];
	let cell = "";
	const trimmed = row.trim().replace(/^\||\|$/g, "");
	for (let index = 0; index < trimmed.length; index += 1) {
		const char = trimmed[index];
		if (char === "\\" && trimmed[index + 1] === "|") {
			cell += "|";
			index += 1;
			continue;
		}
		if (char === "|") {
			cells.push(cell.trim());
			cell = "";
			continue;
		}
		cell += char;
	}
	cells.push(cell.trim());
	return cells;
}

function renderTable(rows) {
	const parsed = rows.map(splitTableRow);
	const [head, , ...body] = parsed;
	return [
		"<table>",
		"<thead><tr>",
		...head.map((cell) => `<th>${inlineMarkdown(cell)}</th>`),
		"</tr></thead>",
		"<tbody>",
		...body.flatMap((row) => [
			"<tr>",
			...row.map((cell) => `<td>${inlineMarkdown(cell)}</td>`),
			"</tr>",
		]),
		"</tbody>",
		"</table>",
	].join("");
}

function renderMarkdown(markdown) {
	const lines = [];
	const paragraph = [];
	const list = [];
	const source = markdown
		.replace(/<!-- generated by website\/scripts\/gen-api-docs\.mjs; do not edit -->\n?/g, "")
		.trim()
		.split(/\r?\n/);
	let code = null;
	let table = null;

	for (const line of source) {
		if (line.startsWith("```")) {
			flushParagraph(paragraph, lines);
			flushList(list, lines);
			if (table) {
				lines.push(renderTable(table));
				table = null;
			}
			if (code) {
				lines.push(`<pre><code>${escapeHtml(code.lines.join("\n"))}</code></pre>`);
				code = null;
			} else {
				code = { lang: line.slice(3).trim(), lines: [] };
			}
			continue;
		}
		if (code) {
			code.lines.push(line);
			continue;
		}
		if (/^\|.*\|$/.test(line.trim())) {
			flushParagraph(paragraph, lines);
			flushList(list, lines);
			table ??= [];
			table.push(line);
			continue;
		}
		if (table) {
			lines.push(renderTable(table));
			table = null;
		}
		if (line.startsWith("## ")) {
			flushParagraph(paragraph, lines);
			flushList(list, lines);
			const heading = line.slice(3).trim();
			lines.push(`<h2 id="${escapeAttr(slugify(heading))}">${escapeHtml(heading)}</h2>`);
			continue;
		}
		if (line.startsWith("- ")) {
			flushParagraph(paragraph, lines);
			list.push(line.slice(2).trim());
			continue;
		}
		if (line.trim() === "") {
			flushParagraph(paragraph, lines);
			flushList(list, lines);
			continue;
		}
		flushList(list, lines);
		paragraph.push(line.trim());
	}
	if (code) lines.push(`<pre><code>${escapeHtml(code.lines.join("\n"))}</code></pre>`);
	if (table) lines.push(renderTable(table));
	flushList(list, lines);
	flushParagraph(paragraph, lines);
	return lines.join("\n");
}

function extractToc(markdown) {
	return markdown
		.replace(/<!-- generated by website\/scripts\/gen-api-docs\.mjs; do not edit -->\n?/g, "")
		.split(/\r?\n/)
		.filter((line) => line.startsWith("## "))
		.map((line) => {
			const label = line.slice(3).trim();
			return { label, href: `#${slugify(label)}` };
		});
}

const primaryNav = [
	{ id: "api", label: "API", href: "/api/" },
	{ id: "solutions", label: "Solutions", href: "/solutions/" },
	{ id: "examples", label: "Examples", href: "/examples/" },
	{ id: "demos", label: "Demos", href: "/demos/" },
	{ id: "recipes", label: "Recipes", href: "/recipes/" },
	{ id: "integrations", label: "Integrations", href: "/integrations/" },
	{ id: "compare", label: "Compare", href: "/compare/" },
];

const externalNav = [
	{ label: "GitHub", href: "https://github.com/graphrefly/graphrefly-ts" },
	{ label: "npm", href: "https://www.npmjs.com/package/@graphrefly/ts" },
	{ label: "Packages", href: "https://graphrefly.dev/packages/" },
];

function siteLink(href) {
	if (/^https?:\/\//.test(href)) return href;
	if (href === "/") return basePath;
	return `${basePath}${href.replace(/^\//, "")}`;
}

function navLink(item, activeSection) {
	const className = item.id === activeSection ? ` class="active"` : "";
	return `<a${className} href="${siteLink(item.href)}">${escapeHtml(item.label)}</a>`;
}

let searchIndexJson = "[]";

function searchBox() {
	return `<div class="search" role="search">
      <label class="sr-only" for="site-search">Search TypeScript docs</label>
      <input id="site-search" type="search" autocomplete="off" placeholder="Search API, examples, recipes" />
      <div id="search-results" class="search-results" role="listbox" hidden></div>
    </div>`;
}

function searchScript() {
	return `<script>
const searchItems = ${searchIndexJson};
const searchInput = document.getElementById("site-search");
const searchResults = document.getElementById("search-results");
function htmlEscape(value) {
  return String(value).replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;");
}
function renderSearchResults(query) {
  const term = query.trim().toLowerCase();
  if (!term) {
    searchResults.hidden = true;
    searchResults.innerHTML = "";
    return;
  }
  const matches = searchItems.filter((item) => {
    const haystack = [item.title, item.section, item.description].join(" ").toLowerCase();
    return haystack.includes(term);
  }).slice(0, 8);
  searchResults.innerHTML = matches.length
    ? matches.map((item) => '<a role="option" href="' + htmlEscape(item.href) + '"><span>' + htmlEscape(item.section) + '</span><strong>' + htmlEscape(item.title) + '</strong><small>' + htmlEscape(item.description) + '</small></a>').join("")
    : '<p>No matches</p>';
  searchResults.hidden = false;
}
searchInput?.addEventListener("input", (event) => renderSearchResults(event.currentTarget.value));
document.addEventListener("click", (event) => {
  if (!event.target.closest(".search")) searchResults.hidden = true;
});
</script>`;
}

function pageShell({ title, description, body, sidebarHtml = "", rightSidebarHtml = "", activeSection = "" }) {
	const packageNav = primaryNav.map((item) => navLink(item, activeSection)).join("");
	const utilityNav = externalNav
		.map((item) => `<a href="${escapeAttr(item.href)}">${escapeHtml(item.label)}</a>`)
		.join("");
	const hasSidebar = sidebarHtml.trim().length > 0;
	const hasRightSidebar = rightSidebarHtml.trim().length > 0;
	const layoutClass = [
		"layout",
		hasSidebar ? "has-left" : "no-left",
		hasRightSidebar ? "has-right" : "no-right",
	].join(" ");
	return `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>${escapeHtml(title)} | @graphrefly/ts</title>
  <meta name="description" content="${escapeAttr(description)}" />
  <link rel="stylesheet" href="${basePath}assets/ts-docs.css" />
</head>
<body>
  <header class="topbar">
    <a class="brand" href="${basePath}"><span>GraphReFly</span><strong>TS</strong></a>
    ${searchBox()}
    <nav class="topbar-primary" aria-label="TypeScript docs">${packageNav}</nav>
    <nav class="topbar-utility" aria-label="Package links">${utilityNav}</nav>
  </header>
  <div class="${layoutClass}">
    ${hasSidebar ? `<aside class="sidebar" aria-label="API reference navigation">${sidebarHtml}</aside>` : ""}
    <main class="content">${body}</main>
    ${hasRightSidebar ? `<aside class="rightbar" aria-label="Page contents">${rightSidebarHtml}</aside>` : ""}
  </div>
  ${searchScript()}
</body>
</html>
`;
}

function sidebarLink(link) {
	const normalized = link.replace(/^\//, "").replace(/\/$/, "");
	return `${basePath}${normalized}/`;
}

function renderApiSidebar(sidebar, { activeSlug = "", openGroupLabel = "" } = {}) {
	const apiNav = sidebar
		.map((group) => {
			const hasActive = group.items.some((item) => item.link.replace(/^\/api\//, "").replace(/\/$/, "") === activeSlug);
			const items = group.items
				.map((item) => {
					const itemSlug = item.link.replace(/^\/api\//, "").replace(/\/$/, "");
					const className = itemSlug === activeSlug ? ` class="active"` : "";
					return `<li><a${className} href="${sidebarLink(item.link)}">${escapeHtml(item.label)}</a></li>`;
				})
				.join("");
			const open = hasActive || group.label === openGroupLabel ? " open" : "";
			return `<details${open}><summary>${escapeHtml(group.label)}<span>${group.items.length}</span></summary><ul>${items}</ul></details>`;
		})
		.join("\n");
	return `<section class="sidebar-panel api-tree"><p class="sidebar-kicker">API Reference</p>${apiNav}</section>`;
}

function apiGroupDescription(label) {
	const descriptions = new Map([
		["Core", "Build graph nodes, state, batching, and runtime primitives."],
		["Graph", "Create, inspect, restore, and describe graph topology."],
		["Operators", "Compose reactive values with mapping, filtering, merging, buffering, and timing helpers."],
		["Sources", "Connect events, timers, HTTP, sockets, processes, and host signals to graph inputs."],
		["Storage", "Use codecs, key-value tiers, append logs, content addressing, and persistence helpers."],
		["Adapters", "Bind GraphReFly to framework and host runtime edges."],
		["Solutions", "Use package-level bundles for layout, memory, work items, and orchestration."],
		["Patterns", "Start from higher-level composition patterns built from package primitives."],
		["Testing", "Exercise graph behavior with package test helpers."],
		["Scoring", "Normalize and compare score signals."],
		["Render", "Turn graph state into renderable views and text layouts."],
		["Messaging", "Work with graph-visible message and event surfaces."],
		["Memory", "Store, retrieve, and project graph-visible memory records."],
		["Orchestration", "Coordinate work queues, scheduling, and execution surfaces."],
		["CQRS", "Build command, event, projection, and work-queue flows."],
		["Cqrs", "Build command, event, projection, and work-queue flows."],
		["Data Structures", "Use graph-aware maps, collections, and supporting containers."],
		["Composition", "Package small graph units into reusable compositions."],
		["Executors", "Run tool providers, adapters, and execution queues."],
		["Inspection", "Inspect package boundaries and graph-visible runtime metadata."],
		["Json", "Encode, validate, and canonicalize strict JSON data."],
		["Reactive Layout", "Measure text, break lines, and arrange blocks through graph-visible layout helpers."],
		["Work Queue", "Submit, schedule, and settle graph-visible work."],
	]);
	return descriptions.get(label) ?? "Browse APIs in this package area.";
}

function sectionDescription(id) {
	const descriptions = new Map([
		["api", "Find package APIs by area, then open a symbol for signatures, parameters, return values, and examples."],
		["solutions", "Higher-level bundles for reactive layout, agentic memory, work-item orchestration, and capability admission."],
		["examples", "Small TypeScript entry points for state, derived values, effects, topology inspection, and source adapters."],
		["demos", "Runnable TypeScript demos that show the package in UI and framework contexts."],
		["recipes", "Reusable graph patterns for CQRS, executor queues, resilience, memory, and durable state."],
		["integrations", "HTTP, WebSocket, SSE, Cron, process, NestJS, storage, and framework adapter surfaces."],
		["compare", "How the TypeScript package differs from RxJS, framework stores, transports, and queue runtimes."],
	]);
	return descriptions.get(id) ?? "@graphrefly/ts documentation.";
}

function renderIndexGroups(sidebar) {
	return sidebar
		.map((group) => {
			const groupSlug = slugify(group.label);
			return `<section class="index-group">
  <p class="card-kicker">API Area</p>
  <h2>${escapeHtml(group.label)}</h2>
  <p>${escapeHtml(apiGroupDescription(group.label))}</p>
  <a class="text-link" href="${siteLink(`/api/category/${groupSlug}/`)}">Open group <span aria-hidden="true">-&gt;</span></a>
</section>`;
		})
		.join("\n");
}

function renderToc(markdown) {
	const items = extractToc(markdown);
	if (items.length === 0) return "";
	return `<nav class="toc">
  <p class="sidebar-kicker">On this page</p>
  ${items.map((item) => `<a href="${escapeAttr(item.href)}">${escapeHtml(item.label)}</a>`).join("")}
</nav>`;
}

function renderCardGrid(cards) {
	return `<div class="card-grid">${cards
		.map(
			(card) => `<article class="doc-card">
  <p class="card-kicker">${escapeHtml(card.kicker)}</p>
  <h2>${escapeHtml(card.title)}</h2>
  <p>${escapeHtml(card.body)}</p>
  <a href="${siteLink(card.href)}">${escapeHtml(card.action)} <span aria-hidden="true">-&gt;</span></a>
</article>`,
		)
		.join("")}</div>`;
}

function writePage(relativePath, html) {
	const output = path.join(routeRoot, relativePath, "index.html");
	mkdirSync(path.dirname(output), { recursive: true });
	writeFileSync(output, html);
}

if (!existsSync(apiDir)) {
	throw new Error(`API docs directory does not exist: ${apiDir}`);
}

rmSync(distRoot, { force: true, recursive: true });
mkdirSync(path.join(routeRoot, "assets"), { recursive: true });
if (customDomain.trim().length > 0) {
	writeFileSync(path.join(distRoot, "CNAME"), `${customDomain.trim()}\n`);
}

const { apiSidebar } = await import(`${pathToFileURL(sidebarPath).href}?t=${Date.now()}`);
const apiFiles = [...new Set(apiSidebar.flatMap((group) => group.items.map((item) => item.link)))]
	.map((link) => link.replace(/^\/api\//, ""))
	.sort();

const pages = [];
for (const slug of apiFiles) {
	const markdownPath = path.join(apiDir, `${slug}.md`);
	if (!existsSync(markdownPath)) throw new Error(`sidebar references missing API page: ${slug}`);
	const { frontmatter, body } = parseFrontmatter(readFileSync(markdownPath, "utf8"));
	const title = frontmatter.title ?? slug;
	const description = frontmatter.description ?? `Generated @graphrefly/ts API page for ${title}.`;
	pages.push({ title, slug, description, body });
}

searchIndexJson = JSON.stringify([
	...primaryNav.map((item) => ({
		title: item.label,
		section: "Docs",
		description: sectionDescription(item.id),
		href: siteLink(item.href),
	})),
	...apiSidebar.map((group) => ({
		title: `${group.label} API`,
		section: "API group",
		description: apiGroupDescription(group.label),
		href: siteLink(`/api/category/${slugify(group.label)}/`),
	})),
	...pages.map((page) => ({
		title: page.title,
		section: "API",
		description: page.description,
		href: siteLink(`/api/${page.slug}/`),
	})),
]).replaceAll("<", "\\u003c");

for (const page of pages) {
	writePage(
		path.join("api", page.slug),
		pageShell({
			title: page.title,
			description: page.description,
			sidebarHtml: renderApiSidebar(apiSidebar, { activeSlug: page.slug }),
			rightSidebarHtml: renderToc(page.body),
			activeSection: "api",
			body: `<article><p class="eyebrow">API Reference</p><h1 class="api-symbol-title">${escapeHtml(page.title)}</h1>${renderMarkdown(page.body)}</article>`,
		}),
	);
}

for (const group of apiSidebar) {
	const groupSlug = slugify(group.label);
	const items = group.items
		.map((item) => `<li><a href="${sidebarLink(item.link)}">${escapeHtml(item.label)}</a></li>`)
		.join("");
	writePage(
		path.join("api", "category", groupSlug),
		pageShell({
			title: `${group.label} API`,
			description: apiGroupDescription(group.label),
			sidebarHtml: renderApiSidebar(apiSidebar, { openGroupLabel: group.label }),
			activeSection: "api",
			body: `<article><p class="eyebrow">API Category</p><h1>${escapeHtml(group.label)}</h1><p class="lede">${escapeHtml(apiGroupDescription(group.label))}</p><ul class="symbol-list">${items}</ul></article>`,
		}),
	);
}

const apiIndexGroups = renderIndexGroups(apiSidebar);

const sectionCards = [
	{
		kicker: "Reference",
		title: "API",
		body: "Find package APIs by area, then open a symbol for its signature, parameters, return value, and examples.",
		href: "/api/",
		action: "Open API",
	},
	{
		kicker: "Use cases",
		title: "Solutions",
		body: "Higher-level bundles for reactive layout, agentic memory, work-item orchestration, and capability admission.",
		href: "/solutions/",
		action: "Browse solutions",
	},
	{
		kicker: "Learning path",
		title: "Examples",
		body: "Small TypeScript entry points for state, derived values, effects, topology inspection, and source adapters.",
		href: "/examples/",
		action: "Start with examples",
	},
	{
		kicker: "Interactive",
		title: "Demos",
		body: "Runnable TypeScript demos that show the package in UI and framework contexts.",
		href: "/demos/",
		action: "Open demos",
	},
	{
		kicker: "Composition",
		title: "Recipes",
		body: "Reusable graph patterns for CQRS, executor queues, resilience, memory, and durable state.",
		href: "/recipes/",
		action: "Read recipes",
	},
	{
		kicker: "Boundaries",
		title: "Integrations",
		body: "HTTP, WebSocket, SSE, Cron, process, NestJS, storage, and framework adapter surfaces.",
		href: "/integrations/",
		action: "Review integrations",
	},
	{
		kicker: "Migration",
		title: "Compare",
		body: "How the TypeScript package differs from RxJS, framework stores, transports, and queue runtimes.",
		href: "/compare/",
		action: "Compare options",
	},
];

writePage(
	".",
	pageShell({
		title: "TypeScript Package Docs",
		description: "@graphrefly/ts API reference, examples, demos, recipes, integrations, and comparisons.",
		body: `<article class="home">
  <section class="hero">
    <div class="hero-copy">
      <p class="eyebrow">TypeScript Runtime</p>
      <h1 class="package-title"><span>@graphrefly</span><span>/ts</span></h1>
      <p class="lede">TypeScript APIs, runnable demos, recipes, integrations, and comparisons for building graph-driven reactive systems.</p>
      <div class="hero-actions">
        <a class="button primary" href="${siteLink("/api/")}">API reference</a>
        <a class="button" href="https://www.npmjs.com/package/@graphrefly/ts">npm package</a>
      </div>
    </div>
    <div class="console-card" aria-label="Package artifact summary">
      <div class="console-head"><span></span><span></span><span></span></div>
      <pre><code>$ npm i @graphrefly/ts
import { graph } from "@graphrefly/ts/graph";
import { map } from "@graphrefly/ts/operators";</code></pre>
      <dl>
        <div><dt>Install</dt><dd>npm</dd></div>
        <div><dt>Graph</dt><dd>graph</dd></div>
        <div><dt>Operators</dt><dd>map</dd></div>
      </dl>
    </div>
  </section>
  <section class="section-band">
    <p class="eyebrow">Docs map</p>
    <h2>Everything you need for the TypeScript package.</h2>
    ${renderCardGrid(sectionCards)}
  </section>
</article>`,
	}),
);

writePage(
	"api",
	pageShell({
		title: "API Reference",
		description: "@graphrefly/ts API reference grouped by package area.",
		sidebarHtml: renderApiSidebar(apiSidebar),
		activeSection: "api",
		body: `<article><p class="eyebrow">Reference</p><h1>API Reference</h1><p class="lede">Choose a package area first, then open a symbol for its signature, parameters, return value, and examples.</p><div class="index-grid">${apiIndexGroups}</div></article>`,
	}),
);

writePage(
	"solutions",
	pageShell({
		title: "Solutions",
		description: "@graphrefly/ts solution bundles and higher-level package surfaces.",
		activeSection: "solutions",
		body: `<article><p class="eyebrow">Solutions</p><h1>Solution surfaces for product workflows.</h1><p class="lede">Start here when you want a ready-made TypeScript bundle instead of wiring every primitive by hand.</p>${renderCardGrid([
			{
				kicker: "Layout",
				title: "Reactive Layout",
				body: "Graph-visible block flow, line breaking, measurement adapters, and platform-specific browser, Node canvas, React Native, and Skia entry points.",
				href: "/api/category/reactive-layout/",
				action: "Open API group",
			},
			{
				kicker: "Memory",
				title: "Agentic Memory",
				body: "Retention, consolidation, context packing, record admission, and KG projection bundles for auditable agent memory flows.",
				href: "/api/category/agentic-memory/",
				action: "Open API group",
			},
			{
				kicker: "Operations",
				title: "Work Item",
				body: "Workspace proposals, scheduling, work queues, action plans, and runtime surfaces for coordinated work execution.",
				href: "/api/category/work-item/",
				action: "Open API group",
			},
			{
				kicker: "Policy",
				title: "Capability Admission",
				body: "Typed admission projectors and proposal helpers for deciding which graph-visible capabilities may enter a workflow.",
				href: "/api/capabilityadmissionprojector/",
				action: "Open API",
			},
		])}</article>`,
	}),
);

writePage(
	"examples",
	pageShell({
		title: "Examples",
		description: "@graphrefly/ts examples and first-use paths.",
		activeSection: "examples",
		body: `<article><p class="eyebrow">Examples</p><h1>Small, inspectable starts.</h1><p class="lede">Start with short TypeScript snippets for graph state, operators, sources, and topology inspection.</p><div class="example-grid">
<section><h2>State and derived values</h2><pre><code>import { graph } from "@graphrefly/ts/graph";

const g = graph();
const count = g.state(0);
const doubled = g.derived([count], ([n]) => n * 2);</code></pre><a class="text-link" href="${siteLink("/api/graph/")}">Graph API</a></section>
<section><h2>Operators</h2><pre><code>import { map, distinctUntilChanged } from "@graphrefly/ts/operators";

const normalized = source.pipe(
  map((value) => value.trim()),
  distinctUntilChanged(),
);</code></pre><a class="text-link" href="${siteLink("/api/category/operators/")}">Operator API</a></section>
<section><h2>Sources</h2><pre><code>import { fromWebSocket } from "@graphrefly/ts/sources";

const events = fromWebSocket("wss://example.test/events");</code></pre><a class="text-link" href="${siteLink("/api/category/sources/")}">Sources API</a></section>
</div></article>`,
	}),
);

writePage(
	"demos",
	pageShell({
		title: "Demos",
		description: "@graphrefly/ts demos and runnable package examples.",
		activeSection: "demos",
		body: `<article><p class="eyebrow">Demos</p><h1>Runnable TypeScript package demos.</h1><p class="lede">Open a demo to see the package running inside a real UI or framework context.</p>${renderCardGrid([
			{
				kicker: "Layout",
				title: "Reactive Layout",
				body: "Interactive layout demo driven by the TypeScript reactive-layout solution surface.",
				href: "/demos/reactive-layout/",
				action: "Open demo",
			},
			{
				kicker: "Frameworks",
				title: "Compat Matrix",
				body: "React, Vue, Solid, and Svelte bindings over the same underlying graph shape.",
				href: "/demos/compat-matrix/",
				action: "Open demo",
			},
		])}</article>`,
	}),
);

writePage(
	"recipes",
	pageShell({
		title: "Recipes",
		description: "@graphrefly/ts recipes for composing package primitives.",
		activeSection: "recipes",
		body: `<article><p class="eyebrow">Recipes</p><h1>Composition recipes for common flows.</h1><p class="lede">Use these patterns when you need command handling, durable queues, resilience, memory, or persistence.</p>${renderCardGrid([
			{
				kicker: "Messaging",
				title: "CQRS over message bus",
				body: "Command submission, event outbox, message acknowledgement, and work queue recipe surfaces.",
				href: "/api/cqrsmessagingrecipe/",
				action: "Open recipe API",
			},
			{
				kicker: "Execution",
				title: "Executor work queue",
				body: "Durable executor dispatch recipe over graph-visible work queue lifecycle commands.",
				href: "/api/executorworkqueuerecipe/",
				action: "Open recipe API",
			},
			{
				kicker: "Memory",
				title: "Semantic memory graph",
				body: "Patterns for fact storage, KG projection, context packing, and memory consolidation.",
				href: "/api/category/patterns/",
				action: "Open patterns",
			},
			{
				kicker: "Resilience",
				title: "Retry and breaker bundles",
				body: "Backoff, retry policy, breaker state, and operational recovery helpers.",
				href: "/api/breakerbundle/",
				action: "Open API",
			},
		])}</article>`,
	}),
);

writePage(
	"integrations",
	pageShell({
		title: "Integrations",
		description: "@graphrefly/ts integration surfaces for runtime edges and frameworks.",
		activeSection: "integrations",
		body: `<article><p class="eyebrow">Integrations</p><h1>Runtime edges and host frameworks.</h1><p class="lede">Connect graphs to network streams, host processes, storage layers, and UI runtimes without losing package-level boundaries.</p>${renderCardGrid([
			{
				kicker: "Network",
				title: "HTTP, SSE, WebSocket",
				body: "Source and driver surfaces for request, stream, and socket-driven graphs.",
				href: "/api/category/sources/",
				action: "Open sources",
			},
			{
				kicker: "NestJS",
				title: "Nest boundary adapters",
				body: "Guards, interceptors, exception filters, message bridges, lifecycle events, and WebSocket bindings.",
				href: "/api/createnestgraphboundaryrunner/",
				action: "Open Nest API",
			},
			{
				kicker: "Storage",
				title: "Append logs and key-value backends",
				body: "Content-addressed storage, file backends, append log frames, and persistence-oriented package APIs.",
				href: "/api/category/storage/",
				action: "Open storage",
			},
			{
				kicker: "UI",
				title: "Framework compatibility",
				body: "React, Vue, Solid, and Svelte demo surfaces connected to shared graph state.",
				href: "/demos/compat-matrix/",
				action: "Open demo",
			},
		])}</article>`,
	}),
);

writePage(
	"compare",
	pageShell({
		title: "Compare",
		description: "@graphrefly/ts comparisons and migration notes.",
		activeSection: "compare",
		body: `<article><p class="eyebrow">Compare</p><h1>Where @graphrefly/ts fits.</h1><p class="lede">Use these notes to choose between GraphReFly, RxJS, framework stores, queue runtimes, and transport libraries.</p>${renderCardGrid([
			{
				kicker: "Migration",
				title: "Coming from RxJS",
				body: "Operator names may look familiar, but GraphReFly centers inspectable graph topology and wave behavior.",
				href: "https://github.com/graphrefly/graphrefly-ts/blob/main/docs/coming-from-rxjs.md",
				action: "Read migration note",
			},
			{
				kicker: "Scope",
				title: "API surface",
				body: "Use this section to decide whether you need a TypeScript API, a runtime pattern, or the broader GraphReFly model.",
				href: "/api/",
				action: "Open API",
			},
			{
				kicker: "Framework stores",
				title: "Compat matrix",
				body: "A live comparison against React, Vue, Solid, Svelte, Jotai, Nanostores, and Zustand binding shapes.",
				href: "/demos/compat-matrix/",
				action: "Open demo",
			},
		])}</article>`,
	}),
);

for (const demo of ["reactive-layout", "compat-matrix"]) {
	const source = path.join(repoRoot, "demos", demo, "dist");
	if (existsSync(source)) {
		cpSync(source, path.join(routeRoot, "demos", demo), { recursive: true });
	}
}

writeFileSync(
	path.join(routeRoot, "assets", "ts-docs.css"),
	`@import url("https://fonts.googleapis.com/css2?family=Big+Shoulders+Display:wght@500;600;700;800;900&family=JetBrains+Mono:wght@400;500;600&family=Sora:wght@400;500;600;700;800&display=swap");
:root{--ice:#f4f7f9;--ice-warm:#eceff2;--paper:#fff;--navy:#0a1628;--navy-deep:#050e1a;--ink:#07121e;--ink-mute:#5c6675;--ink-faint:#98a0ac;--rule:rgba(7,18,30,.1);--rule-deep:rgba(7,18,30,.22);--lime:#c8ff00;--lime-deep:#9bc400;--magenta:#ff2d75;--display:"Big Shoulders Display",system-ui,sans-serif;--sans:"Sora",system-ui,sans-serif;--mono:"JetBrains Mono",ui-monospace,monospace}
*{box-sizing:border-box}html{font-family:var(--sans);color:var(--ink);background:var(--ice);scroll-behavior:smooth}body{margin:0;overflow-x:hidden}a{color:inherit}.sr-only{position:absolute;width:1px;height:1px;margin:-1px;overflow:hidden;clip:rect(0 0 0 0);white-space:nowrap;border:0}.topbar{position:sticky;top:0;z-index:10;display:grid;grid-template-columns:auto minmax(240px,520px) minmax(0,1fr) auto;align-items:center;gap:18px;padding:12px 24px;border-bottom:1px solid var(--rule);background:rgba(244,247,249,.94);backdrop-filter:blur(14px)}.brand{display:flex;align-items:center;gap:10px;color:var(--ink);text-decoration:none}.brand span{font-family:var(--display);font-size:1.42rem;font-weight:900;letter-spacing:.01em;text-transform:uppercase}.brand strong{display:inline-grid;place-items:center;min-width:28px;height:28px;border-radius:6px;background:var(--lime);font-family:var(--mono);font-size:.78rem;color:var(--ink)}.search{position:relative;min-width:0}.search input{width:100%;height:40px;border:1px solid var(--rule);border-radius:8px;background:var(--paper);padding:0 14px;font:500 .9rem var(--sans);color:var(--ink);outline:none}.search input:focus{border-color:var(--navy);box-shadow:0 0 0 3px rgba(200,255,0,.38)}.search-results{position:absolute;top:46px;left:0;right:0;z-index:20;display:grid;gap:4px;max-height:420px;overflow:auto;border:1px solid var(--rule);border-radius:8px;background:var(--paper);box-shadow:0 18px 50px rgba(7,18,30,.16);padding:8px}.search-results[hidden]{display:none}.search-results a{display:grid;gap:3px;border-radius:6px;padding:9px;text-decoration:none}.search-results a:hover{background:var(--ice)}.search-results span{font-family:var(--mono);font-size:.65rem;font-weight:700;text-transform:uppercase;letter-spacing:.08em;color:var(--ink-faint)}.search-results strong{font-size:.88rem}.search-results small{color:var(--ink-mute);line-height:1.45}.search-results p{margin:8px;color:var(--ink-mute)}.topbar nav{display:flex;align-items:center;gap:6px;flex-wrap:wrap}.topbar a{text-decoration:none}.topbar-primary{justify-content:center}.topbar-primary a,.topbar-utility a{border-radius:999px;padding:7px 10px;font-family:var(--mono);font-size:.72rem;font-weight:600;text-transform:uppercase;letter-spacing:.08em;color:var(--ink-mute)}.topbar-primary a:hover,.topbar-utility a:hover,.topbar-primary a.active{background:var(--navy);color:#fff}.topbar-utility{justify-content:flex-end}.layout{display:grid;min-height:calc(100vh - 65px)}.layout.has-left.no-right{grid-template-columns:minmax(260px,320px) minmax(0,1fr)}.layout.has-left.has-right{grid-template-columns:minmax(260px,320px) minmax(0,960px) minmax(190px,240px)}.layout.no-left{grid-template-columns:minmax(0,1fr)}.layout.no-left .content{width:min(1240px,100%);margin:0 auto}.sidebar,.rightbar{position:sticky;top:65px;align-self:start;max-height:calc(100vh - 65px);overflow:auto}.sidebar{border-right:1px solid var(--rule);background:rgba(236,239,242,.58);padding:18px}.rightbar{padding:28px 18px;border-left:1px solid var(--rule);background:rgba(244,247,249,.74)}.sidebar-panel{padding:0}.sidebar-kicker,.eyebrow,.card-kicker{margin:0 0 12px;font-family:var(--mono);font-size:.72rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--ink-mute)}.toc{display:grid;gap:4px}.toc a{display:block;border-radius:5px;padding:6px 8px;text-decoration:none;font-size:.78rem;line-height:1.35;color:var(--ink-mute)}.toc a:hover{background:var(--paper);color:var(--ink)}.api-tree details{border-bottom:1px solid var(--rule)}.api-tree details[open]{background:rgba(255,255,255,.68)}.api-tree summary{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:10px 9px;border-radius:6px;cursor:pointer;font-weight:800;color:var(--ink)}.api-tree summary span{font-family:var(--mono);font-size:.68rem;color:var(--ink-faint)}.api-tree ul,.symbol-list{list-style:none;margin:0;padding:0 0 10px}.api-tree li{margin:1px 0}.api-tree a,.symbol-list a{display:block;border-radius:5px;padding:5px 9px;text-decoration:none;color:#2b7956}.api-tree a:hover,.api-tree a.active,.symbol-list a:hover{background:rgba(200,255,0,.18);color:var(--ink)}.content{min-width:0;padding:50px min(5vw,72px) 88px}.content>article{max-width:1120px}h1{font-family:var(--display);font-size:clamp(2.1rem,3.2vw,3.5rem);line-height:1.08;margin:0 0 24px;font-weight:850;letter-spacing:0;text-transform:uppercase;color:var(--ink);overflow-wrap:normal}.api-symbol-title{font-family:var(--sans);font-size:clamp(2.05rem,4vw,4.15rem);font-weight:800;line-height:1.05;text-transform:none;overflow-wrap:anywhere}.package-title{font-size:clamp(2.65rem,4.4vw,4.6rem);line-height:1;max-width:720px}.package-title span{display:inline}.package-title span+span{margin-top:0}h2{font-size:clamp(1.35rem,2vw,2rem);line-height:1.08;margin:38px 0 14px;color:var(--ink)}h3{margin:28px 0 10px}p{line-height:1.72}.lede{max-width:760px;font-size:1.06rem;color:var(--ink-mute)}.hero{display:grid;grid-template-columns:minmax(0,1fr) minmax(320px,420px);align-items:end;gap:min(6vw,72px);padding:20px 0 48px;border-bottom:1px solid var(--rule)}.hero-copy,.console-card{min-width:0}.hero-actions{display:flex;gap:12px;flex-wrap:wrap;margin-top:28px}.button{display:inline-flex;align-items:center;justify-content:center;min-height:44px;border:1px solid var(--rule-deep);border-radius:999px;padding:0 18px;text-decoration:none;font-weight:800;color:var(--ink);background:var(--paper)}.button.primary{border-color:var(--navy);background:var(--navy);color:#fff}.button:hover{transform:translateY(-1px)}.console-card{border-radius:8px;background:var(--navy-deep);color:#dfe9f7;box-shadow:0 24px 80px rgba(7,18,30,.18);overflow:hidden}.console-head{display:flex;gap:7px;padding:14px;border-bottom:1px solid rgba(255,255,255,.1)}.console-head span{width:9px;height:9px;border-radius:50%;background:var(--lime)}.console-head span:nth-child(2){background:#64d2ff}.console-head span:nth-child(3){background:var(--magenta)}.console-card pre{margin:0;padding:22px;background:transparent;color:#f3f8ff}.console-card dl{display:grid;grid-template-columns:repeat(3,1fr);margin:0;border-top:1px solid rgba(255,255,255,.1)}.console-card div{padding:14px}.console-card dt{font-family:var(--mono);font-size:.66rem;text-transform:uppercase;letter-spacing:.08em;color:#92a3ba}.console-card dd{margin:4px 0 0;font-family:var(--display);font-size:1.4rem;font-weight:800;color:var(--lime)}.section-band{padding:46px 0}.card-grid,.index-grid,.example-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:18px;margin-top:22px}.index-grid{grid-template-columns:repeat(auto-fit,minmax(260px,1fr))}.doc-card,.index-group,.example-grid section{display:flex;flex-direction:column;min-height:190px;border:1px solid var(--rule);border-radius:8px;background:var(--paper);padding:20px}.index-group{min-height:178px}.doc-card h2,.index-group h2,.example-grid h2{margin:0 0 10px;font-size:1.18rem}.doc-card p,.index-group p{color:var(--ink-mute)}.doc-card a,.text-link{margin-top:auto;font-weight:800;text-decoration:none;color:var(--ink)}.doc-card a:hover,.text-link:hover{text-decoration:underline}.symbol-list{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:4px;margin-top:24px}pre{background:var(--navy-deep);color:#e8f7de;border-radius:8px;padding:16px;overflow:auto}code{font-family:var(--mono);font-size:.92em}p code,td code{background:#e7ecef;border-radius:4px;padding:2px 5px;color:var(--ink)}table{border-collapse:collapse;width:100%;margin:18px 0;display:block;overflow:auto}th,td{border:1px solid var(--rule);padding:10px;text-align:left;vertical-align:top}th{background:var(--ice-warm)}article>ul:not(.symbol-list){padding-left:1.2rem}article>ul:not(.symbol-list) li{margin:8px 0;line-height:1.6}@media(max-width:1180px){.topbar{grid-template-columns:auto minmax(220px,1fr)}.topbar-primary,.topbar-utility{grid-column:1 / -1;justify-content:flex-start}.layout.has-left.has-right{grid-template-columns:minmax(250px,310px) minmax(0,1fr)}.rightbar{display:none}.hero{grid-template-columns:1fr}.console-card{max-width:560px}}@media(max-width:860px){.topbar{grid-template-columns:1fr;padding:12px 16px}.brand{width:max-content}.search-results{position:fixed;top:108px;left:16px;right:16px}.layout{display:flex;flex-direction:column}.sidebar{order:1;position:static;max-height:44vh;border-right:0;border-bottom:1px solid var(--rule);padding:14px 16px}.content{order:2;padding:30px 20px 72px}.rightbar{display:none}h1{font-size:clamp(2rem,8vw,2.8rem);line-height:1.05}.api-symbol-title{font-size:clamp(1.9rem,9vw,2.8rem)}.package-title{font-size:clamp(2.4rem,11vw,3.4rem)}.package-title span{display:block}.package-title span+span{margin-top:-.04em}.console-card dl{grid-template-columns:1fr}.topbar-primary a,.topbar-utility a{font-size:.68rem;padding:7px 8px}.card-grid,.index-grid,.example-grid{grid-template-columns:1fr}.doc-card,.index-group,.example-grid section{min-height:0}}@media(max-width:560px){.topbar{gap:12px}.topbar .topbar-utility{display:none}.topbar-primary{overflow:auto;flex-wrap:nowrap;padding-bottom:2px}.topbar-primary a{white-space:nowrap}.search-results{top:150px}.content{padding-inline:16px}.sidebar-kicker,.eyebrow,.card-kicker{font-size:.66rem}.symbol-list{grid-template-columns:1fr}}\n`,
);

writeFileSync(
	path.join(routeRoot, "artifact-manifest.json"),
	`${JSON.stringify(
		{
			package: "@graphrefly/ts",
			route: basePath,
			source: path.relative(repoRoot, apiDir),
			pages: pages.length,
			sections: primaryNav.map((item) => item.id),
		},
		null,
		2,
	)}\n`,
);

if (routeRoot !== distRoot) {
	writeFileSync(
		path.join(distRoot, "index.html"),
		`<!doctype html><meta charset="utf-8"><meta http-equiv="refresh" content="0; url=${basePath}"><link rel="canonical" href="${basePath}"><a href="${basePath}">TypeScript docs</a>\n`,
	);
}

console.log(`built ${pages.length} @graphrefly/ts API pages at ${path.relative(repoRoot, routeRoot)}`);
