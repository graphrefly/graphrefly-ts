/**
 * WorkItem runtime migration target (D571).
 *
 * This source barrel is intentionally not package-published yet; the existing
 * runtime facts remain experimental while implementation moves out of generic
 * orchestration in later slices.
 */

export * from "../../../orchestration/work-item-runtime.js";
