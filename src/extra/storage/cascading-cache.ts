/**
 * Cascading multi-tier cache — LRU + persistent fallback.
 *
 * Re-exports from `../cascading-cache.js` (deferred physical move per the
 * consolidation plan §2).
 */

export * from "../cascading-cache.js";
