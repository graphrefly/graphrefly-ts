/**
 * Storage core helpers — `StorageHandle`, `stableJsonString`, `sortJsonValue`.
 *
 * Re-exports from `../storage-core.js` (deferred physical move per the
 * consolidation plan §2).
 */

export * from "../storage-core.js";
