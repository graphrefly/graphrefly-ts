/**
 * Content-addressed storage — canonical JSON + CID-keyed kv.
 *
 * Re-exports from `../content-addressed-storage.js` (deferred physical move
 * per the consolidation plan §2).
 */

export * from "../content-addressed-storage.js";
