/**
 * Storage tier interfaces and shared in-memory backends.
 *
 * Re-exports from `../storage-tiers.js` (deferred physical move per the
 * consolidation plan §2).
 */

export * from "../storage-tiers.js";
