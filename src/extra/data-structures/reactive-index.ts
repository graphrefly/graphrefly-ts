/**
 * Reactive index — keyed → list reactive secondary index.
 *
 * Re-exports from `../reactive-index.js` (deferred physical move per the
 * consolidation plan §2).
 */

export * from "../reactive-index.js";
