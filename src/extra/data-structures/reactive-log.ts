/**
 * Reactive log — append-only reactive log with retention.
 *
 * Re-exports from `../reactive-log.js` (deferred physical move per the
 * consolidation plan §2).
 */

export * from "../reactive-log.js";
