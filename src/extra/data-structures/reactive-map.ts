/**
 * Reactive map — keyed reactive store with retention.
 *
 * Re-exports from `../reactive-map.js` (deferred physical move per the
 * consolidation plan §2).
 */

export * from "../reactive-map.js";
