/**
 * HTTP error helper — re-exports from `../http-error.js`.
 *
 * Sub-file exists for category-level discoverability per the consolidation
 * plan §2; physical code is in `src/extra/http-error.ts` (deferred move).
 */

export * from "../http-error.js";
