/**
 * Backpressure controller — `createWatermarkController`.
 *
 * Re-exports from `../backpressure.js` (deferred physical move per the
 * consolidation plan §2).
 */

export * from "../backpressure.js";
