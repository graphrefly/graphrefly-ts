/**
 * External register helpers — `externalProducer`, `externalBundle`.
 *
 * Re-exports from `../external-register.js` (deferred physical move per the
 * consolidation plan §2).
 */

export * from "../external-register.js";
