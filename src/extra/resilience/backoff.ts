/**
 * Backoff strategies — re-exports from `../backoff.js`.
 *
 * Sub-file exists for category-level discoverability per the consolidation
 * plan §2; physical code is in `src/extra/backoff.ts` (deferred move).
 */

export * from "../backoff.js";
