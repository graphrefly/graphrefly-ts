---
"@graphrefly/ts": minor
---

Emit GraphBlueprint v2 with stable graph-owned subgraph mount identities, retain strict v1 evidence reads, and add portable parsing, caller-owned hash verification, Blueprint-direct Mermaid rendering, and deterministic fail-closed structural deltas for repository review tools.
