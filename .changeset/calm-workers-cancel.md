---
"@graphrefly/ts": patch
---

Allow managed-cloud PostgreSQL worker drivers to return the protocol-native canceled outcome so hosts can settle admitted cancellation after terminal attempt-credential cleanup without rewriting a completed result.
