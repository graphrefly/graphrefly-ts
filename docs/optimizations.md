# Optimizations — Active Items (TS + PY)

> **This file is the single source of truth** for optimization tracking across both graphrefly-ts and graphrefly-py.
>
> **Resolved decisions, cross-language notes, built-in optimization docs, QA design decisions, and parity fixes have been archived to `archive/optimizations/*.jsonl`.** See `docs/docs-guidance.md` § "Optimization decision log" for the archive workflow.
>
> **PY parity deferral (2026-04-17):** Per `archive/docs/SESSION-rigor-infrastructure-plan.md`, PY parity work is **deprioritized** until rigor infrastructure (Projects 1–3: fast-check protocol harness, TLA+ core spec, TS↔PY executable contract traces) lands. Items tagged `[py-parity-…]` below stay tracked but are intentionally held. The trace-format approach (Project 2) replaces quarterly parity audits as the mechanism for catching divergence.

---

## Consumer-demand policy (pre-1.0, locked 2026-05-18)

The recurring **"defer until a real consumer surfaces"** gate is **relaxed pre-1.0**. It was producing a backlog of ambiguous "waiting" carries, several of which were *already silently resolved* by later refactors (the 2026-05-18 sweep found EH-2, EH-17, γ-7, P11.5-D3 all stale). The rule going forward:

- **Bias to do the right thing now.** If an item is bounded, correctness/safety-positive, and the API shape is already known, **implement it** — "wait for a consumer" carries no design information for these. Pre-1.0 + `feedback_no_backward_compat` means cleanup is cheap and legacy must not accumulate.
- **Otherwise reclassify POST-1.0 explicitly** (not "waiting"). Use POST-1.0 for genuine scale/perf work and for **speculative API surface** where a real consumer is needed to *define the shape* (the §44 "wrap-imperative-as-primitive / premature-generalization" trap is a real reason to wait — that judgment is preserved).
- **Verify the premise first.** Before implementing any gated item, confirm it is not already resolved in code (`feedback_verify_premise_before_greenfield`).

**Two gates are NOT relaxed** (they carry real signal, not consumer-demand stalling):
1. **D196** — `Impl`-contract / `@graphrefly/native` napi widening stays parity-scenario-gated (cross-track/Rust coupling; see `packages/parity-tests/README.md` § D196 + `docs/cross-track-ledger.md`).
2. **§44 over-generalization** — a "reactive primitive" that would just re-expose an imperative helper as a method still waits for a real consumer to justify the abstraction (`feedback_no_imperative_wrap_as_primitive`).

**Reclassified POST-1.0 (2026-05-18 sweep)** — these stay filed but are now explicitly post-1.0, not "awaiting a consumer": Lock 4.B (B)+(C) `registerMutable` + dev-mode Proxy (Rust port may obviate via ownership+`Drop`); EH-19 `validateNoIslandsReactive` companion; γ-5/γ-6 messaging audit-record schemas; JobFlow `maxInflight` per-stage bound; the Tier 5.2 primitive-side reactive-options widening sub-tickets + per-layer companion-Node tickets (`rateLimiter`/`circuitBreaker`/`timeout`/`retry`/`budgetGate`; `budgetState`/`retryAttempts`/`lastTimeout`); Tier 4.7 `derivedT` typed-deps variant; Harness `executeAndVerify` unified slot; Harness executor/verifier dev-mode ≤1-DATA-per-wave sanity check; process-manager pattern (Audit 3, locked design / no consumer); the switchMap-inner `dispose`-hook residual. Each retains its detailed entry below for the design record; this list is the authoritative POST-1.0 tag (single source of truth — do not duplicate the tag inline per `feedback_single_source_of_truth`).

---

## Active work items

> **Cross-track coordination** (any change that widens the `Impl` contract or
> otherwise couples the presentation track ↔ Rust-port track) is tracked in a
> dedicated ledger: [`docs/cross-track-ledger.md`](cross-track-ledger.md). Do not
> log those here — that doc is the single source of truth for the N1-pattern handoff.

- **🟠 E-ii.2 DEFERRED 2026-05-22 (`/porting-to-rs` Commit 2 HALT — scope was 5-10x my initial estimate; awaits design session. DO NOT PICK UP COLD.)** The `porting-deferred.md` entry "M4.E2 `attach_snapshot_storage` TEARDOWN tier excluded from persistence" was originally framed as a small B4-style fix (`graph.teardown` `change.kind` arm + tier filter widen, ~80 LOC). Premise verification during the E-i+ii+iv batch HALT surfaced the actual scope: the snapshot+diff pipeline today doesn't detect torn-down nodes (`diff_snapshots` checks added/removed/value_changed, but torn-down nodes stay in the namespace with their existing `Completed`/`Errored` status), so widening the tier filter alone produces zero new WAL frames. **Real scope:** Path A (snapshot-driven via diff widening) = `NodeSnapshotStatus::TornDown { from: Box<...> }` variant + diff detection (`is_terminal()` before/after) + `decompose_diff_to_frames` arm + `apply_data_frame` arm + cross-arm coordination (TS today doesn't persist either — explicit cross-arm parity property — so a unilateral Rust change CREATES divergence) + restore-time semantic decision (silent-mark via new `Core::mark_torn_down_silently` substrate surface ⟂ re-fire cascade ⟂ audit-only no-op). User-locked verdict: **Path A is the correct architectural shape** (single source of truth for WAL frames; rejects Path B's dual-source via anti-pattern #5), but **400-1000+ LOC + cross-arm coordination + possibly new substrate surface** — too big to fold into a cleanup batch. Refile pending: (a) a real audit-log consumer surfacing concrete restore semantics (drives R1-vs-R3 in `apply_data_frame`), (b) a spec-level R-ID for "WAL-restore-authoritative vs snapshot-authoritative" (currently unmapped — `feedback_no_autonomous_decisions` blocks autonomously locking the semantic), (c) parity-scenario authored FIRST under `packages/parity-tests/scenarios/storage-wal/` so the cross-arm contract is established before implementation. Mint a new D-number when picked up. **Side-quest required before any implementation:** grep canonical-spec + source-of-truth for WAL-restore semantics R-ID. Cross-ref: `~/src/graphrefly-rs/docs/porting-deferred.md` ("M4.E2 `attach_snapshot_storage` TEARDOWN tier excluded") + `rust-port-decisions.md` D278 (E-cluster Commit 1 doc-hygiene, where this re-deferral surfaced).

- **Release-note carries (2026-05-18 consumer-demand batch /qa, F-A) — pre-1.0 public-API narrowings, no migration shim.** Verified **NOT cross-track** (absent from `packages/parity-tests/`/`Impl` substrate surface — presentation-layer only; release-note carry only, mirroring the archived EC6 precedent). At the next minor bump, changelog: (1) `RefineLoopGraph.best: Node<T | null>` → `Node<T>` and `RefineExecutorResult.best: T | null` → `T | undefined` — downstream `loop.best.cache === null` / `result.best === null` guards must become `=== undefined` (SENTINEL; `Iteration.best`/`pickBest` stay `T | null` as settled-record domain data, unchanged); (2) `SagaController.audit` / `CqrsGraph.audit` / `JobQueueGraph.audit` / `ProcessManagerResult.audit` are no longer reference-identical to `.invocations`/`.dispatches`/`.events`/`.instances` and are now frozen read-only views — `someGraph.audit.append(...)` no longer type-checks or works (mutate the canonical log, not the alias); (3) `rateLimiter` (and `resilientPipeline`'s Node-form `rateLimit`) throws `RangeError` at construction on an un-seeded reactive opts Node or a seeded value omitting `maxBuffer` (D4 fail-loud — previously a silent bounded-`maxBuffer:1` lock). **Smell-sweep additions (2026-05-18, see block below):** (4) `resilientPipeline`'s **value-form** `rateLimit` now also requires `maxBuffer` (type widened to `NodeOrValue<RateLimiterOptions>`; no more `?? Infinity` — `Infinity` is now an explicit opt-in) [F-B-root]; (5) `reactiveSink`'s `backpressure` requires an explicit `maxBuffer` (`ReactiveSinkBackpressureOptions.maxBuffer` is now required; throws `RangeError` if a `backpressure` object omits it) [F-SINK]; (6) `actorPool`'s `depthCap` defaults to a finite **`8`** (was silently `Infinity`) — recursive fan-out deeper than 8 throws unless `depthCap: Number.POSITIVE_INFINITY` is passed explicitly [F-ACTOR]; (7) `fromWebSocketReconnect`'s `maxRetries` is now **required** and `opts` is no longer optional (the `Infinity` default was advertised but already threw via `retry()` D4 — stale JSDoc corrected) [F-WS]; (8) `checkpointToS3`/`checkpointToRedis` default `onError` now `console.error`s a dropped checkpoint write / unparseable stored checkpoint instead of fully swallowing it (pass an explicit `() => {}` to restore silence) — non-breaking, additive [F-CKPT].
  - **✅ RESOLVED + LANDED (2026-05-18 "silent-footgun-default" smell sweep, `/dev-dispatch`).** User-locked all 5 graded findings; fail-loud landed for the asymmetric ones, deliberate-exceptions filed for the observable ones. **F-B-root → fail-loud:** `resilientPipeline`'s value-form `rateLimit` no longer injects `?? Infinity`; the type is now `NodeOrValue<RateLimiterOptions>` (maxBuffer required in BOTH forms, validated by the fail-loud `rateLimiter` primitive — `Infinity` = explicit unbounded opt-in). No more value-vs-Node asymmetry. The 4 other landed/filed findings (F-SINK, F-ACTOR, F-WS, F-CKPT) + the 3 deliberate-exceptions (F-PUMP, F-OVF, F-ADAPT) + the F-CATCH follow-on are recorded in the dedicated smell-sweep block below + `archive/optimizations/resolved-decisions.jsonl`.

- **✅ "silent-footgun-default" smell sweep — catalog + decisions (2026-05-18 `/dev-dispatch`, user-locked all verdicts).** Generalized D4 review lens: a public API path that silently substitutes a risky/unbounded/error-swallowing default — *especially* where a sibling/primitive is fail-loud about the same parameter (the asymmetry is the tell). Fail-loud precedents established by D4: `retry.ts:112` (backoff w/o count throws), `rate-limiter.ts:360` (un-seeded/maxBuffer-omitting opts throw), `timeout.ts:164` (missing ns throws), `budget-gate.ts:318` (constraint-throw is `console.error`'d).
  - **Landed fail-loud (3):**
    - **F-B-root** — `presets/resilience/resilient-pipeline.ts` value-form `rateLimit` `?? Infinity` dropped; type → `NodeOrValue<RateLimiterOptions>`; both forms fail-loud via the primitive. Tests: `resilient-pipeline.test.ts` (F-B-root omit-throws + Infinity-opt-in).
    - **F-SINK** — `base/io/_sink.ts` `ReactiveSinkBackpressureOptions.maxBuffer` now required; construction throws `RangeError` when `backpressure` is set without `maxBuffer` (or with a non-integer/<1 value); `Infinity` = explicit unbounded. Mirrors the `rateLimiter` sibling. Tests: `reactive-sink.test.ts — validation`.
    - **F-WS** — `base/io/websocket.ts` `fromWebSocketReconnect`: `maxRetries` required, `opts` required, stale "Default: Infinity" JSDoc corrected. Was a **live latent bug** (zero test coverage; default opts already threw via `retry()` D4). Tests added in `sources.test.ts`.
  - **Landed surface-loud (1, non-breaking):**
    - **F-CKPT** — `base/io/checkpoint.ts` (S3 + Redis): default `onError` → `console.error` (mirrors budgetGate A3) for serialize/persist failure + Redis unparseable-stored-checkpoint load. Pass explicit `() => {}` to suppress. Tests: `adapters.storage.test.ts`.
  - **Landed bounded-default (1):**
    - **F-ACTOR** — `presets/harness/actor-pool.ts` `depthCap` default `Number.POSITIVE_INFINITY` → finite `8` (user-chosen over fail-loud — recursion is a valid use case; a sane cap beats a throw). Explicit `Infinity` opts back into unbounded. Tests: `actor-pool.test.ts`.
  - **Documented-deliberate-exceptions (3, kept by design — rationale on file):**
    - **F-PUMP** — `harness-loop.ts:705-706` `executeMaxPerPump/verifyMaxPerPump ?? Number.MAX_SAFE_INTEGER`. **Kept.** Rationale: concurrency cap, not buffer/memory growth; JSDoc already documents intent ("matches today's unbounded `merge()` parallelism" — behavior-preservation, not a footgun class). No silent data loss.
    - **F-OVF** — `rate-limiter.ts:382` `onOverflow ?? "drop-newest"`. **Kept.** Rationale: drops are **observable** via the `droppedCount` companion node — not a silent swallow; "drop-newest" is a reasonable, surfaced default.
    - **F-ADAPT** — `adaptive-rate-limiter.ts:144,148,189,193` rpm/tpm `?? POSITIVE_INFINITY`. **Kept.** Rationale: an adaptive limiter with no static caps = signal-only mode *by design*; the `?? Infinity` inside the `Math.min` combine is purely defensive.
  - **✅ RESOLVED + LANDED — F-CATCH scoped follow-on sweep (2026-05-18 `/dev-dispatch`, user-locked all 4 graded verdicts).** Audited 26 bare/`best-effort` error-swallowing `catch` sites outside the resilience/harness/io primary scope (`compat/nestjs/*`, `utils/job-queue`, `utils/cqrs`, `utils/surface/*`, `utils/inspect/audit.ts`) with the same fail-loud-vs-documented-deliberate lens. Triage: **16 NOT-A-SMELL** (already re-throw or surface to a client/reactive companion — `explorer.ts:349` console.warn; `gateway.ts:465/516` `sender({type:"err"})`; `cqrs:891/928` re-throw [928 + `cmdNode.meta.error`]; `cqrs:1153/1180` `throw new RebuildError`; `cqrs:1302` `errNode.emit` + errorPolicy; `reduce.ts:73/84/181` typed `SurfaceError` re-throw; `snapshot.ts:137/155/205/316` typed `SurfaceError` re-throw); **10 DELIBERATE-EXCEPTION** (kept, rationale on file — see below); **4 graded → all decided fail-loud/surface-loud:**
    - **D-1 (landed surface-loud)** — `compat/nestjs/explorer.ts` `resolveInstance`: a DI-miss returned `null` *fully silently*, so the caller's `if (!instance) continue;` skipped ALL of a decorated host's `@OnGraphEvent`/`@Cron`/`@CqrsHandler` wiring with zero signal — asymmetric with sibling `resolveCqrsGraph` (`:349`) which already `console.warn`s. Now `console.warn`s a provider-registration diagnostic. **Covers BOTH failure shapes** — `moduleRef.get` may throw *or* return nullish depending on the container; the original fix only caught the throw (test exposed the nullish path). Tests: `nestjs.test.ts` D-1 throws/returns-null variants.
    - **D-2 (landed surface-loud)** — `compat/nestjs/guard.ts` `fromHeader`: a malformed-JSON actor header was silently indistinguishable from an absent one (→ `DEFAULT_ACTOR`, downstream `policy()` denials read as "default actor denied" not "header malformed"). Now `console.warn`s on parse failure; extractor contract unchanged (still `undefined → DEFAULT_ACTOR`; guard is attribution-only, always allows). Tests: `nestjs.test.ts` (malformed warns; missing header does NOT warn).
    - **D-3 (landed surface-loud)** — `utils/job-queue/index.ts` no-requeue nack (`:622` sync `workFn` throw + `:688` result-Node `ERROR`): the failure *outcome* was observable as a `"nack"` `JobEvent` but the *cause* was fully lost. Added `error?: unknown` to `JobEvent` + `nack(id, {requeue,error})`; the cause is recorded **only** on the terminal (`requeue:false`) nack (a requeue is a retry, not a failure). Both pump paths thread the cause symmetrically. Tests: `messaging.test.ts` (direct nack carries cause; requeue carries none; jobFlow sync-throw `:622`; jobFlow async-reject `:688`).
    - **D-4 (landed surface-loud)** — `utils/cqrs/index.ts:676` per-aggregate node name-collision fallback: the fallback raw node silently vanished from `graph.describe()`/`observe()` (contradicts the dynamic-graph observability guarantee). Now `console.warn`s the collision so the visibility loss is diagnosable; stream still functions. Tests: `cqrs.test.ts` (simulated collision race → warns + stream still works).
    - **Documented-deliberate-exceptions (10, kept by design):** `explorer.ts:71` (idempotent teardown `graph.remove` post-`destroy`), `gateway.ts:211` (idempotent stream-`cancel()` double-close), `gateway.ts:587` (`defaultSerialize` JSON-fail → `String(value)` graceful fallback, value still conveyed), `gateway.ts:610/618` (`defaultSend`/`trySend` — WS transport error on a disconnected client is normal/unactionable), `job-queue:707-713` (onDeactivation teardown best-effort, qa-F-F hardened), `cqrs:712` (`resolveOptional` — `*Optional`-named + JSDoc; `undefined` IS the contract), `errors.ts:84` (`safeDetails` — **already** documented in `toJSON` JSDoc; code+message preserved, only non-serializable `details` dropped to avoid crashing the wire; the swallowed throw IS the detector), `audit.ts:743/751` (`safeNode`/`safeAnnotation` read-only introspection — missing-node is normal during a describe-derived walk; **one-line rationale comment added** this pass). Provenance: `archive/optimizations/resolved-decisions.jsonl` id `f-catch-scoped-sweep-2026-05-18`. **NOT cross-track:** all touched files are presentation/compat; `JobEvent` is a presentation type (no `Impl` widening — verified via layer-boundary clean). **🟡 PY parity follow-up pending:** D-3 (`JobEvent.error` / `nack(error=)`) and D-4 (cqrs per-aggregate collision warn) touch `job-queue`/`cqrs`, which have `graphrefly-py` equivalents at TS↔PY parity (section-22-messaging). The TS-only landing is an intentional-but-tracked gap; mirror in `graphrefly-py` (`job_queue` terminal-nack cause + cqrs collision diagnostic) on the next parity sweep. D-1/D-2 are NestJS-compat (no PY equivalent — no follow-up). Surfaced by /qa F-e 2026-05-18.

- **`agentLoop` `effFullDeny` widened from full-deny-only → any-deny in DS-2.7.A `/qa` (2026-05-19, ND-3 user-locked).** Pre-fix: a *partial*-deny interceptor wave (LLM requested `[a, b]`; interceptor allowed `[a]`, denied `[b]`) would land an assistant message with `tool_use:b` but no matching `tool_result` — an invalid transcript per Anthropic/OpenAI tool-use schemas. The next `adapter.invoke` would fail or behave undefined. **Post-fix:** `effFullDeny` now computes the *denied subset* of every wave (`rawCalls \ gatedCalls` by id) and synthesizes `"[tool call denied by interceptor]"` `tool_result`s for it. Full deny still transitions `status→thinking` (or `done` at maxTurns); partial deny does NOT touch status (`effResults` owns it after `toolExecution` completes). **Pre-1.0 public-behavior change — release note:** consumers who asserted `toolMsgs.length === <allowed-count>` on partial-deny waves will now see `toolMsgs.length === <requested-count>` (one synthetic result per denied call). Updated 2 in-tree assertions accordingly (`ai.test.ts` D9 + `tool-interceptor.test.ts` agentLoop-integration). NOT cross-track (presentation-only). Closes EC-1 finding from /qa. **✅ EC-2 carry RESOLVED 2026-05-21 (`/dev-dispatch` + `/qa`, option (b) per user-locked design):** `effResponse`'s batch in [src/presets/ai/agent-loop.ts:498-518](../src/presets/ai/agent-loop.ts) now also synthesizes `"[tool call denied: maxTurns reached]"` `tool_result`s when `capReached && hasToolCalls`, mirroring `effFullDeny`'s `"[tool call denied by interceptor]"` synthesis pattern with a maxTurns-specific reason. Every `tool_use` block now has a paired `tool_result` even on the cap-reaching turn, so audit/replay consumers see a valid transcript. **Pre-1.0 public-behavior change — release note:** a cap-reached turn now appends an additional `N` tool messages (one per `response.toolCalls`); consumers asserting `toolMsgs.length === N` on the *Wave-A-only* path of a `maxTurns:2` full-deny run must update to `=== N+1`. Updated 1 in-tree assertion accordingly (the "repeated full-deny is bounded by maxTurns" test in [ai.test.ts:1547-1558](../src/__tests__/utils/ai/ai.test.ts) — previously asserted `length===1` pinning the orphan-tool_use bug; now `length===2` locking the valid-transcript contract). New regression tests `"EC-2: cap-reached with pending tool_use synthesizes denial tool_results (valid transcript)"` + `"EC-2 guard: cap-reached WITHOUT tool_use leaves transcript untouched"` (`/qa` BH-6 negative-case hardening). **/qa hardening landed in-pass (2 parallel adversarial reviewers, 7 findings — 0 critical/major, 4 rejected as false-alarm/stylistic-vs-precedent, 4 minor patched):** (BH-4) EC-2 test wraps `mockAdapter` with an invoke-counter + asserts `invokeCalls === 1` so a regression that re-invokes the LLM after cap is caught; (BH-6) added the cap-without-tools negative test above; (EC-E) tightened the cited line range; (EC-H) symmetric line-number anchor on the test cite. **Rejected:** BH-1 (cast pattern matches the `effFullDeny` precedent at `agent-loop.ts:585-588`); BH-2 (vendor schema enforces unique tool_use ids — consistent with `effFullDeny`'s also-no-validation precedent); BH-3 (sync `batch`, `latestAborted` can't flip mid-batch — guard at the fn top is sufficient); BH-5 (wave order IS the chronological contract; order-loose would weaken the test). NOT cross-track (presentation-only). Gates: pure-ts 1234 / graphrefly **2022** (+1 BH-6 negative test) / parity 415(+1 skip) / lint(biome+layers+typecheck) 0 / `pnpm build` + DOM-aware browser-safety guard EXIT 0.
- 🟢 **SUPERSEDED 2026-05-19 by DS-2.7.A (`/design-review` → spec amendment).** Original framing — *"core SENTINEL-dep first-run-gate is order-dependent across subscription ordering"* — **did not reproduce** at the bare-`node` level (3 isolated probes deterministically HELD in every order, clean and after prior SENTINEL-dep subscriptions). The empirical defect was different and bigger: a **3-port parity divergence** on the §2.7 gate's settle predicate + `partial` semantics, plus a TS spec text that never caught up with its own Phase 10.5 impl truth (`packages/pure-ts/src/extra/operators/combine.ts:80–106`). Q1=Rust-canonical locked + rule IDs `R2.7.0`–`R2.7.3` minted (DS-2.7.A `/qa` 2026-05-19): RESOLVED does NOT settle (`R2.7.0`); terminal does NOT settle by default (`R2.7.1` — Reduce-class opts in via new `NodeOptions.terminalAsRealInput?: boolean`); `partial: true` truly disables the gate, fn body MUST guard SENTINEL slots (`R2.7.2`); first-run scope = `_hasCalledFnOnce`, INVALIDATE does not re-arm (`R2.7.3`). Spec amended at `GRAPHREFLY-SPEC.md` §2.7 + §2.5 partial row + §1.2 SENTINEL definition; session record: `archive/docs/SESSION-DS-2.7.A-first-run-gate.md`. Cross-track-ledger §2 row added (TS converging; **native predicate-pre-conformant, flag-surfacing slice pending** — Rust dispatches terminal-aware behavior by hardcoded `OperatorOp` discriminant, not yet via a user-facing `NodeOptions.terminalAsRealInput`; PY carved out per Q4). **Two follow-on slices ride next session(s)**: TS slice (`node.ts` gate-scan predicate + `terminalAsRealInput` flag + opt `reduce`/`scan`/`last` into it + `parity-tests/scenarios/core/first-run-gate.test.ts`) AND a thin Rust slice (surface the existing Reduce-class terminal-aware behavior as a `NodeOptions.terminalAsRealInput` flag matching `R2.7.1`). Until both land the documented contract for `toolInterceptor` / `toolSelector` is unchanged: "always seed enabled/predicate/constraint nodes with an `initial`." Original 2026-05-18 framing preserved below for the design record:
  - ✅ **TS IMPL SLICE LANDED 2026-05-20 (`/dev-dispatch`).** `NodeOptions.terminalAsRealInput?: boolean` added (`packages/pure-ts/src/core/node.ts`) + `_terminalAsRealInput` private field + gate-scan predicate rewritten to drop `d.terminal === undefined` from the sentinel test unless the flag is set (R2.7.0 + R2.7.1; R2.7.2 / R2.7.3 unchanged — `partial:true` and `_hasCalledFnOnce` paths already conformant). **Operators opted in (5):** `reduce` + `scan` (`extra/operators/transform.ts`); `take` (both `count <= 0` and `count > 0` branches), `takeWhile`, `last` (`extra/operators/take.ts`); `first`/`find`/`elementAt` inherit via `take`. **Q5 audit completed in-slice (scope expansion from the DS-locked `reduce/scan/last` list)** — implementation-time audit found that `take`/`takeWhile` also need the opt-in to preserve COMPLETE-on-empty-source semantics (they use `completeWhenDepsComplete: false` and emit COMPLETE from fn — without the opt-in, `take(empty(), n)` would never propagate COMPLETE downstream). User-approved at Phase-2 HALT. **Downstream-consumer audit + fixes (4 sites — required, NOT optional):** `restoreEffect` in `src/utils/process/index.ts:1242` (empty-tier restore was the regression that surfaced this audit — `flattened` delivers COMPLETE-only when `tier.list()` returns `[]`, so `restoreState.emit("completed")` would never fire and the watch valve would never open); `approvalGate.output` in `src/utils/orchestration/pipeline-graph.ts:349` (records teardown-decision on source terminal); `catch` in `src/utils/orchestration/pipeline-graph.ts:610` (its whole job is to fire `recover()` on a terminal-only wave); `toArray` in `src/base/sources/async.ts:201` (Reduce-class shape; emits the accumulated array on COMPLETE). Each has a brief JSDoc comment citing R2.7.1. The 5th audit-flagged site (`error-watcher` in `src/presets/harness/refine-loop.ts:562`) was REVERTED in `/qa` (2026-05-20) — the audit motivation ("immediate-throw strategy") crashes the constructor downstream before the opt-in helps, and every realistic non-seed-throw scenario has fn already past the first-run gate via the iter-0 seed DATA. **Tests (20):** 8 TS unit tests in new `packages/pure-ts/src/__tests__/core/first-run-gate.test.ts` (R2.7.0 RESOLVED-only holds; R2.7.1 default vs opt-in for COMPLETE-only + ERROR-only; R2.7.2 partial:true SENTINEL pass-through + de-tautologized ignored-flag pin via terminal-after-fire; R2.7.3 INVALIDATE-doesn't-re-arm with exact-1 assertion); 5 cross-impl parity scenarios in new `packages/parity-tests/scenarios/core/first-run-gate.test.ts` (structural: `map` over sentinel holds; `map` over `empty()` does NOT emit DATA but auto-COMPLETE still propagates; `reduce`/`last` over `empty()` fire and emit seed/default — runs against both pure-ts and `@graphrefly/native` arms, structural-only since `Impl` was NOT widened per cross-track-ledger §2); 4 consumer-opt-in regression tests pinning the documented motivation (`toArray(empty())` emits `[]`+COMPLETE; `approvalGate` records teardown + forwards COMPLETE; `catch(mode:"completed")` runs recover; `restoreEffect` empty-tier path covered by the pre-existing `stateStorage deletes terminal records (Tier 6.5 3.5)`); 3 operator-on-empty regression tests (`take(empty(),n)` emits COMPLETE; `takeWhile(empty(),pred)` emits COMPLETE — `takeWhile` got an additional fn-body fix [QA F1] alongside the opt-in to actually deliver this; `scan(empty(),…)` pins the new `seed → RESOLVED → COMPLETE` wire shape [QA D1]). **`Impl` parity-tests contract is NOT widened** — `terminalAsRealInput` stays out of `Impl.node()` opts; Rust flag-surfacing remains a separate `/porting-to-rs` slice (R2.7.1 literal parity). Until then, the Reduce-class behavior is asserted via the operator factories (pure-ts via `terminalAsRealInput:true`; rust via `OperatorOp::{Reduce,Last,Valve}` hardcoded dispatch — structural equivalence). **/qa pass (2026-05-20) — 2 parallel adversarial reviewers; 7 findings triaged.** Critical/Major fixes landed: **F1** `takeWhile` fn body got an explicit `ctx.terminalDeps[0] === true` → `[[COMPLETE]]` branch (the opt-in alone left `takeWhile(empty(), pred)` emitting only RESOLVED then stuck — pre-existing latent bug surfaced by the opt-in's documented motivation; `take`'s pattern at `take.ts:91` mirrored). **F2** `toArray` NodeOptions spread order corrected: `{ ...opts, completeWhenDepsComplete: false, terminalAsRealInput: true }` so operator-required flags cannot be overridden by user opts (matches the reduce/scan/take/last substrate pattern). **F3** added 4 consumer regression tests + 3 operator-on-empty regression tests. Test-quality: **F4** `currentOf` test helper docstring corrected (cites `dynamicNode`, not `derived`); **F5** R2.7.2 ignored-flag test de-tautologized (now drives both variants through a real terminal AFTER fn fires); **F6** tightened `>= 1` assertions to exact `toHaveLength` to catch double-fire regressions; **D1** scan-on-empty wire-shape pinned. **Reverted in /qa: ND-1** error-watcher opt-in (audit was incorrect — no observable benefit; refineLoop constructor crashes on immediate-throw seed strategy via a different path; non-seed-throw scenarios have fn already past first-run gate). **NOT cross-track-ledger** beyond the existing DS-2.7.A row.
  - ✅ **`*Map` family follow-on (switchMap / exhaustMap / concatMap / mergeMap) LANDED 2026-05-20 (commit `72e28da`).** The DS-2.7.A 4-site downstream audit missed the `*Map` higher-order operator family — all four use `partial:false` via `operatorOpts(...)` and each has an `if (ctx.terminalDeps[0] === true) { ... a.down([[COMPLETE]]) }` branch that fires on empty source. Without `terminalAsRealInput:true`, the gate held forever on an empty outer (COMPLETE-before-any-DATA), so the terminal branch never executed and COMPLETE never propagated downstream. Caught by parity scenario `packages/parity-tests/scenarios/operators/higher-order.test.ts:54` "switchMap completes when outer completes with no active inner" — pure-ts arm failed; rust-via-napi arm PASSED the same test (Rust's hardcoded `OperatorOp` dispatch is unaffected because higher-order operators are not routed through the terminal-aware Reduce-class path). Surfaced 2026-05-20 *because* `/porting-to-rs` S7 D260+D261 fixed the rust-arm's 35 TSFN-bridged failures — the pure-ts bug was masked by the rust noise pre-D260. Fix: add `terminalAsRealInput: true` to all four operators' NodeOptions in `packages/pure-ts/src/extra/operators/higher-order.ts` (one option per operator, identical semantics to the take/takeWhile/reduce/scan/last opt-ins). The existing in-tree P1 test "switchMap outer-complete waits for inner" never caught the bug because it used `initial: 0` on the outer (gate releases on initial DATA before COMPLETE). 1 new `describe` block in `packages/pure-ts/src/__tests__/extra/edge-cases.test.ts` (`R2.7.1: *Map empty-source COMPLETE forwarding`, 4 `it` cases across the family) + a follow-on `switchMap forwards ERROR when outer ERRORs without DATA` regression case added in the `/qa` pass (closes the ERROR-on-empty coverage gap). Gates green per commit `72e28da`; full parity suite was 371 of 372 (1 file excluded — see the deadlock ledger row below). **NOT cross-track-ledger** beyond the existing DS-2.7.A row — no `Impl` widening (`terminalAsRealInput` stays operator-internal). PY mirror rides under `[py-parity-am]` umbrella.
  - **🟠 `@graphrefly/native` `Graph::remove` / `Graph::destroy` napi TSFN deadlock — cross-track item, filed in `docs/cross-track-ledger.md` §2 (row dated 2026-05-20).** Substrate (TS pure-ts) is conformant; the gap is on the rust binding side and blocks the full parity arm. Workaround: exclude `scenarios/graph/remove.test.ts` from parity gates (suite then passes 371 / 372). See the ledger row for the diagnostic trace (worker stuck in `v8impl::ThreadSafeFunction::AsyncCb` → `napi_call_function`), the cross-track parity-implication contract, and the native-side investigation pointer. **NOT introduced by the `*Map` fix** — pure-ts-only change touched no native binding.
  - **(historical, superseded — kept for the design record) 🟡 OPEN — core: SENTINEL-dep first-run-gate activation is order-dependent (surfaced 2026-05-18, Phase 14.5.2 `toolInterceptor` /qa D2).** A non-partial `node([calls, predNode], fn)` where `predNode` is pure SENTINEL (never emitted DATA, no `initial`): in a *clean* module scope the first-run gate holds the wave (fn never fires — `seen === []`); after prior subscriptions to other SENTINEL-containing nodes in the *same module instance*, the same construction fires on the `calls` wave with the SENTINEL slot read as pass-through (`seen === [["x"]]`). Reproduced repeatedly via isolated probes (clean → held; in-suite / after a prior settled-predicate test → fired). The activation outcome for a never-emitted dep is therefore **not deterministic across subscription ordering**. This is a *core* `node` first-run-gate question (does a SENTINEL dep with no cached value participate in the all-deps-first gate?), not a `toolInterceptor`/`toolSelector` bug — both correctly tell callers to seed an `initial` and do not rely on never-emitted gating. **Deferred:** needs a focused core investigation + a spec line in §1/§2 (first-run gate vs SENTINEL deps) before any primitive can promise never-emitted semantics.

- ✅ **RESOLVED + LANDED 2026-05-19 (`/dev-dispatch`, user-locked "continue, not terminate").** Added `effFullDeny` (Effect 2b) in `src/presets/ai/agent-loop.ts`: a `partial:true` detector on the `[toolCallsRaw, gatedToolCallsNode]` diamond that fires when the raw request delivered non-empty DATA but the post-intercept gate produced RESOLVED (no DATA) that wave = **full deny**. Recovery (user sub-decision = continue): synthesize `"[tool call denied by interceptor]"` tool-results for every denied call + `statusNode.emit(latestTurn >= maxTurns ? "done" : "thinking")` — the loop re-prompts with a valid transcript (every `tool_use` gets a `tool_result`) so the LLM adapts under the policy; the `maxTurns` belt bounds repeated denial (no infinite loop). Only wired when an interceptor is configured (`gatedToolCallsNode !== toolCallsRaw`); partial deny stays on the `effResults` path (early-returns). 2 regression tests in `ai.test.ts` ("does not strand the loop — recovers with synthetic denial results"; "repeated full-deny is bounded by maxTurns"), both with a 4 s explicit timeout so a strand regression fails fast instead of hanging. **Implementation-surfaced confirmation of the ② item:** the first cut used `describeKind:"effect"` WITHOUT explicit `partial`, hit the `node()` core default `partial:false` ([packages/pure-ts/src/core/node.ts:985](../packages/pure-ts/src/core/node.ts:985) `opts.partial ?? false`), and the first-run gate held the fn **forever** (the gated dep never delivers DATA/terminal on the deny path) — i.e. the strand fix initially reproduced the exact open-② SENTINEL-dep-gate hazard. Fixing it = the spec-§2.7 raw-operator contract: explicit `partial:true` + handle the RESOLVED/SENTINEL slot in the fn body. This is **production-proven evidence for the ② design-review**: `partial:true`+explicit-body-handling is the deterministic escape hatch; the non-determinism only bites `partial:false` with a never-DATA dep. NOT cross-track (presentation-only, `presets/ai`, zero `Impl`/substrate change). Presentation-only; gates pending the full-suite run this batch. Original analysis preserved below for the design record:
  - **(historical) 🟡 OPEN — `agentLoop` strands in `"acting"` when ALL tool calls are intercepted away (surfaced 2026-05-18, Phase 14.5.2 `toolInterceptor` dev-dispatch).** `agentLoop`'s `effResponse` ([src/presets/ai/agent-loop.ts:477](../src/presets/ai/agent-loop.ts:477)) computes `nextStatus` from `response.toolCalls` (the LLM's *pre-intercept* request): a non-empty `toolCalls` → `status = "acting"`, which commits the loop to awaiting tool results. `effResults` ([:509](../src/presets/ai/agent-loop.ts:509)) early-returns on `arr.length === 0`, so when the post-intercept stream is fully denied (`toolInterceptor` emits `[RESOLVED]`, or any `interceptToolCalls` transform collapses the batch), `toolResultsNode` never delivers a non-empty wave, `status` stays `"acting"` forever, and `loop.run()` never resolves (5s test timeout). This is a **pre-existing agent-loop edge** (the shipped D9 `interceptToolCalls` test only exercises *partial* deny — drops one of two tools), not introduced by `toolInterceptor`; the primitive's node-level contract (filter → `[RESOLVED]` on full-deny, mirroring `toolCallsRaw`'s own empty-batch gate) is correct in isolation and for partial deny. **Fix shape (agent-loop, out of `toolInterceptor` scope):** `effResults` (or a sibling effect keyed off "had pre-intercept tool calls AND post-intercept resolved empty") must drive a turn-advance/terminal transition so a fully-denied tool turn finalizes cleanly (likely: feed the LLM synthetic "all tool calls denied by interceptor" tool results, or transition `"acting" → "done"`/`"thinking"` with a denial notice). Needs a design call on whether a denied turn should *terminate* the run or *continue* with a denial message back to the LLM. Tests for the full-deny + kill-switch paths are currently asserted at the **node level only** (`ai.test.ts` "full-deny → RESOLVED…", "kill-switch…"); the agentLoop integration test deliberately covers the supported *partial*-deny path.

- **🟠 SUBSTRATE-PROMOTION PROPOSAL (2 items, design-session-gated) — surfaced by memo:Re Story 6.4 code review (3 adversarial layers), 2026-05-16. Author-prioritization requested.** The 6.4 review's **only High finding** + most of its Med findings (`flush()` retry double-append, concurrent-`flush()` race, `persistedCount` baseline fragility, `loadAllHistory` silent truncation) are **all artifacts of the first consumer hand-rolling substrate-general orchestration** — not domain bugs. That is the empirical signal these two patterns belong in the substrate, not in userland. Both were user-decided "promote, don't fix in memo:Re" pending a design session.

  - **[Promotion 1 — persistent fact store recipe / `simpleFactStore()` — HIGH strategic value] Closes DS-14.7 open-follow-up #2 ("`simpleFactStore()` ergonomic wrapper — back-derive from cognitive-buddy"). memo:Re Story 6.4 IS that back-derivation.** Today a consumer wanting durable agent memory must hand-compose: `reactiveFactStore` + `ingestLog` (0.47.0 `recordIngest:true`) + an `appendLogStorage` tier + replay-on-startup + **replay-dedup** ("replay re-feeds the ingest log; do NOT re-persist it into an append-accumulate tier"). The replay-dedup is universal to *every* such consumer, subtle, and **silently corrupts when wrong** — memo:Re's `persistedCount` suffix-cursor + the High flush-retry-double-append finding are exactly this hazard class. Proposal: a substrate-owned composed factory/wrapper (`persistentReactiveFactStore({ storage })` or `attachFactStorePersistence(graph, tier)`) that owns log↔store↔replay↔dedup correctly, with the substrate aware of replay-vs-live ingest (so the dedup is correct-by-construction, not a consumer cursor). **Stays userland:** only the bytes-`StorageBackend` adapter (memo:Re's Drizzle/Expo-SQLite impl) — the BigInt codec is already upstream (`bigintJsonCodecFor`). **Design-session topics:** replay-mode semantics on `ingest`, where the durable cursor lives, the additive `recordIngest` opt-in on `createMemoryFactStore` (composition vs topology). Payoff: an entire silent-data-corruption bug class disappears for all future consumers; memo:Re deletes its `persistedCount`/flush-retry/concurrent-flush workarounds rather than owning them forever.
  - **[Promotion 2 — `logProjector` cursor primitive with failure policy + dead-letter topic — clean, bounded] Zero domain content; pure stream-processing infra.** A cursor-driven projector over a log where a per-item sink can poison-fail needs a transient-vs-poison distinction + a configurable failure policy + a dead-letter sink. The substrate already ships the building blocks (Pulsar-style `messagingHub`, `SubscriptionGraph` cursors, `reactiveLog` views) but leaves "what happens when a projection step throws" to every consumer — and memo:Re's hand-rolled cursor gets it wrong (it `break`s on *any* throw, conflating "extension absent" with "this entry is poison" → permanent head-of-line block). Proposal: a generic `logProjector`/`projectorCursor` primitive with `onPoison: "halt" | "deadLetter"` + an exposed `deadLetter` topic graph (carrying item id + error + cursor pos). **Scope guard:** v1 = `halt` + `deadLetter` only; do NOT ship retry/backoff/max-retries (avoid the §44 wrap-imperative-as-primitive over-generalization). Lower risk than Promotion 1; likely a thin composition over existing primitives. memo:Re's user decision was explicitly "make a dead-letter topic graph for that."

  Recommended sequencing for the author: Promotion 2 is the cleaner, lower-risk bounded primitive; Promotion 1 is the higher strategic value (DS-14.7 #2 + kills a corruption class) but is the deeper API-design effort warranting the full 9Q walk. memo:Re's 6.4 code-review left 10 localized userland patches as action items deliberately un-applied — several dissolve if Promotion 1 lands substrate-side, so deciding Promotion 1's home *before* the consumer hand-patches is the cost-effective order. Cross-ref: memo:Re `_bmad-output/implementation-artifacts/6-4-basechange-log-projector-cursors.md` § "Review Findings"; DS-14.7 entry follow-up #2; `messagingHub` (`patterns/messaging.ts`).

  - **✅ DESIGN-REVIEW COMPLETE (2026-05-16, full Q5–Q9 on P1 + bounded pass on P2; user lean locked = substrate-owned cursor). Implementation NOT yet approved — two sub-decisions outstanding.**
    - **Promotion 1 → LOCKED `persistentReactiveFactStore<T>(config & { storage }): ReactiveFactStoreGraph<T> & { position: Node<number> }`** (Q8-A, **synchronous factory** — sub-decision (iii) resolved): in `utils/memory` (presentation, universal tier). **NOT an async factory** — user-corrected: `utils/` is internally consumed by presets/solutions and must stay reactive/composable, so an awaited construction path is rejected. The inherently-IO durable load + replay is modeled as an **internal reactive source** (`fromAsyncIter` over a paginated `tier.loadEntries()` async-iterator) wired into `config.ingest` — the async boundary lives in ONE isolated source node (spec §5.10; `feedback_async_sources_binding_layer.md`), NOT the factory signature. Precedent: `consolidateTrigger`/`decay` are already reactive-source-shaped, not awaited. Readiness is reactive (`position: Node<number>`; `answer` is SENTINEL-until-facts-exist per spec §3) — consumers **observe**, never `await`/poll. **Substrate-owned durable cursor** (user lean — purely internal, exposed only as read-only `position: Node<number>`; consumer never tracks `persistedCount`). Flush durability **delegates to the already-QA-hardened `appendLogStorage.flushNow()`** (reject-on-prior-failure + rollback-epoch + chained-drain, cross-track-ledger §2) — the High double-append + concurrent-flush + truncation findings are all consumer-cursor artifacts that dissolve under this. Silent-truncation fix (non-advancing-cursor detection) lives inside the async iterator → surfaces as an error terminal on the source (observable), kept in the async boundary not the reactive layer. `reactiveFactStore` topology otherwise unmodified. **Rejected Q8-C (fold into `simpleFactStore()`)** — conflates persistence-orchestration with default-config bikeshedding; `simpleFactStore()` (DS-14.7 follow-up #2 / Q9-open-8) stays a *separate later slice that wraps* P1. **Rejected Q8-B (`attachFactStorePersistence`)** — re-introduces the replay-ordering footgun. Residuals: generic `rebuildableProjection()` deferred to v2 (deterministic-replay guarantee is fact-store-specific today); raises stakes on the 3 open cross-track-ledger §2 native-storage rows (flag, not block); **verify `fromAsyncIter` Hermes-safe at implementation** (memo:Re is Hermes; expected pure Promise/async-iter, no `node:*` — confirm, don't assume). **All 3 user sub-decisions resolved:** (i) ✅ approve A over C-bundling; (ii) ✅ cursor purely internal (read-only `position` Node, no mutable handle); (iii) ✅ **synchronous factory + reactive async-source boundary** (NOT async factory).
    - **Promotion 2 → recommend `logProjector(source, { sink, onPoison })`** (Q8-A + `ReactiveLogBundle` overload): `utils/messaging` Graph factory **built on `subscription()`** (reuses the hardened `SubscriptionGraph` cursor — kills the durability-skew + parse-leniency findings), exposing a real `deadLetter: TopicGraph<{ item; error; cursorPos }>` + read-only `position: Node<number>`. Genuinely thin composition (locked-scope prediction holds). **Key invariant (doc-enforced, not typed):** transient-vs-poison is the *sink's* responsibility — sink returns normally = handled/skip (cursor advances), sink **throws** = poison (apply `onPoison`); JSDoc MUST carry an explicit transient-case (return-don't-throw) example. v1 locked `"halt" | "deadLetter"` only, **no programmatic retry** (retry = compose a downstream subscription on `deadLetter`; §44 trap). Naming: `logProjector` (product), not `projectorCursor` (mechanism). Sync-constructed Graph; `idle()` is a **test-only** await-convenience — the canonical reactive observable is `position: Node<number>` (consistent with P1). **User sub-decision ✅ confirmed:** `deadLetter` payload shape `{ item: T; error: string; cursorPos: number }` + no-retry-in-v1.
    - **Cross-cutting:** both cursors share the `cursor ≤ durable-length on restart` invariant + the replay-idempotency property — implement both against the *same internal* "durable cursor advanced only after the consuming side-effect's durability resolves" contract so they don't diverge; expose both positions as `position: Node<number>` (consistent naming). Sequencing unchanged: P2 first (design settled by locked scope), P1 second (full 9Q lock above, pending the 3 sub-decisions).

  - **✅ IMPLEMENTED & LANDED (2026-05-16, `/dev-dispatch`; all 4 user sub-decisions resolved — P1 (i)(ii)(iii) + P2 deadLetter-shape/no-retry). Both ship per the locked design; gates green.**
    - **Promotion 2 — `logProjector(name, source, { sink, onPoison })`** shipped in `src/utils/messaging/index.ts` (`LogProjectorGraph`, `logProjector`, `ProjectorPoisonPolicy`, `DeadLetterEntry<T>`), re-exported via the messaging barrel + root `@graphrefly/graphrefly`. Built ON `subscription()` for a `TopicGraph` source / the bundle's `fromCursor` view for a `ReactiveLogBundle` source (both reuse the hardened cursor — the durability-skew + parse-leniency findings dissolve). Real subscribable `deadLetter: TopicGraph<DeadLetterEntry<T>>` mounted on the graph (observable in `describe()`); read-only `position: Node<number>`; `idle()` test-only. Serialized async drain (inFlight chain, ackPump-effect precedent). v1 = `halt|deadLetter` only, **no retry**. Transient-vs-poison is a documented sink contract (return=handled, throw=poison) with an explicit transient example in JSDoc + a regression test. 8 tests in `src/__tests__/utils/messaging/messaging.test.ts` (order, from:'now', deadLetter, halt head-of-line, async serialization/`maxConcurrent===1`, transient-vs-poison, ReactiveLogBundle source, dead-letter-is-real-topic).
    - **Promotion 1 — `persistentReactiveFactStore<T>(config & { storage })`** shipped as `src/utils/memory/persistent-fact-store.ts`, re-exported via the `utils/memory` barrel + root. **Synchronous factory** returning `PersistentReactiveFactStoreGraph<T>` (NOT a Promise — verified by test); the only async is ONE isolated reactive replay source: `fromAny(loadHistory())` (paginated async-iterator; **`fromAny` used per user steer over `fromAsyncIter` for flexibility** — coerces the async-iterable). Replay re-feeds `config.ingest` via a `replayPump` (the sanctioned `.emit`-into-source replay, `decay`-recipe precedent). **Substrate-owned cursor**: persistence wired on the replay source's COMPLETE terminal via `ingestLog.attachStorage([tier])` (delivered-cursor = `ingestLog.size` at attach ⇒ replayed history NOT re-persisted; consumer tracks nothing). **flush() delegates** to the hardened `appendLogStorage.flush`. Non-advancing-`loadEntries` detection throws inside the async iterator (loud, not silent). `position`/`replayedCount` reactive observables. **Implementation-surfaced hardening (correct-by-construction):** a one-shot reconciliation ships the `[replayed, sizeAtAttach)` slice on COMPLETE so live fragments emitted in the construction tick (before the async replay drains) are not mis-counted as replayed history and dropped — steady-state delta-shipping stays the substrate `delivered` cursor. 4 tests added to `src/__tests__/utils/memory/fact-store.test.ts` (sync-factory-shape, first-run-persist + restart byte-equiv + no-double-persist + new-live-persists, position-observability, empty-backend + pre-attach-live).
    - **Hermes residual resolved:** `fromAny`/`fromAsyncIter`/`fromPromise` (`extra/sources/async.ts`) import only `core` + sync `iter` — pure Promise/async-iterator, zero `node:*`/DOM. Universal tier confirmed by `pnpm build` (`assertBrowserSafeBundles` clean) → Hermes-safe for memo:Re.
    - **NOT a cross-track-ledger item.** Both are presentation-only (`@graphrefly/graphrefly` `utils/`), pure compositions over existing substrate (`subscription`/`topic`/`reactiveLog`/`attachStorage`/`appendLogStorage`/`fromAny`). Zero new `Impl` symbols, zero substrate change — no `Impl` widening, no presentation↔Rust coupling. (The underlying `attachStorage`/`appendLogStorage` native-parity obligations already have their own §2 ledger rows from the memo:Re 0.47.0 hardening; these factories add none.)
    - **Gates:** pure-ts 1210 ✓ / graphrefly **1922** (+12) ✓ / parity 353(+1 skip) ✓ / biome + layer-boundary 0 violations (confirms presentation-only) ✓ / `pnpm build` DTS + browser-safe exit 0 ✓. Doc-drift spawn-flagged: pre-existing stale `@graphrefly/graphrefly/patterns/messaging` in `messagingHub` JSDoc (cleave-A residual, out of scope here). **Closes DS-14.7 follow-up #2's persistence half** — `simpleFactStore()` (the default-config ergonomic wrapper) remains the separate later slice that *wraps* `persistentReactiveFactStore`. Run `/qa` for adversarial review.
  - **✅ /qa PASS (2026-05-16) — 2 parallel adversarial reviewers; 7 findings triaged, all resolved in-pass.** **A (HIGH, user-decided):** `admissionFilter` desynced the `replayed ↔ ingestLog.size` accounting (re-applying admission to the already-post-admission persisted stream → silent durable-log divergence under any stateful/non-deterministic filter). **Fix = omit `admissionFilter` from `PersistentReactiveFactStoreConfig`** (`Omit<…, "recordIngest" \| "admissionFilter">`) — the durable log is the post-admission truth; filter once upstream of `config.ingest`. Eliminates the skew class by construction (no filter ⇒ `replayed === ingestLog.size` exactly). **B (MED):** the paginated-loader / non-advancing-cursor "loud on truncation" guard was unreachable dead code (`appendLogStorage.loadEntries` ignores `cursor`/`pageSize`, returns the whole log in one page) — `loadHistory` simplified to a single `loadEntries()`, `pageSize` config field + dead guard removed, module docstring corrected (no partial-truncation risk *by construction*, not "detected"). **C (MAJOR vs the locked "no retry"):** `onPoison:"halt"` re-invoked `sink(poison)` on every later append (implicit unbounded retry, repeated side-effects) — `halt` is now a HARD LATCH (`sink` hits the poison exactly once, then the projector freezes; stalled `position` is the signal). **F (LOW):** silent `undefined`-skip in the reconciliation slice → now throws loud (contiguity is an invariant; a hole is a bug). **E (LOW):** pre-attach-live reconciliation write is fire-and-forget — documented on `flush()` JSDoc (call `flush()` post-construction to confirm durability / surface the rejection). **D (DEFER, pre-existing):** absolute-cursor-vs-trimmed-window is inherited `subscription()`/`fromCursor` behaviour (not introduced here) — added a `logProjector` JSDoc caveat ("use an unbounded source for long-lived projection"); deep fix is a substrate `fromCursor` semantics question, out of scope. **G:** +2 regression tests (halt-latch sink-invoked-exactly-once-after-later-appends; destroy-mid-replay clean abort + idempotent double-dispose), `settle()` ordering contract documented. Non-findings dropped (inFlight chain bounded; rejection handling correct; `.cache` reads sanctioned/precedented; projector fixpoint sound; teardown safe; one reviewer's "cascade/consolidator grows ingestLog" claim verified false). **Gates re-run:** pure-ts 1210 ✓ / graphrefly **1924** (+2 QA) ✓ / parity 353 ✓ / biome + layer-boundary 0 ✓ / build DTS+browser-safe exit 0 ✓.
  - ~~**🟡 DEFERRED follow-up (surfaced by /qa-B) — `appendLogStorage.loadEntries` real cursor pagination.**~~ **✅ LANDED 2026-05-17 (Phase B).** `packages/pure-ts/src/extra/storage/tiers.ts` `appendLogStorage.loadEntries` now honors `opts.cursor`/`opts.pageSize` with a `[start, start+pageSize)` window + forward-only `AppendCursor` (`undefined` = no more); bare `loadEntries()` is byte-for-byte the prior whole-log behaviour (back-compat held). Decoding short-circuits one entry past the window (`want = start+pageSize+1`) so partitioned multi-key logs skip out-of-window keys (single-key blob still decodes in full — per-key codec-blob model; pagination bounds the *consumer's* per-page working set). **/qa-hardened 2026-05-17:** Blind#6 — non-integer/`Infinity`/`NaN` `pageSize` now degrades to the whole-tail path (`Number.isInteger && > 0` gate) instead of `slice`-truncating a float window; Blind#4/#5/Edge#4 — JSDoc now states no-`pageSize`+`cursor` honors `cursor.position` (forward-only tail; pinned by test — unreachable for pre-change callers since `cursor` was never returned without `pageSize`) and that paginated `loadEntries` requires a stable `backend.list()` order + append-only log between page calls (all shipped backends sort keys — safe; custom-backend obligation also captured in the ledger §2 row). Regression tests in `packages/pure-ts/src/__tests__/extra/storage.test.ts` (windowed-pagination + cursor-walk; back-compat + empty + cursor-past-end + no-pageSize-honors-cursor + non-integer-pageSize). **Cross-track-ledger §2 row added** (2026-05-17 — native `appendLogStorage`/WAL replay must mirror the window + cursor protocol + back-compat; handed to `~/src/graphrefly-rs/docs/migration-status.md`). **Consumer follow-up (out of Phase B scope, not blocking):** `persistentReactiveFactStore`'s `loadHistory` (`src/utils/memory/persistent-fact-store.ts`) can now optionally stream pages + re-add the non-advancing-cursor guard (the /qa-A dead-guard removal becomes live again) — its current single-`loadEntries()` call still works (no-pageSize = whole log).

- **🟠 CONSUMER-DRIVEN — Reactive Layout for React Native / Hermes (surfaced by memo:Re §15 zoom-hierarchy UX, SCP-2026-05-19; de-risked consumer-side by memo:Re Story 3.6 spike-gate).** memo:Re's L1/L2 spatial UI needs `reactiveLayout`/`reactiveBlockLayout`/`reactiveList`-per-item for **exact predicted block heights before paint** (gravity-snap timeline can't estimate-then-correct without jitter) on **RN + Hermes + Skia render**. Three gaps for *any* RN consumer, not memo:Re-specific. Per the pre-1.0 consumer-demand policy (bounded + correctness-positive + known API shape ⇒ do now; speculative shape ⇒ POST-1.0):
  - **(a) RN measure-adapter contract + reference adapter — DO-NOW-candidate (API shape known).** Reactive Layout ships only `CanvasMeasureAdapter` (browser/OffscreenCanvas) + `CliMeasureAdapter`. RN has no DOM/OffscreenCanvas. The `MeasureAdapter` interface is *already* the pluggable seam — add a documented **RN adapter contract** + a reference adapter (text measurement via `@shopify/react-native-skia` `Skia.Font`/`Paragraph`, or RN core text metrics). **Substrate ships the contract + reference; the concrete Skia binding stays userland** — exact precedent: the `bytes-StorageBackend` Drizzle/Expo-SQLite adapter stayed userland while `bigintJsonCodecFor` went upstream (Promotion-1 block above). Confirm symbol-glyph widths (✿/❧/⬚, non-emoji, Kalam/Caveat) measure correctly.
  - **(b) `utils/reactive-layout` Hermes-safety audit + bundle-assertion — DO-NOW (bounded, known mechanism).** Same bar/precedent as the `fromAny`/`fromAsyncIter`/`fromPromise` audit (this section, "Hermes residual resolved" — `assertBrowserSafeBundles` clean ⇒ Hermes-safe for memo:Re). Confirm the `reactive-layout` core (minus the browser `CanvasMeasureAdapter`, which is pluggable-out) imports only `core` + sync `iter`, zero `node:*`/DOM; extend `assertBrowserSafeBundles` coverage; document Hermes-safe with the adapter as the sole DOM touchpoint. memo:Re cannot add `@graphrefly/graphrefly/utils/reactive-layout` to its Hermes allow-list until this lands (gated behind Story 3.6, same as the 6.1 spike / Story 2.8 precedent).
  - **(c) `fromRaf` strict background-pause option — DO-NOW-candidate (bounded; battery-correctness).** The Reactive-Layout solution doc admits `fromRaf` falls back to `setTimeout` when hidden ("wrap or replace at the source" for strict pause). For mobile/battery consumers (memo:Re NFR5 < 5%/night + the §15.3.1 idle-timeout drift) a first-class `fromRaf({ pauseWhenHidden: true })` (or an RN-`AppState`-aware source variant) avoids a backgrounded recompute/timer footgun. **This is the "second consumer" the existing fromRaf-drift item explicitly waits for** (this section, "the drift *driver* … Revisit if a second consumer wants a reusable reactive physics integrator" — memo:Re §15 idle-timeout drift IS that consumer; consider co-designing).
  - **Tracked limitations (no new work — already documented fidelity caveats):** CJK per-grapheme breaking ✅ (memo:Re is bilingual zh/en — load-bearing, good); full bidi + emoji-width correction ❌ (acceptable for EN+zh symbol glyphs). Keep visible in the Reactive-Layout vs Pretext comparison.
  - **Reciprocity:** memo:Re Story 3.6 (render/layout substrate spike, Epic-6-gated) produces the on-device Hermes + RN-Skia-adapter evidence; findings flow back here. Cross-ref: memo:Re `_bmad-output/planning-artifacts/sprint-change-proposal-2026-05-19-zoom-hierarchy.md` (architecture Open-Item #10 + Story 3.6); `website/src/content/docs/solutions/reactive-layout.md`.
  - **✅ (a)+(b)+(c) LANDED 2026-05-19 (`/dev-dispatch`, user-approved scope incl. `fromPushNotification`).** Presentation-only, zero `Impl` widening, NOT a cross-track-ledger item.
    - **(a) → `InjectedMeasureAdapter`** (`src/utils/reactive-layout/measurement-adapters.ts`, re-exported via the `utils/reactive-layout` barrel). User sub-decision = **generic injected adapter only** (no Skia-shaped type) — wraps any sync `(text, font) => widthPx` (`MeasureFn`), optional `{cache:true}` memo by `font+text`, fail-loud `TypeError` on a non-fn. The documented RN measure-adapter contract (sync+pure); concrete Skia/RN binding stays userland (the `bigintJsonCodecFor` split, as specified). 6 tests in `measurement-adapters.test.ts` (delegation, contract, cache+clearCache, no-cache default, reactiveLayout drop-in 2-line wrap, non-fn throws). Symbol-glyph correctness is delegated to the host fn by construction (RN measures its own glyphs) — no substrate width table to verify.
    - **(b) → scoped browser-safety bundle assertion in the ROOT `tsup.config.ts` `onSuccess`.** Premise-corrected: pure-ts's `assertBrowserSafeBundles` only covers the *substrate* package; the root presentation package had **NO** post-build guard at all (not "extend pure-ts's" — there was nothing covering presentation entries). Added a faithful trimmed mirror (extractImportSpecs + BFS chunk-graph + bare/`node:` builtin set; no version-token/`restoreNodePrefix` — pure-ts D4 concerns) **scoped to `GUARDED_UNIVERSAL_ENTRIES`** = `utils/reactive-layout/index` + `base/sources/event/index` (NOT all-entries-minus-allowlist — a flag-day audit of every presentation entry is a separate effort; the set is the extension point). Audit result: reactive-layout core statically imports only `@graphrefly/pure-ts/core` + `/graph` + relatives; `_ascii-width.ts` has zero imports; the sole DOM touchpoint (`CanvasMeasureAdapter` `OffscreenCanvas`) is **runtime-guarded** (`typeof`), not a static import ⇒ structurally Hermes-safe, now build-enforced (`pnpm build` EXIT 0 with the guard live). Documented in `website/src/content/docs/solutions/reactive-layout.md` (new "React Native / Hermes" section). memo:Re may now add the subpath to its Hermes allow-list (Story 3.6 gate satisfied substrate-side).
    - **(c) → `fromRaf({ pauseWhenHidden: true })`** (`src/base/sources/event/dom.ts`, new `FromRafOptions` type). When hidden + opted-in, `scheduleNext()` schedules **nothing** (no rAF, no `setTimeout` keep-alive); the `visibilitychange` listener (now registered when `doc && (raf || pauseWhenHidden)`) re-enters `scheduleNext()` on visible → resumes from the next frame. Default `false` = legacy setTimeout-fallback preserved (pinned by a regression test). No `document` (RN/Hermes) ⇒ never auto-pauses; documented that the host drives background pause (AppState gate / unsubscribe). 2 tests (parks-while-hidden+resumes; legacy-default-still-ticks). Co-design with the fromRaf-drift item deferred (this resolves the strict-pause footgun; a reusable physics integrator is still its own slice).
    - **`fromPushNotification` (memory back-log item, user added to scope)** — new universal source `src/base/sources/event/push.ts`, exported via `base/sources/event/index.ts` → root barrel; registered in `gen-api-docs.mjs` (`fromPushNotification.md` generated, `docs:gen:check`+`sync-docs:check` clean). Transport-agnostic DI source (FCM/APNS/Expo/Web-Push/NestJS fan-out): `fromPushNotification(register, opts?)` where `register(deliver) => unsubscribe|void`; the native/network bridge lives entirely in the host callback (spec §5.10 — sanctioned async/source boundary, same shape as `fromEvent`, NOT an imperative trigger; the two-reactive-island transport per the memo:Re memory). Hermes-safe by construction (imports only `@graphrefly/pure-ts/core`; covered by the new (b) guard). 3 tests. **Gates:** pure-ts 1218 / graphrefly **2003** / parity 364(+4 skip) / lint (biome+layers+typecheck) 0 / `pnpm build` + new browser-safety guard EXIT 0 / docs:gen:check + sync-docs:check clean.
  - **✅ /qa PASS (2026-05-19) — 2 parallel adversarial reviewers; 1 critical + 7 graded findings, all resolved in-pass; 1 deferred, 2 rejected.** **F1 (CRITICAL, self-caused):** 2 raw `0x00` NUL bytes in `measurement-adapters.ts` (a typed space became `\x00`) — `:111` live cache-key separator + `:91` JSDoc — made the file binary (git/`file`/tooling hazard) and was a latent cache-collision-on-NUL-strip. Fixed: cache key now the explicit escape `` `${font} ${text}` `` (intentional, visible, collision-safe delimiter); JSDoc prose corrected; 0 NUL bytes, file is text. **F2 (MAJOR — user-decided "also detect bare DOM globals"):** the guard only scanned `node:*`, but the docs/comment claimed it "fully establishes Hermes-safety" (the reactive-layout chunk *does* transitively reach `typeof`-guarded `OffscreenCanvas`). The guard now ALSO flags any `DOM_GLOBALS` identifier referenced bare (not `.prop`) in a reachable chunk with **no `typeof <id>` guard in that file** — mechanically enforces the `CanvasMeasureAdapter` `typeof`-bail convention + catches a new unguarded-`document.x` regression. Verified empirically: passes the legit `typeof`-guarded reactive-layout chunk, flags a synthetic unguarded `document.querySelector`, no `.document` false-positive. Doc/comment wording de-overclaimed (heuristic, not a DOM-freedom *proof*; ultimate safety still = runtime guards + audit). **F3 (MAJOR):** `resolveSpec` silently dropped unresolvable **relative** specifiers → the chunk-graph walk could false-negative; now collects unresolved `.`-relative specs and **fails the build loud** (externals/`node:` still intentionally unresolved). **F4 (MAJOR, test efficacy):** the two `fromRaf` tests passed via the node-env no-`raf` setTimeout fallback, not the visibility/rAF branch they claimed; rewrote with an installed `requestAnimationFrame`/`cancelAnimationFrame` stub + `rafCalls` counter so the `raf && !hidden` gate is genuinely exercised both ways (hidden⇒setTimeout w/ rafCalls===0; visible⇒rAF w/ rafCalls>0; parked⇒0/0). **F5:** dead `.slice(5)` ternary in the builtin filter simplified. **F6:** added an `InjectedMeasureAdapter` cache delimiter-collision test (`("bc","a")` vs `("c","ab")`, `("b c","a")` vs `("c","a b")` — 4 distinct keys, no false hit). **F7:** `fromRaf` added to `gen-api-docs.mjs` REGISTRY (`fromRaf.md` generated — slice's own bar; `fromPushNotification` got it). **F8:** `fromPushNotification` JSDoc `@remarks` — synchronous-`register`-throw propagates as activation failure (mirrors `fromEvent`); never self-emits COMPLETE/ERROR (open-ended transport). **Deferred (pre-existing, not introduced):** `fromRaf` `onVisibilityChange` could double-`scheduleNext` under a *synchronous re-entrant* `visibilitychange` during `a.emit` (no "scheduling" flag) — pre-existing `fromRaf` structure, effectively impossible to trigger; left as a documented low-priority follow-up, no consumer pressure. **Rejected:** per-format double-run of `onSuccess` (matches the proven pure-ts guard shape; not in watch); first-offender `break` (faithful mirror of the pure-ts precedent). **Verified non-findings:** `fromPushNotification` teardown/`done`-guard/re-activation correct (block-scoped `done`, substrate `fromTimer` precedent); BFS `visited` cycle handling sound; `performance.now()` is a pre-existing source-boundary payload (not internal scheduling); all new public symbols barrel-exported at the correct tier (`FromRafOptions` browser-tier alongside `fromRaf`). **Gates re-run:** pure-ts 1218 / graphrefly **2004** / parity 364(+4 skip) / lint 0 / `pnpm build` + DOM-aware guard EXIT 0 / docs:gen:check + sync-docs:check clean. **Remaining open in this block:** "Tracked limitations" (no work — documented caveats) + "Reciprocity" (memo:Re Story 3.6 on-device evidence flows back).
  - **✅ (d) `Intl.Segmenter` undefined on Hermes — RESOLVED 2026-05-21 (commit `e387711`, path (i) per memo:Re bias).** New `SegmentAdapter` injection face shipped in [src/utils/reactive-layout/reactive-layout.ts](../src/utils/reactive-layout/reactive-layout.ts) (the `SegmentAdapter` interface + `segmentAdapter?: SegmentAdapter` opt on `ReactiveLayoutGraph` / `reactiveBlockLayout` / `reactiveFlowLayout`) + reference `IntlSegmentAdapter` + lazy module-shared `getDefaultSegmentAdapter()` in [src/utils/reactive-layout/measurement-adapters.ts](../src/utils/reactive-layout/measurement-adapters.ts). All 6 `new Intl.Segmenter(...)` call sites in `reactive-layout.ts` threaded through the adapter — only `IntlSegmentAdapter`'s eager constructor still touches the global (throws a clear "supply a `SegmentAdapter` via `reactiveLayout({ segmentAdapter })`" error on Hermes, matching the `CanvasMeasureAdapter` precedent). Hermes consumers wire a polyfilled `SegmentAdapter` (e.g. backed by `@formatjs/intl-segmenter`) at construction — substrate stays ICU-byte-free by construction, CJK fidelity preserved by host choice. `_resetDefaultSegmentAdapterForTests` exported as a test seam. Tests + website doc + cross-track-ledger updated in the same commit. Parallel to the just-shipped `InjectedMeasureAdapter` (a) precedent. NOT cross-track-ledger (presentation-only); NOT an `Impl` widening. Original 2026-05-20 framing preserved below for the design record.

  - **🟢 SUPERSEDED 2026-05-21 by ✅ (d) RESOLVED above. (d) `Intl.Segmenter` undefined on Hermes — RUNTIME gap NOT covered by (b)'s import-surface audit (surfaced by memo:Re Story 3.6 on-device spike, 2026-05-20).** `analyzeAndMeasure` / `_graphemeSegmenter()` / the word-segmenter sites call `new Intl.Segmenter(undefined, { granularity: "word" | "grapheme" })`. On Hermes (iOS 26.5 / RN 0.83 — both `Intl.Segmenter typeof=undefined`), the constructor throws `TypeError: Cannot read property 'prototype' of undefined` immediately, which marks `measured-blocks` non-resubscribable terminal, which wedges `block-flow` on first `useSubscribe`. (b)'s `assertBrowserSafeBundles` is import-surface only — `Intl.Segmenter` is a runtime global, not a `node:*` builtin or DOM ident, so it never tripped the guard. **Call sites (6, all in `src/utils/reactive-layout/reactive-layout.ts`):** word-granularity in `analyzeSegments` (`:196`); grapheme-granularity inline at `:262`, `:470`, `:482`, `:1047` + the module-scoped lazy cache `_graphemeSegmenter()` (`:660–665`). All 6 use the same `new Intl.Segmenter(undefined, { granularity })` shape with no abstraction — single chokepoint for whichever resolution path lands. **Evidence:** memo:Re Story 3.6 spike route surfaces `substrate measureBlocks(...) THREW: TypeError: Cannot read property 'prototype' of undefined`; `Intl.Segmenter typeof=undefined`; `cliAdapter.measureSegment(...) OK width=224` (the CLI adapter is unaffected — the segmenter use is intrinsic to the substrate's *segmentation* pipeline, not the *measurement* adapter that (a)/`InjectedMeasureAdapter` already split out). **Three resolution paths in increasing scope:** (i) **inject the segmenter** — extend the adapter-contract face so the host provides word/grapheme segment iterators (smallest substrate-side delta; keeps reactive-layout Hermes-runtime-clean by construction; parallel precedent: `InjectedMeasureAdapter` (a) split — substrate ships contract + reference impl using `Intl.Segmenter` when available, concrete RN/Hermes binding stays userland); (ii) **graceful fallback** to ASCII-word + code-point iteration when `typeof Intl.Segmenter !== "function"` (preserves the current adapter contract; loses CJK/grapheme fidelity on Hermes — acceptable per the existing emoji-width fidelity caveat, but the bilingual zh/en consumer (memo:Re) explicitly relies on CJK per-grapheme breaking per "Tracked limitations" above); (iii) **bundled polyfill** — substrate ships `@formatjs/intl-segmenter` (or equivalent) as a `compat/hermes-intl` opt-in entry, host adds one import. **memo:Re bias:** (i) > (ii) > (iii) — host-injected mirrors the `InjectedMeasureAdapter` precedent that already shipped in this block, keeps the substrate ICU-byte-free by construction, and preserves CJK fidelity (host picks the polyfill it wants). (ii) is the breakage-free fallback if (i) is too invasive for one slice; (iii) is the consumer-easiest but ships ~50 KB to *every* reactive-layout consumer (including ones that already have segmenters available). **Blocker chain:** memo:Re Stories 3.7–3.10 (Epic-3 spatial UI) cannot enter dev until ONE of (i)/(ii)/(iii) lands; the spike Task 6 GATE explicitly cites this finding. **Today's consumer-side unblock (not a substrate ask):** memo:Re polyfills at the host (e.g. `intl-segmenter-polyfill` or `@formatjs/intl-segmenter` imported before any reactive-layout import) — preserves CJK fidelity, ~50 KB to memo:Re only, unblocks the spike immediately while (i) is scoped + landed upstream. **NOT cross-track-ledger** (presentation-only; substrate already universal/Hermes-safe at *import* surface — the runtime gap is the bar, not the import bar). **NOT an `Impl` widening** (the segment-iterator face is presentation-layer adapter glue, sibling to `InjectedMeasureAdapter`, not part of the parity contract). **Cross-ref:** `website/src/content/docs/solutions/reactive-layout.md` "React Native / Hermes" section should be amended on resolution to call the runtime gap out explicitly (today's "Hermes-safe via assertBrowserSafeBundles" line is true at import but reads as a stronger claim than it is).

  - **✅ (e) `compat/react` `useSubscribe` — RESOLVED 2026-05-22 (`/dev-dispatch`).** Surgical patch landed exactly as designed below: `subscribe` + `getSnapshot` closures in `useSubscribe` now built via `useCallback(…, [node])` so `useSyncExternalStore` sees stable refs across renders. `src/compat/react/index.ts:24-40`. Regression test added at `src/__tests__/compat/react.test.ts` ("useSubscribe is stable under StrictMode (no fresh-closure re-subscribe loop)") — mounts under `<StrictMode>` against a real `node()` (which fires push-on-subscribe synchronously) and asserts bounded render count on mount + on a single `down([[DATA, …]])` settle. Pre-patch under StrictMode + push-on-subscribe: `Maximum update depth exceeded`. Post-patch: stabilises in ≤ 5 renders on mount, ≤ 4 additional on settle. `useStore` auto-fixed via internal `useSubscribe` call; `useSubscribeRecord` was already correct (refs come from `useMemo`). Gates: graphrefly suite + parity green, lint (biome + layers + typecheck) clean. Memo:Re may drop its `useStableGraphreflySubscribe` wrapper on the next bump. Original 2026-05-21 framing + patch design preserved below for the design record.

  - **🟢 SUPERSEDED 2026-05-22 by ✅ (e) RESOLVED above. (e) `compat/react` `useSubscribe` — fresh-closure-per-render re-subscribes against `useSyncExternalStore`; loops on push-on-subscribe nodes (surfaced by memo:Re Story 3.6 paired session, 2026-05-21; latent until (d) RESOLVED above unblocked Hermes runtime and consumers reached the React-binding layer).** `src/compat/react/index.ts:24-39` constructs the `subscribe` and `getSnapshot` closures **inline** on every render of `useSubscribe(node)`:
    ```ts
    return useSyncExternalStore(
        (onStoreChange) => { /* fresh closure every render */ … node.subscribe(...) … },
        () => node.cache,
        () => node.cache, // Server snapshot
    );
    ```
    React's `useSyncExternalStore` treats the `subscribe` argument by reference: a new reference ⇒ unsubscribe-old + subscribe-new on every render. Substrate `Node<T>.subscribe(cb)` fires its handshake **synchronously on registration** (the standard push-on-subscribe shape — same wave-attach pattern as `ReactiveLogBundle.attach`'s cached-replay frame). Combined, each render synchronously re-subscribes ⇒ fires the handshake ⇒ schedules a state commit ⇒ next render gets a new `subscribe` closure ⇒ repeat. React 19.2's update-depth guard catches this as `Error: Maximum update depth exceeded` and tears down the route. **Latent until 0.48.0:** before the (a)/(b)/(c) batch + (d) `SegmentAdapter` resolution, `measured-blocks` was non-resubscribable terminal on Hermes from the first activation, so no consumer ever got a *live* layout node into `useSubscribe` to trigger the loop. The 2026-05-21 memo:Re Story 3.6 paired-device session was the first to reach this layer; the loop reproduces on a clean iPad Air 5 / iOS 26.5 / RN 0.83 / Hermes / Expo SDK 55 build the moment `useSubscribe(reactiveBlockLayout.measuredBlocks)` returns a real `MeasuredBlock[]`. **Scope of the bug:** `useSubscribe` (`:24-39`) is the lone offender. `useStore` (`:49-58`) **auto-fixes** because it calls `useSubscribe` internally; setter at `:51-56` already memoizes via `useCallback([node])`. `useSubscribeRecord` (`:75+`) is **NOT affected** — its `store.subscribe` / `store.getSnapshot` come from `useMemo`-built `store` (`:82-…`) so the refs are render-stable across React's diff. Single-file fix, two functions auto-fixed, one peer correct by construction. **Surgical patch (5 lines net add):**
    ```ts
    export function useSubscribe<T>(node: Node<T>): T | undefined | null {
        const subscribe = useCallback(
            (onStoreChange: () => void) => {
                let disposed = false;
                const unsub = node.subscribe(() => { if (!disposed) onStoreChange(); });
                return () => { disposed = true; unsub(); };
            },
            [node],
        );
        const getSnapshot = useCallback(() => node.cache, [node]);
        return useSyncExternalStore(subscribe, getSnapshot, getSnapshot);
    }
    ```
    The load-bearing change is `useCallback([node])` on `subscribe`. Wrapping `getSnapshot` is defensive (React's `useSyncExternalStore` semantics only ref-check `subscribe`; `getSnapshot` is called every render and the return value is `Object.is`-diffed) but the symmetry keeps both closures rebuild-on-`node`-only and removes the foot-gun for any future React internal that ref-diffs `getSnapshot`. **Regression test shape:** mount a component that subscribes to a graphrefly state node which fires its `subscribe(cb)` handshake synchronously on registration (the actual `Node` shape, not a fake — exercise the real `node.subscribe` path), under `<React.StrictMode>` (React 19.2 strict mode amplifies the loop, makes the failing case deterministic), and assert React render count stays bounded (e.g. ≤ 5 commits over 100 ms). Pre-fix: throws `Maximum update depth exceeded`. Post-fix: stabilises at 1–2 commits. Suggested home: `src/__tests__/compat/react/useSubscribe.test.ts` (or whichever existing `compat/react` test file lives — none in the working tree at scan time). **NOT cross-track-ledger** (`compat/react` is presentation-only, no rust impl to converge); **NOT an `Impl` widening** (no `Impl` contract method involved). Behaviour-preserving for any consumer who already memoizes at the call site (rare in practice — most just call `useSubscribe(node)` and trust the binding). Suggested release: `0.48.1` patch alongside the (a)/(b)/(c)/(d) batch reciprocity — memo:Re consumers can drop their local `useStableGraphreflySubscribe` wrapper on the bump. **Memo:Re bias:** push as a `0.48.x` patch — the bug is latent for every RN consumer the moment they reach the React-binding layer with a working substrate. **Today's consumer-side unblock (not a substrate ask):** memo:Re carries `useStableGraphreflySubscribe(node)` in `packages/app/src/features/spatial/` — a 10-line `useCallback`-stable wrapper around the same `useSyncExternalStore` shape; binding rule #3 in memo:Re's Story 3.6 GATE one-pager mandates the wrapper "until upstream lands `useCallback`". Wrapper retires on `0.48.x` bump. **Cross-ref:** memo:Re Story 3.6 paired-session evidence in `_bmad-output/planning-artifacts/story-3-6-gate-result.md` §5(B) (paste-ready PR prose); the workaround helper lives in `packages/app/src/features/spatial/useStableGraphreflySubscribe.ts`.

- **Clock return-type doc-truth (anti-pattern guard, recorded 2026-05-15).** `monotonicNs()` / `wallClockNs()` in `packages/pure-ts/src/core/clock.ts` return **`number`, not `bigint`** — despite the DS-14.7 design doc and a natural reading of CLAUDE.md's "Time utility rule" implying bigint. Code that stores a clock value into a `bigint` field (e.g. `MemoryFragment.t_ns`/`validTo`) MUST wrap `BigInt(monotonicNs())` — that wrap is load-bearing, not redundant (removing it fails the DTS build, `TS2322`). A QA pass (F6) and two agents independently tripped on the wrong "it's already bigint" premise. Do not "simplify away" `BigInt(...)` wraps on clock results; do not assume `bigint` arithmetic on clock values.

- **DS-14.7 `reactiveFactStore` — substrate for MEME L2/L3 + continual learning (v1 LANDED 2026-05-15, QA-hardened).** Static-topology fact store implemented at `src/utils/memory/fact-store.ts` (re-exported via the `utils/memory` barrel), 25 tests in `src/__tests__/utils/memory/fact-store.test.ts`, full suite green (graphrefly 1879). Supersedes the prior "outcome-feedback adaptive `agentMemory()`" thinking (SESSION-agentic-memory-research.md Part 6 §opportunity #1) — that closed-loop is now the `outcome → scoring: Node<ScoringPolicy>` extension face. Canonical design + 9Q lock: `archive/docs/SESSION-DS-14.7-reactive-fact-store.md`. **Open follow-ups (deferred, not blockers):** (1) ✅ **LANDED 2026-05-16** — recipe library `src/utils/memory/recipes/*` (8 exported factories; the decay-face gap is RESOLVED — archived to `archive/optimizations/resolved-decisions.jsonl` id `factstore-decay-face-unwired-resolved-2026-05-16`); (2) `simpleFactStore()` ergonomic wrapper — v1.1, back-derive from cognitive-buddy + MEME-parity usage shape (Q9-open-8); (3) MEME 100-episode parity harness in `packages/parity-tests/scenarios/meme-parity/` (target ≥40% L2 vs 3% baseline, no Opus-judge-at-read) — Phase 15 / DS-15 scorecard; (4) cross-shard cascade `dependentsIndex` strategy + sharding-rebalance recipe (v2; index is unsharded in v1 per Q9-open-1); (5) bi-temporal as a DS-14 `BaseChange` envelope field (v2 — pattern-only in v1 per Q9-open-9); (6) PY parity under the existing `[py-parity-am]` umbrella (NOT split into per-unit tickets until rigor-infra unblocks). v1 implementation note (post-QA, F1/F3/F5): the cascade cycle terminates by a **per-root semantic contract** — only an *obsolete* fact (`validTo !== undefined`) drives the cascade as a root, and each root emits exactly once across waves (`processedRoots` set, cleared on a fresh obsolescence transition via `extractOp`/consolidator wire-back). The still-live predicate skips non-existent dependents (`if (!depFact || depFact.validTo !== undefined) continue;`, F3). `cascadeMaxIterations` + empty-array fixpoint remain as a harmless backstop, but termination no longer depends on the ingest-resettable counter. Cascade is modeled as DATA arrays — spec §1.4 INVALIDATE idempotency does NOT govern it (docstring corrected, F4). `factStore`/`review` carry `equals` to damp per-wave re-fire (F10a). Shard write-back re-triggers the detector directly (no separate trigger node).
- **DS-14.7 follow-up #1 — `reactiveFactStore` recipe library — ✅ LANDED 2026-05-16 (`/dev-dispatch`).** All 8 compositions shipped as **exported, tested factories** (user-chosen shape over "reference snippets") at `src/utils/memory/recipes/*` + `recipes/index.ts`, re-exported via the `utils/memory` barrel (`export * from "./recipes/index.js"`): `scoringByOutcome` (② scoring — continual learning), `decayExponential` (③ ingest+`fromTimer`; the `decay` face is now wired — archive id `factstore-decay-face-unwired-resolved-2026-05-16`), `consolidationRem` (① consolidate + ③ trigger — REM replay), `admissionLlmJudge` (② admissionFilter — sync-face adapter over an upstream verdict stream, deny-by-default), `shardByTenant` (① shardBy — strict per-tenant + overflow / soft hash), `invalidationTracer` (④ cascade — bounded causal-trace ring), `bitemporalQuery` (④ factStore + reactive `asOf` — MEME L3 "as of t"), `influenceAnalysis` (④ dependentsIndex — transitive blast-radius + ranking). Presentation-only (`@graphrefly/graphrefly`, universal tier — `fromTimer` only, no `node:*`/DOM); stays within the `utils/` layer (depends on `base/meta` + substrate, zero presets/solutions). Mitigates the DS-14.7 PART-4.2 steeper-learning-curve trade. **Closes DS-14.7 follow-up #1.** Follow-up #2 (`simpleFactStore()` 80%-case wrapper) still pending — now better-informed (the recipes + memo:Re's persistentMemoryStore are the back-derivation inputs, Q9-open-8).
  - **DS-14.7 follow-up #2 — `simpleFactStore()` — ✅ LANDED 2026-05-17 (`/dev-dispatch`, design-locked + explicit "implement"; closes DS-14.7 follow-up #2 / Q9-open-8).** Shipped `src/utils/memory/simple-fact-store.ts` (`simpleFactStore`, `SimpleFactStoreOptions`, `SimpleFactStoreGraph`, `RememberOptions`, `DEFAULT_DECAY_HALF_LIFE_NS` = 7d, `DEFAULT_DECAY_PERIOD_MS` = 1h), re-exported via the `utils/memory` barrel + root `@graphrefly/graphrefly`; registered in `website/scripts/gen-api-docs.mjs` (`simpleFactStore.md` generated, `docs:gen:check` clean). 7 tests appended to `src/__tests__/utils/memory/fact-store.test.ts` (55/55 in-file; full suite **graphrefly 1950** +7 / pure-ts 1217 / parity 361; lint+build green). **Q-walk locks as built:** **Q1** `extractDependencies` defaults `() => []` (flat; cascade opt-in). **Q2** single factory, optional `storage` → present delegates to `persistentReactiveFactStore`, absent → `reactiveFactStore`. **Q3** wrapper owns the internal `ingest` leaf source; exposes `remember(id, payload, opts?)` — **method name resolved = `remember`** (user sub-decision 2026-05-17) — `.emit`s into the owned source (sanctioned external push, not a spec-§5.9 imperative trigger). **Q4** **auto internal `fromTimer` batteries-included** (user sub-decision 2026-05-17): `decayExponential` **ON by default** (generic; conservative 7d half-life / 1h tick, tunable or `decay:false`); `consolidationRem` wired **iff** the caller supplies `opts.consolidate` — domain-bound asymmetry **dictated by the recipe's own required `summarize` contract** (no generic default summarizer exists), documented in JSDoc + the file header (NOT an autonomous Q4 downgrade). **Q5** `remember` auto-fills `t_ns = BigInt(monotonicNs())` (clock-return-type wrap load-bearing) + `confidence = 1`, `tags`/`sources` = `[]`. **Q6** wraps the existing factories (not a parallel impl). Presentation-only, `utils/memory`, universal tier (`fromTimer` only — confirmed browser-safe by `assertBrowserSafeBundles`). **✅ /qa PASS (2026-05-17) — 2 parallel adversarial reviewers; no critical/major; 1 Needs-Decision + 2 auto-patches applied, 2 rejected.** **EC-LOW-1 (Needs-Decision, applied):** the prior "carry" — `SimpleFactStoreGraph<T>` hiding the persistent surface under `storage` — is **RESOLVED via typed overloads**: `simpleFactStore<T>(opts & { storage })` now returns `PersistentSimpleFactStoreGraph<T>` (= `PersistentReactiveFactStoreGraph<T> & { remember }`), the no-`storage`/optional overload returns `SimpleFactStoreGraph<T>`; single impl body, **zero runtime change**, Q2 "single factory" lock preserved (one factory, two signatures). `position`/`flush`/`replayedCount` are now type-honest on the durable path (no hand-cast — the headline 80%-case). New exports: `SimpleFactStoreRemember<T>`, `PersistentSimpleFactStoreGraph<T>`. **EC-LOW-2 (auto):** decay opts now spread-then-default (`{ ...d, halfLifeNs: d.halfLifeNs ?? DEF, periodMs: d.periodMs ?? DEF }`) — a future `DecayExponentialOptions` field passed via `opts.decay` is forwarded automatically (was silently dropped by the key-by-key enumeration); holds under `exactOptionalPropertyTypes` (caller-site rejects present-but-`undefined` optionals). **BH-1 (auto):** added an explicit `remember(id, p, { confidence: 0 })` → `confidence === 0` assertion (locks the `?? 1` vs `|| 1` falsy-trap correctness). **BH-2 (auto):** added a `opts.consolidate` wired-iff-path test (`consolidated` gains a trigger dep vs `[]` by default). **Rejected:** BH-3 (in-place `out.remember=` freeze-fragile — latent only, same self-augment pattern as `decayExponential`'s `mem.add`, no factory freezes); BH-4 (round-trip test asserts `position` before `flush` — acceptable ergonomic-smoke scope, deep flush-durability covered by the `persistentReactiveFactStore` suite). Gates re-run: lint(biome+layers+typecheck) 0 / build(DTS+browser-safe) 0 / full suite **pure-ts 1217 + graphrefly 1951 + parity 361(+1 todo,+1 skip)** / `docs:gen:check` clean (`simpleFactStore.md` regenerated). **Closes DS-14.7 follow-up #2 — no open carry.**
  - **/qa pass (2026-05-16) — 1 critical + 4 patch clusters, all fixed in-pass; 15 test cases now.** (C1, **critical**) `influenceOf(rootId)` re-add threw on repeat call (`Graph.add` duplicate-name — the JSDoc's own usage crashed on the 2nd call); fixed by per-`rootId` memoization (idempotent + bounds growth). (P2) `decayExponential` mid-tick **resurrection guard** — re-reads the live store's `validTo` immediately before re-ingest so a fact obsoleted by an earlier-in-tick cascade isn't resurrected from the stale fn-entry snapshot. (P2b) `lastTick` was stale on re-ingest (fresh-`t_ns` version reused the prior tick → over-decay) and unbounded → version-aware `since` + prune of ids absent from the snapshot. (P2c) `Number.isFinite(factor)` overflow guard; `epsilon` clamped to `Number.EPSILON` (≤0 prevents quiescence). (P3) JSDoc accuracy across all 8 (decay convergence is conditional + "synchronous this tick"; `scoringByOutcome` load-bearing full-wave-fold / no-`equals` / non-replaying-`outcomes` contract; `influenceAnalysis` true O(keys·closure) + cycle caveat; `admissionLlmJudge` verdict-race-drop + prune-overclaim; `shardByTenant` low-cardinality skew; `recipes/index.ts` two-shape lifecycle + single-apply/`opts.name`). (P4) +5 tests (C1 idempotent, decay `epsilon`-quiescence, batched-wave fold, `recentWindowNs`, overflow branch). Rejected (not bugs): decay `ingest.emit` "reactive-layer violation" (user-locked recipe-only re-ingest; `.emit` into a source is sanctioned, now hardened); decay "wave-desync" (premised on the now-corrected JSDoc; graphrefly is synchronous); `bitemporalQuery` "orphan/leak" (follows the `itemNode` returned-projection precedent — no `keepalive` = no forced retention). Gates: graphrefly **1910/1910**, recipes 15/15, lint (biome + layer-boundary 0 new), `pnpm build` exit 0 (DTS + `assertBrowserSafeBundles` clean).

- **Post-rustImpl-activation parity cleanup (regrouped 2026-05-17; parent "Phase 13.9 follow-on rustImpl arm" archived as `phase-13-9-rustimpl-parity-arm-activated-2026-05-15`).** The `rustImpl` arm is now active (`@graphrefly/native` shipped D203/D206/D207; `packages/parity-tests/impls/rust.ts` consumes `createNativeImpl()`). Residual carries that DID NOT land with activation:
  - ✅ **D1 LANDED 2026-05-18 (`/dev-dispatch` consumer-demand chicken-and-egg batch).** The "defer until consumer pain surfaces" gate was itself the chicken-and-egg — a fresh clone / workspace consumer hit `ERR_PACKAGE_PATH_NOT_EXPORTED` on any `@graphrefly/graphrefly/<subpath>` import before they could ever report the pain. Per the consumer-demand policy (bounded + DX-positive + shape-known ⇒ implement now), shipped Option A (user-chosen): a **guarded `prepare`** on root `package.json` (`node -e "…existsSync(dist/index.js)&&existsSync(packages/pure-ts/dist/index.js)…" || pnpm run build`) + a mirror guarded `prepare` on `packages/pure-ts/package.json`. Self-heals every clone / CI / git-install; **no-op on a warm tree** (skips when `dist/` exists — the common case after first build), so install isn't slowed; zero `exports`-map churn. Root `prepare` builds pure-ts then the shim via the existing `build` chain (covers both `dist/` roots from one hook). **/qa-hardened (same batch):** the guard also requires `tsup.config.ts` to exist — a source-tree marker present in a fresh clone but absent from the published `files:["dist","LICENSE"]` tarball — so `prepare` *skips cleanly* (exit 0) in any non-source context (extracted tarball, partial checkout) instead of attempting a doomed `pnpm run build`. 4-scenario truth table verified: warm(dist+src)→skip, fresh-clone(src,no dist)→build, tarball(dist,no src)→skip, broken(no dist,no src)→skip. No infinite recursion (`pnpm run build` never re-triggers `prepare`); if pnpm fires the nested pure-ts `prepare` on a cold root install it is a no-op via guard ordering (root build writes pure-ts `dist/` first), so "no double-build" is provided by the guard, not by pnpm lifecycle. Accepted non-regression limitation: a git-dependency install without pnpm available would still fail the build attempt — but registry-tarball installs (the real consumer path) never run `prepare` and ship `dist/`, and git-dep install of this scoped package was already broken pre-change. Archived id `d1-shim-dist-precondition-resolved-2026-05-18`.
  - ✅ **D2 LANDED 2026-05-17 (`/dev-dispatch`).** `evals/scripts/{run-harness,run-treatments}.ts` repathed off the dead `packages/pure-ts/src/patterns/*` onto the real post-cleave homes — `agentMemory` → `src/presets/ai/index.js`, `evalVerifier`/`harnessLoop` → `src/presets/harness/index.js`, `actuatorExecutor`/`autoSolidify`/`evalIntakeBridge`/`HarnessExecutor`/`HarnessVerifier`/`TriagedItem`/`VerifyResult`/`IntakeItem` → `src/utils/harness/index.js` (5 dead imports collapsed to 3 barrel imports). **Sibling cleave-drift fixed in the same pass:** `fileStorage` was *removed* (not relocated) in the cleave → renamed to `fileKv` (= `kvStorage(fileBackend(dir))`, identical signature + KV contract; `@graphrefly/pure-ts/extra/node`); 3 stale `src/patterns/*` doc-comment refs (`catalog-overlay.ts`/`contrastive.ts`/`validator.ts`) repointed to `src/utils/harness` / `src/utils/graphspec`. All 7 repathed symbols runtime-verified via `tsx` (how evals actually execute). **Sibling finding — now RESOLVED in the same batch (user expanded scope):** `evals/` had no `tsc` gate (172 pre-existing errors); all fixed + a typecheck gate added. See the ✅ "Untypechecked workspace packages" item below.
  - ✅ **D3 FULLY LANDED 2026-05-17 (`/dev-dispatch`) — intersection wired AND enforced.** `packages/parity-tests/impls/types.ts` now `import type * as native from "@graphrefly/native"` and the 9 tier symbols are `typeof legacy.X & typeof native.X` (literal D3 directive — catches a *missing*/structurally-incompatible native tier export). Factory methods stay on the intentional `ImplNode` abstraction (native has no top-level `node`; the `as unknown as Impl` Proxy cast in `rust.ts` is unavoidable for per-test lazy re-creation). **Enforcement gap CLOSED in the same batch (user expanded scope):** `scripts/check-typecheck.ts` `tsc --noEmit`s parity-tests on every `pnpm lint` (no baseline — package driven to **0 errors**). A `@graphrefly/native` wrapper signature drifting from the `Impl` contract now hard-fails `pnpm lint` — the first-line parity gate D3 asked for is live. See the resolved "Untypechecked workspace packages" item below for the full closure.
  - ✅ **D4 LANDED 2026-05-17 (`/dev-dispatch`).** `packages/pure-ts/src/index.ts` `version` is build-time injected from `package.json` via tsup `define` (`process.env.GRAPHREFLY_PKG_VERSION` token). Verified: `packages/pure-ts/dist/index.{js,cjs}` ship `var version = "0.47.1"` (token replaced, `?? "0.0.0-dev"` DCE'd, no `process` ref → `assertBrowserSafeBundles` clean). Unbuilt source consumers (parity-tests src alias, evals tsx — none read `.version`) get the honest `"0.0.0-dev"` sentinel, not the misleading `"0.0.0"`. `packages/pure-ts/src/__tests__/exports.test.ts` updated to assert the dev sentinel + document the contract.
  - 🟡 **D5 PARTIALLY LANDED 2026-05-17 (`/dev-dispatch`) — 2 of 4 scenarios green on both arms; 2 are genuine blockers, filed.** Added `packages/parity-tests/scenarios/core/`: ✅ `invalidate-cascade.test.ts` (4 tests — positive DIRTY→DATA cascade + INVALIDATE-clears-cache-no-resurrect, both arms) + ✅ `teardown-cascade.test.ts` (2 tests, both arms). Built on operator factories (`impl.map`) — `graph.derived(arbitrary fn)` is rust-`BenchGraph`-unsupported (discovered during authoring). `pause-resume.test.ts` + `set-deps.test.ts` shipped as honest `test.todo` blockers: **(a) PAUSE/RESUME — genuine pure-ts↔native §2.6 divergence → ✅ RESOLVED 2026-05-17** (spec §2.6 R2.6.0 / Option A pinned; `pause-resume.test.ts` default-mode contract green **cross-arm** — native converged same session, user-expanded scope; see the dedicated ✅ item below); **(b) set_deps — not on the `Impl` contract → ✅ TS RESOLVED 2026-05-18** (`/dev-dispatch`; user-locked full-rewire-trio promotion to public `setDeps`/`addDep`/`removeDep` + `Impl` widened; `set-deps.test.ts` now a real 3-test pure-ts-arm scenario; native arm `skipIf`-gated pending the graphrefly-rs `#[napi]` slice — see the dedicated item below + cross-track-ledger §1). See the two items below.

- 🟡 **`set_deps` parity scenario — TS LANDED 2026-05-18 (`/dev-dispatch`); native arm pending (cross-track).** Resolution of the 2026-05-17 D5 (b) blocker. User-locked "full rewire trio" + "pure-ts arm now, native follow-on": `_setDeps`/`_addDep`/`_removeDep` promoted to **public** `setDeps`/`addDep`/`removeDep` on the `@graphrefly/pure-ts` substrate `Node` interface (rename, no behavior change — semantics unchanged + still TLA+-verified `wave_protocol_rewire_MC`); `ImplNode.{setDeps,addDep,removeDep}` added to the `Impl` contract; `set-deps.test.ts` is now a real 3-test scenario (rewire A→B + addDep/removeDep edge wiring + self-dep rejection), `skipIf`-gated to the pure-ts arm. The **`Impl`-widening + native obligation is ledger-tracked** in `docs/cross-track-ledger.md` §1 (D5 row → TS LANDED / native pending; per `feedback_cross_track_ledger`, not here) — this note is just the parity-coverage pointer. **Remaining (native, not this batch):** `@graphrefly/native` must `#[napi]`-export `set_deps` (internal `core_bindings.rs:1489` exists, unexported) + regenerate wrapper + republish — handed to `~/src/graphrefly-rs/docs/migration-status.md` § "NEXT BATCH" as a separate `/porting-to-rs` slice; drop the `skipIf` gate when it ships.

- **Phase 13.9 follow-on: `rustImpl` parity arm + CI gate (opened 2026-05-05, follows Phase 13.9.A cleave landing).** Phase 13.9.A landed `packages/pure-ts/` (cleave), the root shim, and `packages/parity-tests/` scaffold with the M1 dispatcher scenario green against `pureTsImpl`. The `rustImpl` arm is a structural placeholder (`packages/parity-tests/impls/rust.ts` exports `null`) because `@graphrefly/native` does not yet exist as a publishable package — the napi crate at `~/src/graphrefly-rs/crates/graphrefly-bindings-js/` only has `Cargo.toml` + `build.rs` + `src/`, no `package.json` and no per-platform sub-packages. Activation sequence:
  1. Land napi-rs publish scaffolding in `graphrefly-rs` (root `package.json`, `npm/<platform>/` per-binary sub-packages, `index.js` loader, GitHub Actions matrix). Separate session; tracked in `~/src/graphrefly-rs/docs/migration-status.md`.
  2. First publish of `@graphrefly/native` + `@graphrefly/native-<platform>-*` packages.
  3. Update `packages/parity-tests/impls/rust.ts` to import from `@graphrefly/native` and populate the `Impl` shape; flip `rustImpl` from `null` to a real value.
  4. Widen `impls/types.ts` per-Rust-milestone close (Graph methods at Slice E close, operators at M3 close, etc.).
  5. Add a dedicated parity-job CI workflow (`.github/workflows/parity.yml` or step in `ci.yml`) once the rust arm is real — until then, the workspace `pnpm test` already runs the legacy-only parity scenarios, so a separate job adds no signal.
  Per `docs/implementation-plan.md` Phase 13.9.A acceptance bar amendment (2026-05-05). Cross-ref: `archive/docs/SESSION-rust-port-architecture.md` Part 12 Q6.

  **Carries surfaced by /qa on the cleave (2026-05-05):**
  - ✅ **D1 — RESOLVED 2026-05-18** (guarded `prepare`, Option A). See the ✅ D1 entry in the regrouped "Post-rustImpl-activation parity cleanup" item above + archive id `d1-shim-dist-precondition-resolved-2026-05-18`. (Original carry text retained in git history; the chicken-and-egg gate is removed — `pnpm install` on a fresh clone now auto-builds, no-op on a warm tree.)
  - **D2 — Eval scripts and legacy in-tree tests reach into private file paths (oracle-boundary leak).** `evals/scripts/run-harness.ts`, `run-treatments.ts`, and `evals/lib/*.ts` reach `packages/pure-ts/src/patterns/harness/{bridge,loop,types}.js` plus several `core/` and `extra/` files that aren't in the public `exports` map. Tests in `packages/pure-ts/src/__tests__/evals/*.test.ts` traverse 5 ups (`../../../../../evals/lib/...`) to reach workspace-root `evals/`. Acceptable as in-monorepo internal consumers, but the 5-up traversal is a tripwire and the deep imports defeat the package boundary. **Fix shape:** either widen the legacy `exports` map to include the harness internals these consumers need, OR migrate consumers to public-package imports (audit each), OR document the leak with a comment in `run-harness.ts` and accept it. Pre-existing broken import at `evals/lib/catalog-aware-evaluator.ts:35` (`patterns/refine-loop/index.js` was relocated to `patterns/harness/presets/refine-loop.ts`) belongs to the same cleanup.
  - **D3 — Parity-tests `Impl` interface erases structural drift when `rustImpl` activates.** Current `packages/parity-tests/impls/types.ts` defines `Impl.node: typeof legacy.node` — TS structurally accepts whatever fits, so a Rust binding with subtly different signatures would type-check green and only fail at runtime in the assertion. **Fix shape (alongside step 3 of activation sequence):** define `Impl` as the structural intersection of both impls' types: `import type * as native from "@graphrefly/native"; type Impl = { node: typeof legacy.node & typeof native.node; ... }`. Compile-time mismatch becomes the first-line parity gate, not the runtime expectation.
  - **D4 — Hardcoded `version = "0.0.0"` in `packages/pure-ts/src/index.ts:4`.** Pre-existing pre-cleave; the cleave widened the gap (now there are TWO `package.json` versions plus the stale source const). Telemetry / runtime version checks all read 0.0.0. **Fix shape:** build-time inject via `tsup` `define`, OR generate a `version.ts` from `package.json` at build time. Out of scope for the cleave, but compounds with shim/legacy version-drift discipline (see step 5).
  - **D5 — M1 dispatcher scenario coverage is thin.** `scenarios/core/dispatcher.test.ts` exercises only `node([], { initial }) → push-on-subscribe DATA`. The Phase 13.9.A acceptance bar text ("at least the M1 + M2-Slice-D scenarios") is literally satisfied by one test, but the README's milestone-coverage table claims wider M1 surface (PAUSE/RESUME, INVALIDATE→DIRTY→DATA cascade, TEARDOWN cascade, `set_deps` dep replacement, RAII Subscription via terminal-resubscribable). **Fix shape (alongside step 3):** when `rustImpl` activates, expand `scenarios/core/` to add at minimum: `pause-resume.test.ts`, `invalidate-cascade.test.ts`, `teardown-cascade.test.ts`, `set-deps.test.ts`. The runtime-cost is tiny (each scenario takes milliseconds); the parity signal is much stronger.

- **Phase 13.6.B B10 — spec / COMPOSITION-GUIDE edit pass (deferred 2026-05-03).** TS-side cleanup landed across B1–B9. The remaining lock work is documentation in `~/src/graphrefly/`:
  - **Lock 1.C** — three-category `.cache`-read sweep in `COMPOSITION-GUIDE-GRAPH.md` §3.
  - **Lock 1.E** — operator suppression carve-out in COMPOSITION-GUIDE-GRAPH §1.
  - **Lock 2.B** — Meta TEARDOWN ordering test (referenced from spec §2.3).
  - **Lock 2.D** — cross-tier atomicity elevated to top-line invariant in `GRAPHREFLY-SPEC.md`.
  - **Lock 4.F** — `_versioning` vs `_versioningLevel` split note in spec §7.
  - **Lock 5.A** — RESOLVED dual-role paragraph in spec §1.3.3; G.3-companion retirement in COMPOSITION-GUIDE-GRAPH §3 (TS code already aligned: B8 dropped `hasLatest`).
  - **Lock 5.B** — collapse §2.4 + §2.5 + §5.3 + §5.12 into one canonical "messages carry data" paragraph.
  - **Lock 5.C** — replace 4 wiring-order rules with diagram + checklist in COMPOSITION-GUIDE-PROTOCOL.md §5.
  - **Lock 5.D** — administrative pointer (no body); confirm no orphaned references.
  - **Lock 7.A** — move M.7/M.8/M.9/M.14/M.18/M.19 process rules out of canonical invariants set into agent-skill memory.
  - **Lock 1.B sweep** — sharpen which `=== undefined` patterns inside reactive fn bodies are actually dead under the first-run gate; the 26 `batch0 == null` sites in `extra/` are NOT dead per the `FnCtx.data[i]` shape contract.
  Lock-document with exact edit targets: `docs/implementation-plan-13.6-locks-draft.md`. Schedule a dedicated session that walks the COMPOSITION-GUIDE-* files end-to-end. **Note (2026-05-17):** spec **§2.6 was already amended this pass** — new **R2.6.1** (Lock 2.C pre-pause baseline replay, landed + /qa-hardened) supersedes the old "absorbed pulse" paragraph. That edit is independent of the B10 list above (none of which touch §2.6); a future B10 doc-pass should leave R2.6.1 intact (Lock 5.B's §2.4/§2.5/§5.3/§5.12 collapse does not include §2.6).

- **Lock 4.B (B + C) — mutation rollback hardening: `registerMutable` + dev-mode Proxy detection (deferred 2026-05-03 from Phase 13.6.B B9).** Lock 4.B (A) shipped — post-DS-14, `wrapMutation`/`lightMutation` were replaced by `mutate(act, opts)` (`src/base/mutation/index.ts:218`); explicit closure-state rollback is now the `act.down` hook (NOT the old `compensate` opt — terminology updated 2026-05-17). The two complementary mechanisms remain unimplemented (`registerMutable` appears nowhere in `src/` except the tracking comment at `packages/pure-ts/src/__tests__/core/phase-13-6-b9.test.ts:13`):
  - **(B)** `registerMutable(node, value)` — opt-in auto-snapshot extending the `registerCursor`/`registerCursorMap` family. Snapshots common collections (`Map`, `Set`, primitive counters) at batch entry; restores on throw. Use when the mutated state is worth wrapping but bespoke `compensate` isn't ergonomic.
  - **(C)** Dev-mode Proxy detection — wraps `Map`/`Set`/`Array` in a Proxy that logs unregistered mutations during a `wrapMutation` transaction. Production no-op (zero overhead). Catches slips at test time.
  The lock spec marks the trio as **provisional** — Rust port may obviate (B)/(C) via ownership + `Drop` (transactional containers that auto-rollback). Defer until a real consumer surfaces (B), or until /qa flags closure-state rollback gaps that (A)+`compensate` doesn't cover.

- **/qa pass on Phase 13 batch (opened 2026-05-01, closed same day).** Adversarial review by Blind Hunter + Edge Case Hunter on the Phase 13 multi-agent / human-input / spawnable / strategy-axis-extension batch surfaced ~45 raw findings, deduplicated to 13 auto-applicable patches + 1 needs-decision item (N1: agent.in re-entrancy). Auto-applied patches:
  - **A1** — `spawnable.validatorSchema` removed (it never actually validated against the schema, only did envelope-shape checks). Kept `validate?: (req) => boolean` predicate as the only validation hook; `Message.schema` continues to flow through the envelope as a wire convention for caller-supplied validators (ajv / zod / valibot).
  - **A2** — `humanInput` correlationId match fixed to skip the retained-log replay hazard. Subscribes to `responsesTopic.events` and snapshots `events.cache.length` BEFORE publishing the prompt envelope; only considers envelopes at index ≥ that cursor. Eliminates the stale-envelope-with-matching-correlationId crash mode (e.g. id-generator collision across humanInput instances on a long-lived hub).
  - **A3** — Central-clock rule: `humanInput`'s default `idGenerator` and `spawnable`'s `expiresAt` check now route through `wallClockNs()` (`core/clock.ts`) instead of bare `Date.now()` / `Date.parse()` / `performance.now()`. Test suites that monkey-patch the central clock can now pin both deterministically.
  - **A4** — `settle` operator: added `completeWhenDepsComplete: false` so the framework auto-COMPLETE on upstream COMPLETE doesn't double-fire after settle's own convergence-COMPLETE.
  - **A5** — `spawnable._processRequest`: added `_disposed` flag (mirroring `SubscriptionGraph._disposed`); checked at request-handler entry AND inside the per-request loop body so destroying the spawnable mid-iteration doesn't try to mount agents on a torn-down graph.
  - **A6** — `StrategyEntry.presetId` field: harness-loop's two `strategy.record(...)` calls now pass `presetId: DEFAULT_PRESET_ID` in the `extra` decoration object so persisted snapshots have the field populated (instead of undefined-at-runtime under TS's partial-extra contract). Test fixtures retained their pre-existing pattern; the partial-extra hazard is documented at the audited-success-tracker JSDoc.
  - **A7** — `humanInput` output Node: `equals: () => false` so structurally-equal repeat responses propagate (mirrors `agent.out`'s precedent).
  - **A8** — `spawnable` subscription: default `from: "now"` so pre-existing retained spawn requests are NOT replayed at construction. Exposed as a `from?: "now" | "retained" | number` opt for callers who want the replay.
  - **A9** — `spawnable` per-spawn `statusUnsub`: now released inside `onTerminal` (when the agent reaches `done` / `error`), instead of accumulating on the SpawnableGraph's disposer list. Defensive disposer still fires on graph destroy if the spawn is in-flight.
  - **A10** — `spawnable.expiresAt`: invalid (non-parseable) ISO 8601 strings now reject with `reason: "invalid expiresAt"` instead of silently passing through.
  - **A11** — `materialize` mount/unmount: wrapped in `batch()` so external observers see one coherent wave for the slot transition.
  - **A12** — `materialize` returned Node: added `describeKind: "derived"` so inspection tooling categorizes it correctly alongside `selector`.
  - **A13** — `humanInput` envelope: schema is now carried at envelope-level only (Phase 13.B `Message<T>` contract), not duplicated into the payload.
  - **N1(b) — agent input queue via internal hub topic+subscription.** The `bundle.in` Node remains a writable surface, but kicks are now queued through an internal `topic<TIn>` + cursor `subscription` (mounted under the AgentGraph at `input-topic` / `input-sub`). A drain effect depends on `[inputSub.available, loop.status]` — when status is `idle` / `done` / `error` AND avail > 0, it `pullAndAck(1)` and runs the reset+kick batch. Mid-run inputs queue automatically; processed sequentially when the loop returns to ready. Eliminates the prior raw-`subscribe → kick` re-entrancy hazard (mid-run reset corrupting in-flight session, cost leaking across inputs). Two new regression tests in [agent.test.ts](../src/__tests__/patterns/ai/agents/agent.test.ts) "agent() — N1(b): mid-run input queue" pin the behavior: 3-rapid-emit sequential ordering + per-input cost scoping under the queue.
  - **Verification:** 2734 tests pass (+4 from /qa: 2 spawnable from-cursor + invalid-expiresAt regressions, 2 agent mid-run-queue regressions; −2 from removed `validatorSchema` test). Build green (`assertBrowserSafeBundles` honored). Biome lint clean on touched files.
  - **Deferred** (not raised against this batch — minor / consumer-driven):
    - Blind #17 (spawnable depth-cap race in same drain wave — `.emit` updates `.cache` synchronously per protocol, so the for-loop reads the latest size; theoretical only).
    - Blind #24 (`addUsage` triple-evaluation perf — minor; defer until profiler shows the cost rollup as a hot path).
    - Edge #5 (G10 hot-swap deferred — already documented).
    - Edge #6 (selector custom equals throw — defer; trust caller).
    - Edge #12 (closureReads separator — minor; not load-bearing for inspection today).
    - **Archived 2026-05-22** (`/dev-dispatch` + `/qa` sweep): ~~Blind #14~~ (wording premise stale; archive id `qa-phase-13-blind-14-validation-wording-stale-2026-05-22`); ~~Edge #4~~ (resolved-in-code via A11 batch wrap; archive id `qa-phase-13-edge-4-mount-unmount-in-subscribe-resolved-2026-05-22`). Phase 13.K cross-mount `explain` gap → archive id `phase-13-k-topicbridge-explain-soft-forward-edge-resolved-2026-05-22`.

- **Convention adoption sweep — `attachEdgeMeta` consumers (opened 2026-05-22, design-review D follow-on).** Phase 13.K shipped `attachEdgeMeta(targetNode)` as the canonical convention for "this node imperatively publishes into that target" soft forward edges (visible to `Graph.describe()` / `explainPath`, invisible to wave propagation). Other in-tree factories with the same shape have NOT yet adopted: (a) **`src/base/worker/bridge.ts`** publishes into a worker-bound topic from a subscribe handler — same `attachEdge` shape applies. (b) **`src/utils/inspect/lens.ts`** drain effect appends `LensFlowChange` envelopes into the `flowMutations` companion `reactiveLog` — soft edge from drain effect → log's `entries` node. (c) Any future `materializeTo` / external-sink factory. **Defer** until a consumer surfaces the explain opacity (worker-bridge is internal-only today; lens drain effect's target is the same factory's owned log, so describe already includes it via mount paths). Adoption is a 2-line change per site (helper import + meta spread). When a consumer hits an opaque hop in `explain` on a non-topicBridge factory, batch the sweep then.

- **QA follow-ups from Phase 12 /qa pass (opened 2026-05-01).** Adversarial review by Blind Hunter + Edge Case Hunter on the Phase 12 batch (12.A markup + 12.B sibling-file relocation + 12.C verify + 12.D `extends Graph` migration). Auto-applied patches landed in /qa: A1 (`RefineLoopGraph` constructor — public field assignments moved earlier in constructor, defensive against sync-strategy drain reaching downstream consumers before fields are bound); A2 (`RefineLoopGraph` constructor — `tagFactory("refineLoop", placeholderArgs(...))` parity with `agentMemory`, ride-along observability win); A3 (`AgentMemoryGraph` constructor — extractFn-resolution validation moved BEFORE `tagFactory` so an invalid-opts throw doesn't leave a tagged-but-empty graph behind); D1 (`RefineLoopGraph.decideEffect` resume-stall — eliminated all four cross-node `.cache` reads in the effect fn body: `iterationTrigger.cache` replaced by `iter` riding with the new `CandidatesEnvelope<T>` payload (canonical §32 envelope; `candidatesItemsNode` sidecar preserves the `Evaluator<T>` API); `historyState.cache` / `budgetState.cache` / `pauseState.cache` replaced by §28 closure mirrors `latestHistory` / `latestBudget` / `latestPaused` (consistent with the pre-existing `latestStrategy` / `latestFeedback` / `latestPrevCandidates` mirrors in the same constructor); `feedbackEnvelopeNode` gated on `scoresFired` so candidates-only fan-out waves no longer spuriously trigger decideEffect; `evaluateEventNode` / `analyzeEventNode` / `generateEventNode` similarly read iter from the envelope instead of `iterationTrigger.cache`. `Evaluator<T>` JSDoc tightened with the **cancel-on-input contract**). Deferred: **9 resolved sub-item(s) (B2 resolved-by-documentation, B3/B4/B5a/B5c/B5d/B5e/B5f/B5g resolved-in-code) archived** to `archive/optimizations/resolved-decisions.jsonl` (id `qa-follow-ups-phase-12-resolved-subitems-2026-05-18`) on the 2026-05-18 consumption-demand reconciliation sweep (premise-check stale findings — backlog drifted from code; paths corrected). Open items remain below:
  - **D1 follow-up — refineLoop async-evaluator stale-scores wrapper.** The cancel-on-input contract is documented on `Evaluator<T>` ([src/presets/harness/refine-loop.ts](../src/presets/harness/refine-loop.ts)) but not enforced. A user supplying an async evaluator that doesn't `switchMap` over candidates can leak stale scores from a prior iter into `feedbackEnvelopeNode` after `pause()`/`resume()`; the `lastDecidedIteration` de-dup then skips the real iter's scores, stalling the loop. Proposed: `wrapEvaluator(userEval)` helper pre-applying `switchMap(candidates, …)`. **Open** — only a JSDoc back-ref exists today; defer until a consumer reports a stalled loop with an uncancellable async evaluator (genuinely consumer-shape-defining — POST-1.0-eligible per the consumer-demand policy's §44 carve-out).
  - **B5b — `agent-memory` opts mutation freeze.** Caller can mutate `opts` after `agentMemory(...)` returns; `placeholderArgs` deep-clones at tag time (provenance safe) but a subgraph factory reading `opts.X` later (async eval/refresh) sees the mutation. Optional `Object.freeze({...opts})` at constructor entry ([src/presets/ai/agent-memory.ts](../src/presets/ai/agent-memory.ts)). **Open** — pre-existing, no `Object.freeze` yet; defer until a real bug.

- **QA follow-ups from Class B migration /qa pass (opened 2026-04-30).** Adversarial review by Blind Hunter + Edge Case Hunter on the B.2 + B.1 + B.4 batch surfaced 2 critical (auto-applied: missing `node` import in `wrappers.ts:169`; stale `EvalResult` import in `run-harness.ts`), 11 major (most auto-applied), 14 minor findings (Blind Hunter), plus 17 invariant/topology findings (Edge Case Hunter, 3 auto-applied). Auto-applied patches landed in /qa: tracker JSDoc `snapshot`→`entries`, `buildPriorityScores` uses `strategyKey()`, `PipelineGraph` empty constructor + `noUselessTernary` removed, `MemoryWithTiersGraph` context-node registered (local-default branch only — foreign-Node branch documented), agent-memory opts spread order tightened, tracker `record()` caller-contract documented, tracker `lookup()` no-TTL contract documented, `agent-memory.admissionFilter` SENTINEL violation fixed (emit `[[RESOLVED]]` instead of `actions.emit(undefined)`), CqrsGraph `_keepaliveDisposers` cascade-leak migrated to `addDisposer` (override `destroy()` removed), `affectedTaskFilter` static-array `fullTaskSet` registered on the graph for visibility. Deferred: **3 resolved sub-item(s) (EC2/EC7, EC16, EC17) archived** to `archive/optimizations/resolved-decisions.jsonl` (`qa-follow-ups-from-class-b-migration-qa-*-resolved`) on the 2026-05-16 /qa sweep. Open items remain below:
  - ~~**M4 — `MemoryRetrievalGraph.retrieveReactive` accumulates keepalives + state crosstalk.**~~ **✅ ALREADY RESOLVED IN CODE (C1 rework / QA F-9, 2026-04-30→05-01) — archived 2026-05-18** to `archive/optimizations/resolved-decisions.jsonl` (id `m4-cluster-memoryretrievalgraph-per-input-subgraph-resolved-2026-05-01`). Stale: `retrieveReactive(input)` already builds a per-call `sub = new Graph('retrieve_${id}')` mounted on the parent; `projection`'s `onDeactivation` auto-unmounts via `this.remove(segment)` on last-unsubscribe (no keepalive accumulation); the shared `retrieval`/`retrievalTrace` mirrors were dropped (F-9) so no concurrent crosstalk. Regression: `ai.test.ts` "C1: concurrent retrieveReactive calls keep mirrors independent" + "C1: per-call subgraph is removable". Premise-check stale finding (`feedback_verify_premise_before_greenfield`).
  - ~~**M8 — `singleNodeFromAny` keepalive unbounded for DATA-only nodes.**~~ **✅ LANDED 2026-05-18 (`/dev-dispatch` bounded-correctness batch)** — root cause was narrower than "needs a release-on-different-signal design": the eviction watcher only triggered on `ERROR`/`COMPLETE`, so a destroyed DATA-only `state(...)` node (which never terminates) left its `inFlight` Map entry + watcher subscription pinned **past the node's own teardown**. Fix: added `TEARDOWN` to the eviction trigger set ([src/base/composition/single-from-any.ts:171-179](../src/base/composition/single-from-any.ts#L171)), bounding the entry's lifetime to the node's lifetime (the share-while-alive dedup contract is intended; the leak was strictly "entry survives the node's death"). JSDoc contract block rewritten. 2 regression tests + /qa TDZ hardening (hoisted `unsub`). ✅ ARCHIVED 2026-05-18 (id `bounded-correctness-batch-2026-05-18`).
  - **M10 — `pipelineGraph.approvalGate` closure-mirror `latestIsOpen` — KEEP (§28-canonical, deliberate).** [src/utils/orchestration/pipeline-graph.ts:299-313](../src/utils/orchestration/pipeline-graph.ts#L299) (path corrected 2026-05-18). Verified 2026-05-18: `latestIsOpen` is still a closure-mirror and **intentionally so** — the §28 comment (`:299-306`) records the Phase-9 relocation plan was reverted at the design level; reading a closure var is NOT a P3 violation, this is a legitimate Level-3 site (the framework `withLatestFrom partial:false` fix correctly landed for `composite.ts` only). Reclassified from "Defer until consumer" to **KEEP — §28-canonical by design** (not a latent bug; no action owed).
  - ~~**EC3/EC4 — `MemoryRetrievalGraph.retrieve()` imperative + reactive crosstalk.**~~ **✅ ALREADY RESOLVED IN CODE — archived 2026-05-18** (same id as M4). Stale: retrieval is reactive-only — the imperative `retrieve()` API was removed; there is no shared `this.retrieval`/`this.retrievalTrace` writer pair (F-9 dropped the mirrors). No §5.8 `.cache`-in-wave path remains.
  - ~~**EC10/EC15 — strategy is mounted on the harness; `strategy.destroy()` independently breaks the harness.**~~ **✅ LANDED 2026-05-18 (`/dev-dispatch` bounded-correctness batch)** — added an ownership JSDoc note to the `HarnessGraph.strategy` field ([src/presets/harness/harness-loop.ts](../src/presets/harness/harness-loop.ts) `strategy` field): documents that the strategy is harness-owned (disposal cascades via the mount lifecycle), that `strategy.destroy()` must not be called independently (it staleness `triage-input` + `buildPriorityScores`'s cross-graph deps on `strategy.entries`), and that callers should read/subscribe freely and let the harness own the lifecycle. JSDoc-only by decision (the "optional parent-required destroy guard" would alter `AuditedSuccessTrackerGraph.destroy` semantics — more invasive, no consumer demand; deferred). ✅ ARCHIVED 2026-05-18 (id `bounded-correctness-batch-2026-05-18`).
  - ~~**EC12/EC13 — `MemoryRetrievalGraph` internal nodes anonymous.**~~ **✅ ALREADY RESOLVED IN CODE — archived 2026-05-18** (same id as M4). Stale: per-call nodes are named + registered (`sub.derived("context"/"result", …)`, `sub.add(projection, { name: "projection" })`, mounted at `retrieve_${id}`) → walkable as `${parent}::retrieve_${id}::{context,result,projection}` in `describe()`/`explain()`; synthesized context = `this.state("_context", null)`. The `mirror` effect EC12/EC13 named was deleted by QA F-9.
  - ~~**EC14 — `MemoryRetrievalGraph.retrieval` / `retrievalTrace` raw `node()` instead of `this.state`.**~~ **✅ MOOT / SUPERSEDED — archived 2026-05-18** (same id as M4). Stale: `retrieval`/`retrievalTrace` no longer exist (dropped by QA F-9); per-call nodes use `sub.derived(...)` (DS-13.5.C `equals`-plumbing migration) and `_contextNode` is `this.state("_context", …)` — the mechanical `this.state` migration this asked for is done by construction.

- **Class B audit follow-ups from B.2/Alt E migration (opened 2026-04-30).** During the Class B audit Alt E collapse (`effectivenessTracker` deleted; `strategyModel` now a typed alias of the new `auditedSuccessTracker` substrate at `src/extra/composition/audited-success-tracker.ts`; substrate is a `Graph` subclass with `record(key, success, extra?)` / `lookup(key)` / `snapshot` shape) and the parallel Class A `this.derived/effect/state` migration two carries surfaced: **5 resolved sub-item(s) (`GraphDerivedOptions` doesn't expose `guard:`, `TopicGraph` self-resolve path collision, `io/` body extraction (Tier 2., Sibling-file physical relocati, `promptNode` (B.3) widening — ) archived** to `archive/optimizations/resolved-decisions.jsonl` (`class-b-audit-follow-ups-from-b-2-alt-e-*-resolved`) on the 2026-05-16 /qa sweep. (all sub-items resolved; parent retained as pointer.)

- **QA follow-ups from Phase 11.5 ctx-unification /qa pass (opened 2026-04-30).** Adversarial review of `GraphDerivedFn` / `GraphEffectFn` ctx unification + composite.ts withLatestFrom migrations (Phase 11.5, see `archive/docs/SESSION-graph-narrow-waist.md`). Patches landed in-batch: P1 JSDoc snippet on `GraphDerivedFn` corrected from buggy `?? ` form to truthful `data[i] != null && length > 0` form (handles `null` DATA correctly); P2 `FnCtxEffect` JSDoc updated to document `store` + `terminalDeps` exposure; P3 P9 invariant dead variables removed (`prev`, `expectedResolvedMin`); P4 P9 invariant strengthened to assert RESOLVED count strictly increases on each odd input; D1 `latestVals(data, ctx)` test helper shipped in `src/__tests__/test-helpers.ts` and adopted by `graph.test.ts` + `_invariants.ts`; D2 helper renamed `vals` → `latestVals` to signal multi-emit collapse honestly. **Deferred:** **1 resolved sub-item(s) (P11.5-D1 — ✅ landed 2026-04-30) archived** to `archive/optimizations/resolved-decisions.jsonl` (`qa-follow-ups-from-phase-11-5-ctx-unific-*-resolved`) on the 2026-05-16 /qa sweep. Open items remain below:
  - ~~**P11.5-D3: Verifiable trigger-before-source-DATA semantic pin.**~~ **✅ ALREADY COVERED — archived 2026-05-18** to `archive/optimizations/resolved-decisions.jsonl` (id `p11-5-d3-verifiable-trigger-before-source-covered-resolved`). Stale: `src/__tests__/base/composition/composite.test.ts` "trigger-before-source-DATA waits for source via withLatestFrom partial:false" already pins the exact contract (trigger alone → `calls===[]` / `verified.cache` null; then source → fn fires once). Premise-check stale finding (`feedback_verify_premise_before_greenfield`).

- **QA follow-ups from residual-backlog /qa pass (opened 2026-04-29).** Adversarial review of the Tiers R1–R5 batch (5 tiers cleared in one session; 30 files / 1678+/258− touched; 2550 tests passing). Two reviewers (Blind Hunter / Edge Case Hunter) surfaced ~24 raw findings, deduplicated to 5 patches (F-A through F-F applied in-batch) and 5 deferral candidates below. All patches landed in the same /qa pass; the deferrals below are bounded, lower-priority, and consumer-driven. **3 resolved sub-item(s) (D2, D3, D5) archived** to `archive/optimizations/resolved-decisions.jsonl` (`qa-follow-ups-from-residual-backlog-qa-*-resolved`) on the 2026-05-16 /qa sweep. Open items remain below:
  - ~~**D1 — `retention.score` writes into `entryCreatedAtNs.set` from inside the substrate's archival scan.**~~ **✅ ALREADY RESOLVED IN CODE (DS-13.5.F, 2026-05-01) — archived 2026-05-18** to `archive/optimizations/resolved-decisions.jsonl` (id `residual-d1-retention-score-purity-resolved-2026-05-01`). Stale (pointed at pre-cleave `src/patterns/ai/memory/memory-composers.ts#L286`): `src/utils/ai/memory/memory-composers.ts:357-365` `retention.score` now only READS `entryCreatedAtNsRef.get(key)` (pure); the first-write was extracted to a separate `entryCreatedAtNs/sync` effect (`:408-433`) and `entryCreatedAtNs` promoted to a `ReactiveMapBundle` registered via `this.add()` (`:317-318`) — exactly the proposed fix; corroborates the already-archived "Unit 7 Q3 ✅ 2026-05-17". Premise-check stale finding.
- **QA follow-ups from Graph narrow-waist Bundle 1 /qa pass (opened 2026-04-29).** Adversarial review of `graph.derived/effect/produce/batch` (Bundle 1 — additive only, see `archive/docs/SESSION-graph-narrow-waist.md`) landed 4 architectural patches in-batch (B1 produce-Node-rejected at type+runtime, B2 produce-undefined-rejected, B3 self-pruning keepAlive disposer, B4 signal forwarding on all 3 methods) plus 6 test/JSDoc tightenings (A1 keepAlive-disposal test rewritten to count fn invocations, A2 nested-batch coalescing assertion, A3 effect cleanup tightened to `=== 1`, A4 async-iter test waits for COMPLETE, A5 empty-deps JSDoc note, A6 `latest.cache`-inside-fn anti-pattern removed from tests + reframed in JSDoc). 9 new regression tests added (Bundle 1 baseline 20 → 29). Remaining deferred items: **1 resolved sub-item(s) (C3) archived** to `archive/optimizations/resolved-decisions.jsonl` (`qa-follow-ups-from-graph-narrow-waist-bu-*-resolved`) on the 2026-05-16 /qa sweep. Open items remain below:
  - **✅ C1 RESOLVED + LANDED 2026-05-23 (`/dev-dispatch`, D282 minted at Phase-2 HALT).** TS-arm converged to R4.3.2 via per-node `_preBatchSnapshot` + paired flush/rollback hooks (new `batchHooks: Array<{ flush, rollback }>` registry in `packages/pure-ts/src/core/batch.ts`; new `_rollbackBatchPending()` in `packages/pure-ts/src/core/node.ts`). 9-field snapshot scope user-locked at the **invariant level** ("post-rollback observable state ≡ pre-batch observable state"), NOT the field-name level — TS bundles state-machine bookkeeping (`cached`/`status`/`versioning`/`hasCalledFnOnce`/`teardownDone`/3 wave flags/`replayBufferLength`) where Rust splits it. Acceptance bar met: 12 cross-arm-shaped parity scenarios authored FIRST at `packages/parity-tests/scenarios/core/batch-throw-rollback.test.ts` (verified to FAIL on TS arm pre-impl, PASS post-impl; rust arm `test.runIf`-gated until the native `batch_run` napi binding ships — Rust substrate already conformant via `discard_wave_cleanup` + `restore_wave_cache_snapshots`). Closes Slice A reviewer findings E1/E3/E5/E7/E15 + Blind#11 (anti-pattern #7 "tests don't assert no-DATA-leak through subscribers" lift). `Impl` widening (`Impl.batch(fn): Promise<void>`) logged at `docs/cross-track-ledger.md` §1 D282 row; §2 C1 row updated to LANDED-TS + native-pending. Gates: pure-ts 1246 ✓ / parity 427(+13 skip — 12 D282 rust-arm runIf, 1 unrelated) ✓ / lint (biome + layer-boundary + typecheck) ✓. Canonical D-number entry: `docs/rust-port-decisions.md` D282. **Original 2026-05-22 reopen entry preserved below for the design record:**
  - **🔴 C1 REOPENED 2026-05-22 (`/qa` reverted Slice A) — substrate-level R4.3.2 TS-impl convergence; awaits design session. DO NOT PICK UP COLD.** The original C1 carry framed the bug as "DIRTY propagated synchronously, downstream stuck in `dirty` status with stale cache" — that framing is **stale post-Bug-2-coalescing (2026-04-17)**. Empirical `/tmp/probe-prefix.ts` against pre-fix HEAD + post-Slice-A:

    | Probe (`batch(() => { src.emit(42); throw "boom" })`) | Pre-fix (HEAD) | Post-Slice-A | Per canonical R4.3.2 |
    |---|---|---|---|
    | `down.cache` | `42` (leaked) | `undefined` (sentinel) | unchanged from pre-batch |
    | `down.status` | `"settled"` | `"sentinel"` | unchanged from pre-batch |
    | `fnRuns` during throw | `1` | `1` | `0` |
    | Subscriber wave | `[DIRTY, DATA(42)]` | `[DIRTY, DATA(42), DIRTY, INVALIDATE]` | `[]` (no delivery) |

    Root cause: at the catch-path point where `flushHooks.splice(0)` fires, `batchDepth === 0` (decremented) and `flushInProgress === false`, so `isBatching()` returns false → `downWithBatch` dispatches **synchronously** instead of enqueueing. The pre-existing inline comment ("queues are cleared right after, so the side-effects are wiped") was always wrong. Slice A's `touchedInvalidators` layered INVALIDATE on top of an already-delivered wave → "settle-to-sentinel" semantic with intermediate observable side effects (fn runs, subscribers fire, equals fires, replayBuffer pushes). Not a rollback. Reverted.

    **3-gate evaluation (2026-05-22 `/qa`):**
    - **Gate 1 (spec citation) — PASSED.** `docs/implementation-plan-13.6-canonical-spec.md:1183` ships **R4.3.2 — Throw rollback**: *"per-node flush hooks fire (so nodes clear their pending state), all drainPhase queues are cleared, throw re-propagates."* Parenthetical "(so nodes clear their pending state)" makes intent explicit: clear, no delivery. **R4.3.5** (transactional-frame) cross-references R4.3.2 as the substrate contract `mutate(act, { frame:"transactional" })` relies on for "discards reactive emissions on throw."
    - **Gate 2 (cross-arm coordination) — PASSED with asymmetry.** `~/src/graphrefly-rs/crates/graphrefly-core/src/batch.rs:3693` `discard_wave_cleanup()` ALREADY conforms to R4.3.2: takes `pending_notify` **without firing sinks**, **restores wave cache snapshots** via `restore_wave_cache_snapshots` (cache reverts to *pre-batch value*, NOT sentinel), drops `deferred_cleanup_hooks` / `pending_wipes` / deferred handle releases, releases handle refcounts lock-released. TS port is the divergent arm. Subscriber wire shape on throw is parity-observable → **cross-track-ledger §2 row added** (see `docs/cross-track-ledger.md`).
    - **Gate 3 (drain-time nested-batch) — already designed.** R4.3.2 explicitly carves out *"Inner batches inside flushInProgress skip rollback (cross-language decision A4)."* The Blind#1 reviewer's concern is intentional design, not a new hazard to re-design.

    **All three gates pass → mint a NEW D-number ("D[N+1] — Throw-during-batch subscriber atomicity"); ship Path C under D[N+1]'s banner with parity scenarios authored FIRST that assert the full subscriber message stream.** D-number minting deferred to the next design-session touch (no autonomous semantic pick per `feedback_no_autonomous_decisions`). Convergence-target sketch:
    - Modify TS so `batch()` throw clears `_batchPendingMessages` on touched nodes WITHOUT dispatching downstream (mirror Rust's `discard_wave_cleanup` shape).
    - Add a wave-entry cache-snapshot mechanism to `NodeImpl` (mirror Rust's `restore_wave_cache_snapshots`). Cache reverts to pre-batch value, NOT sentinel.
    - Authoring order: `packages/parity-tests/scenarios/core/batch-throw-rollback.test.ts` FIRST (full subscriber-stream assertions, real diamond topologies, `partial:false` consumers, terminal-mid-batch, equals-absorbed throw, drain-time-nested-batch carve-out per A4), then TS impl converges, Rust arm structurally passes from day one.
    - Reviewer findings preserved in `archive/optimizations/resolved-decisions.jsonl` id `slice-a-batch-throw-rollback-reverted-2026-05-22` — Blind Hunter #1–#11, Edge Case E1–E17, including E3 (`partial:false` downstream stuck `"dirty"` post-throw because INVALIDATE branch returns early at `_cached===undefined`), E6 (synthetic-DIRTY double-prefix produces `[DIRTY, DATA, DIRTY, INVALIDATE]`), E7 (terminal-resurrection on rollback — terminal-completed-mid-batch resets `_status="sentinel"`), Blind#1 (drain-time-nested-batch `flushInProgress=true` branch silently clears invalidators without firing — produces the leak), Blind#5 (best-effort try/catch swallows engine bugs in the rollback path). Path C must address each.

    **Test-quality lift (mandatory for Path C regardless of impl shape):** future tests must assert the FULL subscriber message stream (not just final cache), use real diamond topologies (`a → {x,y} → bottom`, NOT two independent sources → one consumer), and probe `partial:false` first-wave-pending consumers + terminal-mid-batch + equals-absorbed throws + drain-time-nested-batch carve-out.
  - **C2: keepAlive + upstream COMPLETE → cache cleared on destroy (RAM cache).** Compute nodes have RAM cache semantics (spec §2.2): when destroy drains the keepAlive disposer, the derived deactivates and `.cache` clears to sentinel. External code holding `n.cache` for post-destroy reporting sees `undefined`. Bundle 1 documented the behavior in `Graph.derived` JSDoc and the disposal test now uses fn-call count instead of cache-frozen-at-snapshot. If post-destroy snapshots are required, snapshot pre-disposer-drain inside the disposer or rely on `describe({detail:"full"})` before destroy.
  - ~~**C4: path-based `graph.derived` reaches across mounts; rewire gap more reachable.**~~ **✅ RESOLVED-AS-BY-DESIGN 2026-05-22 (Slice B design session, `/dev-dispatch`) — archived to `archive/optimizations/resolved-decisions.jsonl` (id `c4-path-based-graph-derived-cross-mount-by-design-resolved-2026-05-22`).** Construction-time path-string freeze is the by-design property of `Graph.derived`; the substrate-level rewire primitives (`Node.setDeps`/`addDep`/`removeDep`) shipped on pure-ts 2026-05-18 (D5) and on @graphrefly/native 2026-05-20 (D264) — callers who need cross-mount rewire use those, NOT a presentation-layer auto-rewire policy. **Pure-A landed:** `Graph.derived` JSDoc sharpened ([packages/pure-ts/src/graph/graph.ts:2351-2374](../packages/pure-ts/src/graph/graph.ts:2351)) with concrete orphan-symptom description + three workarounds (direct-`Node` deps, post-mount `setDeps`, pre-`remove` invariant check). **Marker downgraded:** `describe()`-time `orphaned: true` annotation deferred per check-2 failure (distinguishing string-resolved vs anonymous direct-`Node` deps requires a new per-`Node` field — substrate widening, Impl-coupled per `describe()` membership on `packages/parity-tests/impls/types.ts:190`). Filed as cross-track-ledger §2 follow-on; consumer-driven only.

- **QA follow-ups from polish-batch /qa pass (opened 2026-04-28).** Adversarial review of the polish batch (A: abort regression tests; B: harness Tier 6 reconciliation tests; C: EH-9 / EH-12 / EH-18; D: §35 reentrancy / reflectNode tick / async-evaluator §9a) surfaced 6 patch findings (P1–P6) and 4 deferral candidates (D1–D4) — all of P1-P6, D1, D3, D4 patched in-batch. P-class patches: AuditTrailGraph default-singleton clone, Tier 6.2 tautology fix, reflect tick bound tightening, Tier 6.4 count assertions, bumpCursor message generalization, EH-9 real-synthetic regression. D-class landings: stream-path abort coverage (D1); `state<T>()` zero-arg overload + test migration (D3 final form — initial `sentinelState<T>()` factory removed pre-1.0 in the deviation-audit pass per `feedback_no_backward_compat`); `harness.reflect` field exposure + test migration (D4). Remaining deferred:
  - **B1: bundled 3-way verdict regression test deferred (qa B1).** The polish batch's first attempt at a single test exercising the dispatch effect's verified / retry / structural branches under one mixed-batch was timing-fragile (mockLLM response cycling + 3-item interleaving + retry-attempt timing). Downgraded to per-branch coverage: existing "full 7-stage happy path" / "fast-retry exhaustion" / "structural failure" tests cover the branches individually, plus the new "Tier 6.4 structural verdict still publishes" lock. A bundled test would catch a regression where the dispatch effect's branching logic gets confused mid-iteration (e.g. mixing payloads across branches) — but only under deterministic timing. Defer until either (a) a real cross-branch regression surfaces, or (b) a synchronous-mock pattern lands that lets the bundled test run reliably (e.g. all-stages-fire-in-one-microtask scheduling on `mockLLM`). Estimated: ~30 LOC of test, blocked on the timing-deterministic mock primitive.
  - **D2: Tier 6.1 `__unrouted` test exercises bridge wiring directly, not LLM-hallucinated triage→router path.** The test publishes to `triage-output` directly. A future addition would publish through `intake` with a mock LLM that returns an unknown route — verifies the full triage→router→`__unrouted` path. Pre-existing test pattern in this file; doubling the coverage isn't urgent. Defer until a hallucinated-route bug surfaces.

- **QA follow-ups from Tier 9.1 /qa pass (opened 2026-04-28).** Adversarial review of the Tier 9.1 batch (γ-form full + Tier 9.3 + Tier 10.1 + γ-7-B + Tier 4 reconciliation) surfaced 36 findings via two parallel reviewers; the architecturally significant ones (D1 lens TEARDOWN containment, D2 mapFromSnapshot defense restoration, D3 processManager collision detection, D4 freeze-at-entry on appendRecord, D5 saga aggregateId always-include-key, D7 validateNoIslands false-positive doc) plus 10 auto-applicable fixes (A1–A10) all patched in-batch. **EH-9 / EH-12 / EH-18 landed in the 2026-04-28 polish batch (Batch C).** Remaining deferred items below are pre-existing framework gaps or defensive nice-to-haves that didn't change in the batch: **3 resolved sub-item(s) (EH-12, EH-18, EH-9) archived** to `archive/optimizations/resolved-decisions.jsonl` (`qa-follow-ups-from-tier-9-1-qa-pass-op-*-resolved`) on the 2026-05-16 /qa sweep. Open items remain below:
  - ~~**Framework gap: `Graph._destroyClearOnly` doesn't drain child mounts' `_disposers` (EH-2).**~~ **✅ ALREADY RESOLVED IN CODE (A14, 2026-05-01) — archived 2026-05-18** to `archive/optimizations/resolved-decisions.jsonl` (id `eh2-graph-destroyclearonly-child-disposer-drain-resolved-2026-05-01`). Stale: `packages/pure-ts/src/graph/graph.ts` `_destroyClearOnly()` + `_destroyClearOnlyAsync()` already drain `_disposers` + `_storageDisposers` before structure clear with an explicit `(EH-2)` JSDoc. Premise-check stale finding (`feedback_verify_premise_before_greenfield`).
  - ~~**`processManager.dispose()` doesn't unmount cursor / audit-log nodes (EH-16).**~~ **✅ ALREADY RESOLVED IN CODE (Tier 6.5 3.3) — archived 2026-05-18** to `archive/optimizations/resolved-decisions.jsonl` (id `eh16-processmanager-dispose-removable-childmount-resolved-2026-05-18`). Stale: `src/utils/process/index.ts:556-573` mounts the cursor + audit log under a removable child mount `__processManagers__/${name}`; `dispose()` calls `cqrsGraph.remove(mountName)` at `:1325-1333` — no node accumulation across create/dispose cycles. Premise-check stale finding.
  - **`validateNoIslands` performance on repeated invocations (EH-19).** Each call rebuilds the full `describe({detail:"minimal"})` snapshot (O(N+E)). For long-running CI loops that validate after each mutation on a 10k-node graph, this is non-trivial overhead. Future: ship `validateNoIslandsReactive(graph): Node<ValidateNoIslandsResult>` that subscribes to topology changes. Defer until a real continuous-validation consumer surfaces.
  - ~~**`lightMutation` re-entrancy hazard reachable in `processManager` (EH-17).**~~ **✅ ALREADY RESOLVED IN CODE — archived 2026-05-18** to `archive/optimizations/resolved-decisions.jsonl` (id `eh17-lightmutation-reentrancy-processmanager-batchframe-resolved`). Stale: `src/utils/process/index.ts` (~:586) already wraps the appendRecord path in a batch frame with the comment "frame closes EH-17 (re-entrancy hazard)" — exactly the deferred mitigation. Premise-check stale finding.

- **Tier 8 follow-ups from γ-0 / γ-1..6 batch (opened 2026-04-28; updated post-Tier 9.1).** The Tier 7+8 batch landed `MutationOpts.audit?` optional + 8 of 10 mutation-site migrations + DF12 `promptNode.tools` reactive widening. Tier 9.1 batch (2026-04-28) landed γ-7-B: process appendRecord helper now routes through `lightMutation` for centralized freeze + seq-cursor advance + handlerVersion stamping; the closure `let seq = 0` was promoted to a `registerCursor`-mounted state node (`{name}_process_seq` on the cqrsGraph). Remaining follow-up:
  - ~~**Full `process/start` + `process/cancel` `wrapMutation` migration (γ-7-A vs γ-7-B-extended) — STILL DEFERRED.**~~ **✅ ALREADY RESOLVED (γ-7-A shipped 2026-04-28; `startStrict()` DROPPED) — archived 2026-05-18** to `archive/optimizations/resolved-decisions.jsonl` (id `gamma7-process-wrapmutation-startstrict-resolved-2026-04-28`). Stale duplicate: contradicted by `docs/implementation-plan.md` Phase-14.5 E.1 (full-body `wrapMutation` shipped; speculative `startStrict()` explicitly dropped, archived). Premise-check stale finding.
  - **Messaging audit-record schemas (γ-5 / γ-6).** Tier 8 γ-0 collapsed the "new audit log surface" question by making `audit` optional. Cluster II sites (Topic.publish / Subscription.ack / Subscription.pullAndAck / Hub.delete) now route through the framework with `audit` omitted. If a real audit consumer ever surfaces in messaging, ship `TopicPublishRecord` / `SubscriptionAckRecord` / `HubMutationRecord` types (mirroring `JobEvent` / `DispatchRecord` / `SagaInvocation` naming) plus matching `keyOf` exports, and let users pass `audit: <bundle>` via the existing framework option. Until then: no schemas, no `keyOf`, no `attachAudit` method — keep API surface lean.

- **Tier 6.5 follow-ups from C2 lock (opened 2026-04-28; updated post-D1/D2/D5).** The Tier 6.5 C2 batch landed `executeFlow` JobFlow as the EXECUTE → VERIFY spine; Q1–Q6 lock decisions all in code. /qa pass (2026-04-28) patched F1 (COMPLETE-without-DATA arm), F2 (`adapter.invoke(messages, { signal })` threading), F3 (bridge-layer flakes → `self-correctable`; defensive guard → `structural`), F4 (dispatchEffect length cursor), F5 (parsed-shape validation), F6 (16 new bridge-failure regression tests including §9a coalescing). Deviation-resolution pass (D1/D2/D5, 2026-04-28) landed: `JobFlow.StageDef.maxPerPump` per-stage override; `_oneShotLlmCall` shared helper extraction; eval-verifier `batch()` JSDoc tightening + focused §9a regression test. Remaining primitive-level + behavioral follow-ups: **5 resolved sub-item(s) (Phase 1 adapter-abort path, reflectNode tick count vs verdict count divergence, §35 reentrancy invariant, Tier 6 inspection-only reconciliation audit-test pass, Async-evaluator §9a coverage) archived** to `archive/optimizations/resolved-decisions.jsonl` (`tier-6-5-follow-ups-from-c2-lock-opened-*-resolved`) on the 2026-05-16 /qa sweep. Open items remain below:
  - ~~**Bounded concurrent inflight on JobFlow stages.**~~ **✅ STALE — already landed 2026-04-29 as Tier 6.5 3.1; archived 2026-05-22** to `archive/optimizations/resolved-decisions.jsonl` (id `tier-6-5-3-1-stage-maxinflight-stale-carry-resolved-2026-05-22`). `StageDef.maxInflight?: number` exists at [src/utils/job-queue/index.ts:455](../src/utils/job-queue/index.ts#L455) with the backpressure semantic the carry described: the pump at [:632-639](../src/utils/job-queue/index.ts#L632) `break`s when `inflight.size >= stageMaxInflightCap` and an internal `inflightCounter` re-fires the pump when a settle decrements the count. The carry's `pending.length + inflight.length ≤ maxInflight` framing was misleading — shipped impl caps inflight only, which is the correct semantic for the cited use case ("no more than N concurrent LLM calls regardless of pending depth"). Premise-check stale finding (`feedback_verify_premise_before_greenfield`).
  - **Structural→reingest topology edge — KEEP/PARKED (design-locked).** Dispatch effect imperatively calls `intake.publish(...)` on the structural-failure path ([src/presets/harness/harness-loop.ts:806](../src/presets/harness/harness-loop.ts#L806), gated by `tryIncrementBounded(totalReingestions, …)` `:804`). A topologically-visible `flow.completed → structural-filter → bridge/reingest → intake.latest` edge would strengthen `describe()`/`explain()`, but is blocked on a reactive bounded-counter primitive that **was explicitly decided against** — impl-plan DS-13.5.D D.2 locked "keep `tryIncrementBounded`, NO new primitive" (the wrap-imperative-as-primitive §44 trap). Reclassified "Park" → **KEEP/PARKED by design-lock** (not awaiting a consumer; the unblocking primitive is a closed decision).
  - ~~**`routeJobIds` collision contract on duplicate `trackingKey` (qa D1, deviation P1.1).**~~ **✅ ALREADY RESOLVED IN CODE (qa D1, 2026-04-29) — archived 2026-05-18** (id `tier-6-5-per-claim-eval-mount-routejobids-resolved-2026-05-18`). Stale: `src/utils/harness/_internal.ts:23-41` carries the explicit "Caller contract — uniqueness" JSDoc on `trackingKey` + the `relatedTo[0]` retry/reingest guidance the item asked for.
  - ~~**Per-claim eval-verifier subgraph mounting story.**~~ **✅ ALREADY RESOLVED IN CODE (DS-13.5.D.4) — archived 2026-05-18** (same id). Stale: `evalVerifier` now mounts each claim's pipeline at `eval/${claimId}` on the parent ([src/presets/harness/eval-verifier.ts:84-89,228-240](../src/presets/harness/eval-verifier.ts#L228)) with a per-claimId collision counter + `parent.remove` cleanup → per-claim eval topology IS walkable in `describe()`/`explain()` (same rework that closed the M4 cluster). Premise-check stale finding.

- **QA follow-ups from Tier 5.2 + Wave-AM /qa pass (opened 2026-04-28).** Adversarial review of the Tier 5.2 + Wave-AM batch surfaced ~20 findings; the architecturally significant ones (G1A/G1B/G1C-prime/G2A) plus auto-applicable docs (G2B) all patched in-batch. Remaining deferred items below are doc-only or downstream of unrelated primitive widening: **4 resolved sub-item(s) (G1A "same concept", G1B SENTINEL bridge, G1C-prime switchMap rebuild, G2A flow flush cursor) archived** to `archive/optimizations/resolved-decisions.jsonl` (`qa-follow-ups-from-tier-5-2-wave-am-q-*-resolved`) on the 2026-05-16 /qa sweep. Open items remain below:
  - **EC5 — `audit-no-effect` lint reactivity.** `guardedExecution` checks for per-node guards once at construction ([src/presets/inspect/guarded-execution.ts:270-286](../src/presets/inspect/guarded-execution.ts#L270), path corrected 2026-05-18); late-mounted guarded nodes don't clear the lint. **Open (verified 2026-05-18)** — still one-shot-at-construction; low priority, documented caller-side workaround (mount guards before wrapping). Correctly a by-design KEEP.
  - **EC6 — migration note: `pipeline.error` → `pipeline.lastError` / `pipeline.node` → `pipeline.output`.** `Graph.node(name)` and `Graph.error(name, err)` are existing base-class methods; bundle-property names couldn't be carried over verbatim. Consumers of the pre-Tier-5.2 bundle shape need to update both property accesses AND any `pipeline.describe().nodes.error` / `nodes.node` lookups (now `lastError` / `output`). Pre-1.0 breaking; documented in Tier 5.2 plan note.
  - ~~**EC7 — `meta` companion `resubscribable` propagation.**~~ **✅ ALREADY RESOLVED IN CODE (Tier 4.2, 2026-04-29) — archived 2026-05-18** to `archive/optimizations/resolved-decisions.jsonl` (id `ec7-meta-companion-resubscribable-inherit-resolved-2026-04-29`). Stale: `packages/pure-ts/src/core/node.ts:1018-1035` meta-companion construction now does `if (opts.resubscribable === true) metaOpts.resubscribable = true;` with an explicit "EC7 (Tier 4.2, 2026-04-29): meta companions inherit `resubscribable`" comment — exactly the proposed fix. Premise-check stale finding.
  - ~~**F8 — `as Node<Actor>` cast in `scopedDescribeNode`.**~~ **✅ LANDED 2026-05-18 (`/dev-dispatch` bounded-correctness batch)** — widened `GraphDescribeOptions.actor` from `Actor | Node<Actor>` to `Actor | Node<Actor | null>` ([packages/pure-ts/src/graph/graph.ts](../packages/pure-ts/src/graph/graph.ts) — `actor?` field + `isActorNode`/`resolveActorOption` signatures; `resolveActorOption` maps a `null`/`undefined` cache → `undefined` = "no scoping"). Both `as Node<Actor>` casts dropped in [src/presets/inspect/guarded-execution.ts](../src/presets/inspect/guarded-execution.ts) (the `scopedDescribe` construction site + the `scopedDescribeNode` per-call site). **Cross-track check (D196):** `GraphDescribeOptions` is **not** on the `Impl` parity contract (`packages/parity-tests/impls/types.ts` types only `describe()` / `describe({reactive:true})`, never the `actor` field), the change is additive type-widening (no rename/removal, no new presentation→substrate symbol import), and `Node<Actor>` stays assignable to `Node<Actor | null>` (`emit` is pre-widened with `| null`, method-param bivariance) — so **no cross-track-ledger row required**. 1 regression test. ✅ ARCHIVED 2026-05-18 (id `bounded-correctness-batch-2026-05-18`) — /qa zero-findings.
  - **F9 — `graphLens.flow` reconciliation O(N) per emission — KEEP/POST-1.0 (perf, no consumer).** [src/utils/inspect/lens.ts:308](../src/utils/inspect/lens.ts#L308) — `new Set(Object.keys(desc.nodes))` per changeset/topology emission. Verified 2026-05-18: the 2026-05-17 `lens.flow` mutations-companion pass deliberately left the qa-tuned `flow` derived (incl. this O(N) reconcile) untouched; still pending the "graphLens scaling to 50k-node" perf ticket. Reclassified to the **POST-1.0** bucket (genuine scale work, no consumer pressure — per the consumer-demand policy).

- **Tier 4.7 — narrow `ReactiveMapBundle.entries` callback typing — KEEP option (b) (option (a) foreclosed; verified 2026-05-18).** The inline `((snapshot as ReadonlyMap<…> | undefined) ?? new Map())` casts persist at [src/utils/ai/memory/memory-composers.ts:125,214,419,469,880](../src/utils/ai/memory/memory-composers.ts#L125) (deps widen to `unknown` per the graphrefly contract). **Option (a) is no longer available** — a typed-deps `derivedT`/`effectT` was *explicitly removed from core* ([packages/pure-ts/src/core/sugar.ts:4-5](../packages/pure-ts/src/core/sugar.ts#L4)). So option (b) — **the cast pattern is the accepted documented norm at composer call sites** — is the resolution. Reclassified "Defer until 2nd surface" → **KEEP/WONTFIX (option (b) by decision)**; revisit only if a typed-deps sugar is ever reintroduced to core (a substrate-API decision, not a memory-composer concern).

- **Tier 5.2 follow-up — primitive-side reactive-options widening (opened 2026-04-27, qa-revised 2026-04-28).** `resilientPipeline` accepts `T | Node<T>` for every primitive option (precedent shape, aligned with `FallbackInput<T>`). **Pipeline-side rebuild via switchMap shipped (qa G1C-prime, 2026-04-28)** — the imperative `node.cache` read at construction was replaced with a switchMap subscription that rebuilds the layer on each option emission. State-loss caveat documented (rate-limit pending buffer / breaker failure count / retry attempt count / in-flight timeout reset per rebuild). The deferred sub-tickets below describe the **primitive-side** widening that would preserve internal state across option swaps; once those land, the pipeline forwards Node-form options directly to the primitive (no switchMap rebuild needed).
  - **D7 (rate-limit state companion) — ✅ landed 2026-04-27.** `RateLimiterBundle.rateLimitState: Node<RateLimiterState>` ships now (`{droppedCount, pendingCount, paused}`); `pipeline.rateLimitState` exposes it on the static-options path only. The audit's name is satisfied without renaming `droppedCount` (which stays for back-compat).
  - **D8 (caller `meta` forwarding) — ✅ landed 2026-04-27.** `rateLimiter` / `withStatus` / `withBreaker` / `timeout` / `retry` / `fallback` each accept an `meta?: Record<string, unknown>` option that gets merged into the produced node's `meta` (caller keys first; companion seeds + `factoryTag` win the merge so the audit trail can't be silently overwritten). `budgetGate` already accepted `meta` (Tier 3.3). Pipeline now stamps `domainMeta("resilient", "<kind>")` on each layer.
  - **G1C-prime (pipeline switchMap rebuild) — ✅ landed 2026-04-28.** Caller-supplied `Node<T>` options trigger `switchMap`-style rebuilds on each emission; chain stalls until first DATA. Companions exposed only on static-options path.
  - **Reactive-options sub-tickets (still deferred — primitive-side widening so `Node<Options>` rebinds on emit WITHOUT state loss; once any of these land, the pipeline can forward the Node directly and the switchMap rebuild becomes opt-in or unnecessary):**
    - `rateLimiter(source, opts)` — `opts: NodeOrValue<RateLimiterOptions>`. Subscribe + rebind tokenBucket capacity / refill / overflow policy on emit. Open question: how to reconcile in-flight pending-buffer items with a shrunk `maxBuffer` — drop-oldest seems correct; document explicitly.
    - `circuitBreaker(opts)` / `withBreaker(breaker, options)` — reactive `failureThreshold` / `windowNs` / `resetAfterNs`. Rebind via new `breaker` instance behind a delegating proxy OR reset current breaker to `closed` on threshold change. Document state-reset trade-off.
    - `timeout(source, ns)` — `ns: NodeOrValue<number>`. Every retry already resubscribes; reactive `ns` simply reads the latest cache for the next attempt's deadline. Smallest widening.
    - `retry(source, opts)` — `opts: NodeOrValue<RetryOptions>`. Affects `count` and `backoff`. Re-resolve via `resolveRetryConfig` on each attempt. Footgun: opts swap mid-attempt shrinking `count` below current attempt index — define semantics ("next attempt fails immediately if already exhausted under new count").
    - `fallback(source, fb)` — already reactive-friendly via `FallbackInput<T>`. No-op.
    - `budgetGate(source, constraints)` — `constraints: NodeOrValue<readonly BudgetConstraint[]>`. Subscribe + rebind constraint set; addition/removal is harder semantic. Pairs with a future `budgetState` companion (per-constraint pass/fail visibility).
  - **Per-layer companion-Node tickets (still deferred — primitive-side widening so the data is even available to expose):**
    - `budgetState: Node<…>` on `budgetGate` (per-constraint pass/fail snapshot).
    - `retryAttempts: Node<number>` on `retry` (live attempt count + last error).
    - `lastTimeout: Node<{at_ns, deadlineNs} | null>` on `timeout`. Audit Wave-B Unit 7 §B.2 calls for these; file primitive-side tickets when a real consumer surfaces.

- **Wave 2B Tier 3 audits — deferred QA items (opened 2026-04-27, /qa pass post-Tier-3).** Adversarial review of Tier 2 reorg + Tier 3 audits surfaced ~50 findings; patched ~14 in-batch and deferred the following: **Resolved sub-items DF1/DF2/DF6/DF9/DF10/DF12/DF13 archived** to `archive/optimizations/resolved-decisions.jsonl` (ids `wave2b-tier3-df*-resolved`) on the 2026-05-16 /qa sweep; only open items remain below:
  - ~~**DF3 — HeadIndexQueue compaction worst-case 3× memory.**~~ **✅ PREMISE MOOT — archived 2026-05-18** to `archive/optimizations/resolved-decisions.jsonl` (id `df3-headindexqueue-compaction-moot-2026-05-18`). Verified 2026-05-18: the head-index-growth-then-compact queue this describes **no longer exists** — `packages/pure-ts/src/extra/data-structures/reactive-log.ts` `trimHead` (`:286-303`) is either an immediate `splice(0,removed)` (unbounded path, no amortized retention) or a fixed-capacity ring (`new Array(maxSize)`, `:220`, no growth). No "~2×/~3×/exponential threshold" mechanic in code.
  - **DF4 — ring-slot `undefined`-write V8 deopt — KEEP (intentional GC-release; wording reframed 2026-05-18).** `packages/pure-ts/src/extra/data-structures/reactive-log.ts:296` `this._buf[…] = undefined` is a **fixed-capacity ring-slot clear for early GC release** (intentional defensive choice), NOT the "vs `slice` at compaction" tradeoff the old text implied — that compaction structure (DF3) doesn't exist. Needs a benchmark before any change; correctly a KEEP.
  - **DF5 — rateLimiter `droppedCount` activation-time emit when no subscriber — KEEP (intentional, consistent with eager push-on-subscribe).** [src/utils/resilience/rate-limiter.ts:~397-398](../src/utils/resilience/rate-limiter.ts) — `droppedNode.emit(0)` / `stateNode.emit(initial)` fire unconditionally at producer activation. Verified 2026-05-18: still unguarded; the "guard with `droppedNode.subscribers > 0`" option needs primitive subscriber-count support that doesn't exist. Reclassified design-decision → **KEEP** the eager-emit (consistent with push-on-subscribe semantics; not a latent bug).
  - **DF7 — PY parity for `policyGate` initial-sweep + reactive-paths rebind.** TS uses single-thread guarantee for `latestPaths` mutation between manual sweep and `pathsNode.subscribe` first DATA. PY rewrite needs explicit synchronization (per-subgraph RLock) since free-threaded Python won't give the same guarantee. Tag: `[py-parity-policygate]`.
  - ~~**DF8 — `withStatus` per-subscribe pending+null thrash.**~~ **✅ RESOLVED-AS-DOCUMENTED (2026-04-29 doc lock) — archived 2026-05-18** to `archive/optimizations/resolved-decisions.jsonl` (id `df8-withstatus-thrash-documented-resolved-2026-04-29`). Stale (path was `src/utils/resilience/*`): `src/base/resilience/status.ts:68-78` carries an explicit "**Per-subscribe lifecycle (DF8, 2026-04-29 doc lock).**" JSDoc block documenting the thrash as intended fresh-cycle semantic + the `state()`-snapshot mitigation — the entry's "document or change" → documented.
  - **DF11 — `placeholderArgs(factoryArgs: { x: undefined })` JSON.stringify drop.** Passing `factoryTag("foo", { x: undefined })` keeps `factoryArgs: { x: undefined }`, which `JSON.stringify` drops. Meta-vs-spec disagreement when serialized.
  - **DF14 — `describeNode` specMode sentinel preservation.** State nodes in `"sentinel"` status get reconstructed as `state(undefined)` (not sentinel) by `compileSpec` — `out.sentinel: true` is emitted in describe output but `compileSpec` doesn't consume it. Requires SENTINEL-aware state factory. Non-trivial; defer until a sentinel-state round-trip use case surfaces.

- **Tier 1.5.3 deferred QA items (opened 2026-04-27, /qa pass post-Phase-2.5).** Items the QA pass evaluated and chose to defer rather than patch in the same batch: **1 resolved sub-item(s) (F15) archived** to `archive/optimizations/resolved-decisions.jsonl` (`tier-1-5-3-deferred-qa-items-opened-202-*-resolved`) on the 2026-05-16 /qa sweep. Open items remain below:
  - F16: `_describeReactiveDiff` empty-graph initial cache violates suppression doc — push-on-subscribe of empty changeset on empty graph. Doc-level clarification deferred; behavior is benign (consumer ignores empty events).
  - F17: `_describeReactiveDiff` `prev` race vs first snapshot push — subtle, has not reproduced in tests; document if a consumer surfaces.
  - ~~F18: `verifiable` closure-mirror seeded from `sourceNode.cache` may be `undefined` on startup race — pre-existing pattern.~~ ✅ resolved by Phase 15: `verifiable` in `composite.ts` migrated from closure-mirror to `withLatestFrom` reactive edge.
  - F19: `withStatus` non-recovery DATA branch not wrapped in `batch` — minor consistency; observable transient state-update for one wave.
  - F20: `factoryTag` double-call silently overrides — likely intentional; warn-on-override would add API surface.
  - ~~F21: `tap` observer-arg branch potentially drops user `opts.meta`~~ **✅ VERIFIED NON-ISSUE — archived 2026-05-18** (id `tier-1-5-3-f21-f23-resolved-2026-05-18`): `tap` does `{ ...factoryTag("tap"), ...(opts?.meta ?? {}) }` (control.ts:78,110) — user `meta` is preserved, not dropped.
  - F22: `switchMap`/bundle factoryTag override pattern fragile against future switchMap meta additions — only matters if switchMap adds non-factory meta keys later.
  - ~~F23: `metaEqual` doesn't handle Map/Set/Date — doc the assumption.~~ **✅ DOC-ASK SATISFIED — archived 2026-05-18** (same id): `topology-diff.ts:62-66` JSDoc now explicitly documents the Map/Set/Date plain-object assumption — the only ask is done.
  - F24: ~~Unused `_f`/`_fa` destructure repeated 3× — cosmetic, could extract `stripFactoryKeys` helper.~~ ✅ resolved by Tier 1.5.3 Phase 3 (2026-04-27): `normalizeSpec` deleted entirely; `compileSpec` now uses the centralized `stripFactoryMeta` helper.
  - ~~F25: `composite.ts` `consolidate` closure-mirror pattern parallels F9 (which was patched in agent-memory). If composite's consolidate ever gets a Graph context for `addDisposer`, mirror the F9 fix.~~ ✅ resolved by Phase 15: `distill` consolidated in `composite.ts` migrated from closure-mirror to `withLatestFrom` reactive edge.

- ~~**Wave AM audit closed (2026-04-26) — `patterns/ai/memory/` 6-unit pass.**~~ **✅ SHIPPED IN TS — archived 2026-05-18** to `archive/optimizations/resolved-decisions.jsonl` (id `wave-am-closed-extends-graph-reactiveextractfn-resolved-2026-05-18`). Stale: AM.0–AM.3 all landed — `class AgentMemoryGraph extends Graph` ([src/presets/ai/agent-memory.ts:133](../src/presets/ai/agent-memory.ts#L133)), distill reactive-extractFn Node contract ([src/base/composition/distill.ts:101-103](../src/base/composition/distill.ts#L101)), composer rebuild (`MemoryWith*Graph` subclasses in [src/utils/ai/memory/memory-composers.ts](../src/utils/ai/memory/memory-composers.ts)). The "archive lines 643/656/…" instruction was itself stale (those line numbers long moved). PY parity stays in the umbrella below.

- **PY parity umbrella for `patterns/ai/memory/` (umbrella, opened 2026-04-26 post-Wave-AM; premise partially stale — see note).** **Note (2026-05-18 sweep):** the "`graphrefly-py` does NOT currently have any of…" inventory below is now partially stale — `graphrefly-py` already has `agent_memory` (+ `admission_filter`); the umbrella stays a `[py-parity-am]`-held item per the PY-parity deferral policy (re-inventory PY coverage when rigor-infra unblocks parity work, don't act on the stale list). Original scope retained for the design record: `graphrefly-py` did NOT (as of 2026-04-26) have any of: `agentMemory`, `memoryWithTiers`, `memoryWithVectors`, `memoryWithKG`, `memoryRetrieval`, `llmExtractor`, `llmConsolidator`, `admissionFilter`, `admissionFilterNode`, `admissionFilter3D`, `decay()`, retrieval types, tier types. Per the rigor-infrastructure plan (`archive/docs/SESSION-rigor-infrastructure-plan.md`), PY parity work is deprioritized until Projects 1–3 (fast-check protocol harness, TLA+ core spec, TS↔PY executable contract traces) land. This umbrella holds the scope: port primitives from `src/patterns/memory/` first (Wave A prerequisites); port composers from Wave AM landing; mirror the locked API-style policy (no imperative reads on Phase-4 primitives); PY-specific concerns (thread safety on closure-promoted state nodes; `with batch():` instead of `batch(() => ...)`; `__or__` pipe operator surface). Do NOT split into per-unit tickets until rigor-infra unblocks parity work. Tag: `[py-parity-am]`.

- ~~**`mapFromSnapshot` lone-helper cleanup (rescoped 2026-05-18; the parallel-pair concern is resolved).**~~ **✅ RESOLVED-AS-WONT-FIX — archived 2026-05-18** (id `mapfromsnapshot-lone-helper-wontfix-resolved-2026-05-18`). The parallel-pair half (`extractStoreMap`) was already deleted (Wave AM Unit 5). The surviving `mapFromSnapshot` at [src/base/composition/distill.ts:67](../src/base/composition/distill.ts#L67) (used 5× — `:141,191,206,233`) is **NOT a redundant type-cast helper** — its D2 /qa-lock JSDoc (`:54-66`) documents it as a **load-bearing runtime defense**: on snapshot **restore**, `JsonGraphCodec` serializes a `Map` to `null`/`{}`/`[]` and `Graph.restore` writes that non-Map value back to cache, so `.entries()`/`.size` would silently mis-behave without the `instanceof Map` guard. It was deleted once in Tier 10.1 and had to be re-added for exactly this reason. The optimization's deletion precondition ("local `unknown`-boundary casts tightened") cannot be met — the defense is for a runtime shape mismatch, not a type cast. Deleting (or inlining) it carries no benefit and regresses a /qa-locked invariant. Kept verbatim; entry closed won't-fix.

- **`diffMap<K, V>` operator extraction — KEEP (YAGNI; premise corrected 2026-05-18).** Original premise was that `memoryWithVectors`/`memoryWithKG` (now in [src/utils/ai/memory/memory-composers.ts](../src/utils/ai/memory/memory-composers.ts)) inline a `{added,removed,changed}` diff-indexer (2 consumers). **Verified false 2026-05-18:** both are **full-snapshot re-scans** (`:118-135`, `:207-230`), not diff-indexers — the diff-pattern consumer count is **0**, not 2. The YAGNI hold is *stronger*, not weaker: there is no extraction candidate and no consumer. Keep filed as a speculative future operator only if a real diff-mirroring consumer ever surfaces; signature target unchanged: `diffMap<K,V>(stream, opts?): Node<{added,removed,changed}>`.

- ~~**`extends Graph` pattern consistency sweep (opened 2026-04-26, Wave AM Unit 6 follow-up).**~~ **✅ EFFECTIVELY DONE — archived 2026-05-18** (id `wave-am-closed-extends-graph-reactiveextractfn-resolved-2026-05-18`). Verified 2026-05-18: 47 `extends Graph` subclasses across `utils/`/`presets/`/`base/`; `cqrs`/`process`/`messaging`/`job-queue` all migrated. The lone residual `Object.assign(graph, …)` at `src/utils/memory/fact-store.ts` (`reactiveFactStore`) was **fully migrated 2026-05-18** (`/dev-dispatch` bounded-correctness batch) to `export class ReactiveFactStoreGraph<T> extends Graph` (faithful constructor migration — all wiring moved into the constructor, `Object.assign`→readonly-field assignment, `itemNode` closure→method; the `reactiveFactStore` factory is now a thin `new` wrapper; barrel re-export changed from `type`-only to value+type so `instanceof` narrows). 57 fact-store tests + the new `instanceof`/direct-construction test green. **Sweep now 48/48; zero residual.** ✅ ARCHIVED 2026-05-18 (id `bounded-correctness-batch-2026-05-18`) — /qa (2 parallel reviewers) verified the migration byte-faithful.

- ~~**`reactiveExtractFn` migration tracker (opened 2026-04-26, Wave AM Unit 4 + AM.0).**~~ **✅ COMPLETE — archived 2026-05-18** (id `wave-am-closed-extends-graph-reactiveextractfn-resolved-2026-05-18`). Verified 2026-05-18: substrate `distill()` migrated to the Node contract ([src/base/composition/distill.ts:101-103](../src/base/composition/distill.ts#L101) — `raw: Node<TRaw>, existing: Node<…>`); no old callback-shape `distill()` callers remain. `agentMemory` adapts the *public* ergonomic `(raw, existing)` callback internally via a `(rawNode, existingNode) => switchMap(...)` reactive subgraph ([src/presets/ai/agent-memory.ts:197-228](../src/presets/ai/agent-memory.ts#L197)) — that public callback is an **intentional ergonomic facade**, not an unmigrated caller. Tracker goal met.
- ~~**Operator-layer: `filter` mixed-batch RESOLVED forwarding (2026-04-24)**~~ **RESOLVED 2026-04-30** — archived to `archive/optimizations/resolved-decisions.jsonl` (id: `operator-layer-mixed-batch-resolved-2026-04-24`). Walked as DS-11.10 on 2026-04-30; finding was that the deferral pre-dated spec §1.3.3 "Tier-3 wave exclusivity" + COMPOSITION-GUIDE §41 (which jointly lock the protocol-level rule: RESOLVED only when the entire wave produces zero DATA, never per-dropped-item). All operators (`filter`/`map`/`take`/`skip`/`takeWhile`/`distinctUntilChanged`/`pairwise`/`scan`/`reduce`) conform; per-item RESOLVED tagging is rejected at the protocol level; tier-3 counters must count upstream of operators (codified in `filter` JSDoc at [transform.ts:51-60](../src/extra/operators/transform.ts#L51)).

- ~~**Tool handler signal-aware migration**~~ **DONE 2026-04-24** — archived to `archive/optimizations/resolved-decisions.jsonl` (id: `tool-handler-signal-aware-migration`). See [docs/test-guidance.md § "Tool handlers should thread `signal`"](test-guidance.md#tool-handlers-should-thread-signal).

- **Rigor-infra follow-ups surfaced by QA on batch 2 (opened 2026-04-23):**
  - ~~**`BatchEmitMulti` per-item cache snapshot in `pendingExtraDelivery`.**~~ **RESOLVED 2026-04-23 in batch 3 package 2** — new `BuildBatchPendingItems` + `EnqueuePendingExtraItems` helpers stamp each item in the bundle with cache-at-its-own-emit-time. DIRTYs carry pre-batch cache; settles carry running-per-step cache. `MultiSinkIterationCoherent` (new invariant #18) verifies the fix structurally.
  - ~~**LockId collision across up() and down() origin.**~~ **RESOLVED 2026-04-23 via Package 8 JSDoc.** Documented at [src/core/node.ts](../src/core/node.ts) `up()`: "LockId uniqueness is the caller's responsibility. Locks are stored in a `Set<unknown>`; `Pause(n, 10)` and an upstream `UpPause(child, 10)` that reaches `n` insert the SAME element — one `Resume(n, 10)` clears both origins." Not a bug; an under-specified corner the rigor work surfaced. Runtime unchanged.
  - ~~**Up+down pause composition MC.**~~ **✅ PARKED POST-1.0 2026-05-22 (Slice B design session, `/dev-dispatch`) — archived to `archive/optimizations/resolved-decisions.jsonl` (id `up-down-pause-composition-mc-parked-post-1-0-resolved-2026-05-22`).** Three-gate evaluation per `feedback_three_gate_substrate_convergence` FAILS for this carry: **G1 (spec citation)** — §2.6 R2.6.0/R2.6.1 pin self-pause and pre-pause baseline replay but say nothing about up-origin PAUSE delivery; nothing to cite. **G2 (cross-arm)** — Rust also does not honor upstream-origin PAUSE delivery; nothing to coordinate. **G3 (drain-time edge-cases)** — the entire question. Per the policy, 3-gate-fail = revert+archive. Pure park chosen over a δ spec-text addition because acknowledging "absence is intentional" would itself be a soft autonomous lock that pre-supposes the post-1.0 design call; we preserve the open question instead of nailing it shut. Reopen if (a) a real consumer surfaces a topology that needs upstream-origin PAUSE gating, OR (b) the post-1.0 §2.6 design pass formalizes up-origin delivery and an MC becomes a natural follow-on.

- **Remaining rigor-infra coverage gaps (opened 2026-04-23, batch 3 partial — updated index):** each needs its own focused session; none blocking today.
  - ~~**§2.5 `replayBuffer: N` late-subscriber replay**~~ **RESOLVED in two stages.** TLA+ side **landed 2026-04-23 (batch 5 A)**: `SubscribeSink` now appends `replayBuffer[sid]` contents to the handshake as DATA messages; new ghost `replaySnapshot: NodeIds -> Seq(Values)` captures the buffer at subscribe time and clears on `Resubscribe`. New invariant `LateSubscriberReceivesReplay` verifies the handshake tail matches the snapshot. `StartHandshakeValid` updated with a `baseLen = Len(H) - Len(replaySnapshot[sid])` decomposition. Exercised actively by `wave_protocol_replay_MC` (ReplayBufferSize[A] = 2; state space grew ~1912 → 2265 distinct states). TS runtime side **landed 2026-05-03 (Phase 13.6.B B6 / Lock 6.G)**: `NodeOptions.replayBuffer?: number` + `NodeImpl._replayBuffer` ring; push at `_emit` dispatch site (NOT inside `_updateState` — avoids RESUME-drain re-push corrupting the "values-seen" semantic); replay in `defaultOnSubscribe` after START handshake; cleared by `_deactivate` and `_resetForFreshLifecycle`; INVALIDATE preserves history (independent of `_cached`). 15 audit-sweep tests in `phase-13-6-b6.test.ts`. **Carry:** TLC #20 / #23 fast-check property-mirror port (`ReplayBufferBounded` + `LateSubscriberReceivesReplay`) deferred to a future rigor-infra pass — `_invariants.ts` lists them under "Dropped TLC mirrors" with the new tracking note.
  - ~~**§2.5 `equals` variance — custom / `() => false`** (Package 3).~~ **RESOLVED 2026-04-24 in batch 9 E.** Generalization: `EqualsAbsorbs: NodeIds -> BOOLEAN` replaced by `EqualsPairs: NodeIds -> SUBSET (Values \X Values)` — a per-node relation over value pairs. Identity diagonal = legacy `TRUE`; empty = legacy `FALSE`; arbitrary subset = custom `equals` fn. New helper `IsAbsorbed(n, prev, next)` threaded through all four emission sites (Emit / BatchEmitMulti / SinkNestedEmit / DeliverSettle + DeliverTerminal rescue) — pre-batch-9 only Emit was gated, despite batch 6 claiming otherwise. New exercise MC `wave_protocol_custom_equals_MC` (2-node chain, `EqualsPairs[A]` includes `<<0,1>>` and `<<1,0>>` so 0 and 1 are interchangeable). Load-bearing: reverting `BuildSettleSeq` to raw `=` trips `VersionPerChange`.
  - ~~**§2.5 `resetOnTeardown`.**~~ **RESOLVED 2026-04-23 in batch 3 package 4** — `ResetOnTeardownNodes: SUBSET NodeIds` constant mirrors spec §2.5. `Resubscribe` conditionally clears cache. Existing MCs default to prior behavior (`NodeIds \ SourceIds`, reset all derived); a preserve-all MC variant can be added when a consumer needs it.
  - ~~**§2.5 `completeWhenDepsComplete: false` / `errorWhenDepsError: false`** (Package 5).~~ **RESOLVED 2026-04-24 in batch 8 A.** `AutoCompleteOnDepsComplete` / `AutoErrorOnDepsError` axes shipped batch 3 package 5 + `rescue_MC` exercise MC shipped batch 3 QA round 2; the missing invariant — "a node with gate=FALSE never terminates via dep cascade" — lands this batch as `NoDepCascadeTerminalWhenGateFalse` (#25). Ghost `terminatedBy` classifies every status→"terminated" transition by cause; invariant asserts dep-cascade-* classifications only occur when the matching gate is TRUE. Load-bearing in `rescue_MC`: reverting the gate trips it in ≤3 steps.
  - ~~**§2.4 INVALIDATE + fn cleanup firing** (Package 6).~~ **RESOLVED 2026-04-23/24 across batches 3, 5 B, and 8 D.** Batch 3 package 6 shipped the `Invalidate` action + `cleanupWitness` + `CleanupWitnessInValueDomain` (#19). Batch 5 A/B extended cascade to `DeliverInvalidate`, added diamond-fan-in guard, + `CleanupWitnessNotSentinel` (#24). Batch 8 D adds `CleanupWitnessAccounting` (#26) — a quantitative counting law tying `Len(cleanupWitness[n])` to a new `nonVacuousInvalidateCount[n]` ghost, catching off-by-N regressions between the witness guard and the cache-reset guard. The original scope's "witness before `prevData[i]` reset" is subsumed by the existing witness-captures-pre-reset-cache modeling + the counting law; explicit `prevData` modeling deferred as redundant unless a consumer surfaces a per-dep-slot bug class the shipped invariants miss.
  - ~~**§2.3 meta companion TEARDOWN fan-out ordering** (Package 7).~~ **RESOLVED across batches 3 (MetaTeardownObservedPreReset + axis), 3 QA round 2 (full parent state transition), and 4 (DeliverTeardown cascade).** The entire scope originally listed here ships: `MetaCompanions: NodeIds -> SUBSET NodeIds` constant, `TEARDOWN` tier-5 message, `Teardown(parent)` + `DeliverTeardown(p, c)` actions, `teardownWitness` ghost + `teardownCount` bound, `MetaTeardownObservedPreReset` (#22) invariant pinning the pre-reset fan-out contract, `terminatedBy = "self-teardown" / "teardown-cascade"` classification (batch 8). Exercise MC `wave_protocol_meta_teardown_MC` (4-node topology with meta children at both `A` and `B`) + regression catcher for fan-out reordering. This gap entry was stale — the `Teardown` action's own docstring was updated in batch 9 to reflect the shipped state (not the MVP witness-only decision).

- **switchMap-inner teardown hardening for `refineExecutor` / `evalVerifier` — PREMISE STALE; residual reclassified POST-1.0 (rewritten 2026-05-18):**
  **The 2026-04-23 premise no longer holds.** Neither factory uses the "switchMap project fn building an unregistered inner graph" shape any more: `refineExecutor` was restructured by the C2 lifecycle refactor (Tier 6.5) — no internal `switchMap`; the JobFlow pump owns the per-claim lifecycle (one work-fn invocation per claim, teardown on result-Node unsubscribe; see `src/presets/harness/refine-executor.ts` module doc). `evalVerifier` was restructured by DS-13.5.D.4 — each claim mounts its own child subgraph `eval/${claimId}` on the parent with an `onDeactivation` cleanup (`parentGraph.remove(segmentPath)`) + a per-claimId collision counter (`src/presets/harness/eval-verifier.ts`). The catastrophic-regression test (`destroy() after first convergence does not throw on late inner-loop drain`) landed with v1; emitting into a removed/torn-down node is a safe no-op.
  **POST-1.0 (no consumer signal):** the residual "fuller hardening" — (a) a `dispose` hook on `HarnessExecutor` / `HarnessVerifier`, (b) `refineLoop` tracking its in-flight `switchMap` Promise to suppress late resolutions, (c) a rapid-supersede stress test — is **speculative API surface** (adds a public hook with no concrete consumer; §44 over-generalization risk). Reclassified POST-1.0 per the consumer-demand policy below: the substrate caveat to revisit is "I/O actuators whose side-effect already landed need a late-resolution audit hook" — that is the `actuatorExecutor` `onSupersede`/`dispose` follow-up (already filed separately), not a refineExecutor/evalVerifier concern.

- **Harness `executeAndVerify` unified slot — escape hatch for tight execute/verify coupling (opened 2026-04-23, refineExecutor design):**
  The 2-slot `executor` / `verifier` interface preserves the ergonomic `executePrompt` / `verifyPrompt` sugar and lets users swap just one side (e.g. refineLoop executor + LLM verifier). But it forces redundant work when execution already produces verification evidence: e.g., an executor that internally runs an eval still has to re-run it in the verifier, because execute output is only `ExecuteOutput = {outcome, detail}`, not the raw eval scores.
  **Shape:** add `executeAndVerify?: (input: Node<TriagedItem | null>) => Node<VerifyResult | null>` option; when set, ignore `executor` + `verifier` and replace the whole `executeNode + executeContextNode + verifyNode` subgraph. Eliminates the rule-4 diamond footgun (no `withLatestFrom` pairing). Semantically the user's pipeline produces full `VerifyResult`s directly.
  **Why deferred:** no concrete consumer yet. The hello example uses 2-slot; the flagship dogfood example uses 2-slot. Add when a real user asks for it or when we catch a redundant-eval pattern in the wild.
  **Design note:** when added, the option name must make the tradeoff obvious — "use this only if your execute + verify are one reactive pipeline; prefer `executor` + `verifier` otherwise." JSDoc + a code comment near the harness slot selection should point users back to the 2-slot default.

- **Harness executor/verifier dev-mode sanity check — assert ≤1 DATA per input wave (opened 2026-04-23, refineExecutor design):**
  The `executor` option's contract (rules 1–4 in the design note) is typed as `Node<ExecuteOutput>` — the type system can't enforce "emits at most once per input item" or "fires on result, not on input." Users who write custom executors can silently violate rule (4) "fire on result, not on input" and burn verification budget without noticing.
  **Shape:** in dev mode (gated by `HarnessLoopOptions.debug ?? process.env.NODE_ENV !== "production"`), wrap the executor output in a monitor that counts DATA emissions per `executeInput` wave and `console.warn` when count > 1. Same for verifier. Near-zero overhead, opt-out, catches misuse during development only.
  **Why deferred:** first-shipped `refineExecutor` makes rule 4 structurally unreachable for its consumers; premature to harden against a misuse pattern we haven't seen. Add once an external user ships a custom executor.

- **`refineLoop` persistent re-seed / reset surface (long-term follow-up to `refineExecutor`, opened 2026-04-23):**
  `refineExecutor` ships in the harness EXECUTE slot with per-item `switchMap` lifecycle (Option A — per-item mount-and-cancel-on-supersede). This is clean but sacrifices cross-item learning: the strategy model (e.g. `errorCritique`'s accumulated failure samples) resets on every triaged item; convergence priors can't carry over from related items with the same `rootCause`. Option B (persistent loop + re-seed) is the long-term best shape.
  **Long-term fix:** add a persistent-session surface to `refineLoop`:
  - `refineLoopGraph.setSeed(next: T)` — swap seed, reset `iteration` to 0, append a session-boundary marker to `history`, keep `strategy` scaffolding intact. Must fire *before* `iterationTrigger` inside one `batch()` so the next generate wave reads the fresh seed — mirrors the `lastFeedback` state-mirror pattern already in `decideEffect` (COMPOSITION-GUIDE §32).
  - `refineLoopGraph.reset(mode?: "soft" | "hard")` — soft = keep strategy object + any effectivenessTracker totals; hard = full state wipe.
  **`refineExecutor` then flips** from `switchMap(input, item => mountFreshRefineLoop(...))` to `effect([input], ({item}) => loop.setSeed(config.seedFrom(item)))` — one persistent loop, sparse re-seed events. Benefits: (a) cross-item learning — strategies like `errorCritique` accumulate failure samples across triaged items sharing a `rootCause`, (b) cheaper — no graph mount/unmount per item, (c) richer `describe()` — one inner loop visible instead of N ephemeral mounts, (d) composes with `autoSolidify` (doc below) more cleanly: patterns learned across items are exactly the promotion candidates.
  **Design work required:** (a) history-per-item vs history-across-items semantics — probably `historyByEpoch: Node<readonly EpochHistory<T>[]>` where one epoch = one seed; (b) `best` semantics across re-seed — reset to null, or persist as all-time best with an `epochBest: Node<T | null>` companion; (c) `attachStorage` checkpoint shape must encode epoch boundaries so restore is deterministic; (d) interaction with `pause()` — does re-seed on a paused loop queue until resume? (e) `setStrategy` already exists and is orthogonal — confirm they compose.
  **Blocked on:** `refineExecutor` (Option A) shipping first so we have a concrete consumer driving the re-seed API. Schedule as a follow-up session once the harness dogfood example surfaces real cross-item learning opportunities worth the design surface cost.
- **Rigor-infra inconsistencies surfaced 2026-04-23 (TLA+ ↔ fast-check ↔ spec ↔ code):**
  Auditing the three rigor surfaces against each other revealed four model-vs-reality gaps. All are documented and addressed with the items below; listed here as a single index so the set is enumerable.
  - **I1 — TLA+ `SubscribeSink` idealizes the derived-multi handshake.** [wave_protocol.tla](../../graphrefly/formal/wave_protocol.tla) originally synthesized `<START, DIRTY, DATA>` for any non-terminated derived sink, but the runtime substrate emits `<START, DIRTY, RESOLVED, DIRTY, DATA>` for multi-parent derived (the bug in item 3 below). TLA+ was externally inconsistent with runtime. **Fixed 2026-04-23** by parameterizing on `GapAwareActivation: BOOLEAN` — TRUE synthesizes the gap-aware shape matching the substrate; new invariant `MultiDepHandshakeClean` fails under TRUE, passes under FALSE. This gives TLC a concrete counter-example trace in 2 steps and a concrete green target post-fix.
  - **I2 — Spec §2.7 normalizes the multi-dep gap as "NOT a protocol invariant violation"** while `docs/optimizations.md` item "Multi-dep push-on-subscribe ordering" + fast-check #10 + TLA+ `MultiDepHandshakeClean` all treat it as a bug. The rigor-infra surfaces encode a **stricter contract than the spec** currently guarantees. **Resolved 2026-04-23**: substrate fix landed (sugar-layer first-run gate via `NodeOptions.partial`), fast-check #10 passes, TLC clean MC still green. Spec amendment to §2.7 tracked separately — see "Remaining work" below.
  - **I3 — No `Next`-relation action modeled sink-callback-triggers-nested-emit** until 2026-04-23. COMPOSITION-GUIDE §32's entire bug class was outside TLA+'s explorable state space. **Fixed 2026-04-23** via new `SinkNestedEmit(observer, target, v)` action (parameterized on `SinkNestedEmits` constant) + new `NestedDrainPeerConsistency` invariant + dedicated `wave_protocol_nested_MC` model (enables the action with `SinkNestedEmits = {<<B, A, 2>>}`). 125K distinct states explored; invariant holds under exhaustive exploration — confirms the substrate's `NoDirtyAnywhere` tier guard is load-bearing.
  - ~~**I4 — `EqualsFaithful` is single-source-implicit.**~~ **✅ RESOLVED IN ~/src/graphrefly/formal — archived 2026-05-18** to `archive/optimizations/resolved-decisions.jsonl` (id `rigor-i4-persource-emitcount-resolved`). Stale: the deferred "proper fix" landed — `~/src/graphrefly/formal/wave_protocol.tla:2468` redefines `EqualsFaithful` per-source (+ pauseBuffer), `perSourceEmitCount` ghost declared `:395` + incremented in Emit/BatchEmit/SinkNested; `wave_protocol_nested.cfg:62` re-enables the invariant and its `:51` comment states "I4 fix landed; see docs/optimizations.md". Premise-check stale finding.

- **Domain-level `for/await` pattern — explore TopicGraph + cursor reading as a reactive alternative (opened 2026-04-23, refineLoop QA):**
  `blindVariation` and `errorCritique` now use `Promise.all` (parallel) or `for/await` (sequential) inside strategy `generate`. Both are tolerated under spec §5.10 because the strategy is a **source boundary** bridged by the loop's `fromAny` + `switchMap` — composing user-supplied teacher promises is not "scheduling new reactive work." Precedent: [src/extra/reactive-sink.ts:735](../src/extra/reactive-sink.ts:735) uses `Promise.all(inFlightPromises)` in its transport flush; [src/extra/sources.ts:989](../src/extra/sources.ts:989) uses `Promise.all` during `fromHTTP` init. **Investigation:** can a TopicGraph + `SubscriptionGraph` cursor reader express both patterns uniformly — teacher calls emitted to a topic, cursor consumes at the user's chosen pace (parallel = wide fan-out cursor, sequential = one-at-a-time cursor)? If yes, domain-level strategies could drop `Promise.all` / `for/await` entirely and compose via graph topology — `describe()` would show the concurrency shape, attachStorage could checkpoint in-flight calls, pause/resume work uniformly. **Scope:** design pass on TopicGraph cursor + N-parallel-source fan-out; prototype against `blindVariation` as the smallest case; compare complexity vs. Promise.all baseline. **Deferred** until there's a second domain-level strategy that needs the same pattern (keeps the need concrete).

- ~~**Formal browser vs Node split for the `extra` surface + per-domain pattern packages**~~ **RESOLVED 2026-04-23.**
  Library now ships three-tier subpath conventions: universal (browser + node safe) default, `<subpath>/node` for Node-only additions (filesystem, sqlite), `<subpath>/browser` for DOM-only additions (IndexedDB, WebLLM, Chrome Nano). Deliverables:
  - `extra/index.ts` is now browser-safe — `sources-fs.ts` and `storage.ts` (back-compat barrel) removed from the re-export chain. New `extra/node.ts` barrel aggregates `sources-fs` + `fileStorage` + `sqliteStorage`; new `extra/browser.ts` barrel aggregates `indexedDbStorage` + IDB helpers.
  - **Every `src/patterns/*` domain is now a folder-shaped package** with its own `<name>/index.ts` + `package.json` subpath export — `memory`, `cqrs`, `orchestration`, `audit`, `reduction`, `refine-loop`, `resilient-pipeline`, `guarded-execution`, `lens`, `domain-templates`, `graphspec`, `demo-shell`, `ai`. Already-folder patterns (`harness`, `surface`, `reactive-layout`, `messaging`, `job-queue`) unchanged. Each of the previously single-file patterns was promoted via a one-shot codemod (`TRASH/promote-patterns.mjs`) that moved the file and rewrote relative imports across the tree.
  - **`patterns/messaging` split into `messaging` + `job-queue`.** `TopicGraph` / `SubscriptionGraph` / `TopicBridgeGraph` / `MessagingHubGraph` stay in `patterns/messaging`; `JobQueueGraph` / `JobFlowGraph` moved to new `patterns/job-queue`. Caller-side import rewrite updated all in-tree consumers.
    - **Describe-surface break: meta kind renamed** for queue/flow nodes: `meta.messaging_type` / `meta.messaging: true` → `meta.job_queue_type` / `meta.job_queue: true`. Affected kinds: `queue_depth`, `job_flow_completed_count`, `job_flow_pump` — visible in `graph.describe()` JSON, `observe()` streams, and third-party inspection tooling that filters by domain tag. No in-repo code filters by this key; document in release notes for external tooling authors.
  - **`patterns/ai` subpath rename.** `patterns/ai/node-middleware` → `patterns/ai/node`; `patterns/ai/browser-presets` → `patterns/ai/browser`. New aggregator files [`src/patterns/ai/node.ts`](../src/patterns/ai/node.ts) and [`src/patterns/ai/browser.ts`](../src/patterns/ai/browser.ts) re-export the underlying modules. Pre-1.0 rename with no back-compat shim.
  - **Build-time regression check** in [`tsup.config.ts`](../tsup.config.ts) `onSuccess`: walks the dist import graph from every universal entry and asserts none transitively reaches Node builtins. `nodeOnlyEntries` allow-list guards `extra/node`, `extra/storage-node`, `patterns/ai/node` (the real entry-list — `extra/sources-fs` is no longer a tsup entry after the codemod). New universal entries that accidentally pull a Node builtin fail the build with a clear "via X → Y → Z" chain. The check was hardened in a follow-up QA pass to (a) match both `node:*` AND bare `require("<builtin>")` so a missing `NODE_BUILTINS` entry in `restoreNodePrefix` can't silently escape; (b) use a whitespace-loose regex so minified production output (`export{x}from"./foo.js"`) still parses; (c) include a side-effect-import branch (`import "X"`); (d) seed universal entries from the declared `ENTRY_POINTS` list instead of a chunk-filename heuristic; (e) keep ESM and CJS resolution trees disjoint so a `.cjs` require can't accidentally resolve to an ESM twin; (f) handle directory-as-index resolution. `NODE_BUILTINS` was expanded from 17 names to the full Node builtin set so `restoreNodePrefix` re-attaches `node:` to `child_process`, `dns`, `http`, `tls`, `zlib`, etc. `fromGitHook` — which ran through the old gap — was moved to `src/extra/git-hook.ts` and re-exported via `extra/node`.
  - External consumers updated: `examples/inbox-reducer/adapter-stack.ts` imports `fileStorage` from `@graphrefly/graphrefly/extra/node`. `evals/scripts/run-harness.ts` switched to `src/extra/storage-node.js` direct import.
  - **2037 tests / lint clean / build green** post-split.
  **Follow-up:** per-pattern `packages/<name>/` thin re-export packages (Phase B of the original plan) is deferred to the v1.0 cut when full source extraction becomes worthwhile. Monolith-with-subpaths is the intentional shape until then.

- ~~**§9.8 `refineLoop` reactive optimization loop — Wave 2.5 hero primitive**~~ **SHIPPED 2026-04-22 (TS), QA-hardened same day.**
  Landed at [src/patterns/refine-loop.ts](../src/patterns/refine-loop.ts). Topology: 4 `TopicGraph`s (GENERATE / EVALUATE / ANALYZE / DECIDE) over a reactive feedback loop driven by a single `iterationTrigger: state<number>`. `RefineStrategy<T>` interface + `blindVariation` built-in shipped; `errorCritique` / `mutateAndRefine` / registry / `optimizeCatalog` / `refineExecutor` (Composition E) deferred to post-v1. Evaluator shape 4: both `candidates` and `dataset` reactive; dataset arrays auto-wrapped in `state()`. Convergence: patience / min-score / min-delta / max-evaluations / max-iterations / budget. Human-in-the-loop via `setStrategy(next)` + `pause()` / `resume()`.
  **Composition invariants honored:**
  - §5.10 no raw async primitives: the original `switchMap + fromAny(Promise.resolve(…))` bridge was replaced with a producer node that emits synchronously for sync strategies and only crosses a Promise boundary when the strategy itself returns one — eliminates the per-iteration microtask for sync paths and the per-topic iteration-race it would have introduced. Cancellation still works: switchMap unsubscribes the inner producer on new trigger, and the async branch uses a local `cancelled` flag so late Promise resolutions don't emit into a torn slot.
  - §7 feedback cycle: only `iterationTrigger` is a reactive dep of the generate stage; strategy / feedback / prev-candidates / dataset read via closure-seeded updaters (§28), so mid-run swaps apply to the NEXT iteration — never retrigger the current.
  - §28 factory-time seed for `latestStrategy` / `latestFeedback` / `latestPrevCandidates` so the first activation doesn't drop the multi-dep initial pair.
  - §32 state-mirror: the decide-effect writes `lastFeedback.emit(fb)` BEFORE `iterationTrigger.emit(i+1)` inside one `batch()`. The closure updater for `latestFeedback` drains between them, so by the time the next generate's switchMap fires, the closure is fresh. `lastFeedback` is mirrored regardless of decision (continue / converged / budget / paused) so resume-after-pause gets current feedback — previously the pause branch skipped the mirror, leaving stale feedback in the closure.
  - Inline convergence checks inside `decideEffect` rather than reading `convergedNode.cache` — the derived convergence nodes would require a full drain round-trip before their cache updated (they're downstream of `historyState` / `scoreState`, which `decideEffect` itself writes → §7-style write-read deadlock in one wave). The derived nodes still exist for `describe()` / `observe()` surface consistency. **Budget accounting** lives solely in `decideEffect` (single authority — previously split between `generateEffect.emit(budgetState, +cs.length)` and `decideEffect` reading `.cache + 0`, which was sink-order-dependent).
  - Error handling: the candidates producer has a top-level `try/catch` + `actions.down([[ERROR, err]])` surfacing strategy throws as tier-3 ERROR on `candidatesNode`; a dedicated `errorWatcher` effect with `errorWhenDepsError: false` promotes that to `status = "errored"` (previously the `"errored"` status literal existed but was never assignable by any code path — unhandled rejections would leave `status` stuck at `"running"`).
  - De-dup: `lastDecidedIteration` guard so per-iteration decide fires exactly once even if `feedback` / `scores` / `candidates` re-emit within one wave.
  - `pickBest` returns `T | null` on empty candidate batches (COMPOSITION-GUIDE §3 — `undefined` is the protocol SENTINEL; `null` is the domain "no value" signal). `Iteration<T>.best` and `RefineLoopGraph.best` widen to `T | null` accordingly.
  - `isNode` type-guard checks all three core Node methods (`subscribe` / `down` / `emit`) as callable properties, not just `"subscribe" in x` — rejects mock-adjacent objects.
  - `generateEffect` wraps its three emissions (`generateTopic.publish` + `prevCandidatesState.emit`) in a single `batch()` so they coalesce; `resume()` similarly wraps `pauseState` / `statusState` / `iterationTrigger` in one batch and early-returns unless `statusState.cache === "paused"` (idempotent — converged / budget / errored are permanent).
  - `blindVariation.generate` throws on empty candidate batch instead of returning `[]` (which would silently stall the loop in an infinite zero-candidate cycle). Throw is promoted to `status = "errored"` by the watcher.
  9 tests at [src/__tests__/patterns/refine-loop.test.ts](../src/__tests__/patterns/refine-loop.test.ts) cover: basic 4-topic wiring + maxIterations convergence, minScore / maxEvaluations / budget convergence, strategy swap with pause-swap-resume, pause/resume round-trip, describe() surfacing all topics + state nodes, history accumulation, reactive-dataset swap mid-run.
  **Documented contract:** strategies with truly synchronous `generate` drain the whole loop during factory activation (no async window for `pause()` / `setStrategy()` to interleave — the JSDoc on `RefineStrategy` makes this explicit). Real LLM / eval strategies are async and Just Work; custom sync strategies for tests should be marked `async` to match real cadence. Also: `generate` must return a fresh array — returning the same reference trips default equals absorption and silently stalls the loop.
  **Follow-ups tracked in roadmap §9.8:** PY parity, `mutateAndRefine` built-in, `strategyRegistry` / `autoSelectStrategy`, `optimizeCatalog`, `refineExecutor` (Composition E), eval-caching / parallel-evaluation / multi-objective composition docs, blog + comparison page. **`errorCritique` landed 2026-04-23** — ProTeGi-style critique strategy with `{best, failures, critiqueText}` private payload in `feedback.critique`; tests in [src/__tests__/patterns/refine-loop.test.ts](../src/__tests__/patterns/refine-loop.test.ts). **QA pass same day** added: (a) `EvalResult.candidateIndex` + fan-out-aware `pickBest` aggregation (multi-task evaluators now work correctly); (b) companion `tokens?: Node<number>` on both built-ins + `ctx.reportCost(n)` callback — partial spend is delivered via `finally` on teacher throw so no tokens are lost; (c) `parallel?: boolean` (default `true`, `Promise.all`) with sequential `for/await` opt-out on both built-ins; (d) NaN-threshold fallback in `analyze` (treats all scores as failures when default batch-mean is non-finite); (e) length-guard-first in `generate` so `null` values flow through as domain-valid per spec §1 / COMPOSITION-GUIDE §3; (f) N2 tests rewritten as direct-unit-test invocations of `.generate()` — no conditional assertions.
  **Deferred QA findings** (pre-existing, scoped out): stratify silent classify-exception swallow, stratify rules-dep ERROR swallow, `history`/`best` never emit COMPLETE on termination (design — loop stays observable after convergence), per-topic iteration race on micro-edge async paths. These three belong in a follow-up pass if observed in the field.

- ~~**Category A: move protocol-level operators from `patterns/` to `extra/`**~~ **SHIPPED (TS) 2026-04-22.**
  `stratify` moved to [src/extra/stratify.ts](../src/extra/stratify.ts) (unique primitive; meta-tag simplified from `domainMeta("reduction", …)` to inline `{ kind: … }`). The patterns/orchestration `valve` / `forEach` / `wait` versions were graph-registering duplicates of extra/ operators — removed in favor of `graph.add(op, { name })` composition. Test migrations in orchestration.test.ts / reduction.test.ts / phase5-llm-composition.test.ts; exports test updated. 2025 tests pass. PY parity deferred.

- ~~**Category B: replace `.down([[DATA, value]])` with `.emit()` sugar across `patterns/`**~~ **SHIPPED (TS) 2026-04-22.**
  Note: roadmap originally said `.set()`; the actual sugar is `.emit()` ([src/core/node.ts:303](../src/core/node.ts:303)). Migrated 29 sites across 9 files (memory, reduction, cqrs, domain-templates, ai, orchestration, graphspec, messaging, harness/bridge). `{ internal: true }` option preserved at all 4 cqrs.ts sites that use it. Unused `DATA` imports pruned (memory, domain-templates, cqrs, graphspec, messaging). PY parity deferred.

- ~~**Surface `listSnapshots` returns all tier keys, not just surface snapshots**~~ **RESOLVED 2026-04-22** — option (a) shipped: `saveSnapshot`/`deleteSnapshot` encode keys under the `"snapshot:"` prefix (exported as `SNAPSHOT_KEY_PREFIX`), and `listSnapshots` filters + decodes accordingly. `listSnapshots(tier, { includeUnprefixed: true })` opt-in surfaces unprefixed keys for callers reading pre-namespacing sets. `restoreSnapshot` + `diffSnapshots` transparently fall back to unprefixed keys so bare test fixtures still resolve. Regression tests in [surface.test.ts](../src/__tests__/patterns/surface/surface.test.ts) cover namespacing + legacy interop.

- ~~**`policyEnforcer` audit-mode skips events with no `lastMutation`**~~ **RESOLVED 2026-04-22** — extended `ObserveEventBase` with `actor?: Actor`, stamped on every `data`/`error` event in [graph.ts](../src/graph/graph.ts) observe subscribe loop (reads `target.lastMutation?.actor ?? DEFAULT_ACTOR`). `policyEnforcer` audit mode now prefers the event-stamped actor and falls back to `lastMutation` for back-compat; unattributed writes record under `DEFAULT_ACTOR` (`{type: "system", id: ""}`) rather than being silently dropped. Regression test in [audit.test.ts](../src/__tests__/patterns/audit.test.ts) under "B9". Not populated for tier-1/2 events (`dirty`, `resolved`, `pause`, `resume`, `invalidate`, `teardown`) — those don't carry caller attribution.

- ~~**`measureCache` over-aggressive invalidation in `reactive-layout` + `reactive-flow-layout`**~~ **RESOLVED 2026-04-22** via the `NodeFnCleanup` return-shape discriminator (B5). The type now supports `{ beforeRun?, deactivate?, invalidate? }` granular hooks alongside the legacy `() => void` (which keeps firing on all three transitions). `reactive-layout`, `reactive-flow-layout`, and `reactive-block-layout` migrated to `return { deactivate: flush, invalidate: flush }` — cache survives re-runs, flushes only on deactivation/INVALIDATE. Pre-1.0 rename: the undocumented `{ deactivation }` object key → `{ deactivate }`; 4 in-tree sites in `extra/operators.ts` (switchMap, exhaustMap, concatMap, mergeMap) migrated. Regression coverage in [lifecycle.test.ts](../src/__tests__/core/lifecycle.test.ts) "NodeFnCleanup object form — granular hooks". PY mirror fix pending per standing policy.

- ~~**`reactiveFlowLayout` hard-break semantics: visible paragraph gap vs layout-preserving**~~ **RESOLVED 2026-04-22 (D7; QA-hardened same day to be truly reactive).** Added `paragraphSpacing?: number | null` option to `reactiveFlowLayout` (reactive via `setParagraphSpacing(n | null)`) and to pure `computeFlowLines(segments, ..., { paragraphSpacing })`. **Default semantics (post-QA):** when `null` / unset, the spacing **tracks the current `lineHeight`** reactively — future `setLineHeight` updates propagate through, and `setParagraphSpacing(null)` restores tracking. An explicit number (0, 60, …) pins the gap. Regression in [reactive-flow-layout.test.ts](../src/__tests__/patterns/reactive-layout/reactive-flow-layout.test.ts) incl. "D7: paragraphSpacing default tracks lineHeight reactively".

- **Demo Flow chapter's drift driver is a useEffect-owned rAF subscription, not a reactive sink on `obstaclesNode` (opened 2026-04-18):**
  Current shape: `useEffect(() => { ... fromRaf().subscribe(...) ... }, [paused, chapter])` reads `bundle.graph.get("obstacles")` each tick and writes back via `bundle.setObstacles(next)`. Obstacles ARE the graph's state (read via `useNodeValue`), so the reactive graph is the source of truth. But the drift *driver* (the thing that closes the loop) lives in React effect land, not as a reactive node. A purer design would be: a `derived([fromRaf, baseObstacles], ...) → drifted` that feeds back into obstacles. Cycle risk and stateful velocity-on-bounce make that hairier than imperative drift inside a subscribe callback; the current design is the pragmatic shape. **Revisit** if a second consumer wants a reusable "reactive physics integrator" primitive.

- **Formal TLA+ spec lives at `~/src/graphrefly/formal/` (2026-04-17):**
  Project 3 of the rigor plan landed in the spec repo. `wave_protocol.tla` + `wave_protocol_MC.tla` + `wave_protocol.cfg` encode the 6 fast-check invariants as TLC model-checkable properties over a 4-node diamond with `Values={0,1,2}`, `MaxEmits=3` (~6K distinct states, exhaustive, ~1s runtime). All 6 invariants hold under exhaustive exploration once tier-ordering guards (§1.3 invariant 7) are enforced. See [`~/src/graphrefly/formal/README.md`](../../graphrefly/formal/README.md) for the workflow: TLC counter-examples port directly to new `fast-check` properties in `src/__tests__/properties/_invariants.ts`. **Author-time tool only** — CI does not run TLC. Install via `brew install --cask tla+-toolbox`.

- ~~**Diamond fan-in over-fires inside multi-emit batch**~~ — **RESOLVED 2026-04-17** via two independent fixes (Bug 1: per-message fn-invocation moved to end of dep message-iteration loop; Bug 2: per-node emit coalescing inside `batch(() => ...)` via `_batchPendingMessages` + `registerBatchFlushHook`). Diamond invariant 4's `maxBatchEmits: 1` workaround removed. See rigor infrastructure session (`archive/docs/SESSION-rigor-infrastructure-plan.md`) and `src/__tests__/core/multi-message-delivery.test.ts` for before/after behaviour. Full pre-fix analysis archived to `archive/optimizations/resolved-decisions.jsonl` (id: `diamond-fan-in-over-fires`).

- **PY parity: port `attachStorage` + codec envelope (opened 2026-04-17):**
  TS shipped `Graph.attachStorage` / `Graph.fromStorage` / `StorageTier` + codec envelope (v1 wire format) / `Graph.decode(bytes)` / `snapshot({format: "bytes"|"json-string"})` / `GraphWALDiff` in the graph-module 24-unit review wave (see `archive/docs/SESSION-graph-module-24-unit-review.md`). PY still uses the legacy `auto_checkpoint` + `extra/checkpoint.py` path with no envelope. **Wire format is frozen at v1** in `~/src/graphrefly/GRAPHREFLY-SPEC.md` § "Codec registry and envelope" — a PY implementation must produce byte-identical envelopes. **Scope:** port `StorageTier`, `attach_storage`, `from_storage`, codec registry on `GraphReFlyConfig`, `encode_envelope` / `decode_envelope`, `Graph.decode(bytes)`, `snapshot(format=...)`, `GraphWALDiff` with `nodes_added_full`, `diff_for_wal`. **Deadline:** v1.0 so the wire format doesn't diverge post-release. **Blocked on:** nothing; schedule when PY capacity opens.

- **Message array allocation in hot path (updated 2026-04-17):**
  Collapses into the passthrough `_emit` single-message overload item below. **A2 landed 2026-04-12** (6 payload-free tuples + 5 batch singletons interned), closing tier-1/5 control-signal cost. For tier-3 DATA/ERROR the inner `[type, payload]` tuple is fundamental — can't be eliminated without an `emit(type, payload)`-style API shift (option (b) in the original item, deferred). The remaining eliminable allocs are the outer `[[DATA, v]]` from `actions.emit`/`node.emit` and the `[msg]` passthrough wrappers at [node.ts:1257, :1282, :1291](src/core/node.ts:1257) — **both closed by the single-message `_emit` overload** (next item). No separate work item.

- ~~**Passthrough `[msg]` wrapper allocation — single-message `_emit` overload**~~ **BENCHED 2026-04-22; NOT SHIPPED.** Added passthrough-heavy bench variants to [src/__bench__/graphrefly.bench.ts](../src/__bench__/graphrefly.bench.ts) (5/10/20-level fn-less chains). Baseline shows clean linear scaling (~3.4μs per level; 2× depth ≈ 2× latency). The `[msg]` wrapper is one small allocation amid the full `_emit` pipeline (terminal filter, tier sort, `_frameBatch`, `downWithBatch`, `_updateState` walk); moving it inside `_emit` via an overload changes the call-site but doesn't eliminate the alloc. No measurable cliff justifying the additive API surface. Bench variants kept as regression-catchers.

- **Fan-out scaling — sink notification overhead (profiled 2026-04-23, refined):**
  New profiling harness at [src/__bench__/fanout-profile.ts](../src/__bench__/fanout-profile.ts) — structured measurement cross-checked via `v.version` (spec §7, V0) and `graphProfile()`. Run with `tsx src/__bench__/fanout-profile.ts`. Reports `ns/emit`, `ns/notify` (per sink-callback invocation), and `ns/sub (amort)` across 1/10/50/100/500/1000/5000 subscribers over 20K emits each.
  **Finding (2026-04-23, local Darwin/arm64):** the "7.6× drop per 10× subs" in the hz column is **amortized fixed-overhead**, not a super-linear cliff. Measured `ns/notify`:
  | subs | hz | ns/emit | ns/notify |
  |---|---|---|---|
  | 1 | 2.79M | 359 | 179 |
  | 10 | 2.41M | 415 | 20.8 |
  | 50 | 1.37M | 730 | 7.3 |
  | 100 | 750K | 1,333 | 6.7 |
  | 500 | 172K | 5,812 | 5.8 |
  | 1,000 | 86.7K | 11,539 | 5.8 |
  | 5,000 | 16.3K | 61,256 | 6.1 |
  `ns/notify` stabilizes at ~6 ns from 50 subs and stays flat out to 5000 — scaling is effectively linear once the per-emit fixed cost (tier sort, `_updateState` walk, equals substitution, `downWithBatch` closure/slice alloc) is amortized. `notifyCount === 2 × subs × emits` confirms each emit drives exactly two tier-group sink calls (DIRTY tier-1 + DATA tier-3); `versionDelta === emits + warmup − 1` confirms `v.version` advances per value-changing DATA (off-by-one is the first warmup emit of value 0 landing against initial 0 → RESOLVED, as designed).
  **Reframed opportunity:** the only truly sub-linear-worthy improvements are (a) **reducing the 179 ns fixed per-emit cost** (impacts 1–10 sub workloads most, where fixed cost dominates), or (b) **shaving the ~6 ns/notify stable cost** (impacts 100+ sub workloads). Likely levers for (a): merging the two `_deliverToSinks` calls (DIRTY + DATA) into one batched multi-tier delivery — halves closure/slice allocs but requires `downWithBatch` + `_deliverToSinks` coordination. Likely lever for (b): replacing the `[...this._sinks]` snapshot-array alloc in `_deliverToSinks` (src/core/node.ts:2140) with epoch-stamped iteration over the live `Set` — removes the per-emit N-element copy. Both are pre-1.0-safe refactors; neither is urgent given current scaling is linear. **Deferred unless a concrete workload profile (e.g. a real dashboard with 500+ live subscribers) surfaces the ~6 ns/notify as a bottleneck.**

- **P3 audit: `.cache` reads inside fn/subscribe callbacks (updated 2026-04-17):**
  Call sites reading `.cache` on a node from inside a reactive context (fn body, subscribe callback, or project function) — bypassing protocol delivery. These work "by accident" when execution is synchronous but could return stale values under batch deferral.

  **Open:** _(none — all P3 items landed as of 2026-04-17)_

  **Landed 2026-04-12–17 (archived):**
  - #1, #4, #5 — 2026-04-13–15.
  - #2 verifiable `sourceNode.cache` — **2026-04-17**, factory-time `latestSource` seed + subscribe-handler update (§3.6 boundary); autoVerify-only short-circuits to `switchMap(source, src => verifyFn(src))`.
  - #3 eviction `verdict.cache` — **2026-04-17**, removed entirely; relies on `forEach(verdict, cb)`'s push-on-subscribe to deliver the initial verdict value through the callback.
  - #6 `src/extra/adapters.ts` `fetchCount.cache` — 2026-04-17, rewritten to closure-owned `fetchCountLocal`.
  - #7 extraction/consolidation `store.entries.cache` — **2026-04-17**, factory-time `latestStore` seed + subscribe-handler update; `asReadonlyMap` helper deleted.
  - #8/#9 `src/extra/worker/bridge.ts` + `self.ts` aggregator — 2026-04-12, wave-form `data[i]` read via raw `node([deps], fn)`. The "framework-level wave-final state blocker" claim was stale once the synthetic-DIRTY `_frameBatch` invariant landed (auto-prepended on every tier-3 emission, including external raw `state.down`).
  - #10 worker-bridge handshake timer — 2026-04-12, reactive `fromTimer` + `first(merge(deadline$, ready$))` race. Closes both the `.cache`-in-callback and the raw `setTimeout` central-time violations.
  - #11 `src/patterns/orchestration.ts` gate — `latestIsOpen` closure fed by `isOpenNode.subscribe`.
  - #12 `src/patterns/reduction.ts:132` stratify — factory-time `latestRules` seed (wiring-time boundary read, §3.6-allowed), subscribe handler updates the closure.
  - #13 `src/patterns/reduction.ts:538` budgetGate — activation-time `latestValues[]` seed inside producer fn (wiring-time boundary read).
  - #16 `src/patterns/ai.ts` retrieveFn — Option W (sync pure inline + state writes).

  **Gray zone — self-owned counter reads (same rule, different judgment call):**
  14. ~~`src/patterns/reduction.ts:424` feedback counter.~~ **Landed 2026-04-17** — rewritten to use shared `tryIncrementBounded` helper (extracted to [src/patterns/_internal.ts](src/patterns/_internal.ts)). Archived.
  15. `src/patterns/harness/loop.ts` `fastRetry` now imports the shared `tryIncrementBounded` helper from `patterns/_internal.ts`. **Landed 2026-04-12, refactored 2026-04-17.**
  16. ~~**`agentMemory.retrieve` imperative vs reactive sibling**~~ **RESOLVED 2026-04-22** — added `AgentMemoryGraph.retrieveReactive(queryInput: NodeInput<RetrievalQuery | null>): Node<...>` in [src/patterns/ai.ts](../src/patterns/ai.ts). Factors core retrieval pipeline (vector + KG + ranking + budget pack) into a `runRetrieval` helper shared by both the imperative `retrieve(query)` and the reactive sibling. Reactive variant is subscribe-driven — no cache-reads-from-reactive-fn-body violation. Regression test in [ai.test.ts](../src/__tests__/patterns/ai.test.ts) under "B20".

  **Acceptable — documented boundary / wiring-time exceptions (not violations):**
  - `core/sugar.ts:245, 251` — `dynamicNode` discovery stub (P3 boundary exception, explicitly documented).
  - `core/meta.ts:75, 134` + `graph/profile.ts:100` + `graph/graph.ts:881` — inspection/describe tooling.
  - `extra/resilience.ts:163, 398, 571, 655, 755, 837, 899`, `extra/sources.ts:112, 733, 769`, `patterns/cqrs.ts:243`, `patterns/messaging.ts:92`, `extra/reactive-log.ts:139, 186` — factory-time `initial:` seeding from `source.cache` (foundation-redesign session §3.6 explicitly allows this as "external observer reads at wiring time").
  - `patterns/messaging.ts` (retained/ack/pull/bridgedCount), `patterns/memory.ts` (readMap/readArray), `patterns/ai.ts` (allMessages/register/unregister/execute/getDefinition), `patterns/orchestration.ts:623` — external-consumer API methods called synchronously from outside the graph.
  - `extra/cascading-cache.ts:207` — read during external-consumer `load()` write path (demote-before-evict).
  - `extra/worker/bridge.ts:188` + `self.ts:251` — transport "worker ready" boundary handler; reads current state to build initial snapshot for wire transport.
  - `src/compat/{react,vue,solid,svelte,jotai,nanostores,signals,zustand,nestjs}/…` — framework adapter layer; consumer-framework getSnapshot/render calls.

  **QA findings from 2026-04-12 review:**
  - **H1 — budgetGate first-wave constraint freshness (patched 2026-04-12).** The original factory-time `latestValues = constraints.map(c => c.node.cache)` seed was stale if a caller updated a constraint between factory return and first subscribe. Fix applied: seed moved to inside the producer fn (activation-time boundary read) so constraint updates between factory and subscribe are reflected before `source`'s push-on-subscribe fires `checkBudget()` for the first time. **Follow-up concern:** even activation-time seeding has a narrow window where the subscribe-order matters — source is subscribed first, so its push-on-subscribe fires before any constraint subscribe runs. If the constraint value at activation-time differs from what the constraint would emit on its push-on-subscribe (unlikely but possible if the constraint is a derived that recomputes on subscribe), the first `checkBudget` could still see a stale value. Possible future tightening: reverse the subscribe order (constraints first, source last) so each constraint's push-on-subscribe populates `latestValues` via the handler before source's first DATA arrives. Deferred — the activation-time seed covers all cases observed in the test suite.
  - **BH-1 — retrieveFn inside caller batches (documented 2026-04-12).** `agentMemory.retrieve(q)` reads `distillBundle.store.entries.cache` and `contextNode.cache` at call time. If a caller invokes it inside their own `batch(() => { distillBundle.insert(...); mem.retrieve(q); })`, the store's state-backed mutations are visible (state.down writes cache synchronously), but derived-backed store transforms may not have settled yet. JSDoc caveat added to `AgentMemoryGraph.retrieve`. No runtime fix — this is a semantic of the sync API design.
  - ~~**BH-2 — state-write house style: `[[DATA, v]]` without explicit `[DIRTY]` prefix.**~~ **Resolved 2026-04-17.** Audit confirmed `_frameBatch` at [src/core/node.ts:1570–1608](src/core/node.ts:1570) auto-prepends a synthetic DIRTY for every tier-3 payload on every emission path (including external raw `state.down`), so `[[DATA, v]]` and `[[DIRTY], [DATA, v]]` are semantically equivalent at the wire level. The house style is cosmetic — the framework guarantees spec §1.3.1. No codebase-wide cleanup needed.
  - **M1 — extractStoreMap silent empty-Map for sentinel snapshots (pre-existing, not introduced by the 2026-04-12 diff).** `extractStoreMap<TMem>(snapshot)` at `src/patterns/ai.ts:1537–1540` returns `new Map()` for any non-`Map` snapshot (including sentinel/undefined). Callers of `retrieveFn` can't distinguish "store not populated yet" from "store is empty" — both return `[]`. The new sync `retrieveFn` amplifies the likelihood of being called before the store settles (there's no reactive first-run gate). Defer — helper has been this shape since the original retrieval pipeline landed; if we care, the fix is either (a) throw on sentinel with a "store not ready" error, or (b) JSDoc-document the silent-empty semantics.

  **Remaining work:** none open. (Store-mutation-events / op-log changeset substrate fully landed Phase 14.3/14.4 + `lens.flow` delta companion 2026-05-17 — see entry below; the DS-14 substrate spec-conformance line is closed.)

- **implementation-plan.md Phase 14 status drift (split-out residual carry from archived `store-mutation-events-phase14-3-4-landed-2026-05-17` + `multi-dep-push-on-subscribe-withlatestfrom-resolved-2026-04-29`).** `docs/implementation-plan.md` Phase 14 is still listed as the big open phase, but threads 1–4 (`BaseChange`/`mutations`/`mutate`/`reactiveLog.scan`) + thread 5 (Phase 14.6 WAL replay) + the `lens.flow` companion have all substantially landed in code. Reconcile the plan body to ✅ on the next /qa or maintenance sweep.

- ~~**Reactive adaptive rate limiter → `src/extra/adaptive-rate-limiter.ts`**~~ **RESOLVED 2026-04-21** — shipped as `adaptiveRateLimiter()` + `http429Parser` + `withRateLimiter` middleware together. Archived to `archive/optimizations/resolved-decisions.jsonl` (id: `adaptive-rate-limiter-primitive`). `resilientPipeline()` integration and `evals/lib/rate-limiter.ts` migration intentionally deferred — `evals/lib/*` stays on its current imperative stack, new eval work after §9.1 will use the adapter layer directly.

- **[py-parity-token-bucket-rename] `token_tracker` → `token_bucket` (TS 2026-04-15 → PY deferred):**
  TS consolidated on `tokenBucket` (deleted `tokenTracker` alias). PY still exports `token_tracker`. Rename to `token_bucket` in `~/src/graphrefly-py/src/graphrefly/extra/resilience.py` + update all PY call sites and tests. Pre-1.0, no backward-compat shim needed. Ship with next PY parity pass.

- ~~**`toolInterceptor(agentLoop, opts?)` — Composition C (D9)**~~ **RESOLVED 2026-04-22 (splice shipped; sugar deferred).**
  **Splice shipped:** `agentLoop` now accepts `interceptToolCalls?: (calls: Node<readonly ToolCall[]>) => Node<readonly ToolCall[]>` — a reactive transform inserted between the raw `toolCalls` node and the executor. Public `agent.toolCalls` surfaces the post-intercept view so audit/telemetry match reality. The splice is a real reactive edge (visible in `describe()` / `explain()`), not an imperative handler wrap. Design invariants honored:
    - §24 "Edges are derived" — transform takes + returns nodes; the gate's edges surface in describe.
    - §5.9 "No imperative triggers" — no handler wrapping, no setTimeout; just reactive composition.
    - §5.12 "Developer-friendly Phase 4+" — typed option on existing `agentLoop`, one line.
  **Sugar deferred:** named primitives like `toolInterceptor(agent, gateFn)` can be built on top of this splice once a concrete consumer emerges (see COMPOSITION-GUIDE §31 — selection is UX; interception is security). The `interceptToolCalls` splice is the sanctioned building block for those; avoid registry-wrap approaches (violates §24 + §5.9 — gate invisible to `describe`).

- ~~**Reactive `graph.explain({reactive})`: structured-observe callback re-enters reactive layer per event**~~ **RESOLVED 2026-04-22 (D5, option A; QA-hardened).** `_explainReactive` (and the new `_describeReactive` that shares the pattern) now coalesces per-event `version.emit` calls via `registerBatchFlushHook`. Under batched waves the first event schedules a single bump at the head of the next drain; subsequent events short-circuit via a `pendingBump` flag. Outside an explicit batch the hook fires immediately (no change to sync-wave behavior). QA pass added a `disposed` flag to both methods — guards against a queued flush-hook firing after `dispose()` and resurrecting the derived. Regression test "D5: debounces recompute across a batch" in [audit.test.ts](../src/__tests__/patterns/audit.test.ts).

- ~~**Surface `saveSnapshot` double-prefix on already-prefixed caller ids (Q2, was D8 in QA)**~~ **RESOLVED 2026-04-22.** `saveSnapshot` / `restoreSnapshot` / `diffSnapshots` / `deleteSnapshot` now throw `SurfaceError("snapshot-failed", ...)` when the caller id starts with `SNAPSHOT_KEY_PREFIX`. External id space is disjoint from the encoded tier-key space; the previous short-circuit in `encodeKey` is removed. Regression test "saveSnapshot / restoreSnapshot / deleteSnapshot / diffSnapshots reject ids with reserved prefix" in [surface.test.ts](../src/__tests__/patterns/surface/surface.test.ts).

- **`AgentLoopGraph` switchMap `equals: () => false` (D6, documented 2026-04-22, QA):**
  The switchMap wrapping `adapter.invoke` sets `equals: () => false` so each invocation counts as a new DATA emission, even when a deterministic adapter returns an identical object reference. Without this, `mockAdapter([resp])` (always returns same `resp`) would have the second `llmResponse` wave equals-absorbed into RESOLVED and `effResponse` would not fire — breaking `maxTurns` enforcement on stable-response tests. The contained trade-off is that ANY external subscriber to the *internal* `llmResponse` node would see duplicate DATA; today `llmResponse` is closure-scoped and the public `agent.lastResponse` is the state mirror (whose default `Object.is` equals dedupes same-ref re-emissions). If `llmResponse` is ever exposed publicly, this invariant needs a redesign — e.g. stamping a monotonic "request id" upstream so `llmResponse` keeps normal equals semantics while downstream can still detect "new invocation" via the id.

- ~~**Nested-drain wave-ordering — sink-drain-inside-sink-callback sees stale peer state**~~ **RESOLVED 2026-04-25** — archived to `archive/optimizations/resolved-decisions.jsonl` (id: `nested-drain-wave-ordering`). Protocol layer is correct as-is; `_dirtyDepCount` gating already prevents the mid-wave hazard. State-mirror in `agentLoop` exists for cross-wave reset semantics. Compound-topology fast-check invariant `#12b nested-drain-peer-consistency-compound` shipped at [_invariants.ts](../src/__tests__/properties/_invariants.ts). COMPOSITION-GUIDE §32 reframed cross-repo.

- **Auto-solidify pattern — VERIFY success → catalog entry promotion (proposed 2026-04-20, research: Evolver GEP "Solidify" + Hermes autonomous skill creation):**
  When VERIFY passes and REFLECT records success, the harness should optionally auto-generate a new catalog entry from the successful intervention. "Agent teaches itself new skills by succeeding." **Shape:** `autoSolidify(verifyResult, reflectOutput, catalog, opts?)` — a derived/effect node in REFLECT that: (a) extracts the intervention pattern (prompt template, code transform, composition recipe), (b) wraps it as a `CatalogEntry` with effectiveness metadata from strategy model, (c) writes to the catalog's `templates` topic. Human gate optional for review before solidification. Ties into catalog automation (§9.1b) — today we hand-author catalog entries; this closes the loop.
  **Precedent:** Evolver's GEP "Solidify" step (encode validated mutation as a reusable Gene), Hermes's autonomous skill creation (writes a markdown skill file after complex tasks). Our version is reactive and typed — not a file dump.

- ~~**Frozen context optimization — prefix-cache-friendly prompt composition**~~ **RESOLVED 2026-04-22** — shipped `frozenContext(source, { refreshTrigger?, name? })` in [src/patterns/ai.ts](../src/patterns/ai.ts). Single-shot mode (no `refreshTrigger`) materializes the first settled value and holds. Refresh-on-trigger mode re-emits the current source value only when the trigger dep is involved in a wave — source-only drifts are silently held. Regression tests in [ai.test.ts](../src/__tests__/patterns/ai.test.ts) under "patterns.ai.frozenContext (B11)". **COMPOSITION-GUIDE recipe entry shipped 2026-04-24** as §33 in `~/src/graphrefly/COMPOSITION-GUIDE.md`.

- ~~**Multi-agent handoff primitive — composition recipe**~~ **RESOLVED 2026-04-22** — shipped `handoff(from, toFactory, { condition?, name? })` thin sugar in [src/patterns/ai.ts](../src/patterns/ai.ts). Covers the "full handoff" variant (specialist becomes active when condition is true). The "agents-as-tools" variant is naturally built by registering a `promptNode` as a `ToolDefinition` on another via `toolRegistry` — no new primitive needed. Regression tests in [ai.test.ts](../src/__tests__/patterns/ai.test.ts) under "patterns.ai.handoff (B10)". **COMPOSITION-GUIDE recipe entry shipped 2026-04-24** as §34 in `~/src/graphrefly/COMPOSITION-GUIDE.md`.

- ~~**Dynamic tool selector node — reactive tool availability**~~ **RESOLVED 2026-04-22 (D8).** Shipped `toolSelector(allTools, constraints, opts?)` in [src/patterns/ai.ts](../src/patterns/ai.ts). `allTools: NodeInput<readonly ToolDefinition[]>` (pass a static array, a registry's `.schemas`, or any derived); `constraints: readonly NodeInput<(tool: ToolDefinition) => boolean>[]` — each predicate is itself reactive. A tool is included iff every predicate returns true; `null`/`undefined` predicates pass through (tools aren't silently dropped while constraints settle). Regression test "patterns.ai.toolSelector (D8)" in [ai.test.ts](../src/__tests__/patterns/ai.test.ts). Pairs with `interceptToolCalls` per COMPOSITION-GUIDE §31 — selection is UX (narrow the menu), interception is security (validate the order).

- ~~**Hierarchical context tree for agentMemory retrieval**~~ **RESOLVED 2026-04-22** — extended `RetrievalQuery` with optional `context?: readonly string[]` breadcrumb, `RetrievalEntry` with matching `context?` field, and `AgentMemoryOptions` / `RetrievalPipelineOptions` with `contextOf?: (mem) => readonly string[] | undefined` accessor + `contextWeight?: number` multiplier. Score is scaled by `(1 + contextWeight * sharedDepth / queryDepth)` when both query and entry supply a context; entries without a breadcrumb stay flat-scored. Additive and backward-compatible. Regression test in [ai.test.ts](../src/__tests__/patterns/ai.test.ts) under "B12".

- **Shared test helpers: refactor remaining PY `sink.append` sites (2026-04-09):**
  Unified `collect(node, *, flat=False, raw=False)` helper shipped in both TS and PY. ~127 `sink.append` sites in PY tests (`test_extra_tier1.py`, `test_extra_tier2.py`, `test_edge_cases.py`, etc.) remain to be migrated. Custom extraction (type-only, value-only, filtered) stays inline. See `docs/test-guidance.md` § "Shared test helpers".

- **[py-parity-equals-dispatch-invariant] Equals substitution parity (TS 2026-04-12 → PY deferred):**
  TS landed the dispatch-layer equals-substitution invariant on 2026-04-12: every outgoing DATA payload runs through a single equals-vs-live-cache walk inside `_updateState`, regardless of emission path (`actions.emit`, raw `actions.down`, passthrough forwarding, bundle-wrapped down). Includes synthetic-DIRTY prefix for raw-down substitution (spec §1.3.1 compliance) and equals-throw atomicity (prefix delivered before ERROR). See `archive/docs/SESSION-foundation-redesign.md` §3.5.1–.2 + §9.8 and `src/core/node.ts:_emit` / `_updateState` for the TS impl; `src/__tests__/core/node.test.ts` `§3.5` describe block for the regression suite.
  **PY status:** not yet ported. `~/src/graphrefly-py/src/graphrefly/core/node_base.py` still runs equals inside `_down_auto_value` (pre-foundation-redesign shape), and raw `down` does NOT participate in equals substitution. Matches TS's pre-2026-04-12 behavior. Blocked on PY completing the foundation redesign (see §9.5 PY implementation plan). When that lands, port the TS invariant verbatim: move equals to the dispatch-layer walk, add synthetic-DIRTY prefix rule, implement equals-throw atomicity contract, port the `§3.5` test block. Ship atomically with the PY foundation rewrite.

- ~~**Tier-2 documentation cleanup (composition guide redundancy + decision tables)**~~ **DONE 2026-04-22.** Cross-repo edit at [~/src/graphrefly/COMPOSITION-GUIDE.md](../../graphrefly/COMPOSITION-GUIDE.md). Shipped:
  1. **Pattern registry table** at top of `## Categories` — 18-row "if you're asking… see §N" index for fast scanning.
  2. **§10 trimmed** — removed restated SENTINEL content (cross-refs §3); kept the cascading insight.
  3. **§14 condensed** — PY async-deadlock section tightened from ~40 lines to ~20; symptom + cause + workaround + long-term-fix, no verbosity loss.
  **Not done** (lower ROI, left for future pass): §19 terminal-operators trim (already reasonably tight), top-of-guide "when to use X vs Y" (pattern-registry table substantially covers this use case), cross-reference line-number audit (guide uses `§N` section refs which don't go stale; the few `src/core/node.ts:<line>` refs in the spec repo stay fresh via `/parity` workflow).

- **Codec lazy decode + dormant subgraph eviction (proposed, 2026-04-17, post-1.0):**
  `codec.ts` declares `LazyGraphCodec.decodeLazy()` and `EvictionPolicy` types but neither is implemented. Covers two post-1.0 scale features:
  1. **Lazy decode** — `decodeLazy(buffer)` returns a `GraphPersistSnapshot` whose `nodes` record is Proxy-backed: individual nodes are CBOR-decoded only on access. Enables near-zero cold-start for large graphs (decode envelope + topology, defer node values until read). Requires a codec implementation that supports length-prefixed per-node entries (DAG-CBOR with streaming framing, or Arrow row groups).
  2. **Eviction policy** — when a subgraph hasn't propagated for `idleTimeoutMs`, serialize it using the graph's codec and release JS objects. Re-hydrate on next read/propagation/describe. Needs a "dormant" status marker on `Graph` + re-hydrate trigger.
  Both are post-1.0; the interfaces in `codec.ts` serve as design scaffolds. Paired with `attachStorage` — cold tier eviction is one concrete use case.

---

## Implementation anti-patterns

Cross-cutting rules for reactive/async integration (especially `patterns.ai`, LLM adapters, and tool handlers). **Keep this table identical in both repos' `docs/optimizations.md`.**

| Anti-pattern | Do this instead | Spec ref |
|--------------|-----------------|----------|
| **Polling** | Do not busy-loop on `node.get()` or use ad-hoc timers to poll for completion. Wait for protocol delivery: `subscribe` / `firstValueFrom` / `first_value_from` patterns on the node produced by `fromAny` / `from_any`. | §5.8 |
| **Imperative triggers** | Do not use event emitters, callbacks, or `setTimeout` + manual `set()` to trigger graph behavior. All coordination uses reactive `NodeInput` signals and message flow through topology. If you need a trigger, create a reactive source node. | §5.9 |
| **Raw Promises / microtasks (TS)** | Do not use bare `Promise`, `queueMicrotask`, `setTimeout`, or `process.nextTick` to schedule reactive work. Async boundaries belong in sources (`fromPromise`, `fromAsyncIter`) and the runner layer. | §5.10 |
| **Raw async primitives (PY)** | Do not use bare `asyncio.ensure_future`, `asyncio.create_task`, `threading.Timer`, or raw coroutines for reactive work. Async boundaries belong in sources (`from_awaitable`, `from_async_iter`) and the runner layer. | §5.10 |
| **Non-central time** | Do not schedule periodic work with raw `setTimeout` / `setInterval` / `time.sleep` for graph-aligned sampling. Use `fromTimer` / `from_timer` (or other documented `extra` time sources) and compose reactively. Use `monotonicNs()` / `monotonic_ns()` for event ordering, `wallClockNs()` / `wall_clock_ns()` for attribution. | §5.11 |
| **Hardcoded message type checks** | Do not hardcode `if (type === DATA)` for checkpoint or batch gating. Use `messageTier` / `message_tier` utilities for tier classification. | §5.11 |
| **Bypassing `fromAny` / `from_any` for async** | Do not one-off `asyncio.run`, bare `.then` chains, or manual thread sleeps to bridge coroutines / async iterables / Promises into the graph. Route unknown shapes through `fromAny` / `from_any` so `DATA` / `ERROR` / `COMPLETE` stay consistent end-to-end. | §5.10 |
| **Leaking protocol internals in Phase 4+ APIs** | Domain-layer APIs (orchestration, messaging, memory, AI, CQRS) must never expose `DIRTY`, `RESOLVED`, DepRecord, or settlement internals in their primary surface. Use domain language. Protocol access available via `.node()` or `inner`. | §5.12 |
| **`Node` resolution without `get()`** | When blocking until first `DATA`, prefer `node.get()` when it already holds a settled value, then subscribe only if still pending — avoids hangs when the node does not replay `DATA` to new subscribers. | — |
| **Passing plain strings through `fromAny` (TypeScript)** | `fromAny` treats strings as iterables (one `DATA` per character). For tool handlers that return plain strings, return the string directly; use `fromAny` only for `Node` / `AsyncIterable` / Promise-like after await. | — |

---

## Deferred follow-ups

Non-blocking items tracked for later. **Keep this section identical in both repos' `docs/optimizations.md`** (aside from language-specific labels).

| Item | Notes |
|------|-------|

- **Lock 4.A — drop `() => void` cleanup-shorthand sweep — ✅ FULLY DONE & ARCHIVED 2026-05-16** (`archive/optimizations/resolved-decisions.jsonl` ids `lock-4a-cleanup-shorthand-sweep-landed-2026-05-16` + `lock-4a-legacycleanup-removed-2026-05-16`; closed cross-track-ledger §1 CT-1 by removal). Only the minor test-hygiene residual remains open:
  - **🟡 DEFERRED follow-up (minor, test-hygiene) — residual test-fixture bare `() => void` NodeFn cleanups that do not assert.** Every fixture whose test *asserts* its cleanup fired was migrated (suite green). A few test fixtures still author a bare `return () => {...}` NodeFn cleanup that is now silently a no-op but is not asserted (decorative) — not a gate failure, but the fixture's intent is dead. Migrate to an explicit `{ onDeactivation: () => {...} }` hook on the next touch of the affected `*.test.ts`. No consumer/correctness pressure.

- **Path X — per-attempt Node-returning mutations (deferred 2026-04-25, post-Sessions-1-5 commit).** Design discussion concluded that `cqrs.dispatch(cmd, payload)`, `gate.approve(count)`, etc. should return `Node<TResult>` per call, where the returned Node carries the attempt outcome (`[[DATA, result], [COMPLETE]]` on success / `[[ERROR, errInstance]]` on failure preserving the original Error subclass for `instanceof` checks). Composes with `retry(() => mutation(...), opts)` and `fallback(...)` for protocol-level recovery. **Blocker:** the natural eager-keepalive design (`keepalive(attempt)` so fire-and-forget callers' mutations run without an explicit subscribe) is incompatible with `defaultOnSubscribe` in [src/core/node.ts](../src/core/node.ts) which skips push-on-subscribe for `completed`/`errored` nodes — by the time `retry`'s factory subscribes to the per-attempt Node, the Node has already terminated and `retry` sees no ERROR signal to react to. Two paths forward: (a) drop keepalive → make per-attempt Nodes lazy → mass migration of fire-and-forget callers to `.subscribe(() => {})`; (b) custom `onSubscribe` per per-attempt Node that pushes cached terminal status to new subscribers (protocol-config plumbing). Ship audit-log + `errorType` (already shipped) as the failure-observability mechanism in the meantime; users wanting recovery use try/catch around `dispatch` calls. Re-evaluate when a concrete recovery use case lands.
  - **Potential connection — DS-13.5.A INVALIDATE protocol redesign (opened 2026-05-01).** Under the new INVALIDATE-as-tier-4-settle model (locked this session, implementation deferred to Phase 13.5), per-attempt Nodes that today hit the `defaultOnSubscribe`-skips-push-for-terminal wall might benefit if they emit INVALIDATE *before* going terminal — late subscribers (retry's factory) could see the cleanup signal even though the producer is about to terminate. Worth probing during the DS-13.5.A implementation session: does the new model give us a third path between (a) drop keepalive and (b) custom onSubscribe? Notes the connection so the implementation session considers it; doesn't unblock Path X by itself.

- **`EffectivenessTrackerBundle.record()` thread safety (PY, 2026-04-10):**
  `record()` does read-modify-write on `self._map` (get → compute → set) without a lock. Under free-threaded Python (no GIL), two concurrent `record()` calls for the same key can race: both read the same existing entry, both compute `attempts+1`, and one write overwrites the other. Same pattern exists in other pattern-layer factories (e.g. `strategyModel`). Should be addressed with a per-bundle `RLock`, consistent with the project's threading policy ("per-subgraph `RLock`, per-node `_cache_lock`"). Deferred — all current callers are single-threaded; needs systematic pattern-layer lock audit.

- **`_async_pump` return annotation is `AsyncIterable` not `AsyncGenerator` (PY, 2026-04-09):**
  In `streaming_prompt_node`, the inner `_async_pump` function uses `yield` (making it an `AsyncGenerator`) but is annotated `-> AsyncIterable[Any]`. No runtime impact (`AsyncGenerator` is a subtype of `AsyncIterable`), but the annotation is technically incorrect. Fix: change to `-> AsyncGenerator[Any, None]`.

- **`withStatus` decomposition into `statusOf` + `errorOf` (post-1.0, evaluated + deferred 2026-04-27):**
  Surfaced during D.2.3b audit (`SESSION-public-face-blocks-review.md` § F.7). Proposal: decompose `withStatus(src, opts?) → { node, status, error }` into two primitive companion-deriving operators + a thin bundle:
  - `statusOf(src, opts?: { initialStatus?: StatusValue }) → Node<StatusValue>` — owns lifecycle state machine (`pending` → `active` / `errored` / `completed`).
  - `errorOf(src) → Node<unknown | null>` — emits `null` initially, payload on ERROR, `null` on first DATA after `errored` (preserves recovery semantics).
  - `withStatus(src, opts?)` becomes `{ node: src, status: statusOf(src, opts), error: errorOf(src) }` — no mirror, no `meta` plumbing, one fewer hop.
  **Benefits:** independent companion reuse (callers can drop `errorOf` if only lifecycle is needed); first-class derived nodes visible to describe-traversal (closes the Q3 gray-zone where today's `node([], fn)` empty-deps producer hides companions); smaller, more focused per-primitive tests.
  **Tradeoffs:** 2 subscribes to src instead of 1 (negligible for typical use); recovery batching becomes implicit-via-batch (current explicit `batch(() => { error.set(null); status.set("active") })` replaced by both companions emitting on the same source-DATA in the same drain — downstream subscribers needing atomic-2-companion observation wrap their consumption side); pre-1.0 break — callers relying on `withStatus(src).node` carrying `resubscribable: true` + `completeWhenDepsComplete: false` shape now read those properties from `src` directly.
  **Why deferred:** no current use case for independent-companion reuse; the abstraction complicates surface for hypothetical demand. Re-evaluate post-1.0 when concrete demand emerges (e.g. user wants `statusOf` alone without paying for error tracking, or a third companion sits alongside status/error in a custom bundle).

- **`projection` scan-mode out-of-order cross-stream events (2026-04-25):**
  When multiple event streams are merged for a projection in scan mode, events arriving with a `timestampNs` earlier than the current sort cursor are silently skipped from the incremental sweep. This is an acceptable trade-off for incremental fold — strict cross-stream ordering requires a watermark or full-replay approach. Users needing strict ordering should use `mode: "replay"` or implement watermark logic in their reducer. The caveat is now documented inline on `ProjectionOptions.mode` in [src/patterns/cqrs/index.ts](../src/patterns/cqrs/index.ts).

- **process-manager pattern (Audit 3, locked 2026-04-24, deferred):** Design is fully locked in `archive/docs/SESSION-orchestration-messaging-cqrs-review.md` § "Audit 3" but has no current consumer driving the ship date. Locked shape: per-instance state via event-sourcing with `correlationId` as `aggregateId` (C.1 pattern); discriminated `ProcessStepResult<TState>` union (`continue` / `terminate` / `fail`); step-level retry policy (`retryMax` + `backoffMs`); compensation runs on step `kind: "fail"` throw and on explicit `processManager.cancel(correlationId)`; persistence opt-in via `AppendLogStorageTier` + `SnapshotStorageTier` (Audit 4); audit trail via `createAuditLog` (Audit 2); synthetic event types `_process_<name>_state` + `_process_<name>_timer`; watch-event subscription filtered by `correlationId` via `mergeReactiveLogs` (Audit 1). Not blocking any current consumer; ships when a real consumer surfaces. Would land at [`src/patterns/process/index.ts`](../src/patterns/process/index.ts).

- **`processManager` lone `queueMicrotask` in timer cleanup (2026-04-25; path corrected 2026-05-17):**
  [src/utils/process/index.ts:839](../src/utils/process/index.ts#L839) calls
  `queueMicrotask(() => timerUnsub?.())` as a TDZ guard around `fromTimer`'s
  subscription cleanup. The comment marks the path as defensive — `fromTimer`
  is async (setTimeout-backed) so the inner subscribe callback cannot fire
  before the outer `subscribe()` returns, making the TDZ branch unreachable
  in practice. Per spec §5.10 this is a soft violation of the "no bare
  `queueMicrotask` / `setTimeout` / `process.nextTick` in the reactive
  layer" rule — even unreachable. Two paths: (a) restructure subscribe to
  capture cleanup synchronously and delete the dead branch, or (b) replace
  with a state-mirror node that owns its disposal. Trivial cleanup; out of
  Phase 8 scope, queued for a follow-up pass.

- **`actuatorExecutor` to migrate to protocol ERROR when Path X lands (2026-04-25).** Currently maps thrown errors and inner ERRORs to `outcome: "failure"` DATA via `onError` ([src/patterns/harness/actuator-executor.ts](../src/patterns/harness/actuator-executor.ts) lines 112–118). When Path X (per-attempt Node-returning mutations, see line ~796) resolves the eager-keepalive vs `defaultOnSubscribe` blocker, this primitive should emit `[[ERROR, err]]` and let users compose `retry` / `fallback` / `catchError` for recovery. Migration is ~30 LOC: drop `onError` config, drop the synchronous-throw catch at line 200, let producer's natural ERROR flow.

- **`actuatorExecutor` `dispose` hook + late-resolution suppression (2026-04-25).** Track alongside the existing inner `refineLoop` / evaluator lifecycle hardening item. When a long-running actuator's `apply` callback resolves AFTER `switchMap` supersedes the inner producer, the resolution is currently silently dropped (correct behavior for cancelled work); but for I/O actuators where the side-effect already landed (file written, DB committed), there is no audit hook. Three-part hardening: (a) optional `onSupersede?: (item, reason) => void` config, (b) `actuatorExecutor` tracks in-flight `AbortController` so callers can introspect, (c) stress test rapid supersede with shared sink to prove no double-write.

- **`actuatorExecutor` `mode` option deferred (2026-04-25).** Today `switchMap` is hardcoded ("supersede in-flight on new arrival"). For test-running actuators (`codeAndLintActuator` running `pnpm test`), supersede mid-run corrupts the result; `concatMap` (queue, run serially) is the right policy. Other policies: `exhaustMap` (drop new arrivals while busy). Surface as `mode?: "supersede" | "queue" | "drop"` (default `"supersede"`) when a concrete consumer needs it. Premature today.

### Reactive data structures (Wave 4 audit, 2026-04-15)

**Audit context:** Wave 4 of the `src/extra/` hardening pass covers `reactive-map`, `reactive-list`, `reactive-index`, `reactive-log`, `pubsub`. Stress tests shipped in `src/__tests__/extra/reactive-map-stress.test.ts` and `reactive-list-stress.test.ts`. The items below are queued as one refactor batch so fix + optimization work across all five data structures ships together.

**Refactor plan — three-layer approach:**

1. **This round (Wave 4 shipment):** Apply version-counter + lazy-snapshot across all five structures. Add `*Backend` interfaces (`MapBackend<K,V>`, `ListBackend<T>`, `IndexBackend<K,V>`, `LogBackend<T>`) that default-delegate to native `Map` / `Array`. Reactive emission layer reads `version` from the backend and defers snapshot materialization. The version counter becomes the canonical equality signal; identity equality (`a === b` on always-new snapshots) retires. Ships the `size`-emits fix and `setMany`/`appendMany` bulk APIs at the same time, since all depend on the same shared plumbing.
2. **Post-1.0 (follow-up):** Op-log changesets (option A). Emit `{ version, ops: [...], rootRef? }` instead of full snapshots. Composes with version counter from step 1 (version field = the counter). Protocol-level shift requiring consumer reconciliation logic — worth doing once the primitive layer is stable.
3. **Always optional (user-pluggable):** Persistent data structures (option C — RRB-tree for lists, HAMT for maps) via user-supplied backend. Library bundles only the native backend (~30-50 LOC per structure). Users who need structural sharing / time travel / cheap snapshot plug in `@funkia/list`, `immutable-js`, `mori`, or their own via the `*Backend` interface. We may ship reference adapters as separate packages (`@graphrefly/backends-rrb`, `@graphrefly/backends-hamt`) post-1.0 without adding to core bundle size.

**Rationale:** Bundled RRB-tree / HAMT would add ~5-15 KB gzipped to core for a feature not every user needs. The `*Backend` interface costs ~30-50 LOC and zero runtime overhead (native backend is a thin pass-through). Users opting into persistent semantics pay the bundle cost; everyone else gets the same small core they have today.

**Cross-cutting semantic finding — subscriber callback delivery contract:**
Verified by stress test `S7`. The `downWithBatch` delivery rules (`batch.ts:245-310`) — not previously surfaced in user-facing docs — are:

1. **Within one `down()` call, same tier** → messages coalesced into one array, delivered in one `sink()` call. E.g., `down([[DIRTY], [DIRTY]])` produces one `sink([[DIRTY], [DIRTY]])`.
2. **Within one `down()` call, different tiers** → one `sink()` per non-empty tier group (immediate, deferred, terminal). E.g., `down([[DIRTY], [DATA, 42]])` produces `sink([[DIRTY]])` then `sink([[DATA, 42]])`.
3. **Across different `down()` calls (same batch)** → always separate `sink()` invocations, never merged. Each queued closure in `pendingPhase2` is its own sink call.
4. **No semantic dedup** of repeated idempotent messages (e.g., 5 DIRTYs stay 5 DIRTYs).

Implication: `pushSnapshot()` today uses two separate `down()` calls (one for DIRTY, one for DATA), producing 2 sink invocations per mutation. Even rewriting to a single `down([[DIRTY], [DATA, v]])` produces 2 sink invocations (tier split). Batching helps with ordering/glitch-freedom and DATA drain timing — it does NOT reduce callback count. Bulk APIs (`appendMany`, potential `setMany`) are the only way to collapse N mutations into 1 DIRTY + 1 DATA pair (2 callbacks total instead of 2N). This contract should be documented in the `subscribe` / `node.down` JSDoc.

| Item | Status | Notes |
|------|--------|-------|
| **All five: version counter + lazy snapshot + `*Backend` interface** | **Wave 4 refactor COMPLETE (2026-04-15)** — all five structures shipped | Each structure gets: (a) internal `version: number` counter incremented on every mutation, (b) snapshot materialization delegated to backend, (c) `MapBackend` / `ListBackend` / `IndexBackend` / `LogBackend` / `PubSubBackend` interface with default native implementation. ~30-50 LOC additional per structure. No external dep. Sets up the architecture for op-log changesets (post-1.0) and user-pluggable persistent backends. |
| **reactiveIndex: pilot refactor (backend + version counter + O(1) primary lookup + bulk APIs + has/get)** | **Shipped 2026-04-15** | `IndexBackend<K, V>` interface + default `NativeIndexBackend` (parallel `Map<K, IndexRow>` for O(1) `has`/`get`, O(log n) bisect, monotonic `version`). New bundle APIs: `has(primary)`, `get(primary)`, `size` (all O(1)), `upsertMany(rows)`, `deleteMany(primaries)`. Users can plug in persistent / B-tree backends via the `backend` option. 27 stress tests in `reactive-index-stress.test.ts`. O(n) filter pass eliminated from upsert; worst-case still O(n) due to array splice (pluggable backend gives users the escape hatch for truly O(log n) mutations). |
| **reactiveLog: full refactor (backend + ring buffer + memoized views + size/at/slice + removed standalone logSlice)** | **Shipped 2026-04-15** | `LogBackend<T>` interface + default `NativeLogBackend`. Ring buffer when `maxSize` is set (O(1) append + trim via `_head` index + modular arithmetic); flat array otherwise. New bundle APIs: `size` (O(1)), `at(index)` (O(1)), `slice(start, stop?)` as a bundle method. `tail(n)` and `slice(...)` are memoized — repeat calls with same arguments return the same node, bounding keepalive-subscription count to one per unique argument. **Breaking change:** standalone `logSlice()` export removed; use `log.slice(start, stop?)` instead. `appendMany` pre-trims oversize input under `maxSize` (skips values that would be immediately evicted). 26 stress tests in `reactive-log-stress.test.ts`. |
| **reactiveMap: full refactor (backend + version counter + size-emits fix + setMany/deleteMany)** | **Shipped 2026-04-15** | `MapBackend<K, V>` interface + default `NativeMapBackend` (native `Map<K, {value, expiresAt?}>` with LRU via insertion order, monotonic `version`). New bundle APIs: `setMany(entries, opts?)` and `deleteMany(keys)` — collapse N mutations into 1 snapshot emission (2 callbacks vs 2N). **Fixed correctness gap:** `size` getter now prunes expired entries AND emits when prune removes anything (S3 stress test inverted to assert fresh snapshot). Reads on expired keys (`get`/`has`) prune the target key + bump version + emit, keeping the reactive surface consistent with the read's return value. LRU touching does NOT bump version (internal optimization). Users can plug in persistent / HAMT backends via the `backend` option. 17 new stress tests added to `reactive-map-stress.test.ts` (setMany, deleteMany, NativeMapBackend direct, pluggable backend); S3 updated. |
| **reactiveList: `appendMany`** | **Shipped 2026-04-15** | Added to `ReactiveListBundle<T>`. Bulk push collapses N single `append()` calls (N × O(n) copies, 2N subscriber callbacks) into 1 snapshot (2 callbacks). Stress test coverage in `reactive-list-stress.test.ts`. |
| **reactiveList: full refactor (backend + version counter + size/at/insertMany)** | **Shipped 2026-04-15** | `ListBackend<T>` interface + default `NativeListBackend` (flat mutable array, monotonic `version`). New bundle APIs: `size` (O(1)), `at(index)` with Python-style negative indexing (O(1)), `insertMany(index, values)` — bulk insert collapses N × O(n) splice + 2N callbacks into 1 snapshot. No `maxSize` — insert/pop-anywhere semantics make eviction-under-cap ambiguous; use `reactiveLog` for bounded append-heavy workloads. Users can plug in persistent / RRB-tree backends via the `backend` option. 16 new stress tests (new APIs + NativeListBackend direct + pluggable backend). |
| **reactiveList: unbounded growth** | **Resolved 2026-04-15** | Documented as intentional design: no `maxSize` cap because insert/pop-anywhere semantics make eviction ambiguous. For bounded append-heavy workloads, use `reactiveLog` with `maxSize` (head-trim is well-defined for append-only). |
| **pubsub: full refactor (closure factory + thin PubSubBackend + has/size/topicNames/publishMany)** | **Shipped 2026-04-15** | Converted class `PubSubHubImpl` to closure factory for consistency with other `extra/` factories. `PubSubBackend` interface + default `NativePubSubBackend` (registry-only — tracks topic name set + monotonic `version` counter on create/remove; does NOT own per-topic storage). New APIs: `has(name)` (O(1)), `size` getter (O(1)), `topicNames()` iterator, `publishMany(entries)` bulk publish in one batch. `removeTopic` → `TEARDOWN` semantics preserved. 18 new stress tests in `pubsub-stress.test.ts` (lazy creation, multi-subscriber, push-on-subscribe, TEARDOWN, republish-recreates, new APIs, NativePubSubBackend direct, pluggable audit backend). |
| **messagingHub (new in `patterns/messaging.ts`)** | **Shipped 2026-04-15** | Pulsar-inspired lazy topic registry that wraps `TopicGraph` with hub semantics. `MessagingHubGraph extends Graph`: topics are mounted as child subgraphs (lifecycle managed by `Graph.remove`). APIs: `topic<T>(name, opts?)` (lazy create), `publish(name, value)`, `publishMany(entries)` (single outer batch), `subscribe<T>(subName, topicName, opts?)` (cursor-based `SubscriptionGraph`), `removeTopic(name)` (unmounts + TEARDOWN), `has(name)`, `size`, `topicNames()`, `version` counter (advances on create/remove). `defaultTopicOptions` for per-hub retention defaults. Covers orchestration / messaging / job-queue patterns with full Pulsar semantics (retention, absolute cursors via existing `SubscriptionGraph`). Distinct from `extra/pubsub` which is the lightweight last-value hub. 11 new tests in `patterns/messaging.test.ts`. |
| **reactiveLog: derived view keepalive leak** | **Resolved 2026-04-15** | Fixed via memoization (alternative C in the Wave 4 audit): `tail(n)` and `slice(start, stop?)` cache derived nodes per-argument inside the bundle. Repeat calls with identical arguments return the same node — keepalive subscription count is now bounded by the number of unique arguments rather than the number of call sites. Stress test S7 locks in the memoization invariant. |
| **Op-log changesets (option A)** | ✅ FULLY LANDED (Phase 14.3 + lens.flow 2026-05-17) | `BaseChange<T>` + `MapChange`/`ListChange`/`LogChange`/`IndexChange`/`PubSubChange`/`SpecChange`/`LensFlowChange` shipped at `packages/pure-ts/src/extra/data-structures/change.ts`; `mutationLog` companion on `reactiveMap`/`reactiveLog`/`pubsub`; `graphLens(_, {mutations})` `flowMutations` companion (`src/utils/inspect/lens.ts`, 2026-05-17); DS-14 BaseChange-wrapped WAL frames `graph/graph.ts:5449,6575`. Fully closed — archived to `resolved-decisions.jsonl` id `store-mutation-events-phase14-3-4-landed-2026-05-17`. |
| **Persistent backend adapters (user-pluggable)** | Post-1.0 reference packages | Reference `@graphrefly/backends-rrb` (RRB-tree for `reactiveList`) and `@graphrefly/backends-hamt` (HAMT for `reactiveMap`) as separate packages. Core bundle unaffected. Users pass them via `reactiveList({ backend: rrbBackend() })` / `reactiveMap({ backend: hamtBackend() })`. Ships after Wave 4 refactor establishes the `*Backend` interfaces. |

### AI surface (Phase 4.4) — deferred optimizations

| Item | Status | Notes |
|------|--------|-------|
| **Re-indexes entire store on every change** | Deferred | Decision: diff-based indexing using internal version counter to track indexed entries. Deferred to after Phase 6 — current N is small enough that full re-index is acceptable pre-1.0. |
| **Budget packing always includes first item** | Documented behavior | The retrieval budget packer always includes the first ranked result even if it exceeds `maxTokens`. This is intentional "never return empty" semantics — a query that matches at least one entry always returns something. Callers who need strict budget enforcement should post-filter. |
| **Retrieval pipeline auto-wires when vectors/KG enabled** | Documented behavior | When `embedFn` or `enableKnowledgeGraph` is set, the retrieval pipeline automatically wires vector search and KG expansion into the retrieval derived node. There is no explicit opt-in/opt-out per retrieval stage — the presence of the capability implies its use. Callers who need selective retrieval should use the individual nodes directly. |
| **agentMemory tier closure-state promotion (Unit 7 Q3)** | ✅ Resolved 2026-05-17 | Archived `agentmemory-tier-closure-state-promotion-unit7-q3-landed-2026-05-17`. `src/utils/ai/memory/memory-composers.ts:294-295,311-318`: `permanentKeys`/`entryCreatedAtNs` promoted to `ReactiveMapBundle` reactive maps `add()`-registered on the graph (inline comment cites Unit 7 Q3). Entry was stale-Open. |
| **agentMemory tierClassifier feedback cycle (Unit 7 Q2 / C1 wiring)** | ✅ Resolved 2026-05-17 | Archived `agentmemory-tierclassifier-retention-wired-unit7-q2-landed-2026-05-17`. `src/utils/ai/memory/memory-composers.ts:250-251,274-276,357-381`: effect-based `tierClassifier` retired; archival wired via `reactiveMap.retention` into the distill `mapOptions` at construction (comments cite "eliminating the §7 feedback cycle"). Entry's "effect-based classifier still runs" was false. |
| **`llmJsonCall` per-call subgraph allocation (Unit 9 deferred)** | Open / accepted | Each `llmExtractor`/`llmConsolidator` invocation allocates a fresh `state(input)` + `promptNode` topology (~4 nodes). Reclaimed via `distill`'s switchMap supersede. The alternative — share a long-lived `state(input)` written per call — would race on concurrent extractions and break `distill`'s `(raw, existing) → NodeInput<Extraction>` contract (see QA review 2026-04-24). Accepted as the doc-locked decision; revisit only if hot-path allocation surfaces in a profile. |
| **`promptNode` SENTINEL semantics — initial vs mid-flow distinction (resolved 2026-04-24)** | Resolved | True SENTINEL on initial no-DATA, `null` on mid-flow drop-out. Implementation falls out for free from the universal first-run gate + standard prevData semantics: dropped `initial: []` from `messagesNode` so the gate naturally blocks fn until any dep DATA's at least once → switchMap doesn't fire → outer cache stays `undefined`. Once any dep has DATA'd (per `ctx.prevData[i] !== undefined`), nullish values produce `[]` from messagesNode → switchMap dispatches `state(null)` → outer emits `null`. No closure flag needed — `prevData === undefined` IS the sentinel marker. Includes a regression test covering pure SENTINEL, initial-null, real-then-null transitions. |
| **`agentLoop` `interceptToolCalls` add() name collision (resolved 2026-04-24)** | Resolved | Inner raw tool-call node renamed to `"toolCallsRaw"`; post-intercept value registers as `"toolCalls"`. When no interceptor is configured (`this.toolCalls === toolCallsRaw`), only the post-intercept name is registered to avoid the per-graph instance-collision check. |
| **`agentLoop` add()-ordering for topology event-stream consumers (resolved 2026-04-24)** | Resolved | All `add()` calls relocated to a single block AFTER every dependent node is constructed. Topology event-stream consumers subscribed during construction now see registrations in an order where every edge between two registered nodes is already valid — no transient partial graphs. |
| **`suggestStrategyReactive` destroyed-graph guard (resolved 2026-04-24)** | Resolved | Added public `Graph.destroyed: boolean` getter; `suggestStrategyReactive` checks it and emits `null` (matches "input not ready" semantic) instead of letting `g.describe()` throw → spurious ERROR. Tiny race window between check & sync `suggestStrategy(g, ...)` invocation is negligible. |
| **`graphFromSpecReactive` / `suggestStrategyReactive` superseded LLM call abort (resolved 2026-04-24)** | Resolved | Earlier QA flag noted that `cancelled` only suppressed emission while the underlying LLM call kept running. Fix landed: `AbortController.signal` is threaded through `graphFromSpec` / `suggestStrategy`'s new `signal?` option, and the producer's cleanup calls `controller.abort()` on supersede. `graphFromSpecReactive` also now `g.destroy()`s any Graph that lands post-abort. |
| **`agentMemory.retrieveReactive` Q4 mirror via co-mounted effect (resolved 2026-04-24)** | Resolved | Earlier QA flag noted the in-derived-body `state.emit()` calls were a §7-adjacent gray zone. Refactored: derived now returns pure `{ packed, trace }`; a co-mounted `effect([result], …)` writes the observability state nodes (proper §32 state-mirror — visible in `describe()`). Reference-equality dedup also added on `retrievalOutput` so subscribers don't wake up on identical packed arrays. |

### Intentional cross-language divergences

Archived to `archive/optimizations/cross-language-notes.jsonl` (entries with `id` prefix `divergence-`). The `/parity` and `/qa` skills read the archive to avoid re-raising confirmed divergences.
