# Cross-track ledger — presentation ↔ Rust-port coordination

> **Single source of truth** for any change that couples the two parallel tracks:
> the **presentation track** (`@graphrefly/graphrefly`, this repo, `src/`) and the
> **Rust-port track** (`@graphrefly/native`, `~/src/graphrefly-rs`).
>
> These tracks are decoupled *by design* (D206: `@graphrefly/pure-ts` is the sole
> working sync substrate; presentation never consumes native). The **only** coupling
> surface is the `Impl` contract at `packages/parity-tests/impls/types.ts`. This doc
> exists so that coupling stays **explicit and logged**, never implicit.
>
> Item-level optimization state still lives in [`docs/optimizations.md`](optimizations.md);
> this doc is *only* the cross-track coordination ledger. Do not lump general
> optimization items here.

---

## When to add a row here

Add a row to the relevant section below **before** landing the change, whenever:

1. **`Impl`-contract widening** — presentation starts importing a NEW symbol from a
   `@graphrefly/pure-ts` public barrel (`core`, `extra`, …). This widens the contract
   `@graphrefly/native` must satisfy → it is a public-API decision.
2. **Substrate behavior change with parity implications** — a bug fix / semantic
   change to substrate code (`packages/pure-ts/src/`) that the `@graphrefly/native`
   parity arm must mirror (even if no new symbol).
3. **Substrate-boundary rename / removal** — any pre-1.0 rename or removal at the
   `pure-ts` public surface (collision resolution, barrel reshape, etc.).
4. **Anything else that forces both tracks to move together.**

Never widen `Impl` or change substrate behavior implicitly. The Rust-port track
consumes this ledger via `~/src/graphrefly-rs/docs/migration-status.md` § "NEXT
BATCH" (the **N1 → item-8** handoff is the reference pattern).

## When to close / archive a row

Mark a row ✅ when the native side has shipped the symbol/behavior AND the parity
arm is tight (`packages/parity-tests/impls/rust.ts` casts `as Impl`, not
`as unknown as Impl`). On the next docs sweep, move closed rows to
`archive/optimizations/resolved-decisions.jsonl` per
[`docs/docs-guidance.md`](docs-guidance.md) § "Optimization decision log".

---

## §1 — `Impl`-contract widening ledger

| Date | Symbols | Presentation consumer(s) | TS side (`types.ts` + `pure-ts.ts`) | Native side (`@graphrefly/native` + `rust.ts` tight cast) | Status |
|---|---|---|---|---|---|
| 2026-05-15 (N1) | `RingBuffer`, `ResettableTimer`, `describeNode`, `sha256Hex`, `sourceOpts` — `wrapSubscribeHook` dropped by Group-3 Edge #2 → N1 is **5, not 6** | `src/utils/resilience/*`, `src/base/sources/async.ts`, `src/base/io/*`, `src/utils/ai/adapters/*` | ✅ `7437fb7` (Impl-contract pin) | ✅ Option C: rs `5b5c041`, ts `d441648` (`rust.ts` tight `as Impl`); `@graphrefly/native@0.0.1` published | ✅ **RESOLVED** (archive on next sweep) |
| 2026-05-16 (CT-1, Lock 4.A) | `legacyCleanup` (transient migration shim) | (was 42 presentation files) | **Resolved by REMOVAL, not by pin.** `legacyCleanup` was deleted entirely — helper, `@graphrefly/pure-ts/core` barrel export, and all 115 call sites (52 source files + test fixtures) narrowed to explicit `{ onDeactivation }` / `{ onInvalidate }` hooks. No substrate-peer symbol, no presentation import, no `Impl` widening. | **N/A — symbol no longer exists.** Nothing for `@graphrefly/native` to implement; the hazard class is gone by construction. | ✅ **RESOLVED 2026-05-16** (same-session removal). `grep -rn 'legacyCleanup' src packages/pure-ts/src --include='*.ts'` == empty. Gates: pure-ts 1209 / graphrefly 1935 / parity 353(+1 skip) / lint+build clean. Archive on next sweep. |

| 2026-05-20 (S6 QA F3) | **`ImplReactiveMap.has`/`get` shape change: sync → async (Promise-returning).** The Rust substrate's `ReactiveMap::has`/`get` are NOT read-only when TTL is configured: an expired-key check calls `prune_expired_inner` + `self.emitter.emit(core, snapshot)` (`crates/graphrefly-structures/src/reactive.rs` `has` body line ~1280, `get` body line ~1320), which fires subscribers. Under the prior sync `Impl` shape the napi binding mapped this onto `actor.run_sync` (libuv-blocking sync_channel round-trip); if any subscriber to the map's snapshot node uses `bridge_sync` (Blocking TSFN call), libuv would block on the actor reply WHILE the actor is blocked on TSFN dispatch → 3-way deadlock. Widening the Impl contract to async lets libuv stay free during the actor round-trip. | `packages/parity-tests/scenarios/structures/reactive-map.test.ts` (`await map.has(...)` / `await map.get(...)` at all callsites). Internal: post-S6 nothing in presentation imports from `pure-ts` for these specific methods (they're parity-tests-only via the Impl interface). | ✅ **LANDED 2026-05-20** — `packages/parity-tests/impls/types.ts` `ImplReactiveMap.has/get` widened to `Promise<…>`; `packages/parity-tests/impls/pure-ts.ts` reference arm wraps in `async`; `packages/parity-tests/scenarios/structures/reactive-map.test.ts` callsites await. | ✅ **LANDED 2026-05-20 (S6 QA)** — `crates/graphrefly-bindings-js/src/structures_bindings.rs` `BenchReactiveMap::has`/`get` converted to `async fn` routing through `actor.run`; wrapper.js consumer (`crates/graphrefly-bindings-js/wrapper.js` `reactiveMap()`) widened to `async has/get` with `await map.has/get` calls. | ✅ **TS ↔ native aligned 2026-05-20.** Both arms async. Archive on next sweep. |
| 2026-05-23 (D283) → **TS LANDED 2026-05-23** → **AMENDED 2026-05-24 (D284)** → **NATIVE LANDED 2026-05-24 (D285 + D286 + D287)** | **`ImplGraph.tagFactory(factory, factoryArgs?): Promise<void>` + `ImplGraph.resourceProfile(opts?): Promise<ImplGraphProfileResult>` + new local types `ImplNodeProfile` + `ImplGraphProfileResult` — `Impl` widening so R3.1.2 (factory provenance for `describe({detail:"spec"})` round-trip via `decompileSpec`/`compileSpec`) and R3.6.3 (snapshot-based runtime profile: per-node stats, hotspots, orphans) parity scenarios can run cross-arm (E-iv.4 — port-coverage scenario authoring per D196's "parity scenarios are the consumer pressure signal" rule).** **D284 amendment (2026-05-24):** dropped `valueSizeBytes` per node + `totalValueSizeBytes` aggregate + `hotspots.byValueSize` from the Impl contract — pre-design caught these as pure-ts-inferred fields the canonical R3.6.3 spec does NOT mandate; the cross-arm narrower shape is what both arms can honestly deliver (Rust cache is a `HandleId`, true value-size would require a `BindingBoundary::value_size_of` FFI widening that pre-design closed instead of building, per D196 + value-#6). Both methods already exist on the pure-ts `Graph` (`packages/pure-ts/src/graph/graph.ts:1684` for `tagFactory`, `:3550` for `resourceProfile` → delegates to `graphProfile()` in `packages/pure-ts/src/graph/profile.ts`); pure-ts keeps computing the dropped fields internally — the parity wrapper strips them via `projectToImplProfile` at the cross-arm boundary. The D004 R3.1.2 + R3.6.3 deferrals were lifted in graphrefly-rs (`setVersioning` R3.2.4 stays deferred per Phase 14 op-log counter dependency). Drops the spec's `this`-chain return on `tagFactory` per the D077 async-everywhere parity contract — same trade-off `mount` already took. | 8 scenarios across 2 new files: `scenarios/graph/tag-factory.test.ts` (4 tests covering meta surfacing in `describe()` / QA F8 clear-on-reassign invariant / undefined-arg key-omit / topology-dirty bump on `describe({reactive:true})`) + `scenarios/graph/resource-profile.test.ts` (4 tests covering nodeCount+edgeCount+subgraphCount topology accuracy / per-node subscriberCount with unsub recovery / orphan categorization for `idle-derived` kind / hotspots descending-sort + `topN` cap). 7 of 8 lifted to cross-arm by D287; resource-profile test #1 stays pure-ts-only because `g.derived(name, deps, fn)` arbitrary-fn round-trips differently across arms (pure-ts `legacy.map` registers an intermediate node; native `withLatestFrom` collapses to a single OperatorOp — `nodeCount` parity requires a BenchGraph arbitrary-fn `g.derived` widening that this slice didn't take). | ✅ **LANDED 2026-05-23 (D283)** — `packages/parity-tests/impls/types.ts` `ImplGraph.tagFactory` + `ImplGraph.resourceProfile` methods + `ImplNodeProfile` + `ImplGraphProfileResult` types added; `packages/parity-tests/impls/pure-ts.ts` wraps the sync legacy methods in async. **AMENDED 2026-05-24 (D284)** — dropped 3 value-size fields from `ImplNodeProfile` + `ImplGraphProfileResult`; `pure-ts.ts` adapter strips them via `projectToImplProfile` helper. **D287 (2026-05-24)** dropped `runIf` gates in 7/8 scenarios + emptied `NOT_ON_NATIVE_GRAPH_METHODS` registry (registry pattern kept for future widenings). Gate: 445 / 17 (1 prior + 1 D287 carve-out + 15 D282-still-gated). | ✅ **LANDED 2026-05-24 (D285 substrate + D286 napi binding, paired `/porting-to-rs` slice).** D285: `Graph::tag_factory(factory, factory_args)` substrate method + `GraphInner.factory` / `factory_args` fields + `GraphDescribeOutput.factory` / camelCase `factoryArgs` JSON keys (skip-if-none, byte-for-byte parity with pure-ts `graph.ts:3508-3509`); `Graph::resource_profile(core, opts?) -> GraphProfileResult` in new `crates/graphrefly-graph/src/profile.rs` (post-D284 narrower shape; D196-conformant); new `CoreFull::sink_count_of(node_id) -> usize` trait widening (substrate-internal, NOT on the parity `Impl` contract). 11 cargo regression tests (5 tag_factory + 6 resource_profile). D286: `BenchGraph::tag_factory(factory, factory_args_json)` + `BenchGraph::resource_profile(top_n)` async napi methods (mirror existing `describe_json` `actor.run` pattern); wrapper.js `NativeGraph.tagFactory(factory, factoryArgs?)` + `NativeGraph.resourceProfile(opts?)` exposure (JSON-stringify args at boundary; JSON.parse the profile return); `GraphProfileResult` / `NodeProfile` / `Hotspots` serde structs gain `#[serde(rename_all = "camelCase")]` to match the cross-arm wire contract (`subscriberCount` / `bySubscriberCount` / etc.) — a +1 `d286_napi_camel_case_json_keys` cargo regression pin. graphrefly-rs gate: 866 / 866 (was 865 post-D285). Native local commit pending; not pushed per D265 hold-local. | ✅ **TS ↔ native aligned 2026-05-25 (closed by D292 D.1).** Resource-profile test #1's `runIf(runIfPureTs)` carve-out LIFTED via D292's `BenchGraph::derived(name, deps, fn)` async TSFN reroute (D263 `register_user_derived` + `bench.add`; closure-cell map auto-evicted on `graph.remove`/`destroy`/`impl.close` cascade per F3). All 4 `resource-profile.test.ts` tests now `describe.each(impls)` cross-arm. Cross-arm activation on the rust arm depends on user-gated `@graphrefly/native@0.1.0` republish (D265 hold-local). Archive together with §1 D282 + §1 D292/D293 on the next sweep post-republish. |
| 2026-05-23 (D282) → **TS LANDED 2026-05-23** → **Path D LOCKED 2026-05-24 (D288)** → **NATIVE BINDING LANDED 2026-05-25 (D289)** → **TS PAIRED SLICE LANDED 2026-05-25 (D290)** → **D291 PARTIAL 2026-05-25 (Case 5 cross-arm; Case 15a stays open pending D292 async-commit)** | **`Impl.batch(fn: (ctx: BatchCtx) => void): Promise<void>` — `Impl` widening so `batch-throw-rollback` parity scenarios can run cross-arm (D282 — Throw-during-batch subscriber atomicity).** TS-arm converges to Rust's pre-existing `BatchGuard::discard_wave_cleanup` + `restore_wave_cache_snapshots` semantic (substrate at `graphrefly-rs/crates/graphrefly-core/src/batch.rs:3693`) per spec R4.3.2. Rust substrate is conformant; TS gains the snapshot+paired-rollback-hooks shape via per-node `_preBatchSnapshot` + new `batchHooks: Array<{ flush, rollback }>` registry. **2026-05-24 pre-design HALT (D287):** the locked `Impl.batch(fn: () => void)` shape had a structural problem on the native arm — the 12 parity scenarios use sync internal pure-ts API (`(src.inner as { down }).down([[DATA, 42]])`) inside the `fn` body, and post-D267 every Core mutation routes through `actor.run` (async) → a sync fn body cannot await actor calls. **D288 (2026-05-24) reverses D287's deferral off a `/design-review`: Path D locked** (sync `BenchBatchContext` napi handle extending α-shape's parked-thread + sync-channel pattern locally to the batch frame — deadlock-safe because R4.3.1/R4.3.2 defer sink fires to commit; no TSFN callbacks during the held-guard window). Path D respects D255 α-shape + D256 no-re-entrant-actor + invariant-watch #4 (no `.await` in actor closure) while preserving the locked `Impl.batch(fn: () => void)` shape. B fails Value #6 (forces a `BatchScript` primitive + same-arm divergence); C structurally rejects D256. Q1–Q5 sub-decisions all locked 2026-05-24 in `archive/docs/SESSION-DS-D288-native-batch-path-D.md`. **D289 (2026-05-25, this row's native landing) ships the binding mechanism** — `BenchBatchContext` napi + parked-actor `BatchOp` loop + `Cell<bool>` tripwire + 5 cargo regressions in `crates/graphrefly-bindings-js/src/batch_bindings.rs`. | 12 scenarios in `packages/parity-tests/scenarios/core/batch-throw-rollback.test.ts` (real diamond `a → {x,y} → bottom`, `partial:false` first-wave consumers, terminal-mid-batch, equals-absorbed throw, drain-time-nested-batch A4 carve-out, nested-batch sanity invariant, multi-node atomicity); all `test.runIf(impl.name === "pure-ts")`-gated. Scenario rewrite against the `Impl.batch(fn: (ctx) => void)` widened shape is in-scope for the paired TS `/dev-dispatch` slice (Q3 lock). A new cross-arm regression `post-frame-ctx-throws` (Q3 lifetime contract) adds a 13th scenario in that slice. | ✅ **LANDED 2026-05-23** — `packages/parity-tests/impls/types.ts` `Impl.batch(fn): Promise<void>` added; pure-ts arm: 12 / 12 D282 parity tests passing. Pure-ts ergonomic preservation (D288 Q5): existing sync `legacy.batch(fn)` stays the substrate-level API; the cross-arm `ctx` arg becomes a thin pure-ts wrapper that forwards `ctx.down(node, msg)` to `(node.inner as NodeImpl).down([msg])` (added in the paired `/dev-dispatch` slice). | ✅ **NATIVE BINDING LANDED 2026-05-25 (D289, `/porting-to-rs` slice).** New `crates/graphrefly-bindings-js/src/batch_bindings.rs` (~970 LOC incl. tests): `BatchMessage` enum (5 substrate tiers Data/Complete/Error/Invalidate/Teardown) + `BatchOp` envelope (Q1: outer `crossbeam-channel` + per-op `std::sync::mpsc::sync_channel<R>(1)` reply carried IN envelope) + `BatchContextInner` (pure-Rust mechanism) + `BenchBatchContext` `#[napi]` class with full Message-tier surface (10 `down_*` methods incl. `down_pause`/`down_resume` returning `Result` shapes, + `commit` + `rollback`) + `DURING_BATCH_HANDLE: Cell<bool>` thread_local + `BatchHandleGuard` RAII + `assert_no_batch_handle(callsite)` tripwire (Q2: `Cell<bool>` not `AtomicBool` per D248/D252/D254 Tier A precedent) + `run_batch_loop` parked-actor closure body + `BenchCore::open_batch()` sync napi factory via `actor.dispatch_detached(...)`. Tripwires wired at the 3 `bridge_sync*` sites (operator/structures bindings + `bridge_sync_unit`). Rollback uses ack-first → `std::panic::resume_unwind(payload)` so `BatchGuard::Drop` takes the `std::thread::panicking()` branch → `discard_wave_cleanup` (R4.3.2 parity); actor's outer `catch_unwind` (`core_actor.rs:356`) catches → BenchCore not bricked. Per-frame lifetime contract (Q3): `BatchContextInner::Drop` posts best-effort `Rollback` if neither commit/rollback called; post-close `ctx.down_*(...)` returns `"BenchBatchContext used after batch closed"`. 5 cargo regression tests (sink-fire-zero, rollback-parity-R4.3.2, post-frame-ctx-throws, commit-fires-pending-notify-once-per-tier, bonus tripwire mechanism test). `Cargo.toml` `crate-type` widened to `["cdylib", "rlib"]` + `build.rs` emits `dynamic_lookup` (macOS) / `--unresolved-symbols=ignore-all` (Linux) for the test binary. graphrefly-rs gate: 866/866 default-members + 5/5 batch_bindings. Native local commit pending; not pushed (D265 hold-local). | TS ✅ landed 2026-05-23 (D282 substrate convergence); native ✅ binding landed 2026-05-25 (D289). ✅ **TS PAIRED SLICE LANDED 2026-05-25 (D290, `/dev-dispatch`).** Widened `Impl.batch(fn: (ctx: BatchCtx) => void): Promise<void>` + new `BatchCtx` interface in `packages/parity-tests/impls/types.ts` with D288 Q3 per-frame-lifetime + Q4 D080-non-precedent JSDoc; 3-line ctx-injecting `legacy.batch` wrapper in `pure-ts.ts` (D288 Q5 — substrate API unchanged); wrapper.js `impl.batch` in `graphrefly-rs/crates/graphrefly-bindings-js/wrapper.js` (JS-only edit, no Rust recompile) opens `state.core.openBatch()` → `BenchBatchContext`, dispatches `ctx.down(node, msg)` by tier into `downHandle/downComplete/downErrorInt|Str/downInvalidate/downTeardown/downPause/downResume`, commit-on-success / rollback+rethrow on throw, `closed` flag first-line check on every `ctx` method; rust.ts dropped the throwing stub + `has` trap (pass-through). 14 `batch-throw-rollback.test.ts` scenarios rewritten from `(src.inner as {down}).down([[X]])` → `ctx.down(src, [X])`; Cases 1/2/3/4/6/8/9/12 + new **Case 15b (throw-path post-frame ctx.down throws)** run cross-arm; Cases 5/7/10/10b/11/13/14/15a stay `runIf(pure-ts)` with inline comments naming the deferred widening that would lift each (`Impl.legacyBatch` for 7/11; `Impl.node` `versioning` widening (R3.2.4) for 10/10b; `Impl.isPaused`/`Impl.pauseLockCount` for 13; `Impl.cleanupTracker`/`Impl.statusOf` for 14). **Two graphrefly-rs substrate gaps surfaced by the parity rebase (LANDED 2026-05-25 by D291, Bucket A `/porting-to-rs` slice):** (i) **Case 5 — terminal-status restoration LANDED:** new `wave_terminal_snapshots: AHashSet<NodeId>` + `wave_dep_terminal_snapshots: AHashSet<(NodeId, usize)>` on `WaveState`; `terminate_node` inserts on the `None → Some(_)` transition (gated on `in_tick`); `Core::restore_wave_terminal_snapshots(s, ws)` resets the slots on panic-discard and releases any ERROR-tier handles lock-released; wired into `BatchGuard::discard_wave_cleanup` + success-path drain alongside `drain_wave_cache_snapshots`. Pin: `crates/graphrefly-core/tests/d291_terminal_rollback.rs` (3 tests). Case 5 cross-arm green. **Deferred (D291 scope):** `has_received_teardown` flag + `pause_state` drain + `replay_buffer` drain rollback — Case 5 doesn't exercise them; tracked in `~/src/graphrefly-rs/docs/porting-deferred.md` §"D291 deferred-scope". (ii) **Case 15a — substrate Q2 tripwire ordering fix LANDED; cross-arm lift PARTIAL.** Substrate fix: explicit `drop(handle_guard)` BEFORE `drop(guard_holder.take())` in `BatchOp::Commit` arm of `run_batch_loop` so `DURING_BATCH_HANDLE` is clear when `BatchGuard::Drop`'s success path runs `fire_deferred`; rollback path's decl-order reverse-drop preserved (Q2 invariant on `discard_wave_cleanup` intact). Pin: `crates/graphrefly-bindings-js/src/batch_bindings.rs::tests::d291_commit_path_clears_during_batch_handle_before_sinks_fire` (uses Rust-side `Rc` sinks). **However, cross-arm vitest verification surfaced a SEPARATE structural blocker the D288 Path D design did NOT anticipate:** `BenchBatchContext::commit` is sync napi → blocks libuv on `rx.recv()` → `BatchGuard::Drop`'s success-path `fire_deferred` can't reach TSFN-backed JS sinks → 3-way deadlock between the JS commit caller, libuv, and the parked actor. The D289 substrate test passes because it uses Rust-side `Rc<dyn Fn>` sinks; only real TSFN-backed JS consumers expose the deadlock. **Case 15a stays `runIf(impl.name === "pure-ts")` pending D292** (async `BenchBatchContext::commit`/`rollback` conversion — same shape as D267's `BenchGraph` async-read fix; queued in `docs/optimizations.md` "🔴 DESIGN SESSION FIRST" + `~/src/graphrefly-rs/docs/porting-deferred.md` § "D291 → D292 carry"). **Bonus (D289 QA F2):** new `scripts/check-tripwire-wiring.sh` + `mise run check:tripwire-wiring` + wired into `gate.sh` Step 0 — fail-loud if a future `bridge_sync*` site omits `assert_no_batch_handle(`. ✅ **Row CLOSED 2026-05-25 (Case 15a cross-arm lifted by D292 D.2):** `BenchBatchContext::commit`/`rollback` converted from sync `pub fn` to async `pub async fn` via `tokio::task::spawn_blocking(move || rx.recv())` (R2: NOT `actor.run` per D255 α-shape; `spawn_blocking` keeps libuv free during the actor's sink-fire-via-TSFN drain → TSFN-backed JS sinks can fire in `BatchGuard::Drop`'s success-path `fire_deferred` without the libuv-vs-actor deadlock). Symmetric R3: widened `BatchOp::Commit`/`Rollback` reply to `SyncSender<Result<(), String>>` + `catch_unwind` around the success-path drop → sink panics convert to rejected Promises (closes BH15). Case 15a `runIf(runIfPureTs)` gate dropped in `scenarios/core/batch-throw-rollback.test.ts`. Cross-arm activation depends on republish (D265 hold-local). Archive together with §1 D283 + §1 D292/D293 on the next sweep post-republish. **D297 AMENDMENT (2026-05-26):** the original "Rust substrate is conformant" claim was scoped to the cache-rollback path that was the TS bug. Rust has been incrementally catching up on the full R4.3.2 status-snapshot completeness contract via the D291 + D297 chain: D291 added terminal-slot rollback (Case 5); **D297** adds `has_received_teardown` flag rollback (closes D291 deferred-scope Item 1 — pre-fix a retry of `teardown(node)` after a panic-discarded batch was silently no-op'd by the idempotency guard, leaving the node in a contradictory state `terminal=None` ∧ `has_received_teardown=true` unreachable from the public surface). Bilateral convergence direction: pure-ts has been conformant on this field since D282 (`_preBatchSnapshot.teardownDone` at `packages/pure-ts/src/core/node.ts:3993` + `:4136` restore); D297 catches Rust up. Items 2 (pause_state) + 3 (replay_buffer drain) + 3b (replay_buffer push) catch-up remains tracked under `~/src/graphrefly-rs/docs/porting-deferred.md` § "D291 deferred-scope" per consumer-pressure gates. |
| 2026-05-25 (D292 + D295 batch) — **TS + NATIVE LANDED 2026-05-25 (local; D265 hold-local — npm latest still `@graphrefly/native@0.0.5` until user-gated tag push triggers OIDC `@graphrefly/native@0.1.0` republish)** | **`@graphrefly/native@0.1.0` lifecycle batch (D292 design lock via `/design-review` + D295 paired-batch implementation slice via `/porting-to-rs`).** Three coupled targets locked together because the answers cross-cut: **D.1** — async `BenchGraph::derived(name, deps, fn)` arbitrary-fn widening (lifts D287 carve-out on `resource-profile.test.ts` test #1 cross-arm via D263 `register_user_derived` TSFN reroute + 2 napi crossings + JS-side closure-cell map auto-evicted on `graph.remove`/`destroy`/`impl.close`; R1 anti-pattern #5 watch JSDoc on `BenchGraph.derived`); **D.2** — async `BenchBatchContext::commit`/`rollback` via `tokio::task::spawn_blocking(move || rx.recv())` (R2: NOT `actor.run` — D255 α-shape single-worker-per-Core would serialize concurrent reads; `spawn_blocking` keeps libuv free); R3: symmetric `catch_unwind` + widened `BatchOp::Commit`/`Rollback` reply to `SyncSender<Result<(), String>>` so sink panics convert to rejected Promises (closes BH15 + lifts D291 Case 15a cross-arm); `closed: Cell<bool>` → `AtomicBool` for cross-thread safety; `Mutex<BatchContextInner>` wrapper dropped (Send+Sync now); **D.3** — 5 sub-items: Item 1 = `FinalizationRegistry` + new `BenchCore::finalize_async()` sync napi posting to `napi::bindgen_prelude::spawn_blocking` so GC-on-libuv-finalizer doesn't block JS event loop (closes D293 WATCH (i)); Item 2 = `impl.close` rejects on actor errors (drop `.catch(() => {})` per F2 + closes D293 WATCH (ii)); Item 3 = deferred per D196 (nested `Symbol.asyncDispose` on Graph/Subscription); Item 4 = close-waits/drain in-flight commit/rollback (R4 `Impl.close` JSDoc one-liner "stuck closure → close hangs / Promise.race for bounded close time"); Item 5 = default-off opt-in `createNativeImpl({ autoCloseOnBeforeExit: true })` (R5 README rubric: 3 conditions — reliable beforeExit + can't sequence explicit close + accept synchronous close-drain). **Cross-cutting findings folded in:** F1 — close drains (NOT rollback-unclosed; D288 Q3's BatchContextInner::Drop handles zombies); F2 — panic-propagation uniform across commit/rollback/close; F3 — closure-cell registry evicted on the 3 teardown paths; F4 — `_dispose` dropped entirely per value #1 (parity-tests `rust.ts` afterEach swept to public `close()`). **Predecessor:** D293 (patch-tier MVP; `CoreActor::shutdown` + `BenchCore::close` + `Impl.close()` + top-level `Symbol.asyncDispose`) shipped 2026-05-25 as v0.0.8 candidate. Pre-D293 state row preserved below for design record.

**Pre-D293 framing (preserved for design record):** `Impl.close(): Promise<void>` — `Impl` widening so the "process never exits" hang for every napi consumer (test frameworks vitest/jest/mocha, CLI scripts, serverless cold-start, AWS Lambda) closes via an additive cross-arm lifecycle hook.** Native arm: `CoreActor::shutdown(&self)` (idempotent flag + wake-closure + take-and-join `JoinHandle`) + `BenchCore::close()` async napi (drain subs best-effort + `actor.shutdown`) + `wrapper.js` `impl[Symbol.asyncDispose] = impl.close` (Node 22+ `await using`). Pure-ts arm: no-op `async close()` (no actor thread; symmetry only — pure-ts has no equivalent "process never exits" hazard). Post-close on the native arm rejects with broadened `"... (actor is shut down or shutting down)"` (D293 /qa Q2a covers `Drop` trigger + explicit shutdown uniformly). **Two pre-locked-scope shifts simplified the landing vs the originally-scoped scope (per `docs/optimizations.md` D293 pre-locked-scope block):** (a) `MailboxOp::Shutdown` variant NOT added — that enum lives in `graphrefly-core/src/mailbox.rs` as the cross-thread producer/timer channel, not the `CoreActor` closure-dispatch channel; (b) `closed: AtomicBool` per-method guards NOT added — `actor.run`'s existing `sender.send(...).map_err(...)` branch surfaces the broadened napi error automatically post-shutdown, so Q2a's clear-error contract is met without 15+ per-method guard sites. **D292 watch items (fold into D292's v0.1.0 lifecycle design session — in-code `// D292 WATCH:` comments at both code sites + `~/src/graphrefly-rs/docs/porting-deferred.md` § "D293 → D292 carries"):** (i) **Drop-running-join on libuv thread** — GC of `BenchCore` on the libuv JS thread (napi-rs invokes the `napi_class` finalizer on libuv when JS collects the object) blocks the JS event loop on `handle.join()` until the worker's current closure completes; visually indistinguishable from the very hang D293 closes. Lift = async-shutdown-from-finalizer (`FinalizationRegistry`-driven `napi::bindgen_prelude::spawn` of the shutdown work). (ii) **Silent close-error swallow in `wrapper.js`** — `impl.close = async () => { await state.core.close().catch(() => {}); }` preserves backward-compat with the prior `_dispose` shape (afterEach ignored errors) but loses error visibility on the new public surface; a future `close()` failure (network-attached storage tier timeout, async-commit panic propagation per D292 D.2 Q2.5, etc.) is hidden from the JS caller. Lift = D292 lifecycle session locks reject-vs-swallow. | `packages/parity-tests/impls/pure-ts.ts:919-934` (`async close()` no-op). No presentation-layer (`src/`) consumer yet — test-harness lifecycle only; `Symbol.asyncDispose` wired on the native arm via `wrapper.js`. | ✅ **LANDED 2026-05-25** — `packages/parity-tests/impls/types.ts:608-624` `Impl.close(): Promise<void>` with full JSDoc (cites the cross-arm asymmetry: native rejects on post-close method calls, pure-ts behavior "may differ — no symmetry requirement until a cross-arm consumer surfaces it"); `packages/parity-tests/impls/pure-ts.ts` no-op `async close()` (D293-commented); new `packages/parity-tests/scenarios/usage/cleanup.test.ts` (5 cross-arm cases: Case 1 close-exists-and-resolves, Case 2 double-close idempotent no-throw, Case 3 post-close-method-rejects [native, arm-conditional matcher matches `/actor is shut down or shutting down/`] / stays-live [pure-ts], Case 4 `Symbol.asyncDispose` wired, **Case 5 implicit "vitest exits cleanly" — load-bearing pin: if `close()` didn't kill the worker, vitest would hang at file end waiting for worker process termination; vitest run exit IS the regression**). Pure-ts gate green; cross-arm runs implicitly via vitest exit. | ✅ **LANDED 2026-05-25 (D293, `/porting-to-rs` patch slice; v0.0.8 candidate).** Substrate (`crates/graphrefly-bindings-js/src/core_actor.rs`): `shutdown_flag: Arc<AtomicBool>` field on `CoreActor`; worker loop top-of-iteration `if worker_flag.load(Acquire) { break; }`; `pub(crate) fn shutdown(&self)` (idempotent flag-swap + no-op wake closure + take-and-join `JoinHandle`); `Drop for CoreActor` simplifies to `self.shutdown()` + defensive sender-swap-and-drop safety net. Binding (`crates/graphrefly-bindings-js/src/core_bindings.rs`): `BenchCore::close()` async napi — drains subscriptions (existing dispose body, best-effort on error) then calls `self.actor.shutdown()`; `BenchCore::dispose()` becomes a thin async alias delegating to `close()` per Q1b (grep-verified safe — all 3 prior `dispose()` callers are test-end / lifecycle-end patterns, none do dispose-then-use). JS surface (`crates/graphrefly-bindings-js/wrapper.{js,d.ts}`): `impl.close()` public async method; `impl[Symbol.asyncDispose] = impl.close` so `await using impl = createNativeImpl()` works on Node 22+; `impl._dispose = impl.close` as parity-only alias; `wrapper.d.ts` typed with `close(): Promise<void>` + `[Symbol.asyncDispose](): Promise<void>` on `NativeImpl`. New cargo regression `d293_shutdown_idempotent_and_post_close_errors_are_clear` in `crates/graphrefly-bindings-js/src/core_bindings.rs::tests` (idempotent shutdown + post-close error message contains the broadened parenthetical). Error message in `CoreActor::run` + `run_sync` broadened from "(actor shutdown raced with napi call)" to "(actor is shut down or shutting down)" per Q2a (covers both `Drop` trigger AND explicit shutdown uniformly). NEW `crates/graphrefly-bindings-js/README.md` "Closing a NativeImpl" section (modern `await using` + compat `try/finally` + test-framework + behavior-contract patterns; `process.exit()` escape-hatch sub-section; "what ships in v0.0.8 / what's coming in v0.1.0 (D292)"). graphrefly-rs gate: 869/869 default-members nextest --profile ci + 7/7 bindings-js (incl. the new D293 test); clippy + fmt clean; `#![forbid(unsafe_code)]` preserved. **D265 hold-local:** bindings crate source bumped to `0.1.0` locally; npm latest still `@graphrefly/native@0.0.3` until the user-gated tag push triggers OIDC republish (would re-include D289 + D290 wrapper.js + D291 substrate + binding fix + D293 close lifecycle). | ✅ **TS ↔ native aligned 2026-05-25** post-D292 (D292 D.1 lifts D287 test #1 cross-arm; D.2 lifts D291 Case 15a cross-arm + closes BH15 sink-panic-hang; D.3 Item 1 + Item 2 close the two D293 WATCH items; D.3 Item 5 + R5 README rubric ship the opt-in safety net; F4 drops `_dispose`). graphrefly-rs gate GREEN 869/869 substrate + 12/12 bindings-js (5 new D292 + 7 pre-existing). Cross-arm parity activation on the rust arm depends on user-gated tag push (`@graphrefly/native@0.0.5` → `0.1.0` republish via OIDC). **Archive this row + §1 D282 + §1 D283 together on the next sweep post-republish.** |
| 2026-05-26 (D298) — **SUBSTRATE LANDED 2026-05-26 (graphrefly-rs `6fadc5d`); Impl widening + napi binding + cross-arm scenario deferred per D196 post-sweep** | **`GraphObserveOne.up(messages)` canonical R3.6.2 primary — Rust substrate-only landing (Q1 user-locked Option 2, 2026-05-26).** Adds `GraphObserveOne::up(&self, core: &Core, messages: &[Message]) -> Result<(), UpError>` to `crates/graphrefly-graph/src/observe.rs:102+` alongside existing typed `pause/resume/invalidate`. **Two coexisting shapes with DIFFERENT semantics** (NOT sugar wrappers): `up(messages)` = canonical upstream-to-deps via `Core::up` (matches TS pure-ts `Node.up(messages)` at `packages/pure-ts/src/core/node.ts:1430` — open-vocab, recursive plain-forward to leaves, tier-3/5 rejection); typed methods = direct mutation of the observed node (Rust-idiomatic ergonomic shortcuts). For a leaf state node, `up([Pause])` is a no-op (no upstream) while typed `pause(lock)` DOES pause the node — verified semantic divergence locked-by-test. Pre-D298 Rust shipped only the typed methods citing `feedback_no_imperative` + non-allocating ergonomics; D298 reverses that as a deliberate **override of D196 + `feedback_no_imperative` for the `/porting-to-rs clear all... regardless if they need a valid consumer demand` sweep** (user-locked 2026-05-26). 7 cargo regression tests in `crates/graphrefly-graph/tests/observe.rs::{d298_up_pause_pauses_deps_not_observed_node,d298_up_pause_on_leaf_state_node_is_noop,d298_up_invalidate_plain_forwards_to_leaves_per_r1_4_2,d298_up_rejects_tier_3_data,d298_up_rejects_tier_5_complete,d298_up_empty_messages_slice_is_ok,d298_up_stops_dispatch_on_first_error}`. | None this slice. Pre-1.0 → cross-arm Impl widening + napi binding + parity scenario authoring deferred per the per-Q follow-on plan (lift trigger: a cross-arm consumer surfacing the need for `observe(path).up(messages)`). | DEFERRED — not in this slice. `packages/parity-tests/impls/types.ts` `ObserveSubscription` interface (line 369-380) currently exposes only `subscribe(sink)` + `dispose()`; widening to add `up(messages): Promise<void>` waits for the napi binding work + a cross-arm consumer. | DEFERRED — not in this slice. `BenchObserveReactiveHandle` (`crates/graphrefly-bindings-js/src/graph_bindings.rs:879+`) currently exposes only `dispose()`; adding an `up(messages)` napi method requires routing through the actor (the handle stores `(node_id, sub_id)` for `OneSub`, so `node_id` is reachable for `core.up(...)` dispatch) + `wrapper.js` exposure + Impl interface widening + cross-impl parity scenario. Per D265 hold-local for binding edits + the per-Q follow-on plan. | ⚠ **OPEN (substrate-only landing).** TS pure-ts arm is already conformant via existing `Node.up(messages)`. Native arm's `up(messages)` lift is gated on a real cross-arm consumer surfacing the need (or a future parity scenario authoring pass that wants to drop the existing `legacy.up`-style indirection). Archive together with the corresponding TS + native widening + parity scenario commit when lifted. |
| 2026-05-26 (D301) — **SUBSTRATE LANDED 2026-05-26 (graphrefly-rs `1f7c5c7`); cross-arm parity scenario authoring deferred per D196 post-sweep** | **Anon-dep describe wire-shape convergence (Q4 user-locked Option B + B.a + B.b, 2026-05-26).** Rust converges describe-output rendering for unnamed deps from `_anon_<NodeId.raw()>` to the empty string `""` matching TS pure-ts (`packages/pure-ts/src/core/meta.ts:257` — `node.name ?? ""`). **Two render sites converged together (caveat #1):** `crates/graphrefly-graph/src/describe.rs:229` (`NodeDescribe.deps` render) + `crates/graphrefly-graph/src/graph.rs:1055` (`edges_in` qualified-path resolution). **B.a sub-decision:** dropped `NameError::ReservedPrefix` variant + `_anon_` prefix checks in `validate_name` (`graph.rs:808`) + `is_valid_name` (`graph.rs:254`) + `From<NameError> for MountError` pattern collapse in `mount.rs:71` (value #14 vestigial-cleanup precedent — D253/D266/D267 lineage; empty-string can't collide with any user name, so the guard is vestigial post-B). **B.b sub-decision (caveat #4):** snapshot encode at `snapshot.rs:310` KEEPS the `_anon_<rawid>` marker — verified during the pre-lock check that `SnapshotError::UnresolvableDeps` Debug-format (`snapshot.rs:84` — `"unresolvable deps for node `{0}` (deps: {1:?})"`) preserves the marker verbatim through `{1:?}` (Vec<String> Debug). Converging snapshot to `""` would degrade the diagnostic to `(deps: ["", ""])` — indistinguishable collisions for multiple unresolvable anon deps. **Persistence-vs-presentation distinction is principled** — snapshot is decode-time diagnostic; describe is presentation wire-shape. **Caveat #3:** loses Rust within-describe anon-dep disambiguation; no verified consumer at lock time (no parity scenario asserts deps[] equality cross-arm at lock-time). Upgrade to bilateral monotonic counter or structured `{anon: position}` form remains forward-compatible non-breaking widening. **Override of D196 + the prior "v1 acceptable" deferral** per the `/porting-to-rs clear all... regardless if they need a valid consumer demand` directive (user-locked 2026-05-26). Test renames + assertion rewrites: `crates/graphrefly-graph/tests/describe.rs::unnamed_dep_surfaces_as_empty_string_per_d301` (caveat #2 — single test pins BOTH render sites via `nd.deps[0]` (describe) + `d.edges[*].from.is_empty()` (edges_in)) + `crates/graphrefly-graph/tests/gap_fills.rs::edges_cross_graph_dep_shows_as_empty_string_per_d301`. | TS pure-ts already conformant (`node.name ?? ""`); no presentation-layer (`src/`) consumer needs adjustment. Cross-arm parity scenario authoring (asserting BOTH `describe()` deps[] AND `edges()` render `""` for unnamed deps cross-arm) deferred to the per-Q follow-on. | ✅ Pure-ts arm conformant pre-D301 — no change needed. `ImplGraph.describe()` returns `unknown`; the wire-shape change is binding-transparent at the Impl-contract level. | ✅ **NATIVE substrate-side LANDED 2026-05-26** (graphrefly-rs `1f7c5c7`). The describe-output JSON (already exposed via `BenchGraph::describe_json` napi → `wrapper.js` `describeJson`) propagates the empty-string render unchanged through the existing napi surface; no new napi binding work needed. Cross-arm activation depends on user-gated `@graphrefly/native@0.1.0` republish (D265 hold-local) for the rust-arm parity-tests run to pick up the wrapper-layer-transparent substrate change. | ⚠ **OPEN (cross-arm scenario pending).** Substrate landed both arms (TS via pre-existing conformance; Rust via D301). Cross-arm parity scenario authoring tracked as per-Q follow-on; lift when a consumer surfaces or a future cross-impl describe-shape audit pass authors it. Archive when the cross-arm scenario lands + `@graphrefly/native` republishes. |
| 2026-05-17 (D5) → **TS LANDED 2026-05-18** → **NATIVE LANDED 2026-05-20** | **`setDeps`/`addDep`/`removeDep` — `Impl` widening (TS + native LANDED).** Dynamic dep-replacement (`project_rewire_gap`, TLA+-verified `wave_protocol_rewire_MC` 2026-05-03). 2026-05-18 (`/dev-dispatch`, user-locked "full rewire trio" + "pure-ts arm now, native follow-on"): the Phase-13.8 internal trio `_setDeps`/`_addDep`/`_removeDep` was **promoted to public** `setDeps`/`addDep`/`removeDep` on the `@graphrefly/pure-ts` substrate `Node` interface (rename, no behavior change — pre-1.0, `feedback_no_backward_compat`), and `ImplNode.setDeps/.addDep/.removeDep` added to the `Impl` contract. `set-deps.test.ts` is a **3-test cross-arm scenario** (rewire A→B + addDep/removeDep + self-dep rejection; gate dropped 2026-05-20). Logged per the "never widen `Impl` implicitly" rule. | parity `set-deps.test.ts` (pure-ts arm), `src/utils/graphspec` consumers | ✅ **LANDED 2026-05-18** — `ImplNode.{setDeps,addDep,removeDep}` in `types.ts`; `LegacyNode` impl + `implFnToNodeFn` adapter in `pure-ts.ts`; `Node` interface widened in `packages/pure-ts/src/core/node.ts`; `Graph._rewire/_addDep/_removeDep` internal calls + 4 rewire test files retargeted. | ✅ **LANDED 2026-05-20 (D263/D264, `/porting-to-rs` slice).** P1 strategy: added a generic TSFN-backed `register_user_derived(deps, fn) -> node_id` napi in `crates/graphrefly-bindings-js/src/operator_bindings.rs`; rerouted `impl.map` in `wrapper.js` through it with a JS-side closure cell. `setDeps`/`addDep`/`removeDep` methods added to `NativeNode<T>` (`wrapper.{js,d.ts}`): they call `Core::set_deps` and rebind the JS-side closure cell + update tracked deps list. `addDep`/`removeDep` compose on `set_deps` (no new `#[napi]` `add_dep`/`remove_dep` on `BenchCore`). `Core::set_deps` already exposes the substrate primitive; no graphrefly-core change. Discovery during HALT: the doc claim that `set_deps` was non-`#[napi]` was stale — it IS `#[napi]` (`core_bindings.rs:1552`). Trade-off: `impl.map` on rust arm is no longer `OperatorOp::Map` (loses M3 operator-specific optimization); other ops keep their OperatorOp paths. Native local commit pending; not pushed (D265 hold-local). | ✅ **TS ↔ native aligned 2026-05-20** — `skipIf(nativePending)` gate dropped in `scenarios/core/set-deps.test.ts`; 3 tests run cross-arm. |

## §2 — Substrate behavior / parity-coupled changes (no new symbol)

| Date | Change | Substrate site | Parity implication | Native-side status | Status |
|---|---|---|---|---|---|
| 2026-05-22 (Slice B C4 follow-on) | **`describe()` `orphaned: true` marker on cross-mount-orphaned `graph.derived` deps — DEFERRED (consumer-driven).** Slice B design session locked C4 closure as pure-A (JSDoc-only on `Graph.derived`, [`packages/pure-ts/src/graph/graph.ts:2351-2374`](../packages/pure-ts/src/graph/graph.ts)). Optional follow-on: annotate dep slots in `describe()` output whose construction-time path string no longer resolves under any mount as `{ orphaned: true }` so consumers can detect the trap programmatically (today: caller is responsible per JSDoc + three workarounds). **Why deferred (verification path):** distinguishing "construction-time-string-resolved dep whose mount is gone" from "anonymous direct-`Node`-ref dep that was never named" requires storing the construction-time path string on the derived node — that's a new per-`Node` field (substrate-widening). Both arms (`@graphrefly/pure-ts` + `@graphrefly/native`) would need to add the field and emit it through `describe()`. **Parity-observable** per `describe()`'s `Impl`-contract membership at `packages/parity-tests/impls/types.ts:190` (`describe(): unknown | Promise<unknown>`) — the semantic-shape parity contract would be broken if TS adds a marker Rust doesn't. | Substrate: `packages/pure-ts/src/core/node.ts` `NodeOptions` (new optional opaque slot for construction-time path); `packages/pure-ts/src/graph/graph.ts` `Graph.derived` (stores per-string-dep path on the resulting node); `Graph.describe()` resolution loop (per-dep, check `nodeToPath.has(dep.node)` AND surface stored path → emit `orphaned: true` when the construction-path no longer resolves anywhere in the tree). | Native obligation if marker ships: `@graphrefly/native` `NodeOpts` widened to carry the construction-time-path opaque slot; `BenchGraph::describe_json` emits the `orphaned: true` marker on the same condition. `Impl` widening if exposed on the typed contract (currently `describe()` returns `unknown`, but the marker would become a documented field consumers depend on). | ⏳ **Deferred — no consumer pressure today.** Reopen when an external consumer files a foot-gun report against C4's by-design freeze and needs programmatic detection (the JSDoc workarounds (a) direct-Node deps, (b) post-mount-change `Node.setDeps`, (c) pre-`graph.remove` invariant check handle most cases). | Pure park; consumer-driven only. C4 itself archived 2026-05-22 (id `c4-path-based-graph-derived-cross-mount-by-design-resolved-2026-05-22`). |
| 2026-05-22 (D279 — E-ii.3, `/porting-to-rs` Commit 2 of E-cluster slice) | **`Graph::describe()` sentinel-vs-JSON-null disambiguation — Rust converges to TS's existing `sentinel?: boolean` + optional `value?` shape (TS already canonical; Rust catching up).** Side-quest verification of the original /porting-to-rs Q3 ("`DescribeValue::Rendered(Value::Null)` JSON-indistinguishable from `Option::None`") confirmed: TS `meta.ts:9-27` declares `DescribeNodeOutput { …, value?: unknown, sentinel?: boolean, … }`; `meta.ts:303` sets `out.sentinel = true` when `node.status === "sentinel"`; `out.value = node.cache` (`undefined` for sentinel cache → `JSON.stringify` drops the key). Serialized shape: sentinel → `{ "sentinel": true }` (no `value` key); legitimate JSON-null user value → `{ "value": null }` (no `sentinel` key); any value → `{ "value": <v> }`. **Rust pre-D279 diverged** — `NodeDescribe.value: Option<DescribeValue>` always serialized as `"value":null` for sentinel (no `skip_serializing_if`); `Rendered(serde_json::Value::Null)` produced identical JSON, making the two cases JSON-indistinguishable. Rust DOES already always emit `status: "sentinel"` (which TS does NOT by default in describe minimal/standard modes), so cross-impl shape was *similar but not identical*. **D279 converges Rust to TS shape:** adds `sentinel: Option<bool>` field with `#[serde(skip_serializing_if = "Option::is_none")]` (set to `Some(true)` when `status == NodeStatus::Sentinel`); adds `skip_serializing_if` to `value: Option<DescribeValue>` (omit when sentinel cache). Same precedent as D-WAL-fmt (2026-05-17, line 65) where TS converged toward Rust's pre-existing `format_version`. | `crates/graphrefly-graph/src/describe.rs` `NodeDescribe` struct (`value` + new `sentinel` field) + `describe_of` (set `sentinel = Some(true)` when `status == NodeStatus::Sentinel`). TS substrate `packages/pure-ts/src/core/meta.ts` `DescribeNodeOutput` + `describeNode` (already canonical — no change). | **Parity obligation:** the `describe()` JSON output shape is parity-coupled when any consumer reads it cross-arm. Today no parity scenario asserts on the sentinel-vs-null disambiguation cross-arm; once D279 lands, a consumer can write a single deserializer that handles both arms. No `Impl` widening needed — `Graph::describe()` is NOT on `packages/parity-tests/impls/types.ts` `Impl` interface today (TS describe is `Graph.describe()`, Rust describe is `BenchGraph::describe_json()` via napi — different shapes structurally). When `describe()` parity scenarios are added, they'll bake in the converged shape. **Methodology pattern:** TS canonical exists, Rust catches up. Side-quest-first verification per anti-pattern-#4 — verify TS shape BEFORE designing Rust convergence (avoids preemptively reinventing an option TS doesn't use). | ✅ **TS already canonical (no change).** | TS ✅ pre-conformant; Rust ⏳ D279 (Commit 2 of E-cluster slice — about to land). |
| 2026-05-22 (`/qa` Slice A reverted) → ✅ **TS LANDED 2026-05-23 (D282)** | **✅ `batch()` throw-rollback: TS-arm converged to canonical R4.3.2 (D282, 2026-05-23). Direction: TS converges to Rust** — Rust substrate is pre-conformant via `discard_wave_cleanup` + `restore_wave_cache_snapshots` (`graphrefly-rs/crates/graphrefly-core/src/batch.rs:3693`); TS gains the snapshot + paired-rollback-hooks shape via `_preBatchSnapshot` (9-field; cache/status/versioning/hasCalledFnOnce/teardownDone/3 wave flags/replayBufferLength — mirror at the invariant level "post-rollback observable state ≡ pre-batch observable state", NOT the field-name level) + new `batchHooks: Array<{ flush, rollback }>` registry. Slice A's failed attempt is preserved in `archive/optimizations/resolved-decisions.jsonl` id `slice-a-batch-throw-rollback-reverted-2026-05-22` (28 reviewer findings); D282 closes E1/E3/E5/E7/E15 + Blind#11 via the parity scenarios. Mint authority: `docs/rust-port-decisions.md` D282. **Original 🔴 framing preserved below for the design record:** canonical `docs/implementation-plan-13.6-canonical-spec.md:1183` **R4.3.2 — Throw rollback** specifies: "per-node flush hooks fire (so nodes clear their pending state), all drainPhase queues are cleared, throw re-propagates." Parenthetical "(so nodes clear their pending state)" makes intent explicit: clear, **no downstream delivery**. **Rust arm IS conformant** — `~/src/graphrefly-rs/crates/graphrefly-core/src/batch.rs:3693` `discard_wave_cleanup()` takes `pending_notify` without firing sinks, restores wave cache snapshots via `restore_wave_cache_snapshots` (cache reverts to pre-batch value), drops `deferred_cleanup_hooks`/`pending_wipes`/deferred handle releases, releases handle refcounts lock-released. **TS arm DIVERGED** — empirical `/tmp/probe-prefix.ts` against pre-fix HEAD shows subscriber wave on throw: `[DIRTY, DATA(42)]` (not `[]`); downstream fn runs once; `down.cache=42` settled (not pre-batch value `0`). Root cause: at the catch-path point where `flushHooks.splice(0)` fires (`packages/pure-ts/src/core/batch.ts:186-193`), `batchDepth===0` (decremented) and `flushInProgress===false`, so `isBatching()` returns false → `downWithBatch` dispatches synchronously instead of enqueueing. Pre-existing impl bug; predates Bug-2-coalescing-2026-04-17. R4.3.5 (`mutate(act,{frame:"transactional"})`) explicitly depends on R4.3.2's "discards reactive emissions on throw" — so the TS transactional-frame primitive was also-affected (no-op on throw beyond closure-state). **Subscriber wire shape on throw is parity-observable** = cross-track-coupled change. | Canonical `docs/implementation-plan-13.6-canonical-spec.md:1183` **R4.3.2 — Throw rollback** specifies: "per-node flush hooks fire (so nodes clear their pending state), all drainPhase queues are cleared, throw re-propagates." Parenthetical "(so nodes clear their pending state)" makes intent explicit: clear, **no downstream delivery**. **Rust arm IS conformant** — `~/src/graphrefly-rs/crates/graphrefly-core/src/batch.rs:3693` `discard_wave_cleanup()` takes `pending_notify` without firing sinks, restores wave cache snapshots via `restore_wave_cache_snapshots` (cache reverts to pre-batch value), drops `deferred_cleanup_hooks`/`pending_wipes`/deferred handle releases, releases handle refcounts lock-released. **TS arm DIVERGES** — empirical `/tmp/probe-prefix.ts` against pre-fix HEAD shows subscriber wave on throw: `[DIRTY, DATA(42)]` (not `[]`); downstream fn runs once; `down.cache=42` settled (not pre-batch value `0`). Root cause: at the catch-path point where `flushHooks.splice(0)` fires (`packages/pure-ts/src/core/batch.ts:186-193`), `batchDepth===0` (decremented) and `flushInProgress===false`, so `isBatching()` returns false → `downWithBatch` dispatches synchronously instead of enqueueing. Pre-existing impl bug; predates Bug-2-coalescing-2026-04-17. R4.3.5 (`mutate(act,{frame:"transactional"})`) explicitly depends on R4.3.2's "discards reactive emissions on throw" — so the TS transactional-frame primitive is also-affected (no-op on throw beyond closure-state). **Subscriber wire shape on throw is parity-observable** = cross-track-coupled change. | `packages/pure-ts/src/core/batch.ts` `batch()` finally / `_flushBatchPending`; `packages/pure-ts/src/core/node.ts` `_dispatchOrAccumulate` (mirror Rust's wave-entry cache-snapshot + drop-without-dispatch shape). | **TS obligation:** converge to R4.3.2 — `batch()` throw must clear `_batchPendingMessages` on touched nodes WITHOUT dispatching downstream, AND restore each touched node's cache to its pre-batch value (mirror Rust's `restore_wave_cache_snapshots`). Parity scenarios authored FIRST at `packages/parity-tests/scenarios/core/batch-throw-rollback.test.ts` (full subscriber-stream assertions cross-arm; real diamond topologies; `partial:false` first-wave consumers; terminal-mid-batch; equals-absorbed; drain-time-nested-batch A4-carve-out). Inner-batch-inside-`flushInProgress` skip-rollback explicitly preserved per R4.3.2's "cross-language decision A4" carve-out. | ✅ Rust conformant (`discard_wave_cleanup` + `restore_wave_cache_snapshots`); no native work needed. Slice A's failed attempt (`touchedInvalidators` registry layering INVALIDATE on top of the already-delivered wave → "settle-to-sentinel" semantic with intermediate observable side effects) **reverted 2026-05-22**; preserved in `archive/optimizations/resolved-decisions.jsonl` id `slice-a-batch-throw-rollback-reverted-2026-05-22` with the 28 reviewer findings (Blind#1–#11 + E1–E17). | TS ✅ **LANDED 2026-05-23 (D282)** — per-node `_preBatchSnapshot` + paired flush/rollback hooks via new `batchHooks` registry; 12 D282 parity scenarios at `packages/parity-tests/scenarios/core/batch-throw-rollback.test.ts` green on TS arm (rust arm `test.runIf`-gated until napi `batch_run` ships per the §1 D282 widening row above). Native ✅ conformant (substrate `discard_wave_cleanup`); binding ⏳ pending napi `batch_run` for the `Impl.batch(fn)` cross-arm gate to lift. Archive both rows together on the next sweep, post-napi-batch landing. **D297 AMENDMENT (2026-05-26):** "Native ✅ conformant" was scoped to cache-rollback. Full R4.3.2 status-snapshot completeness is an incremental catch-up chain on the Rust side: D291 (terminal slots) + D297 (`has_received_teardown`) landed; Items 2 (pause_state) + 3 (replay_buffer drain) + 3b (replay_buffer push) deferred per consumer-pressure gates. Pure-ts is canonical (D282 `_preBatchSnapshot` already covers all 4 + a pause sub-snapshot). |
| 2026-05-22 (Phase 13.K dispatch D) | **`meta.attachTarget` describe-time Node-ref → path-string resolution + `explainPath` soft-forward-edge BFS (TS lands; native lazy).** New `attachEdgeMeta(target: Node)` helper (`src/base/meta/attach-edge-meta.ts`) lets factories declare a "soft forward edge" into a target Node — visible to `Graph.describe()` (resolves to qualified path string via the existing `nodeToPath` map, same mechanism as `deps`) and `explainPath` (BFS walks soft predecessors alongside declared deps; outgoing soft hops carry `via_attach_edge: true` on the `CausalStep` and pretty-print as `↝` with a `(via attach-edge)` suffix). Wave propagation is UNCHANGED — soft edges are explainability-only. `topicBridge` migrated as the reference consumer (replaces a free-floating `source.subscribe → target.publish` helper with `this.effect("attach", ["output"], …, { meta: { ...attachEdgeMeta(target.events) } })`); new COMPOSITION-GUIDE-PATTERNS §48 documents the convention. **No `Impl`-contract widening** — `explainPath` / `CausalStep` are pure-ts substrate types not on `packages/parity-tests/impls/types.ts`; substrate-level `meta.attachTarget` is a presentation-track meta-snapshot detail that flows through the generic `describe()` shape. | `packages/pure-ts/src/graph/graph.ts` describe loop (resolves `meta.attachTarget` Node ref → qualified path string) + `packages/pure-ts/src/graph/explain.ts` (`buildSoftReversePred` + augmented `bfsShortestPath` + `via_attach_edge` step marker + pretty-print arrow `↝`). | Native parity: when a TS-side consumer eventually inspects `describe().nodes[path].meta.attachTarget` cross-impl AND the same factory is mirrored under `@graphrefly/native`, the native describe path must (a) detect a Node-shaped value in `meta`, (b) resolve via the equivalent reverse-lookup (Rust's `BenchCore`/`BenchGraph` already maintain a node→path map for describe), (c) return a path string in the snapshot. Today no consumer exercises this cross-arm (no parity scenario uses `topicBridge` + `describe`; explain is not on `Impl`). Trigger to land native side: first parity scenario that asserts on `meta.attachTarget` in a `describe()` snapshot. | ⏳ **Native lazy / deferred per consumer-pressure (D196).** Today's only consumer (`topicBridge`) is presentation-only; explain is pure-ts-only. If/when a substrate or shared utility factory adopts `attachEdgeMeta` AND has a cross-arm parity scenario, the native `BenchGraph::describe`/`describe_json` will need the matching Node-ref-shape detection + resolution. Logged so the obligation is explicit when that pressure surfaces. | TS ✅ landed 2026-05-22 (Phase 13.K dispatch D); native ⏳ deferred — no consumer pressure today. |
| 2026-05-22 (B4 / D275) | **`apply_wal_frame` now handles `graph.mount` + `graph.unmount` (Rust ships ahead of TS).** Pre-B4 the Rust `apply_wal_frame` (`crates/graphrefly-storage/src/graph_integration.rs`) had `// graph.mount, graph.unmount — deferred (Phase 14.6+)` and the TS `applyWalFrame` (`packages/pure-ts/src/graph/graph.ts:6717`) had the same gap. **Decompose was always wired on both sides** — only the apply side ignored mount/unmount frames, so a snapshot+WAL round-trip silently dropped subgraph topology changes. B4 wires the Rust apply arms with idempotent skips (`graph.child_names().iter().any(|n| n == path)` guard). **No new `WalTag` variants** — `WalTag` stays the unit-struct discriminator; payload-kind is via the `change.kind` JSON field same as `graph.add` / `graph.remove`. 3 cargo regression tests in `crates/graphrefly-storage/tests/graph_integration.rs`. **No `Impl` widening** (storage tier internals; the napi `attach_storage` is F18-deferred — no consumer scenario yet). | Rust: `crates/graphrefly-storage/src/graph_integration.rs` `apply_wal_frame` (`Lifecycle::Spec` match) — new `"graph.mount"` / `"graph.unmount"` arms. | **TS obligation:** mirror in `packages/pure-ts/src/graph/graph.ts` `applyWalFrame` (line 6717 stub-out `// graph.mount, graph.unmount, schema.upgrade — Phase 14.6 deferred`). TS decompose side already emits the matching `graph.mount` / `graph.unmount` payloads (`graph.ts:6633,6641`), so the gap is also apply-side-only on TS. Recommend a `/dev-dispatch` slice mirroring B4's Rust apply arms + 3 vitest scenarios. Until then, Rust ↔ TS WAL-replay diverges on graphs with subgraphs (Rust replays mount/unmount; TS silently no-ops). **Cross-impl WAL byte format is unchanged** — only the apply behavior differs; a Rust-written WAL replayed on TS-side will silently skip the mount frames (forward-compatible: TS reader sees an unknown `kind` in the existing default-arm and falls through). | ✅ **LANDED 2026-05-22 (B4).** Rust ships the apply arms. **TS LANDED 2026-05-22 (`/dev-dispatch`)** — `applyWalFrame` `Lifecycle::Spec` switch now has `case "graph.mount"` (walks the qualified path; auto-creates missing intermediate mounts via per-segment idempotent skip mirroring Rust's `child_names()` guard) + `case "graph.unmount"` (walks to parent, idempotent `remove(leaf)` only when leaf is mounted). Module-local `_mounts(g)` accessor casts through `Graph._mounts` (matches `fromSnapshot`'s own internal access pattern). 4 vitest scenarios in `packages/pure-ts/src/__tests__/extra/storage-wal.test.ts` (top-level mount; unmount; double-mount-then-double-unmount idempotency; nested `outer::inner` auto-create-parent). Rust ↔ TS WAL-replay no longer diverges on graphs with subgraphs. **No native binding work in this slice** — F18 (`attach_storage` napi) stays deferred; the binding-layer surfaces this through the existing `graph.snapshot` + `graph.restore_snapshot` napi paths. | Rust ✅ B4 landed; TS ✅ landed 2026-05-22; binding ⏳ exposed via existing graph napi paths only. Archive on next sweep. |
| 2026-05-22 (M4.E1 / D276) | **`Graph::snapshot` + `Graph::from_snapshot` now round-trip cross-mount derived deps (Rust ships ahead of TS).** Original M4.E1 entry framed this as a decode-only gap; pre-implementation premise verification surfaced **bidirectional breakage** — pre-D276 `snapshot_of` fell through to `format!("_anon_{}", dep_id.raw())` for any cross-mount dep, destroying the reference at serialization time. Fix: (1) **encode** — tree-wide `id_to_tree_path: HashMap<NodeId, String>` (`build_id_to_tree_path`) + `absolute_to_owner_relative` converter; cross-mount deps serialize as owner-relative paths (`..::name`, `child::name`, `..::sibling::name`) using the syntax `Graph::try_resolve` natively accepts (Slice V3 path machinery); same-graph deps collapse to bare local names (back-compat). (2) **decode** — replace per-graph recursive `hydrate_subgraph`/`hydrate_nodes` with **tree-wide four-pass hydration**: Pass 0 mounts all subgraphs + builds `graph_map`; Pass 1 creates all state nodes; Pass 2 collects all non-state into ONE shared retry-loop queue resolving via `entry.owner_graph.try_resolve(dep_name)`; Pass 3 status restore. 6 new cargo tests in `crates/graphrefly-graph/tests/snapshot.rs` (encode + round-trip in both directions + 3-level deep chain + back-compat bare-name shape). **No `Impl` widening** — `Graph::snapshot`/`from_snapshot` shapes unchanged; on-disk snapshot format gains owner-relative path encoding but the JSON `deps: Vec<String>` shape is the same. | Rust: `crates/graphrefly-graph/src/snapshot.rs` (`snapshot_of` → `build_id_to_tree_path` + `snapshot_of_with_tree_paths` + `absolute_to_owner_relative`; `from_snapshot` → `mount_subgraphs_recursive` + `create_state_nodes_recursive` + `collect_derived_recursive` + `create_derived_with_retry` + `apply_status_recursive`). | **TS obligation:** **same bidirectional gap exists in pure-ts** per the original M4.E1 entry ("The TS impl has the same limitation"). Mirror D276 in `packages/pure-ts/src/graph/graph.ts` `snapshotOf` + `fromSnapshot`: same tree-wide `idToTreePath` + `absoluteToOwnerRelative` on encode; same four-pass tree-wide hydration on decode; same 6 vitest scenarios. **Cross-impl on-disk shape:** the owner-relative path syntax (`..::name`, `child::name`) IS Slice V3's `try_resolve` path syntax — so a Rust-encoded snapshot with cross-mount deps is byte-compatible with a future D276-aware TS decoder. Until TS mirrors, Rust ↔ TS snapshots with cross-mount deps diverge: a TS-produced snapshot drops cross-mount refs as `_anon_<rawid>`; a Rust-produced snapshot encoded post-D276 has owner-relative paths that pre-D276 TS would reject as `UnresolvableDeps`. | ✅ **LANDED 2026-05-22 (M4.E1 / D276).** Rust ships the bidirectional fix. **TS premise VERIFIED DIVERGENCE-FREE 2026-05-22 (`/dev-dispatch`)** — the "same bidirectional gap exists in pure-ts" claim above was based on the original (pre-bidirectional-reframe) M4.E1 entry and is **stale**. Verification: 3 regression-pinning scenarios in new `packages/pure-ts/src/__tests__/graph/snapshot-cross-mount.test.ts` exercising (a) cross-mount sibling round-trip with live propagation through the rebuilt edge, (b) 3-level deep chain across three mounts, (c) same-graph bare-local-name back-compat — all 3 pass against current `main` with **zero code change**. Root reason TS doesn't have the Rust bug: the TS encode path is **tree-wide via `describe`'s `nodeToPath` map** (`packages/pure-ts/src/graph/graph.ts:3282`), built by a BFS over `mounts` + `_nodes` from the snapshotted root, so cross-mount deps already serialize as fully-qualified-from-root paths (e.g. `"c1::a"`). The decode side at `fromSnapshot:5421` keys `created` by the same qualified path, so flat `created.has(dep)` resolves cross-mount sibling refs by construction. Rust's pre-D276 single-graph `snapshot_of` had no equivalent tree-wide id→path map (fell through to `_anon_<rawid>`); the TS encoder structurally bypasses that class. **Anonymous-dep fallback divergence kept on file** — per user direction Q3, TS keeps `?? d.node.name ?? ""` (empty-string fallback) for deps not in `nodeToPath`; full `_anon_<rawid>` convention adoption deferred pending a real cross-arm scenario for operator-internal anonymous deps (would warrant a separate D-number covering snapshot + describe + explainPath + reserved-name validation together, not a piecemeal snapshot-only adoption). **Sub-concern surfaced 2026-05-22 by /qa (F5): the current TS empty-string `""` fallback is structurally WORSE than Rust's `_anon_<rawid>` — an empty-string dep silently collides with any future bare-name dep whose path resolves to `""`, producing incorrect topology with no error; Rust's `_anon_<rawid>` would fail decode loudly. The future `_anon_` D-number MUST scope the empty-string fallback as part of its work (replace `?? ""` with `?? \`__anon_${stableId}__\`` so failures are loud, not silent rewires) — do NOT mint a piecemeal "snapshot-only fallback rename" slice; pair the fix with the full convention adoption above.** Regression pin remains so a future refactor of `Graph.snapshot` / `Graph.fromSnapshot` can't regress the bidirectional cross-mount contract. **No native binding work in this slice** — `Graph::snapshot` / `Graph::from_snapshot` napi signatures unchanged. | Rust ✅ M4.E1 landed; TS ✅ verified divergence-free 2026-05-22 (no code change needed, 3 regression pins added); binding ✅ N/A (transparent). Archive on next sweep. |
| 2026-05-17 (R2.6.0 self-pause) | **PAUSE/RESUME §2.6 self-pause contract pinned — Option A (`GRAPHREFLY-SPEC` R2.6.0).** **Default `pausable: true`** gating is dep-wave-scoped only: a self-paused **leaf source** (no deps) doing its own direct `down([[DATA, v]])` has no dep wave to coalesce → DATA is **delivered immediately** (cache advances at `down()` time, RESUME replays 0, no PAUSE tier synthesized). `"resumeAll"` is NOT equivalent — it explicitly buffers & replays the leaf source's direct push (default-vs-resumeAll boundary raw-API-pinned in `packages/pure-ts/src/__tests__/core/protocol.test.ts` "R2.6.0 boundary"). **No new symbol; no API/signature change** — clarifies existing `pause`/`resume`/`down` behavior. pure-ts is pre-conformant in default mode by construction (`_pauseBuffer` allocated only for `"resumeAll"`; the default-mode gate lives in the dep-wave path a leaf source never traverses — `packages/pure-ts/src/core/node.ts`). | `packages/pure-ts/src/core/node.ts` (pause lock-set + `_emit`/`_settleWave`) — **no change** (pre-conformant) | `@graphrefly/native` must converge to R2.6.0 **default mode**: a self-paused leaf source's own `down(DATA)` delivers **immediately**, no PAUSE tier surfaced, no post-pause reorder, `cache` advances at `down()` time, RESUME replays 0. **✅ CONVERGED 2026-05-17 (user expanded scope — overrode the original "defer native" decision).** Root cause: `graphrefly-rs` `crates/graphrefly-core/src/batch.rs` `Core::queue_notify` `mode_buffers_tier3` had `PausableMode::Default => rec.is_state()` — a state-node's direct self-emit was buffered+replayed, i.e. the **`ResumeAll`** contract mis-applied to **Default**. Surgical fix `PausableMode::Default => false` (state nodes have no fn/dep settle pipeline to gate; their external emit is outside pause scope per R2.6.0). graphrefly-rs local commit **`ab133d7`** (6 files, +172/−5; **NOT pushed** — push auto-publishes `@graphrefly/native`; push gated on user). Gate green (832/832 nextest, clippy+fmt clean, `#![forbid(unsafe_code)]` intact, hot single-thread floor preserved — change strictly removes an `is_state()` call). napi `.node` rebuilt into the linked parity arm. graphrefly-rs tree was clean (B-2 Step 2c NOT started) → no porting collision; pinning parity first protects R2.6.0 against the future Step 2c rework. | ✅ RESOLVED (native converged, commit `ab133d7` local-only; `pause-resume.test.ts` runs the SAME assertion cross-arm, both green). Archive on next /qa sweep. **Open follow-up: graphrefly-rs `ab133d7` is unpushed by design** — user decides when to push (auto-publishes `@graphrefly/native`). | TS ✅ pinned 2026-05-17 (spec R2.6.0 + parity default-mode pin + pure-ts raw boundary test); native ✅ converged `ab133d7` (local), parity cross-arm green |
| 2026-05-17 (D-WAL-fmt) | **`WALFrame<T>.format_version` — TS spec-conformance, converged toward existing Rust.** Added `readonly format_version: number` + `WAL_FORMAT_VERSION = 1` to `packages/pure-ts/src/extra/storage/wal.ts`; production frames carry it. Rust **already** had `#[serde(default = "default_format_version")] u32` (`graphrefly-storage/src/wal.rs`) — TS converged toward Rust, no new Rust obligation. **Load-bearing parity invariant:** `format_version` is **EXCLUDED from the checksum body** — TS `canonicalFrameBody` stays `{t,lifecycle,path,change,frame_seq,frame_t_ns}`, mirroring Rust `wal_frame_checksum`'s `ChecksumBody` (which also omits it). A codec bump must NOT invalidate stored checksums; both impls' WAL checksums stay byte-identical. | `packages/pure-ts/src/extra/storage/wal.ts` `WALFrame`/`canonicalFrameBody` | When the storage parity arm activates, frame fixtures must carry `format_version` (Rust already does) AND the cross-impl checksum-byte-equivalence scenario must assert the checksum is invariant under a `format_version` change (the exclusion is the contract, not an accident). | ✅ Rust already ships `format_version` + checksum-exclusion (`ChecksumBody`); no change needed. | ✅ **TS ↔ native aligned** (TS landed 2026-05-17, +2 regression tests; native pre-existing). Archive on next sweep. |
| 2026-05-16 | **memo:Re P0 — `appendLogStorage` `flush()` durability fix.** No-`debounceMs` tier schedules a microtask-chained `doFlush` per `appendEntries` wave; `flush()` early-returned when `pending` was empty and resolved before the in-flight `flushChain` drained → only wave #1 durable (silent data loss). Fix: `flushNow()` returns the outstanding `flushChain` when `pending` is empty. **No new symbol; no API/signature change** — `flush()` now honors its existing durability contract. | `packages/pure-ts/src/extra/storage/tiers.ts` `appendLogStorage.flushNow` | Rust `@graphrefly/native` `appendLogStorage`/WAL-flush equivalent must mirror **two** semantics, not just durability: **(1) durability-on-resolve** — `flush()` (and tier shutdown/`destroyAsync` drain) awaits all in-flight chained/buffered writes (TS bug was a chained-microtask artifact; Rust must not have the analogous "flush returns before queued writes commit" gap); **(2) error-surfacing (QA/D3)** — post-fix TS `flush()` *rejects* if a prior in-flight chained write failed (was: silently resolved); the Rust arm must make the same reject-vs-swallow decision and test the rejection path, not only the happy path. **(3) F9 debounced-flush driver (QA, now FIXED — was a divergence-avoidance note, now a positive parity obligation):** the `appendLogStorage` **tier** still has no internal timer, but `attachStorage` now drives each debounced tier's `flush()` from a **reactive timer source** (`fromTimer(d,{period:d})`) + a final drain on detach/teardown. The Rust `@graphrefly/native` reactive-log `attach_storage` equivalent must mirror this: drive debounced-tier flush from the **Rust reactive timer source** (NOT an internal tier timer, NOT a raw OS timer in the reactive layer) + drain on teardown. **(4) `rollback()` strong semantic (QA, now FIXED):** TS added a `rollbackEpoch` generation token — `rollback()` bumps it + clears `pending`; `doFlush` captures the epoch at schedule time and skips at entry if it advanced, so in-flight chained writes scheduled pre-rollback are discarded (best-effort: a `backend.write` already past the check can't be un-sent). The Rust arm must mirror the epoch/abort semantic (not just `pending`-clear). Verify all four in the M4/M5 storage parity arm. | ✅ **NATIVE LANDED 2026-05-21 (D268).** All 4 semantics verified/mirrored: (a) **durability-on-resolve** — structurally satisfied: Rust `AppendLogStorage::flush` is sync (no async chained-microtask hazard); when `flush()` returns `Ok`, data has hit the backend; (b) **error-surfacing** — structurally satisfied: returns `Result<(), StorageError>` from `flush`/`append_entries`; (c) **F9 reactive-timer driver** — `AttachStorageHandle` provides per-flush manual drive; the binding-layer `attach_storage` reactive-timer wiring follows the established `attach_snapshot_storage` pattern (drive flush via `StorageHandle::flush_all` from a `temporal::interval` timer source). Pattern documented in `crates/graphrefly-storage/src/graph_integration.rs`; same applies to reactive-log attach storage; (d) **rollback epoch** — D268: `AppendLogStorage.rollback_epoch: AtomicU64`; `rollback()` bumps epoch + clears pending; `flush()` captures the epoch at start, checks before each per-bucket write, aborts on bump (concurrent rollback-during-flush window). 2 new cargo tests in `crates/graphrefly-storage/tests/tier.rs`: sequential rollback-then-flush; **concurrent rollback-during-flush aborts remaining writes** (BarrierBackend-based deterministic interleave). 31/31 tier tests green. | TS ✅ landed 2026-05-16; native ✅ rollback epoch D268, durability+reject structurally satisfied, F9 driver pattern established. Archive on next sweep. |
| 2026-05-16 | **memo:Re P1 — `appendLogStorage` `mode: "append" \| "overwrite"` option.** Additive `AppendLogStorageOptions.mode` (default `"append"` = existing read-merge accumulate; `"overwrite"` = snapshot, replace key per flush, skip backend read). `Impl` carries `appendLogTier(backend, opts?: TierOpts)` (`types.ts:504`) so `TierOpts` is parity-coupled. | `packages/pure-ts/src/extra/storage/tiers.ts` `AppendLogStorageOptions` / `doFlush` | Rust `@graphrefly/native` `appendLogTier` must add `mode` to its `TierOpts` and implement the overwrite branch (no read-merge). **QA hardening (2026-05-16):** the tier now also **exposes `.mode`** on `AppendLogStorageTier`, and `reactiveLog.attachStorage` **throws** if handed an `"overwrite"` tier (delta-shipping into overwrite silently truncates — the memo:Re-P0 failure class). Native parity: the native append-log tier must expose `mode` and the native `attach_storage` must reject an overwrite tier with an equivalent error. Add both an `overwrite`-flush scenario AND an `attachStorage(overwrite)`-rejects scenario to the storage parity arm. | ✅ **NATIVE LANDED 2026-05-21 (D269).** `AppendLogMode { Append, Overwrite }` enum added to `graphrefly_storage::tier`; `AppendLogStorageOptions.mode` field; `AppendLogStorage::flush()` Overwrite branch (no read-merge — bucket IS the full contents). `AppendLogStorageTier::mode()` accessor exposes the configured mode (Rust analog of the TS `.mode` getter). Napi binding: `BenchValueAppendLogTier::create` accepts a `mode_str: Option<String>` ("append"/"overwrite"); `BenchValueAppendLogTier::mode` (`#[napi(getter)]`) returns the string. Wrapper `impl.storage.appendLogTier` accepts `opts.mode` and exposes a `mode` getter. Parity `TierOpts.mode` + `ImplAppendLogTier.mode` widened (D269). Cargo tests: `append_log_overwrite_mode_replaces_bucket_per_flush`, `append_log_mode_accessor_reports_configured_mode`. **`attach_storage` rejection** — substrate `ReactiveLog::attach_storage` doesn't currently reject overwrite tiers; documented in `porting-deferred.md` as a follow-on (the binding-layer attach_storage napi is also deferred per F18, so the rejection point shifts when that lands). | TS ✅ landed 2026-05-16; native ✅ mode + overwrite branch landed (D269). Archive on next sweep. |
| 2026-05-17 | **`appendLogStorage.loadEntries` windowed cursor pagination.** Previously ignored `opts.cursor`/`opts.pageSize` and always returned the whole log + `cursor: undefined`. Now: `pageSize` set → returns `[start, start+pageSize)` + a forward-only `AppendCursor` (`undefined` = no more); `pageSize` undefined/≤0 → whole-tail-from-cursor + `cursor: undefined` (a bare `loadEntries()` is byte-for-byte the prior behaviour — back-compat held). Decoding short-circuits one entry past the window (`want = start+pageSize+1`) so a *partitioned* multi-key log skips decoding keys beyond the window; a single-key log still decodes its one blob (per-key codec-blob model — pagination bounds the *consumer's* per-page working set, not the tier's per-key decode). **No new symbol** — `AppendCursor`/`pageSize`/`AppendLoadResult` already existed on the `AppendLogStorageTier` interface; only the previously-ignored params became honored. `Impl` carries `appendLogTier(backend, opts?)` so the `loadEntries` *effective contract* is parity-coupled. | `packages/pure-ts/src/extra/storage/tiers.ts` `appendLogStorage.loadEntries` | Rust `@graphrefly/native` `appendLogStorage`/`appendLogTier` `loadEntries` (or its WAL `listByPrefix`/replay equivalent) must mirror: (1) honor `cursor.position` + `pageSize` with the same `[start, start+pageSize)` window + forward-only-cursor / `undefined`-means-done protocol; (2) bare `loadEntries()` (no opts) returns the whole log + `cursor: undefined` (back-compat parity); (3) `pageSize`-larger-than-log and cursor-past-end both yield empty/partial page + `cursor: undefined`; (4) deterministic key ordering across `backend.list` so the flattened `position` offset is stable across calls. Add a windowed-pagination + back-compat + cursor-walk scenario to the M4/M5 storage parity arm. | ✅ **NATIVE LANDED 2026-05-21 (D269).** New types in `graphrefly_storage::tier`: `AppendCursor { position: u64, tag: Option<u64> }`, `LoadEntriesOpts<'a> { cursor, page_size, key_filter }`, `AppendLoadResult<T> { entries, cursor }`. Trait method `AppendLogStorageTier::load_entries(opts)` returns `AppendLoadResult<T>`. Pre-D269 `load_entries(key_filter)` shape preserved as `load_entries_all(key_filter)` for the back-compat call sites (existing tests + binding swept). Implementation: deterministic lex-ASC backend key sort; flatten by-key entry order; slice `[start, start+pageSize)`; cursor advances by exactly `pageSize`; short-circuits decoding past `start + page_size + 1` to bound consumer working set. Napi binding widened: `BenchValueAppendLogTier::load_entries(key_filter, cursor_position, page_size)` — paginated mode returns `{ "entries": [...], "cursor": { "position": N } \| null }` JSON object; bare (no pagination args) returns the bare entries array (byte-for-byte back-compat). Wrapper exposes both `loadEntries(keyFilter)` (back-compat array shape) and `loadEntriesPaged({ keyFilter, cursor, pageSize })` (paginated shape). 4 new cargo tests verify: page+cursor-walk; bare back-compat; cursor-past-end empty page; pageSize>log-size. | TS ✅ landed 2026-05-17; native ✅ D269 (windowed cursor pagination + back-compat preserved via `load_entries_all`). Archive on next sweep. |
| 2026-05-16 | **memo:Re P2 — `ReactiveLogBundle.attach(upstream, { skipCachedReplay? })`.** Additive optional 2nd arg; drops the subscribe-handshake replay (cached value OR full replay buffer), keeps live emissions. `Impl` carries `attach(upstream): Promise<UnsubFn>` (`types.ts:381`) so the signature is parity-coupled. | `packages/pure-ts/src/extra/data-structures/reactive-log.ts` `attach` | Rust `@graphrefly/native` `attach` must accept `skipCachedReplay` and suppress the on-subscribe replay (NOT subsequent live emits). **QA hardening (2026-05-16):** the suppression is NOT a synchronous-window flag — `defaultOnSubscribe`'s replay is `downWithBatch`-split by tier (`[START]` phase-1 sync, replay `[DATA…]` phase-2 sync-or-`batch()`-deferred). TS now skips the **first DATA-bearing delivery**, gated on `upstream.cache !== undefined` (so a cold upstream's first live emit is NOT dropped), robust to batch-deferral. Native must replicate this exact contract (gate on cache-present; drop the one replay slice incl. a multi-value replay-buffer; survive being called inside a batch). Parity scenarios: `skipCachedReplay` with (a) cached single value, (b) `replayBuffer:N`, (c) cold upstream, (d) `attach` inside `batch()`. | ✅ **NATIVE LANDED 2026-05-21 (D270).** New `graphrefly_structures::AttachOptions { skip_cached_replay: bool }` struct. New method `ReactiveLog::attach_with_options(core, upstream, read_value, opts)`; the no-options `attach()` delegates with `AttachOptions::default()` for back-compat. Implementation: `Core::cache_of(upstream)` checked at attach time; suppression gated on `cache != NO_HANDLE`; per-batch sink callback checks-and-clears a `parking_lot::Mutex<bool>` skip flag on the first DATA-bearing batch; subsequent batches deliver normally. Napi binding: `BenchValueLogBundle::attach(upstream, skip_cached_replay)` (optional 2nd arg). Wrapper `impl.reactiveLog().attach(upstream, opts)` accepts `{ skipCachedReplay }`. Parity `ImplReactiveLog.attach(upstream, opts?)` widened. 3 new cargo tests: (a) cached single value drops handshake replay; (b) cold upstream's first live emit DOES land (suppression no-op when sentinel); (c) default attach (no opts) still replays cached DATA. | TS ✅ landed 2026-05-16; native ✅ D270 (`AttachOptions`). The (b)/(d) TS sub-cases (`replayBuffer:N` + `attach` inside `batch()`) aren't covered cross-arm — Rust substrate doesn't have a `replayBuffer` shape; documented in `porting-deferred.md`. Archive on next sweep. |
| 2026-05-18 (D243 / D237-A-AMEND) | **`CoreFull` trait widened with 6 read-only inspection methods** — `cache_of`/`has_fired_once`/`kind_of`/`deps_of`/`is_terminal`/`is_dirty`. S2b/β graphrefly-graph reactive describe/observe routes the topology-sub re-entry (`set_deps`→`DepsChanged`/`NodeTornDown`, fired inside a Core wave) through `MailboxOp::Defer(FnOnce(&dyn CoreFull))` (D233); `describe_of` runs there and needs inspection. The 6 methods are pure reads (no `C`/`T` — `HandleId`/`NodeId`/enum only). **No public-API/signature change for existing callers; additive trait surface.** | `crates/graphrefly-core/src/node.rs` `CoreFull` trait + blanket `impl<C: StateCell> CoreFull for Core<C>` | `CoreFull` is a graphrefly-core substrate trait, NOT on the `Impl` parity contract (`packages/parity-tests/impls/types.ts`) — no parity-tests widening. `@graphrefly/pure-ts` has no `CoreFull` analog (TS reactive-describe re-enters synchronously; no mailbox-defer). Logged because it widens the Core owner-side re-entry surface the graph/operator layer depends on; revisit only if `CoreFull` is ever promoted to the cross-impl contract. | N/A — graphrefly-core internal trait; no `@graphrefly/native` obligation unless a native `CoreFull` surface ships. | ✅ **LANDED 2026-05-18** (`cargo check -p graphrefly-core --lib` clean). |
| 2026-05-20 → **RESOLVED 2026-05-21 (D266/D267)** | **✅ `@graphrefly/native` `Graph::remove` / `Graph::destroy` napi TSFN deadlock — root cause confirmed + structurally fixed.** Reproduction (2026-05-21) pinpointed the actual deadlock: not D070/D077 directly but **TSFN-sink-callback re-entrance into `actor.run_sync`** — `scenarios/graph/remove.test.ts` test 3 (R3.7.3 ordering) calls `g.nameOf(s)` from inside the TEARDOWN sink; `bench.nameOf` used `actor.run_sync` which blocks libuv on the actor reply while the actor worker is itself blocked on the TSFN sink reply. 3-way deadlock confirmed via `sample(node_pid)` ⇒ main thread in `ThreadSafeFunction::AsyncCb` → `napi_call_function`. Tests 1+2 didn't deadlock because they didn't call sync read methods from inside a sink. **Fix (F1):** every sync `#[napi]` method on `BenchGraph` that uses `actor.run_sync` (`name`/`node_count`/`node_names`/`child_names`/`is_destroyed`/`try_resolve`/`name_of`/`edges`/`describe_json`) + `BenchCore::describe_node` promoted to `async fn` + `actor.run`. The parity `Impl` contract widened to `T | Promise<T>` for `tryResolve`/`nameOf`/`edges`/`describe`/`describeNode` so the pure-ts arm returns sync values unchanged; rust arm returns Promises (or sync via a JS-side reverse cache `nodeIdToName` populated on `state`/`add` and cleared in `remove`/`destroy` AFTER `bench.remove/destroy` resolves — preserves R3.7.3 sync-observability of names from inside the TEARDOWN sink cross-arm). Constructor `from_core` + structures `create` factories keep `run_sync` (documented safe — no subscribers exist at construction). `--exclude` workaround dropped. Full parity suite **36/36 files, 377 tests + 1 skip green cross-arm** (R3.2.3 + R3.7.3 ordering tests all pass on both arms). Canonical: `~/src/graphrefly-ts/docs/rust-port-decisions.md` D267. (Original entry follows.) **🟠 `@graphrefly/native` `Graph::remove` / `Graph::destroy` napi TSFN deadlock** — surfaced 2026-05-20 during the `*Map` follow-on `/dev-dispatch`. Full parity suite hangs at `packages/parity-tests/scenarios/graph/remove.test.ts` (rust impl arm); the file is currently the only blocker that prevents `pnpm exec vitest run` from completing on the parity-tests workspace. `sample(worker_pid)` shows worker thread stuck in `v8impl::(anonymous namespace)::ThreadSafeFunction::AsyncCb(uv_async_s*)` → `napi_call_function`, never returning. Triggers: `await g.remove("temp")` / `await g.destroy()` on an `impl.Graph`. Matches the D070/D077 hazard class (napi surface is async-only — Core on a tokio blocking pool; sync calls deadlock) and the S7 D260+D261 pattern (35 TSFN failures fixed, more uncovered). **NOT introduced by the switchMap `*Map` follow-on** (pure-ts-only change touched no native binding). | N/A — the substrate (TS pure-ts) is conformant; gap is on the rust binding side. Test source: `packages/parity-tests/scenarios/graph/remove.test.ts` (R3.2.3/R3.7.3 `remove(name)` + namespace-during-cascade ordering). | Workaround **today**: `pnpm exec vitest run --exclude '**/scenarios/graph/remove.test.ts'` from `packages/parity-tests/` passes 35/36 files (371 / 372 tests, 1 skipped). Parity gate is functional for everything else. The 36th file's expected coverage (`describe.each(impls)` over `Graph::remove` + `Graph::destroy` + namespace ordering) is the contract that must run cross-arm once the binding is fixed. | ✅ **RESOLVED 2026-05-21 (D266/D267).** Root cause confirmed via reproduction (NOT D070/D077 directly — sink-callback re-entrance into `actor.run_sync`). F1 fix applied: dropped `run_sync` from every `BenchGraph` read method + `BenchCore::describe_node`; widened parity `Impl` to `T | Promise<T>` for the affected reads; JS-side reverse cache (`nodeIdToName`) preserves R3.7.3 sync-observability. Parity gate fully green cross-arm 36/36 files. Archive on next sweep. | TS ✅ `Impl` widened (compatible — pure-ts returns sync, TS resolves non-promise via `await`); native ✅ binding-layer async-everywhere fix landed; cross-arm parity green. |
| 2026-05-19 (DS-2.7.A — user-locked Q1=Rust-canonical) → **NATIVE FLAG SURFACED 2026-05-20** | **§2.7 first-run-gate cross-port contract LOCKED.** Three-port divergence on the gate's settle-predicate + `partial` semantics resolved by adopting Rust's contract as canonical, with TS converging. Locked rules: (a) **RESOLVED does NOT settle the gate** (TS+Rust already conformant; PY diverges — deferred per Q4); (b) **terminal does NOT settle by default** — Reduce-class operators (`reduce`/`scan`/`last`) opt in via new `NodeOptions.terminalAsRealInput?: boolean` (Rust pattern); (c) **`partial: true` truly disables the gate** — fn body MUST guard SENTINEL slots explicitly (Rust D011 / R2.5.3 / R5.4 lock; supersedes the older TS first-run-gate text's claim that `partial: true` "fires regardless of sentinel deps" without the explicit-handling contract). Spec amended at `GRAPHREFLY-SPEC.md` §2.7. Closes `optimizations.md` ② (premise — "non-deterministic across subscription ordering" — unreproducible; real defect was 3-port divergence + stale spec text). Session record: `archive/docs/SESSION-DS-2.7.A-first-run-gate.md`. **No `Impl` parity-tests symbol added in this DS** — `terminalAsRealInput` is a node-options field; whether it lands on the `Impl` contract (`packages/parity-tests/impls/types.ts`) depends on the follow-on TS slice's design call (likely YES since it's now a public spec-level option every port must accept). | TS impl follow-on: `packages/pure-ts/src/core/node.ts` gate-scan predicate (drop `d.terminal !== undefined` from the sentinel exclusion when `!_terminalAsRealInput`) + `NodeOptions.terminalAsRealInput` flag + opt the existing `reduce`/`scan`/`last` (`packages/pure-ts/src/extra/operators/{transform.ts,take.ts}`) into it + new `packages/parity-tests/scenarios/core/first-run-gate.test.ts` covering: RESOLVED-only dep holds; `partial:true`+SENTINEL pass-through; `terminalAsRealInput` opt-in fires on COMPLETE-only dep. | **Predicate pre-conformant — flag-surfacing slice pending** (corrected /qa 2026-05-19). Rust's gate predicate already excludes RESOLVED (`crates/graphrefly-core/src/node.rs:1497–1512` `has_sentinel_deps`) and excludes terminal for non-Reduce-class operators (`crates/graphrefly-core/src/batch.rs:1156–1159` + `:1784–1792` `fire_operator` first-run gate; D011/R2.5.3/R5.4 `partial`-bypass). HOWEVER: Rust's terminal-aware behavior is dispatched by hardcoded `OperatorOp` discriminant (`Reduce`/`Last`/`Valve` in `node.rs:152–158`), NOT by a user-facing `NodeOptions.terminalAsRealInput` flag. Promoting it to a flag on Rust NodeOptions to mirror `R2.7.1` is a thin follow-on slice (no semantic change for the existing dispatch — purely surfaces the existing per-op behavior as a public option). Until that slice lands, the cross-impl parity scenario can only assert the structural behavior (Reduce-class fires on COMPLETE-only; others don't), not a literal `{ terminalAsRealInput: true }` opt-in. | ✅ **NATIVE FLAG SURFACED 2026-05-20 (D263, `/porting-to-rs` slice)** — `NodeOpts.terminal_as_real_input: bool` (default `false`) added to `crates/graphrefly-core/src/node.rs`; threaded through `NodeRegistration` → `NodeRecord` → the `fire_fn` first-run-gate predicate at `crates/graphrefly-core/src/batch.rs` (now treats a terminal-dep as real input when the flag is set, mirroring `fire_operator`'s existing dispatch). Per-operator `OperatorOp` dispatch unchanged (still always-terminal-aware via `fire_operator`); the new flag surfaces that behavior to user-built derived nodes. **NOT widened onto `Impl`** (per `scenarios/core/first-run-gate.test.ts` header "NOT widened onto Impl" + D196 consumer-pressure gate; no napi widening). New cargo test `crates/graphrefly-core/tests/d263_terminal_as_real_input.rs` (3 tests). TS impl + Impl-contract widening + parity scenario remains a separate `/dev-dispatch` slice. PY (no `partial` flag today; treats RESOLVED as DATA-equivalent) is carved out of this DS (Q4) — tracked separately under `[py-parity-am]` rigor-infra umbrella. | PY (no `partial` flag today; treats RESOLVED as DATA-equivalent) is carved out of this DS (Q4) — tracked separately under `[py-parity-am]` rigor-infra umbrella. |
| 2026-05-19 (D248 / D249 — S2c, user-locked) | **Substrate `Sink`/`TopologySink` `Send + Sync` DROPPED → `Core`/`OwnedCore`/`Graph` are now `!Send + !Sync` (full single-owner).** D246/S2c collapsed the `StateCell` generic + deleted the §7 cross-thread wave machinery; D248 (user-locked Opt-1) relaxed the substrate subscriber-callback types `graphrefly_core::Sink` (`Arc<dyn Fn(&[Message])>`) / `TopologySink` (the `Send+Sync` was shared-Core-era legacy — a single-owner Core fires sinks on one thread). Consequence: the `Core`-owned subscriber map holds `!Send` sinks ⇒ `Core`/`OwnedCore`/`Graph` (`Rc<RefCell<GraphInner>>`, D247) are `!Send + !Sync`. The **only** cross-thread bridge stays the id-only `Arc<CoreMailbox>` (timer `Emit`/`SendDeferFn`). D249 (user-locked) added the owner-only `!Send` `DeferQueue` split. **`BindingBoundary` is UNCHANGED — still `Send + Sync` (FFI trait); only the subscriber callbacks relaxed.** **Reverses "CLAUDE.md Rust invariant 2: Core stays Send+Sync"** + the S2b `Core: Send` premise (documented consequence; the actor model = independent per-worker Cores, never a Send-relocated Core with live sinks). | `~/src/graphrefly-rs` `crates/graphrefly-core/src/{node.rs,topology.rs}` (`Sink`/`TopologySink` aliases), `crates/graphrefly-graph/src/{graph,describe,observe}.rs` (`NamespaceChangeSink`/`DescribeSink`) | NOT on the `Impl` parity contract — `Sink`/`TopologySink` are graphrefly-core substrate types, not parity-tests symbols; `@graphrefly/pure-ts` has no analog (TS sinks are JS closures, no Send/Sync). Logged because it is a **substrate-contract widening with a downstream napi obligation**: the `@graphrefly/native` napi binding holds `Core` and (per D206) marshals it across the tokio async boundary — a `!Send` `Core` interacts with the deferred async-everywhere reconciliation (D080/D206). The napi-side D248/D249 reconciliation is **NOT in S2c scope** (bindings excluded from default-members; native is an honest async preview per D206). | ✅ **RESOLVED 2026-05-19 by S6/D255 (α/γ-converged actor model).** `@graphrefly/native` binding now holds `Arc<CoreActor>` (Send+Sync); `Core` lives on a dedicated `std::thread::spawn`-ed worker thread per `BenchCore`. The `invoke_fn_with_core` (D245/D246-r5) override threads `&dyn CoreFull` to producer build closures (no thread-local needed; D256). `!Send` resources (Graph, StorageHandle, LogView/ScanHandle/ReactiveSub) live in the actor's worker-thread-local `WORKER_EXTRAS` registry; the `BindingBoundary`-side `Bench*` napi wrappers store `(actor, key)` pairs that survive the Send+Sync requirement. `cargo build`/`clippy`/`pnpm build` GREEN. CI `bindings-js` + `napi-build-matrix` gates restored. | ✅ **TS-side N/A** (substrate-internal type relaxation; no presentation/`Impl` coupling); **native reconciliation LANDED 2026-05-19** (S6). Archive on next sweep. |
| 2026-05-19 (S6 close — D255/D256/D257/D258) | **`@graphrefly/native` napi binding actor-model reconciliation against post-S2c `!Send + !Sync` Core.** The full napi binding crate (~5900 LOC across 6 source files: core/operator/graph/storage/structures + new `core_actor.rs`) reconciled against the post-S2c substrate. **No new `Impl` symbol; no substrate-trait widening** — D245's `invoke_fn_with_core` was already in the substrate with a default delegating impl, S6 just overrides it in `BenchBinding`. **No new public-API surface** — Option C wrapper.js (D206/D207) unchanged; the same `createNativeImpl()` shape backs the parity arm. Substrate behavior unchanged (S6 is a binding-only slice). | `~/src/graphrefly-rs/crates/graphrefly-bindings-js/src/{core_actor.rs,core_bindings.rs,operator_bindings.rs,graph_bindings.rs,storage_bindings.rs,structures_bindings.rs}` — internal-only. | When the rust parity arm runs against the post-S6 native build, runtime tests of the napi surface (subscribe/dispose/observe/storage/structures) start exercising the actor model (vs. the pre-S6 typecheck-only state). The 3 pre-existing typecheck-only gaps (per `migration-status.md` "Step 7" §) should resolve. | ✅ **Native LANDED 2026-05-19 (S6).** Local commit pending in graphrefly-rs; not yet pushed (push auto-publishes `@graphrefly/native` per N1/D203 flow; user-gated). | ✅ TS-side ✅ N/A; native ✅ landed; awaiting pub-gate user push for the parity arm to exercise the new build. Archive on next sweep. |

---

## Related

- `packages/parity-tests/impls/types.ts` — the `Impl` contract itself (the coupling surface).
- `~/src/graphrefly-rs/docs/migration-status.md` § "NEXT BATCH" — where the porting track picks up handoffs.
- [`docs/rust-port-decisions.md`](rust-port-decisions.md) — D196 (napi widening = parity-scenario gated), D206 (native substrate contract).
- [`docs/optimizations.md`](optimizations.md) — item-level optimization state (not cross-track coordination).
