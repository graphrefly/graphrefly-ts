# Static QA — private release comparison

Two skill-required independent static reviewers examined the collector, child, driver, source builder, independent rebuild/verifier, and private oracle against fc5acae0 §§2–6. Neither reviewer executed tests or consumers.

Auto-applicable findings fixed within the approved private tooling scope:

- Bind the complete ten-source qualification inventory plus five qualification evidence files; reject stale or omitted bindings.
- Independently rebuild B/C from archived, git-object-bound loaded source and compare actual bundles and metafiles. Check frozen bundle/reservation identities before each dispatch.
- Preserve failed-child identity and partial raw evidence in terminal verification; never turn failure into a successful child.
- Build the full P2 expected view from the existing plain instance and independent publication oracle, not candidate output. Canonicalize actual/expected snapshot encodings outside timers.
- Treat either nonpositive p95 as unresolved. Keep position medians separate.
- Fail closed on live-child RSS monitoring failure; verify observation maxima/timestamps. Reap process groups on preparation/verification deadlines, including Node/esbuild descendants.

Both final static reviews report no remaining concrete blockers. Loaded offline tests cover boundary/cleanup/slot/order/entry faults, numeric inversion/indexing/residual/position faults, PID reuse, failed terminal retention and fake collector timeout/RSS failure. These are tool qualifications, not new performance observations or formal method qualification.
