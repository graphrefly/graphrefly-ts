# Lifecycle allocation attribution

Same owner: `graphrefly-ts:CAUSAL-PRESET-ASSEMBLY-TS`. User continuation authorized the stated next offline allocation investigation. This is diagnostic evidence, not a new decision, library implementation or renewed performance capture.

One process loaded the hash-verified frozen B and B-copy bundles; B alone executed six existing preflight instances and 100 exact candidate lifecycles. Named call frames identify construction, six-step input, duplicate input, snapshot, cleanup and validation. Inspector sampling interval was 32768 bytes with collected-major/minor-GC objects included. Forced GC occurred before profiling and every 20 cycles. Timeout 30 seconds, no retry, checkpoint RSS ceiling 256 MiB. Checkpoints do not bound transient peaks. No timing samples were collected, and no profiler instrumentation was added to the library.

The profile estimates about 2.24 GB of cumulative allocations across 100 cycles, including objects subsequently collected. This is neither simultaneously live memory nor RSS, and sampling estimates are not exact byte counts. First input accounts for about 60%, duplicate input 35%, construction 2.6%, cleanup 1.5%; snapshot and validation together less than 0.5%. These phase totals are disjoint call-tree selfSize sums. They describe this instrumented B workload only, not C, full performance acceptance, or exact original-run memory attribution.

Top self-allocation sites include sort, sortedJsonValue, getOwnPropertyDescriptor, getOwnPropertyNames, publication visit, and dataKey. Frozen bundle section markers locate sortedJsonValue in packages/ts/src/json/codec.ts, dataKey and sha256 in packages/ts/src/solutions/causal-occurrence/identity.ts, and visit in examples/spending-alerts/causal-publication.ts. Native frames without a source URL remain unattributed in the function table; do not count a parent's inclusive cost again. The raw profile and exact bundle hashes are retained for deeper caller attribution.

Combined with the previous flat post-GC live heap, this points toward repeated input normalization/material validation allocation as a priority, rather than treating graph construction or retained authority records as the established cause. It still does not prove absence of leaks. No authority lifecycle/evidence record should be discarded merely to reduce RSS, and no unchecked identity cache or serialization shortcut is approved.

Next: trace repeated canonicalization of the same material through input and duplicate paths, then propose a narrowly scoped way to avoid redundant work while preserving passive-JSON rejection, immutable-input proof, digest identity and graph-owned lifecycle. No code optimization, threshold increase or new capture was started here. Work and formal performance acceptance remain incomplete.
