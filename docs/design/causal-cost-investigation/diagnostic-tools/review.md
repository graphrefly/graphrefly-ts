# Qualification repairs and boundaries

Unique owner: graphrefly-ts:CAUSAL-PRESET-ASSEMBLY-TS. User continuation after design commit 3d0f4a6e authorizes tooling preparation and offline qualification, not the proposed 24-process capture.

- Independent source review added strict inverse/AST allowlisting and 56 source cases. It found observer finish/async disconnect errors could escape and hide the earlier workload error; finish and callback paths now retain those errors. Fake observer fault checks cover the fixes.
- Initial loaded-stub negative case failed because its fs mocks had not been synchronized into imported ESM bindings. The 24 main coordinates had completed; synchronization repaired the test setup, and later complete loaded runs passed. This was not a consumer performance failure.
- First real adapter qualification failed by serializing already canonical graphSnapshot evidence/obligation strings again. Original failed result retained; corrected comparator passed. Final flags/environment/cleanup qualification also passed. Three actual qualification attempts created nine instances each (27 total, including plain instances); no timed consumer loop ran.
- Full fake capture exposed macOS temporary-directory aliases (/var versus /private/var). Capture now resolves entry paths before comparing containment; rejection and single-use tests remain.
- Process CPU includes whole-block work; no elapsed-minus-process-CPU scheduling conclusion. Native GC/deopt timing stays unknown. Observer GC evidence is a captured subset only.
- The independent child and whole-run verifiers do not import collector arithmetic. Whole-run verification does not authenticate approval prose or re-execute the Node binary; digests and the explicit user grant retain their separate roles.

- Final capture review found missing scenario/runtime boundary rechecks and insufficient same-grant reuse protection. Snapshot digests now come from the qualification/approval bytes already read, source/scenario/runtime are rechecked before each dispatch and finally, and an exclusive adjacent approval claim binds the output. Fake pipeline tests reject scenario/runtime drift and reuse with a fresh output directory. No real capture approval claim was made.
