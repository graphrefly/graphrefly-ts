# Pre-execution qualification note

Authorized preparation follows da7d02ad and user continuation. No library or consumer change.
Exact rational verifier implements the reviewed absolute sleep-offset interval, V8 integer rounding and libuv truncation. The performance floating bound is four ulps: uint64-to-double nearest conversion contributes at most about one final-scale ulp and division rounding another; four covers both across binade boundaries. Python Fraction preserves the exact input double, all subsequent arithmetic is exact, qualification compares rational width to 1/10 ms. Display float width is informational; exact fraction endpoints are retained.

Independent read-only QA found two false acceptance/rejection risks: named function prologue/epilogue outside internally placed windows, and GC skipping parent envelope validation. Fixed before freezing: caller-side window timestamps; shared native/anchor validation; live mach_timebase_info persisted and compared to frozen platform. Five synthetic tests pass, with fourteen corrupt-input cases, mapping failure distinct from width failure, sleep/scheduling widening, GC envelope and missing delivery rejection, single-use stop under a fake supervisor, and four resource violations. No subprocesses are launched by those tests (native timebase read only). The prior 3000-residue/54000-case design arithmetic remains bound.

At most CPU/CPU/CPU/GC sequential fresh processes, first failure stops, zero retries. Up to1s profile per CPU,10s per process,40s total,256MiB observed RSS/directory,32MiB CPU file,200k samples,100k nodes, GC<100k events. GC alone permits two forced GC calls. Explicit environment and Node executable are frozen in plan.json. Raw CPU and GC data are retained even if qualification fails. Probe success is not consumer capture-tool readiness or permission for a real capture.
