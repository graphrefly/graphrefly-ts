# Complete single-version evidence — performance inconclusive

**完整采集与复核已完成；本方法结束，不追加采样。** 32/32串行consumer进程、3840样本
（640warmup、3200measured）及224不计时预检实例全部完成。没有失败、未运行项或本轮内重试。
用户“同意修复，直到可以采集到所有证据”授权了修复后采集；原失败和本轮之前的29/32
部分尝试均独立保留，没有拼样本或重写为成功。

## 性能结果

同一P2 summary consumer，B8253488b/C9c30967c，源码/输入/样本边界与原单版本方法相同。
八组平衡BCCB/CBBC，按冻结规则分别判断p50/p95，不通过更换指标升级结论。
低于1表示候选较低，范围为八组的几何C/B比值。

| 指标 | 比值范围 | 同版本控制 | 结论 |
| --- | --- | --- | --- |
| 构造p50 | 0.8344–0.9058 | 2/16越界，范围0.9451–1.0554 | 控制不稳定，收益未确认 |
| 构造p95 | 0.7587–0.9559 | 9/16越界，范围0.9022–1.4069 | 控制不稳定，收益未确认 |
| actionSum p50 | 0.9798–1.0070 | 全部在范围内 | mixed，非一致下降 |
| actionSum p95 | 0.8924–1.0439 | 4/16越界 | 控制不稳定 |

构造的八组p50和八组p95都向下降方向，属于保留的改善线索；但不能把同版本控制失败
忽略掉。各进程构造p50的中位数B0.415459ms、C0.359958ms仅作绝对量级描述。
业务动作总成本没有呈现一致下降，不能宣称整体加速。
初始输入、重复输入、清理、wall/residual的完整逐进程绝对p50/p95/sum、八组对比和
同版本控制见[独立报告](report.json)。没有统计显著性、正式D169通过或单graph内存结论。

## 运行与观测资格

- 全轮47.158043秒；最大观察RSS156.40625MiB，最大正数RSS观察间隔0.137788秒。
- 329次ps观测全部保留，其中1次终态零值被确认退出后正确分类；本轮没有触发E/Z额外
  退出等待，等待路径由假进程资格证明。终态原文没有删除或当成零内存。
- 256MiB/30秒/900秒、wake与双时钟约束保持；轮询仍不是连续内存上限保证。
- 每进程独立verifier通过后才启动下一个；源码、固定单模块路径、释放/occurrence、
  plain/reference预检和原始样本绑定检查通过。未更改library/API/wave。
- 572文件[归档](../../../../archive/evals/causal-single-version-rss-v2-capture-v1/artifact-index.json)
  逐项散列检查，新解压目录只运行Python verifier，完整报告逐字一致，回放consumer数0。

## 修复历史与边界

原f967f265保留的1进程失败没有进程状态，仍失败且原因不能被追溯证明。
本次第一版修复保留[29/32部分尝试](../single-version-rss-capture/README.md)：第30个进程
出现?Es零值、即时poll未退出以及清理EPERM，停止剩余2项。546文件失败归档可复核。
本机ps(1)明确E为正在退出，因此第二版修复允许最多0.5秒等待确认，完整保留等待证据；
不确定/超时/错误退出仍失败，信号失败也不再跳过wait。独立复查及假场景全部完成。
两份准备归档分别196文件，修复后的新轮从头运行，旧数值从未用于填补这32项。

[授权](authorization-context.json)、[采集日志](collector.log)、[回放收据](archive-replay.json)
均保留。复核：按索引验证归档与成员，安全解压，运行
`python3 -B prepared/tools/verify.py prepared`，对比`evidence/report.json`，不运行collector。

本批“修复并采集完整证据”已完成；原CAUSAL-PRESET-ASSEMBLY-TS与正式D169未完成。
不再为得到漂亮数值继续本方法。保留候选为“有改善线索、收益未确认”，不能标整体通过。
