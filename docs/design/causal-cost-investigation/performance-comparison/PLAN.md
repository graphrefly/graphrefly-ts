# Currentness before/after diagnostic — frozen before dispatch

Owner: graphrefly-ts:CAUSAL-PRESET-ASSEMBLY-TS. User authorizes continuation until this performance comparison is reviewable. This is an execution/evidence batch, not a new product decision or formal performance acceptance method.

B = 60190fcc2f660df1164a0c351ff2083925b71baf; C = f7ea7a5281550060620c2e671d12d06885a07fd2. Same private candidate summary consumer; compare currentness optimization only. Freeze both full reachable closures, prove the sole difference is the approved identity comparator, rebuild bundles from retained sources. Reuse existing semantic plain/reference preflight, expected snapshot, action windows and cleanup driver. B-copy has identical bytes at a distinct module URL.

Two rows in fixed order: P2-lifecycle (construction, six initial inputs, duplicate, cleanup, action sum, wall span and residual) and inactive-1 (negative control: group release without currentness). Exact archived P2 input SHA256 44f1165557fc1444540731a73846233ce3d71da7a0af3a3d4fa2137e249c2079.

Each row has six pairs: control, four main, control. Each pair has two repetitions in separate fresh processes; each repetition has complementary U/V processes. Freeze 24 random order bits before dispatch. Each process loads M0 then M1, runs 20 warmup + 50 measured samples contiguously in each slot. U positions M0/M1; V M1/M0. Total 48 serial child processes, 6720 lifecycles (1920 warmup, 4800 measured), 384 untimed preflight instances (336 P2 and 48 negative control). No individual-sample interleaving, no forced GC/profiler, no extra timed process or retry.

This deliberately shortens VM history relative to the failed release-counting diagnostic. It does NOT retry or complete that historical method, establish stationary steady performance, or relax its resource guard. No comparison with its historical timing samples is allowed. Fifty measured samples make p95 noisy; retain both repetitions and positions, no confidence interval. No duration is inferred from allocation profile percentages.

Hard bounds: 256 MiB RSS soft guard observed by parent every 100 ms; 30 seconds per child, 900 seconds whole capture; source/runtime/wake drift, invalid clocks, semantic failure, process error or resource failure stops all remaining children. No redraw, outlier removal, pair replacement, sample retry, or result-dependent extension. Control instability is reported rather than prompting more sampling. Preparation/build/qualification invokes zero timed performance factories. Default-off library recorder remains untouched. Environment excludes NODE_OPTIONS/coverage/compile cache. No provider/live/spend or library modifications.

Independent Python verification must validate exact schedule, source/runtime/tool/input identities, child PID/exit, counts, raw coordinates, eight/two clocks, expected semantic snapshot digest, release and occurrence invariants, RSS observations and stop posture. Recompute p95 via sorted element 48 of 50 (nearest rank), p50 via middle-pair mean, matched-position C/B ratios across U/V, and per-pair-position median across the two repetitions. Durations are milliseconds. RSS polling is a 100 ms target, not a continuous bound; report the largest observed interval including start/end gaps. Retain absolute phase durations, action sum, wall span and residual. Controls stable only when all four control pair-position medians lie in [1/1.05, 1.05]. An observed consistent decrease requires stable controls and all eight main pair-position medians below 1; increase analogously above 1; otherwise mixed/inconclusive. These are descriptive labels, not pass/fail budgets.

Freeze qualification and source/tool hashes before dispatch. Independently verify each completed child before the next. Preserve complete and failed raw data; replay verification from a fresh archive extraction without executing consumers. Qualify arithmetic and invalid evidence rejection with deterministic fixtures; reuse unchanged sample-boundary fake-host tests and library correctness/mutation evidence. Review private tooling with two static reviewers before capture. Commit report, index, raw archive and reproduction instructions. Performance significance, allocation/post-GC live-heap changes, broad workloads and graded hiding remain outside this timing/RSS comparison.
