# Frozen consumer offline semantic preflight

Owner remains `graphrefly-ts:CAUSAL-PRESET-ASSEMBLY-TS`. User continuation after `ed90f747` authorized the stated next step: actual offline consumer semantic preflight and commit. This is evidence, not a new decision or performance grant.

One process completed 33 instances across the frozen B, C and byte-identical B-copy modules. Each module ran cold and duplicate-input candidate/reference/plain preflights (six instances), one exact candidate lifecycle (one), and four release micro fixtures (four). All passed. The runner uses the unchanged qualified external driver with a constant-zero clock; it never enters the timed child's sampling loop. There were zero performance samples, no external effect execution, and no retries.

Duplicate input retained exactly one occurrence. Candidate snapshots matched independently derived expected business vectors and the plain/reference preflight checks. All three module expectations agreed. Each exact candidate path checked 54 owned nodes, two roots and empty graph membership after cleanup. Inactive groups of 60, 2 and 1 nodes released; the active diamond emitted 11, rejected release at the subscribed join without changing topology, then cleaned successfully after unsubscribe. These assertions execute inside the frozen wrapper whose bytes were loaded from the verified build archive.

The retained recovery-port report still lists conservation as missing on re-observation in both candidate and reference. This batch does not establish full recovery-port replay, lifecycle termination policy, user-tier hiding, human/agent ergonomics or new mutation qualification. An admitted effect with no outcome remains active in the snapshot; it is not described as a completed business effect. No library, public API or wave protocol was changed.

Independent Python verification checks raw bindings, exact module/row inventory, semantic snapshots, occurrence counts and release outcomes. A fresh archive extraction produces the identical verifier result without running consumers again; three corrupted evidence controls are rejected. The archive includes original bundles, input, runner, driver, command logs, exit identity and full results. Historical evidence and the six root dirty files were preserved.

Build and real semantic preflight are now ready for the proposed finite comparison. Performance impact remains unresolved: this run supplies no ratio and does not qualify D169 or the formal matrix. The earlier single-use capture grant is consumed. The next consequential step is a fresh explicit grant for the already-defined fixed comparison (same revisions, five rows, 60 children, 144000 samples, no retries); it has not been launched here. Owner work remains incomplete.
