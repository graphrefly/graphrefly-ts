# Renewed fixed comparison: stopped at RSS boundary

Same owner: `graphrefly-ts:CAUSAL-PRESET-ASSEMBLY-TS`. The user approved a fresh fixed capture after `0bb8149d`: five rows, 60 children, 144000 samples, no retry. `approval.json` binds this grant to the unchanged original design and budget. Only the collector's exclusive output and qualification directory changed from v1 to v2; no algorithm, schedule, resource threshold, public API, protocol or library change. Current private tool tests passed before dispatch; historical counting regression evidence was reused explicitly.

Preparation, source binding and independent admission passed. The first process was the P2 B/B-copy control, variant V. Fourteen real preflight instances passed. At 3.969961 seconds child elapsed, parent observation found RSS 268582912 bytes, exceeding the fixed 268435456-byte (256 MiB) guard by 147456 bytes. The parent killed the process and stopped the attempt. Total attempt elapsed was 10.676567 seconds. Zero children completed; one was dispatched; 59 remain not-run. There are 329 raw lines: 100 warmup and 229 measured, all in the first block/position. No C sample was taken and no complete comparison is available. This is a resource-bound capture failure, not a library performance verdict or proof of a leak.

The complete failed directory, immutable reservation, original tool/source bytes, RSS observations, partial samples and exit evidence are archived (189 files). Terminal-mode independent verification passed and reproduced byte-for-byte after fresh extraction, without new samples. A prefix-only verification call correctly rejected the extra failed child directory; its rejection is retained separately, followed by the correct terminal-mode result. No capture retry occurred. The renewed grant is consumed.

All prior archives, root dirty files, library, examples and fixture sources remain unchanged. This batch does not qualify D169, graded access, full recovery or human/agent ergonomics. Existing conservation re-observation limitations remain. The next useful work is an offline investigation of the resource budget and repeated lifecycle memory behavior, using the retained observations first. Do not silently increase RSS allowance or rerun sampling. Any revised consequential capture needs a concrete reviewed boundary and fresh approval; no new decision is needed merely to record this attempt.
