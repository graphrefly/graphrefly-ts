# Offline memory investigation

Owner: `graphrefly-ts:CAUSAL-PRESET-ASSEMBLY-TS`. User asked to continue investigating the 256.14 MiB RSS failure. This batch records diagnostic evidence; no durable decision, library fix, performance retry or budget increase.

The original archive has 37 RSS observations: early process launch readings are not a reliable Node baseline; RSS rises in steps, reaches about 230 MiB at 1.54 seconds, then stays around 246 MiB before crossing the guard. Only 104270 bytes of sample JSON were written. The child writes each record and drops its snapshot; there is no accumulating sample array in the actual child. The fixture keeps latest view values per run, disconnects subscriptions, releases registered membership, then loses the local run reference when the driver returns. Frozen source review does not establish a live root retaining every completed graph. Codegraph returned historical diagnostic copies, so conclusions were checked against the exact archived B sources instead.

A separate bounded diagnostic loaded the exact hash-verified B and B-copy artifacts, ran B's six plain/reference/candidate preflight instances, then 400 exact B candidate lifecycles with a constant-zero action clock. It recorded process.memoryUsage and requested GC every 50 cycles. One process, 30-second timeout, no retry; zero performance samples. The 256 MiB threshold was checked at memory checkpoints, not with the original parent 100ms observer, so this diagnostic does not certify transient peak RSS compliance. B-copy was loaded but not exercised; the original first timed slot was B-copy. Instrumentation and GC deliberately change execution history.

Before import: RSS about 47 MiB, heapUsed 4.2 MiB. After both imports: RSS 54.9 MiB, heapUsed 6.8 MiB. After 400 cycles: RSS 207 MiB, post-GC heapUsed 9.45 MiB. Across checkpoints 50–400, post-GC heapUsed stays approximately 9.35–9.55 MiB. Pre-GC heapUsed grows and is reclaimed, while heapTotal and RSS remain high. This supports allocation and retained runtime heap capacity as an important contributor, rather than a demonstrated per-cycle growth in live JS objects. It does not prove absence of a leak or uniquely explain the original peak. Native memory and exact retaining paths remain unresolved.

Do not divide whole-process RSS by node count or call 256 MiB the cost of one graph. Do not add forced GC to the measured library or silently relax the original guard. The next useful diagnostic is allocation-site attribution on the same lifecycle, distinguishing business canonicalization/snapshots from graph construction, before proposing a concrete budget or code change. Existing performance acceptance, owner work, lifecycle retention and user-tier hiding remain incomplete.
